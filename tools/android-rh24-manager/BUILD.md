# Build-Hinweis

Diese Branch-Datei stößt den geprüften Android-CI-Build für den RH24 STRATO Manager v1.0 an. Das APK wird zunächst unsigniert erzeugt und anschließend außerhalb des öffentlichen Repositories signiert.
