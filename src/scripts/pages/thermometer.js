/* Aus thermometer.html ausgelagerte Seitenlogik. */

const esc=s=>String(s??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));fetch('shop-products.php',{cache:'no-store'}).then(r=>r.json()).then(d=>{const rows=(d.products||d||[]).filter(p=>String(p.category||'').toLowerCase().includes('thermometer')&&p.shop_visible!==false&&p.status!=='inactive');const el=document.getElementById('thermoGrid');if(!rows.length){el.innerHTML='<div class="thermoEmpty"><b>Noch keine Thermometer online.</b><br>Sobald im Orgaboard ein Artikel der Kategorie Thermometer online gestellt wird, erscheint er hier automatisch.</div>';return}el.innerHTML=rows.map(p=>`<article class="thermoCard">${p.image?`<img src="${esc(p.image)}" alt="${esc(p.name)}" decoding="async" fetchpriority="high">`:''}<small>Art.-Nr. ${esc(p.article_no||'–')}</small><h3>${esc(p.name)}</h3><p>${esc(p.description||'')}</p><div class="thermoPrice">${Number(p.price??p.base??0).toLocaleString('de-DE',{style:'currency',currency:'EUR'})}</div><a href="${esc(p.url||('artikel.php?id='+encodeURIComponent(p.id)))}" style="display:inline-block;margin-top:10px;font-weight:700;color:#8b4a22">Details ansehen →</a></article>`).join('')}).catch(()=>document.getElementById('thermoGrid').innerHTML='<div class="thermoEmpty">Artikel konnten gerade nicht geladen werden.</div>');
