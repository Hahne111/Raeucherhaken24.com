(()=>{
let contentSearchTerm='',contentTypeFilter='all';
'use strict';
// V61 repair: restored complete Orgaboard modules from V60 and retained V61 marketplace protection.
const cfg=window.RH24_ORGABOARD||{};
const $=(s,r=document)=>r.querySelector(s), $$=(s,r=document)=>[...r.querySelectorAll(s)];
const ws=$('#workspace'), title=$('#viewTitle'), kicker=$('#viewKicker');
let state=null,current='dashboard',searchTerm='',productSearchTerm='',salesCustomerId='',salesAdvice=null,salesConsultationId='',earningsRepId='',dealerCalendarLoaded=false,productSortMode='name_asc',productAnalysisPeriod='previous_half',productionSearchTerm='',productionPriorityFilter='all',productionStationFilter='all',productionDueFilter='all',productionSortMode='priority_due',productionStepFilter='all',productionPhaseFilter='all',productionViewMode='table',productionSectionMode='orders',territoryBookRows=[],territoryBookRowsCode='',territoryBookLoading=false,territoryBookAdminState='',territoryBookStatusFilter='all',territoryBookCategoryFilter='all',territoryBookSearch='',territoryDirectoryContactFilter='all',territoryDirectoryOffset=0,territoryDirectoryTotal=0,territoryDirectoryPageSize=150,territoryNationalSyncRunning=false,territorySearchTerm='',territorySelectedCode='',territorySearchTimer=0,tripLogData=null,tripLogLoading=false,tripLogPeriod=new Date().toISOString().slice(0,7),tripLogRepId='',tripRouteDate=new Date().toISOString().slice(0,10),tripRouteResult=null,appointmentData=null,appointmentLoading=false,appointmentPeriod=new Date().toISOString().slice(0,7),appointmentRepId='',appointmentView='month',appointmentSelectedDate=new Date().toISOString().slice(0,10),appointmentSearch='',appointmentCustomerSearchTimer=0,appointmentSearchTimer=0,documentSearchTerm='',documentTypeFilter='all',documentStatusFilter='all',documentSearchTimer=0,productSearchTimer=0;
let inventoryModule=localStorage.getItem('rh24-inventory-module')||'cockpit',warehouseSearchTerm='',warehouseFilter='all';
const isAdmin=()=>Boolean(state?.current_user?.role==='admin'||cfg.isAdmin);
const can=p=>isAdmin()||(state?.permissions||[]).includes('*')||(state?.permissions||[]).includes(p);
const defaultProductionPerms=['view_dashboard','view_production','edit_production','view_inventory','view_messages','send_messages','change_own_password'];
const defaultFieldPerms=['view_dashboard','view_customers','edit_customers','view_sales','view_products','view_messages','send_messages','view_own_stats','view_earnings_calculator','view_leaderboard','view_star_stats','view_sales_calendar','view_dealer_visits','manage_dealer_visits','view_territory_book','contact_territory_book','view_triplog','edit_triplog','view_appointments','edit_appointments','change_own_password'];
const viewPermission={dashboard:'view_dashboard',orders:'view_orders',production:'view_production',prototypes:'view_prototypes',customers:'view_customers',sales:'view_sales',products:'view_products',productbuilder:'view_products',nature_spices:'view_products',product_analysis:'view_products',inventory:'view_inventory',shipping:'view_shipping',documents:'view_documents',messages:'view_messages',mystats:'view_own_stats',earnings:'view_earnings_calculator',leaderboard:'view_leaderboard',salescalendar:'view_sales_calendar',appointments:'view_appointments',triplog:'view_triplog',territorybook:'view_territory_book',newsletter:'manage_newsletter'};
const viewNames={dashboard:['Dashboard','GESCHÄFTSFÜHRUNG · LIVE-LEITSTAND'],orders:['Bestellungen','AUFTRÄGE & ZAHLUNGEN'],production:['Produktion','FERTIGUNG & QUALITÄT'],production_staff:['Produktionsteam','MITARBEITER · ZUWEISUNG · AUSFÜHRUNG'],prototypes:['Prototypen','SONDERANFERTIGUNGEN'],customers:['Kunden','KUNDENAKTEN'],sales:['Produktberatung','BERATUNG & PRODUKTEMPFEHLUNG'],territories:['Festgebiete Deutschland','GEBIETSSCHUTZ · GEBIETSBÜCHER · BUNDESLANDFARBEN'],salesreps:['Kundenberater','VERTRIEB · GEBIETE · PERFORMANCE'],dealers:['Händler','B2B & RABATTSTUFEN'],products:['Produktzentrale','SORTIMENT · PRODUKTE · STATUS · LÖSCHEN'],productbuilder:['Produkt-Baukasten','NEUE ARTIKEL · LIVE-VORSCHAU · VERÖFFENTLICHEN'],nature_spices:['Naturgewürze','135 ARTIKEL · BILDER UND PREISE PFLEGEN'],product_analysis:['Produkt-Analyse','BESTSELLER · LANGSAMDREHER · HALBJAHRESBERICHT'],inventory:['Lager & Logistik','COCKPIT · BESTAND · BESCHAFFUNG · BEWEGUNGEN'],shipping:['Versand','VERSAND & TRACKING'],documents:['Rechnungen & Lieferscheine','DOKUMENTE & VERSIONEN'],messages:['Nachrichten','INTERNE KOMMUNIKATION'],mystats:['Meine Provision','VERKAUF · HERKUNFT · GUTSCHRIFT · ABRECHNUNG'],earnings:['Verdienstrechner','PROVISION · ZIELE · MONATSPLANUNG'],leaderboard:['Rangliste & Sterne','VERDIENST · MONATSSTERNE · JAHRESSTATISTIK'],salescalendar:['Vertriebskalender','HÄNDLERTOUREN · 14-TAGE-RHYTHMUS · ABRECHNUNG'],appointments:['Termine','KALENDER · KUNDEN · ROUTEN · WIEDERVORLAGEN'],triplog:['Fahrtenbuch','ROUTEN · TERMINE · KILOMETER · FINANZAMT'],territorybook:['Gebietsbuch','GEBIETSPERFORMANCE · BRANCHEN · KONTAKTE'],profile:['Mein Zugang','PASSWORT & PROFIL'],mobileapp:['Handy-App','ORGABOARD MOBIL INSTALLIEREN'],calculator:['Produkt-Kalkulator','KOSTEN · MARGE · VERKAUFSPREIS'],calculator_help:['Kalkulator-Hilfe','FELD FÜR FELD RICHTIG KALKULIEREN'],reviews:['Bewertungen','KUNDENFEEDBACK'],content:['Rezepte & Inhalte','CONTENTVERWALTUNG'],ai:['Smoky / KI','BERATUNGSANALYSE'],administration:['Administration','BENUTZER · RECHTE · ÄNDERUNGSPROTOKOLL'],rights:['Zugriffsrechte','RECHTEZENTRALE FÜR MITARBEITER'],newsletter:['Newsletter','KUNDENBINDUNG · DOUBLE-OPT-IN'],shopdesign:['Shop-Design','SAISON · AKTION · DARSTELLUNG'],marketplace:['An- & Verkaufen','MITGLIEDER · ANZEIGEN · FREIGABEN'],settings:['Einstellungen','SYSTEM & SICHERHEIT']};
const esc=v=>String(v??'').replace(/[&<>'"]/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[c]));
const money=v=>Number(v||0).toLocaleString('de-DE',{style:'currency',currency:'EUR'});
const weightLabel=g=>{g=Number(g||0);if(g<=0)return'–';return g>=1000?(g/1000).toLocaleString('de-DE',{maximumFractionDigits:3})+' kg':g.toLocaleString('de-DE')+' g'};
function carrierBadge(carrier){carrier=String(carrier||'').toUpperCase();return carrier==='DHL'?'<span class="carrierMark dhlMark">DHL</span>':carrier==='DPD'?'<span class="carrierMark dpdMark">DPD</span>':`<span class="carrierMark">${esc(carrier||'–')}</span>`}
function productSearchToolbar(value=''){return `<div class="productSearchToolbar"><div class="productSearchMain"><span class="searchGlass">⌕</span><input id="productViewSearch" type="search" value="${esc(value)}" placeholder="Produktname, Art.-Nr., SKU, EAN / Barcode …" autocomplete="off" inputmode="search"><button class="btn primary productSearchRun" id="productSearchRun" type="button">Suchen</button><button class="scanBtn" id="productScanBtn" type="button" title="Barcode mit Kamera oder Scanner erfassen">▤ Barcode scannen</button><button class="clearSearchBtn" id="productSearchClear" type="button" title="Produktsuche löschen">×</button></div><div class="productSearchHint"><b>Produktsuche:</b> Name, Artikelnummer, SKU, interne ID sowie EAN/Barcode werden durchsucht. Enter startet die Suche sofort.</div></div>`}
function bindProductSearch(renderFn){const inp=$('#productViewSearch');if(!inp)return;const run=(focus=true)=>{clearTimeout(productSearchTimer);productSearchTerm=inp.value.trim();renderFn();if(focus)setTimeout(()=>{const n=$('#productViewSearch');if(n){n.focus();try{n.setSelectionRange(n.value.length,n.value.length)}catch(e){}}},0)};inp.oninput=()=>{productSearchTerm=inp.value;clearTimeout(productSearchTimer);productSearchTimer=setTimeout(()=>run(true),280)};inp.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();run(true)}else if(e.key==='Escape'){e.preventDefault();productSearchTerm='';inp.value='';run(true)}};const go=$('#productSearchRun');if(go)go.onclick=()=>run(true);const clear=$('#productSearchClear');if(clear)clear.onclick=()=>{clearTimeout(productSearchTimer);productSearchTerm='';inp.value='';renderFn();setTimeout(()=>$('#productViewSearch')?.focus(),0)};const scan=$('#productScanBtn');if(scan)scan.onclick=()=>startBarcodeScanner(value=>{productSearchTerm=value;inp.value=value;renderFn();setTimeout(()=>$('#productViewSearch')?.focus(),0)})}
async function startBarcodeScanner(onValue){
  if(!navigator.mediaDevices?.getUserMedia||!('BarcodeDetector' in window)){const inp=$('#productViewSearch');if(inp){inp.focus();inp.select()}toast('Kamera-Scanner wird von diesem Browser nicht unterstützt. USB/Bluetooth-Scanner funktioniert direkt im Suchfeld.');return}
  let stream=null,stopped=false;openModal(`<div class="modalHead"><small>BARCODE-SCANNER</small><h2>Artikel scannen</h2><p>Barcode oder EAN ruhig vor die Kamera halten.</p></div><div class="modalBody"><div class="scannerViewport"><video id="barcodeVideo" autoplay playsinline muted></video><div class="scannerLine"></div></div><div class="formActions"><button class="btn" id="cancelScanner">Abbrechen</button></div></div>`);const video=$('#barcodeVideo');const stop=()=>{stopped=true;if(stream)stream.getTracks().forEach(t=>t.stop())};$('#cancelScanner').onclick=()=>{stop();closeModal()};try{stream=await navigator.mediaDevices.getUserMedia({video:{facingMode:{ideal:'environment'}}});video.srcObject=stream;await video.play();const detector=new BarcodeDetector({formats:['ean_13','ean_8','code_128','code_39','upc_a','upc_e','itf','qr_code']});const tick=async()=>{if(stopped)return;try{const found=await detector.detect(video);if(found?.length){const val=String(found[0].rawValue||'').trim();if(val){stop();closeModal();onValue(val);toast('Barcode erkannt: '+val);return}}}catch(e){}requestAnimationFrame(tick)};tick()}catch(e){stop();closeModal();toast('Kamera konnte nicht gestartet werden. Scanner kann weiterhin direkt ins Suchfeld schreiben.',true)}
}
function shippingIntegration(carrier){return (state.shipping_integrations||[]).find(x=>x.carrier===carrier)||{carrier,environment:'sandbox',configured:false,status:'not_configured',last_message:''}}
function shippingLabelMeta(orderNo){return (state.shipping_labels||[]).find(x=>x.order_no===orderNo)||null}
function integrationStatus(i){const cls=i.status==='connected'?'connected':i.status==='error'?'error':'pending';const txt=i.status==='connected'?'Verbindung steht':i.status==='error'?'Prüfung fehlgeschlagen':i.configured?'Gespeichert · noch prüfen':'Nicht eingerichtet';return `<span class="integrationStatus ${cls}"><i></i>${txt}</span>`}
function shippingDraftHtml(o,carrier,tracking){const c=o.customer||{},w=Number(o.totals?.shipping_weight_g||0);return `<div class="shippingDraft"><div class="shippingDraftTop">${carrierBadge(carrier)}<b>VERSANDLABEL · VORSCHAU</b></div><div class="shippingDraftAddress"><small>EMPFÄNGER</small><strong>${esc(c.name||'–')}</strong><span>${esc(c.company||'')}</span><span>${esc(c.street||'')}</span><span>${esc(c.zip||'')} ${esc(c.city||'')}</span></div><div class="shippingDraftFacts"><span><small>Bestellung</small><b>${esc(o.order_no||o.id)}</b></span><span><small>Versandgewicht</small><b>${weightLabel(w)}</b></span><span><small>Tracking</small><b>${esc(tracking||'noch offen')}</b></span></div><div class="shippingDraftNote">Entwurfsansicht im Orgaboard – ein offizielles DHL-/DPD-Label wird hier angezeigt, sobald es hinterlegt oder über die Schnittstelle erzeugt wurde.</div></div>`}
function dataUrlToBlobUrl(data,mime){try{const raw=atob(data),arr=new Uint8Array(raw.length);for(let i=0;i<raw.length;i++)arr[i]=raw.charCodeAt(i);return URL.createObjectURL(new Blob([arr],{type:mime||'application/pdf'}))}catch(e){return''}}

const dt=v=>{if(!v)return'–';const d=new Date(v);return Number.isNaN(d.getTime())?'–':d.toLocaleString('de-DE',{dateStyle:'short',timeStyle:'short'})};
const donly=v=>{if(!v)return'–';const d=new Date(v);return Number.isNaN(d.getTime())?'–':d.toLocaleDateString('de-DE')};
const status=(s,labels=state?.order_statuses||{})=>`<span class="status s-${esc(s)}">${esc(labels[s]||s||'–')}</span>`;
const pay=s=>`<span class="payTag ${esc(s)}">${({pending:'Zahlung offen',paid:'Bezahlt',refunded:'Erstattet',cancelled:'Storniert'})[s]||esc(s||'offen')}</span>`;
function toast(msg,error=false){const t=$('#toast');t.textContent=msg;t.className='toast show'+(error?' error':'');clearTimeout(t._tm);t._tm=setTimeout(()=>t.className='toast',2600)}
async function api(payload){
  const r=await fetch('api.php?v=83.3',{method:'POST',cache:'no-store',credentials:'same-origin',headers:{'Content-Type':'application/json','X-CSRF-Token':cfg.csrf},body:JSON.stringify(payload)});
  const raw=await r.text();let d=null;
  try{d=raw?JSON.parse(raw):null}catch(e){const clean=String(raw||'').replace(/<[^>]*>/g,' ').replace(/\s+/g,' ').trim().slice(0,280);throw new Error(`Serverantwort ist kein gültiges JSON (HTTP ${r.status}). ${clean||'Keine Antwort erhalten.'}`)}
  if(!r.ok||!d?.ok)throw new Error(d?.error||`Aktion fehlgeschlagen (HTTP ${r.status})`);return d
}
const productImagePreparedCache=new WeakMap();
function productImageExt(file){const n=String(file?.name||'').toLowerCase(),m=n.match(/\.([a-z0-9]+)$/);return m?m[1]:''}
function productImageAllowed(file){const ext=productImageExt(file),mime=String(file?.type||'').toLowerCase();return ['jpg','jpeg','png','webp','avif','heic','heif'].includes(ext)||['image/jpeg','image/png','image/webp','image/avif','image/heic','image/heif','image/heic-sequence','image/heif-sequence'].includes(mime)}
function productImageIsHeic(file){const ext=productImageExt(file),mime=String(file?.type||'').toLowerCase();return ['heic','heif'].includes(ext)||mime.includes('heic')||mime.includes('heif')}
async function decodeProductImage(file){
  if('createImageBitmap' in window){try{return await createImageBitmap(file,{imageOrientation:'from-image'})}catch(e){try{return await createImageBitmap(file)}catch(_e){}}}
  return await new Promise((resolve,reject)=>{const url=URL.createObjectURL(file),img=new Image();img.onload=()=>{URL.revokeObjectURL(url);resolve(img)};img.onerror=()=>{URL.revokeObjectURL(url);reject(new Error('Bild konnte im Browser nicht dekodiert werden'))};img.src=url})
}
async function prepareProductImage(file){
  if(productImagePreparedCache.has(file))return productImagePreparedCache.get(file);
  const job=(async()=>{
    if(!productImageAllowed(file))throw new Error('Erlaubt sind JPG, JPEG, PNG, WebP, AVIF, HEIC und HEIF.');
    if(Number(file.size||0)>20*1024*1024)throw new Error('Das Ausgangsbild darf maximal 20 MB groß sein.');
    try{
      const img=await decodeProductImage(file),w=Number(img.width||img.naturalWidth||0),h=Number(img.height||img.naturalHeight||0);
      if(!w||!h)throw new Error('Bildabmessungen konnten nicht gelesen werden');
      const max=2000,scale=Math.min(1,max/Math.max(w,h)),cw=Math.max(1,Math.round(w*scale)),ch=Math.max(1,Math.round(h*scale));
      const canvas=document.createElement('canvas');canvas.width=cw;canvas.height=ch;const ctx=canvas.getContext('2d',{alpha:true});if(!ctx)throw new Error('Bildoptimierung wird von diesem Browser nicht unterstützt');
      ctx.imageSmoothingEnabled=true;ctx.imageSmoothingQuality='high';ctx.drawImage(img,0,0,cw,ch);if(typeof img.close==='function')img.close();
      const blob=await new Promise(resolve=>canvas.toBlob(resolve,'image/webp',0.86));
      if(blob&&blob.size>0&&blob.type==='image/webp'){const base=String(file.name||'produkt').replace(/\.[^.]+$/,'').replace(/[^A-Za-z0-9._-]+/g,'-')||'produkt';return {blob,name:base+'.webp',converted:true,width:cw,height:ch,sourceType:file.type||productImageExt(file)}}
    }catch(e){
      // HEIC/HEIF may not be browser-decodable. In that case the original is sent to the server,
      // where Imagick/libheif can convert it when available.
    }
    return {blob:file,name:file.name||('produkt.'+(productImageExt(file)||'jpg')),converted:false,width:0,height:0,sourceType:file.type||productImageExt(file)}
  })();productImagePreparedCache.set(file,job);return job
}
async function uploadProductImage(id,file){
  const prepared=await prepareProductImage(file),fd=new FormData();fd.append('id',id);fd.append('image',prepared.blob,prepared.name);fd.append('original_name',file.name||'');fd.append('client_optimized',prepared.converted?'1':'0');fd.append('csrf',cfg.csrf);
  const r=await fetch('product-image.php?v=83.2',{method:'POST',headers:{'X-CSRF-Token':cfg.csrf},body:fd});const d=await r.json().catch(()=>({ok:false,error:'Ungültige Serverantwort'}));if(!r.ok||!d.ok)throw new Error(d.error||'Bild-Upload fehlgeschlagen');return d
}
async function previewPreparedProductImage(file,target){
  try{const prepared=await prepareProductImage(file);if(target._rh24PreviewUrl)URL.revokeObjectURL(target._rh24PreviewUrl);if(productImageIsHeic(file)&&!prepared.converted){target.innerHTML='<span>HEIC/HEIF ausgewählt · Konvertierung erfolgt beim Speichern</span>';return}const url=URL.createObjectURL(prepared.blob);target._rh24PreviewUrl=url;target.innerHTML=`<img src="${url}" alt="Vorschau">`}catch(e){target.innerHTML='<span>Produktbild</span>';throw e}
}
async function load(silent=false){if(!silent)ws.innerHTML='<div class="loadingState"><div class="spinner"></div><p>Orgaboard wird geladen …</p></div>';const ctl=new AbortController(),tm=setTimeout(()=>ctl.abort(),20000);try{const r=await fetch('api.php',{cache:'no-store',signal:ctl.signal}),d=await r.json();if(!r.ok||!d.ok)throw new Error(d.error||'Laden fehlgeschlagen');state=d;ensureWarehouse();updateBadges();updateNavVisibility();render();setTimeout(maybeCelebrateStars,250)}catch(e){const msg=e?.name==='AbortError'?'Serverantwort dauert länger als 20 Sekunden. Bitte neu laden oder API prüfen.':(e.message||'Laden fehlgeschlagen');ws.innerHTML=`<div class="panel panelPad"><h2>Orgaboard konnte nicht geladen werden</h2><p class="muted">${esc(msg)}</p><button class="btn primary" onclick="location.reload()">Neu laden</button></div>`}finally{clearTimeout(tm)}}
function updateBadges(){const open=(state.orders||[]).filter(o=>!['complete','cancelled'].includes(o.status)).length;const proto=(state.prototypes||[]).filter(o=>!['complete','cancelled'].includes(o.status)).length;const low=(state.inventory||[]).filter(x=>Number(x.stock)<=Number(x.minimum)).length;const unread=(state.messages||[]).filter(m=>m.recipient_user_id===state.current_user?.id&&!m.read_at).length;
 if($('#navOrderBadge'))$('#navOrderBadge').textContent=open;if($('#navProtoBadge'))$('#navProtoBadge').textContent=proto;if($('#navStockBadge'))$('#navStockBadge').textContent=low;if($('#navMessageBadge'))$('#navMessageBadge').textContent=unread;const marketOpen=Number(state.marketplace?.stats?.pending_listings||0)+Number(state.marketplace?.stats?.open_reports||0);if($('#navMarketBadge')){$('#navMarketBadge').textContent=marketOpen;$('#navMarketBadge').style.display=marketOpen?'':'none'}}
function syncGlobalSearchContext(){const g=$('#globalSearch');if(!g)return;const productMode=['products','productbuilder','nature_spices','product_analysis'].includes(current);g.placeholder=productMode?'Produkt, Art.-Nr., SKU, EAN / Barcode …':'Bestellung, Kunde, E-Mail …';g.value=productMode?productSearchTerm:searchTerm}
function setView(v,keepSearch=false){const perm=viewPermission[v];if(perm&&!can(perm)){toast('Für diesen Bereich fehlt die Berechtigung.',true);return}current=v;if(!keepSearch)searchTerm='';const [n,k]=viewNames[v]||[v,''];title.textContent=n;kicker.textContent=k;$$('.navItem').forEach(b=>b.classList.toggle('active',b.dataset.view===v));$$('[data-quick-view]').forEach(b=>b.classList.toggle('active',b.dataset.quickView===v));$$('[data-mobile-view]').forEach(b=>b.classList.toggle('active',b.dataset.mobileView===v));$$('.navSection').forEach(s=>s.classList.toggle('hasActive',Boolean(s.querySelector('.navItem.active'))));$('#sideNav').classList.remove('open');syncGlobalSearchContext();render()}
function updateNavVisibility(){if(!state)return;$$('.navItem').forEach(b=>{const p=viewPermission[b.dataset.view];if(p&&!isAdmin())b.style.display=can(p)?'':'none'});$$('[data-mobile-view]').forEach(b=>{const p=viewPermission[b.dataset.mobileView];if(p&&!isAdmin())b.style.display=can(p)?'':'none'});$$('[data-quick-view]').forEach(b=>{const p=viewPermission[b.dataset.quickView];if(p&&!isAdmin())b.style.display=can(p)?'':'none'});$$('.navSection').forEach(s=>{const visible=[...s.querySelectorAll('.navItem')].some(b=>b.style.display!=='none');s.classList.toggle('navSectionEmpty',!visible)})}
function render(){if(!state)return;({dashboard:renderDashboard,orders:renderOrders,production:renderProduction,production_staff:renderProductionStaff,prototypes:renderPrototypes,customers:renderCustomers,sales:renderSales,territories:renderTerritories,salesreps:renderSalesReps,dealers:renderDealers,products:renderProducts,productbuilder:renderProductBuilderHub,nature_spices:renderNatureSpices,product_analysis:renderProductAnalysis,inventory:renderInventory,shipping:renderShipping,documents:renderDocuments,messages:renderMessages,mystats:renderMyStats,earnings:renderEarnings,leaderboard:renderLeaderboard,salescalendar:renderSalesCalendar,appointments:renderAppointments,triplog:renderTripLog,territorybook:renderTerritoryBook,profile:renderProfile,mobileapp:renderMobileApp,calculator:renderCalculator,calculator_help:renderCalculatorHelp,reviews:renderReviews,content:renderContent,ai:renderAI,administration:renderAdministration,rights:renderRights,newsletter:renderNewsletter,shopdesign:renderShopDesign,marketplace:renderMarketplace,settings:renderSettings}[current]||renderDashboard)()}
function orderCustomer(o){return o.customer?.name||o.customer_name||'–'}
function orderEmail(o){return o.customer?.email||o.email||''}
function orderTotal(o){return Number(o.totals?.gross??o.total??0)}
function itemCount(o){return (o.items||[]).reduce((s,x)=>s+Number(x.qty||1),0)}
function itemMeta(x){const m=x.meta||{};return [m.length,m.material,m.tip==='extra'?'Extra scharf':m.tip==='standard'?'Standard geschärft':''].filter(Boolean).join(' · ')}
function todayOrders(){const t=new Date().toDateString();return (state.orders||[]).filter(o=>new Date(o.created_at).toDateString()===t)}
function currentMonthOrders(){const n=new Date();return (state.orders||[]).filter(o=>{const d=new Date(o.created_at);return d.getMonth()===n.getMonth()&&d.getFullYear()===n.getFullYear()&&!['cancelled'].includes(o.status)})}
function employeePerformanceHtml(st){st=st||{};const daily=Array.isArray(st.daily)?st.daily:[],max=Math.max(1,...daily.map(x=>Number(x.net||0))),net=Number(st.net_revenue||0),comm=Number(st.commission||0),eff=Number(st.effective_rate||0),next=st.next_threshold,gap=Number(st.gap_to_next||0);const bars=daily.map((x,i)=>{const h=Math.max(2,Math.round(Number(x.net||0)/max*100));const label=(i===0||((i+1)%5===0)||i===daily.length-1)?String(i+1):'';return `<div class="perfBarWrap" title="${i+1}. · ${money(x.net||0)}"><i style="height:${h}%"></i><small>${label}</small></div>`}).join('');return `<section class="panel performancePanel"><div class="panelTitle"><div><h3>Meine Monatsleistung</h3><span>bezahlter Nettowarenumsatz · täglich</span></div><b>${money(net)}</b></div><div class="performanceGrid"><div class="performanceChart">${bars||'<div class="empty">Noch keine Tagesumsätze.</div>'}</div><div class="performanceFacts"><div><small>Provision aktuell</small><strong>${money(comm)}</strong><span>effektiv ${eff.toLocaleString('de-DE',{maximumFractionDigits:2})} %</span></div><div><small>Aktueller Grenzsatz</small><strong>${Number(st.commission_rate||5).toLocaleString('de-DE')} %</strong><span>nur für den aktuellen Umsatzbereich</span></div><div><small>Nächste Stufe</small><strong>${next?money(next):'Top-Stufe'}</strong><span>${next?'noch '+money(gap):'20 % auf Mehrumsatz ab 75.000 €'}</span></div></div></div></section>`}
function renderProductionDashboard(){const orders=(state.orders||[]).filter(o=>!['complete','cancelled','shipped'].includes(o.status)),uid=state.current_user?.id||'',mine=orders.filter(o=>o.production_assignee_user_id===uid),started=orders.filter(o=>productionPhaseOf(o)==='started'),waiting=orders.filter(o=>productionPhaseOf(o)==='waiting'),quality=orders.filter(o=>productionPhaseOf(o)==='quality'),due=orders.filter(o=>prodDueMeta(o).days<=1).sort((a,b)=>prodDueMeta(a).days-prodDueMeta(b).days),low=(state.inventory||[]).filter(x=>+x.stock<=+x.minimum).length;ws.innerHTML=`<div class="roleBanner fieldRole productionRole"><div><b>Produktion · Fertigungszugang</b><span>Aufträge bearbeiten, Fortschritt dokumentieren und jeden ausgeführten Arbeitsschritt automatisch deinem Benutzerkonto zuordnen.</span></div><strong>${esc(state.current_user?.display_name||'')}</strong></div><div class="quickCreateBar"><div><b>Fertigung</b><span>Keine Vertriebs-, Umsatz- oder Provisionsdaten in diesem Zugang.</span></div><div class="quickCreateActions"><button class="btn primary" id="prodDashOpen">Produktion öffnen</button>${can('view_inventory')?'<button class="btn" id="prodDashStock">Lager ansehen</button>':''}${can('view_messages')?'<button class="btn" id="prodDashMessages">Nachrichten</button>':''}</div></div><div class="metricGrid">${metric('Meine Aufträge',mine.length,'mir zugewiesen')}${metric('Arbeit begonnen',started.length,'aktive Fertigung')}${metric('Wartet auf Start',waiting.length,'bezahlt · noch nicht begonnen',waiting.length?'attn':'')}${metric('Qualitätsprüfung',quality.length,'in QS')}${metric('Heute / morgen fällig',due.length,'Priorität prüfen',due.length?'attn':'')}${metric('Lagerwarnungen',low,'unter Mindestbestand',low?'attn':'')}</div><div class="dashGrid"><section class="panel"><div class="panelTitle"><h3>Meine Fertigungsaufträge</h3><span>${mine.length} aktiv</span></div>${mine.length?mine.slice(0,10).map(o=>`<button class="orderMini" style="width:100%;border:0;background:#fff;text-align:left" data-production-edit="${esc(o.order_no||o.id)}"><b>${esc(o.order_no||o.id)}</b><span class="customer"><strong>${esc(orderCustomer(o))}</strong><small>${esc(prodStepMeta(o.production_step)[1])} · ${prodProgress(o)} % · ${esc(o.production_station||'kein Arbeitsplatz')}</small></span>${prodPriorityBadge(o.production_priority||'normal')}</button>`).join(''):'<div class="empty"><b>Keine Aufträge zugewiesen</b>Neue Aufträge erscheinen nach der Zuweisung durch die Administration oder wenn du einen freien Auftrag übernimmst.</div>'}</section><section class="panel"><div class="panelTitle"><h3>Als Nächstes</h3><span>Fälligkeit</span></div><div class="quickList">${due.slice(0,8).map(o=>quick(o.order_no||o.id,prodDueMeta(o).label,`${orderCustomer(o)} · ${prodStepMeta(o.production_step)[1]}`)).join('')||'<div class="empty">Keine kurzfristig fälligen Aufträge.</div>'}</div></section></div>`;$('#prodDashOpen').onclick=()=>setView('production');if($('#prodDashStock'))$('#prodDashStock').onclick=()=>setView('inventory');if($('#prodDashMessages'))$('#prodDashMessages').onclick=()=>setView('messages');$$('[data-production-edit]').forEach(b=>b.onclick=()=>productionEditModal(b.dataset.productionEdit))}

function executiveOrderMonthStats(){
 const orders=(state.orders||[]).filter(o=>o.status!=='cancelled'),now=new Date(),ym=(d)=>`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`;
 const cur=ym(now),prevDate=new Date(now.getFullYear(),now.getMonth()-1,1),prev=ym(prevDate);
 const curRows=orders.filter(o=>String(o.created_at||'').slice(0,7)===cur),prevRows=orders.filter(o=>String(o.created_at||'').slice(0,7)===prev);
 const gross=a=>a.reduce((s,o)=>s+Number(orderTotal(o)||0),0),paid=a=>a.filter(o=>o.payment_status==='paid').reduce((s,o)=>s+Number(orderTotal(o)||0),0);
 const currentGross=gross(curRows),previousGross=gross(prevRows),delta=previousGross?((currentGross-previousGross)/previousGross*100):(currentGross?100:0);
 return {curRows,prevRows,currentGross,previousGross,currentPaid:paid(curRows),previousPaid:paid(prevRows),delta,aov:curRows.length?currentGross/curRows.length:0};
}
function executiveDonut(value,label,sub,cls='green'){value=Math.max(0,Math.min(100,Math.round(Number(value)||0)));return `<div class="execDonut ${cls}" style="--p:${value}%"><div><b>${value}%</b><span>${esc(label)}</span><small>${esc(sub)}</small></div></div>`}
function executiveMiniBars(items,maxValue=0){const max=Math.max(maxValue,...items.map(x=>Number(x.value||0)),1);return `<div class="execBars">${items.map(x=>`<div class="execBarRow"><span>${esc(x.label)}</span><div><i style="width:${Math.max(2,Math.round(Number(x.value||0)/max*100))}%"></i></div><b>${typeof x.display==='string'?x.display:Number(x.value||0).toLocaleString('de-DE')}</b></div>`).join('')}</div>`}
function executivePriorityItems(){const orders=state.orders||[],wh=ensureWarehouse(),low=warehouseLowInventory(),lowPack=warehouseLowPackaging(),items=[];const payment=orders.filter(o=>o.payment_status!=='paid'&&!['cancelled','complete'].includes(o.status));if(payment.length)items.push({level:'red',title:`${payment.length} Zahlungen offen`,text:'Offene Zahlungen prüfen, bevor Produktion oder Versand blockiert wird.',view:'orders'});const ready=orders.filter(o=>o.status==='ready');if(ready.length)items.push({level:'orange',title:`${ready.length} Aufträge versandbereit`,text:'Pakete, Label und Tracking abarbeiten.',view:'shipping'});if(low.length+lowPack.length)items.push({level:'red',title:`${low.length+lowPack.length} Lagerwarnungen`,text:'Bestände bzw. Packmittel liegen am oder unter Meldebestand.',view:'inventory'});const territories=(state.sales_territories||[]).filter(t=>!t.owner_sales_rep_id);if(territories.length)items.push({level:'gold',title:`${territories.length} Weißgebiete`,text:'Festgebiete ohne verantwortlichen Kundenberater.',view:'territories'});const nat=state.territory_directory_national_summary||{};if(Number(nat.candidate||0)>0)items.push({level:'blue',title:`${Number(nat.candidate||0).toLocaleString('de-DE')} Rechercheadressen`,text:'Gefundene Branchenkontakte warten auf Prüfung/Freigabe.',view:'territorybook'});if(!items.length)items.push({level:'green',title:'Keine kritischen Engpässe',text:'Der operative Leitstand zeigt aktuell keine dringenden Blocker.',view:'dashboard'});return items.slice(0,6)}
function renderExecutiveDashboard(){
 const orders=state.orders||[],stats=executiveOrderMonthStats(),today=todayOrders(),open=orders.filter(o=>!['complete','cancelled'].includes(o.status)).length,prod=orders.filter(o=>['production','quality','packing'].includes(o.status)).length,ready=orders.filter(o=>o.status==='ready').length,wh=ensureWarehouse(),low=warehouseLowInventory(),lowPack=warehouseLowPackaging(),reps=publicSalesReps(),activeReps=reps.filter(r=>r.status==='active'),territories=state.sales_territories||[],fixed=territories.filter(t=>t.owner_sales_rep_id).length,national=state.territory_directory_national_summary||{},contactQuality=Number(national.total||0)?Math.round(Number(national.complete||0)/Number(national.total)*100):0,activeRate=Number(national.total||0)?Math.round(Number(national.active||0)/Number(national.total)*100):0,stockRows=state.inventory||[],stockStable=stockRows.length?Math.round(stockRows.filter(x=>Number(x.stock)>Number(x.minimum)).length/stockRows.length*100):100,po=warehouseOpenPurchases(),todayRevenue=today.filter(o=>o.status!=='cancelled').reduce((s,o)=>s+Number(orderTotal(o)||0),0),priority=executivePriorityItems(),topReps=[...activeReps].sort((a,b)=>Number(b.stats_current?.gross_goods||b.revenue||0)-Number(a.stats_current?.gross_goods||a.revenue||0)).slice(0,5),topTerritories=[...territories].sort((a,b)=>Number(b.revenue||0)-Number(a.revenue||0)).slice(0,5);
 const deltaClass=stats.delta>=0?'positive':'negative',deltaText=`${stats.delta>=0?'+':''}${stats.delta.toLocaleString('de-DE',{maximumFractionDigits:1})} % ggü. Vormonat`;
 ws.innerHTML=`<section class="executiveHero"><div><small>GESCHÄFTSFÜHRUNG · LIVE-LEITSTAND</small><h2>Räucherhaken24 Unternehmenscockpit</h2><p>Verkauf, Fertigung, Lager, Vertrieb und Gebietsbücher in einer priorisierten Managementansicht.</p><div class="executiveHeroActions"><button class="btn primary" data-exec-view="orders">Aufträge</button><button class="btn" data-exec-view="inventory">Lager & Logistik</button><button class="btn" data-exec-view="salesreps">Vertrieb</button><button class="btn" data-exec-view="territorybook">Gebietsbücher</button></div></div><div class="executivePulse"><span>LIVE</span><b>${new Date().toLocaleDateString('de-DE',{weekday:'long',day:'2-digit',month:'2-digit',year:'numeric'})}</b><small>${esc(state.current_user?.display_name||'Administrator')} · Vollzugriff</small></div></section>
 <div class="executiveKpiGrid"><article><small>Umsatz heute</small><strong>${money(todayRevenue)}</strong><span>${today.length} Auftrag/Aufträge heute</span></article><article><small>Umsatz Monat</small><strong>${money(stats.currentGross)}</strong><span class="${deltaClass}">${deltaText}</span></article><article><small>Ø Warenkorb Monat</small><strong>${money(stats.aov)}</strong><span>${stats.curRows.length} Aufträge im Monat</span></article><article><small>Operativ offen</small><strong>${open}</strong><span>${prod} Fertigung · ${ready} versandbereit</span></article><article><small>Lagerwarnungen</small><strong>${low.length+lowPack.length}</strong><span>${po.length} offene Beschaffung</span></article><article><small>Festgebiete besetzt</small><strong>${fixed}/16</strong><span>${activeReps.length} aktive Vertriebskonten</span></article></div>
 <div class="executiveGrid executiveHealthGrid"><section class="panel execHealth"><div class="panelTitle"><h3>Unternehmensgesundheit</h3><span>4 Kernindikatoren</span></div><div class="execDonutGrid">${executiveDonut(stockStable,'Lagerstabilität',`${low.length} Artikel kritisch`,'green')}${executiveDonut(Math.round(fixed/16*100),'Gebietsabdeckung',`${16-fixed} Weißgebiete`,'brown')}${executiveDonut(contactQuality,'Datenqualität',`${Number(national.complete||0).toLocaleString('de-DE')} vollständig`,'blue')}${executiveDonut(activeRate,'Gebietsbuch aktiv',`${Number(national.active||0).toLocaleString('de-DE')} freigegeben`,'gold')}</div></section><section class="panel execPriority"><div class="panelTitle"><h3>Chef-Prioritäten</h3><span>nach Dringlichkeit</span></div><div class="execPriorityList">${priority.map((x,i)=>`<button data-exec-view="${esc(x.view)}" class="execPriorityItem ${x.level}"><i>${i+1}</i><span><b>${esc(x.title)}</b><small>${esc(x.text)}</small></span><strong>→</strong></button>`).join('')}</div></section></div>
 <div class="executiveGrid"><section class="panel"><div class="panelTitle"><h3>Vertrieb · Top Leistung</h3><span>aktueller Stand</span></div>${topReps.length?executiveMiniBars(topReps.map(r=>({label:r.name,value:Number(r.stats_current?.gross_goods||r.revenue||0),display:money(r.stats_current?.gross_goods||r.revenue||0)}))):'<div class="empty">Noch keine Vertriebsumsätze.</div>'}<div class="formActions"><button class="btn" data-exec-view="salesreps">Vertriebsorganisation öffnen</button></div></section><section class="panel"><div class="panelTitle"><h3>Gebiete · Umsatzschwerpunkte</h3><span>Festgebiete</span></div>${topTerritories.length?executiveMiniBars(topTerritories.map(t=>({label:t.state_name,value:Number(t.revenue||0),display:money(t.revenue||0)}))):'<div class="empty">Noch keine Gebietsumsätze.</div>'}<div class="formActions"><button class="btn" data-exec-view="territories">Deutschlandkarte öffnen</button></div></section></div>
 <div class="executiveGrid"><section class="panel"><div class="panelTitle"><h3>Auftragsfluss</h3><span>${open} offen</span></div>${executiveMiniBars([{label:'Zahlung offen',value:orders.filter(o=>o.payment_status!=='paid'&&!['cancelled','complete'].includes(o.status)).length},{label:'Produktion',value:prod},{label:'Versandbereit',value:ready},{label:'Abgeschlossen',value:orders.filter(o=>o.status==='complete').length}])}</section><section class="panel"><div class="panelTitle"><h3>Gebietsbuch Deutschland</h3><span>${Number(national.total||0).toLocaleString('de-DE')} Kontakte</span></div>${executiveMiniBars([{label:'Aktiv / geprüft',value:Number(national.active||0)},{label:'Recherche',value:Number(national.candidate||0)},{label:'Mit Telefon',value:Number(national.with_phone||0)},{label:'Mit E-Mail',value:Number(national.with_email||0)},{label:'Vollständig',value:Number(national.complete||0)}])}</section></div>`;
 $$('[data-exec-view]').forEach(b=>b.onclick=()=>setView(b.dataset.execView));
}

function renderDashboard(){
 if(isAdmin())return renderExecutiveDashboard();
 if(state.current_user?.role==='production')return renderProductionDashboard();
 const orders=state.orders||[],protos=state.prototypes||[],today=todayOrders(),month=currentMonthOrders();
 const open=orders.filter(o=>!['complete','cancelled'].includes(o.status)).length,payOpen=orders.filter(o=>o.payment_status!=='paid'&&!['cancelled'].includes(o.status)).length,prod=orders.filter(o=>['production','quality','packing','ready'].includes(o.status)).length,ship=orders.filter(o=>o.status==='ready').length,low=(state.inventory||[]).filter(x=>+x.stock<=+x.minimum).length;
 const monthRevenue=month.reduce((s,o)=>s+orderTotal(o),0),todayRevenue=today.reduce((s,o)=>s+orderTotal(o),0),recent=orders.slice(0,8),hdClicks=state.hd_hahne_click_stats||{};
 const adminActions=isAdmin()?`<button class="btn orange" id="dashNewProduct">+ Produkt</button><button class="btn" id="dashOrders">Bestellungen</button><button class="btn" id="dashInventory">Lager</button><button class="btn" id="dashCalculator">Kalkulator</button><button class="btn" id="dashAdmin">Administration</button>`:'';
 ws.innerHTML=`<div class="roleBanner ${isAdmin()?'adminRole':'fieldRole'}"><div><b>${isAdmin()?'Vollzugriff Administrator':state.current_user?.role==='production'?'Produktion · Fertigungszugang':'Außendienst · Vertriebszugang'}</b><span>${isAdmin()?'Alle Bereiche einsehen, bearbeiten und protokolliert verändern.':state.current_user?.role==='production'?'Fertigungsaufträge bearbeiten, Fortschritt dokumentieren und Arbeitsschritte personenbezogen protokollieren. Keine Vertriebs- oder Provisionsrechte.':'Sie sehen nur die vom Administrator freigegebenen Bereiche. Kundendaten können standardmäßig angelegt und bearbeitet werden.'}</span></div><strong>${esc(state.current_user?.display_name||'')}</strong></div>
 <div class="quickCreateBar"><div><b>Schnellzugriff</b><span>Die wichtigsten Vorgänge ohne Umwege.</span></div><div class="quickCreateActions">${can('edit_customers')?'<button class="btn primary" id="dashNewCustomer">+ Kunde</button>':''}${can('view_sales')?'<button class="btn" id="dashSales">Produktberatung</button>':''}${can('view_messages')?'<button class="btn" id="dashMessages">Nachrichten</button>':''}${can('view_earnings_calculator')?'<button class="btn" id="dashEarnings">Verdienstrechner</button>':''}${can('view_leaderboard')?'<button class="btn" id="dashLeaderboard">Rangliste</button>':''}${can('view_sales_calendar')?'<button class="btn" id="dashCalendar">Vertriebskalender</button>':''}${can('view_triplog')?'<button class="btn" id="dashTriplog">Fahrtenbuch</button>':''}${adminActions}</div></div>
 ${semiannualProductNoticeHtml()}${!isAdmin()&&can('view_own_stats')?employeePerformanceHtml(state.my_sales_stats||{}):''}<div class="metricGrid">${metric('Umsatz heute',money(todayRevenue),today.length+' Bestellungen')}${metric('Umsatz Monat',money(monthRevenue),month.length+' Aufträge')}${metric('Offene Aufträge',open,'noch nicht abgeschlossen',open?'attn':'')}${metric('Zahlung offen',payOpen,'prüfen & nachfassen',payOpen?'attn':'')}${metric('In Fertigung',prod,'inkl. Qualitätsprüfung')}${metric('Versandbereit',ship,'Pakete bearbeiten',ship?'attn':'')}${isAdmin()?metric('HD Hahne Klicks',Number(hdClicks.total||0).toLocaleString('de-DE'),`${Number(hdClicks.today||0).toLocaleString('de-DE')} heute · ${Number(hdClicks.last_30_days||0).toLocaleString('de-DE')} / 30 Tage`):''}</div>
 <div class="dashGrid"><section class="panel"><div class="panelTitle"><h3>Neueste Bestellungen</h3><span>${orders.length} insgesamt</span></div>${recent.length?recent.map(orderMini).join(''):'<div class="empty"><b>Noch keine Bestellungen</b>Neue Shopaufträge erscheinen hier automatisch.</div>'}</section>
 <div><section class="panel"><div class="panelTitle"><h3>Betriebsstatus</h3><span>Live-Übersicht</span></div><div class="quickList">${quick('Offene Prototypen',protos.filter(p=>!['complete','cancelled'].includes(p.status)).length,'Sonderanfertigungen')}${quick('Kundenakten',(state.customers||[]).length,'Kundenstamm')}${quick('Außendienst',(state.sales_reps||[]).filter(r=>r.status==='active').length,'aktive Mitarbeiter')}${quick('Produkte',(state.catalog||[]).length,'Artikel im System')}${quick('Lagerwarnungen',low,'unter Mindestbestand')}${quick('Versandbereit',ship,'Aufträge')}${isAdmin()?quick('Letzter HD-Hahne-Klick',hdClicks.last_click_at?dt(hdClicks.last_click_at):'Noch keiner',hdClicks.last_page||'Powered-by-Link'):''}</div></section><div class="attentionBox"><h3>Prioritäten</h3><ul>${priorityLines(payOpen,ship,low,protos)}</ul></div></div></div>`;
 if($('#dashNewCustomer'))$('#dashNewCustomer').onclick=()=>customerModal(null);if($('#dashSales'))$('#dashSales').onclick=()=>setView('sales');if($('#dashMessages'))$('#dashMessages').onclick=()=>setView('messages');if($('#dashEarnings'))$('#dashEarnings').onclick=()=>setView('earnings');if($('#dashLeaderboard'))$('#dashLeaderboard').onclick=()=>setView('leaderboard');if($('#dashCalendar'))$('#dashCalendar').onclick=()=>setView('salescalendar');if($('#dashTriplog'))$('#dashTriplog').onclick=()=>setView('triplog');
 if(isAdmin()){if($('#dashNewProduct'))$('#dashNewProduct').onclick=()=>productModal(null);if($('#dashOrders'))$('#dashOrders').onclick=()=>setView('orders');if($('#dashInventory'))$('#dashInventory').onclick=()=>setView('inventory');if($('#dashCalculator'))$('#dashCalculator').onclick=()=>setView('calculator');if($('#dashAdmin'))$('#dashAdmin').onclick=()=>setView('administration');if($('#dashProductAnalysis'))$('#dashProductAnalysis').onclick=()=>setView('product_analysis');if($('#dashProductReportPdf'))$('#dashProductReportPdf').onclick=()=>printProductAnalysisReport('previous_half',true);if($('#dashProductReportPrint'))$('#dashProductReportPrint').onclick=()=>printProductAnalysisReport('previous_half',false);if($('#dashProductReportAck'))$('#dashProductReportAck').onclick=()=>{const due=semiannualDuePeriod();if(due){localStorage.setItem('rh24-product-report-ack-'+due.reportKey,'1');renderDashboard()}}}
}
function metric(l,v,s,c=''){return `<div class="metric ${c}"><small>${l}</small><strong>${v}</strong><span>${s}</span></div>`}
function quick(l,v,s){return `<div class="quickItem"><div><b>${l}</b><small>${s}</small></div><strong>${v}</strong></div>`}
function priorityLines(payOpen,ship,low,protos){const a=[];if(payOpen)a.push(`<li>${payOpen} Zahlung(en) noch offen.</li>`);if(ship)a.push(`<li>${ship} Bestellung(en) sind versandbereit.</li>`);if(low)a.push(`<li>${low} Lagerposition(en) unter Mindestbestand.</li>`);const p=protos.filter(x=>['new','paid','review'].includes(x.status)).length;if(p)a.push(`<li>${p} Prototyp-Projekt(e) benötigen Bearbeitung.</li>`);if(!a.length)a.push('<li>Aktuell keine kritischen Aufgaben.</li>');return a.join('')}
function orderMini(o){return `<button class="orderMini" style="width:100%;border:0;background:#fff;text-align:left" data-open-order="${esc(o.order_no||o.id)}"><b>${esc(o.order_no||o.id)}</b><span class="customer"><strong>${esc(orderCustomer(o))}</strong><small>${dt(o.created_at)} · ${esc(o.sales_channel_label||salesChannelLabel(o.sales_channel||o.source))} · ${itemCount(o)} Position(en)</small></span><span class="amount">${money(orderTotal(o))}</span>${status(o.status)}</button>`}

function filteredOrders(){const q=(searchTerm||$('#orderFilter')?.value||'').toLowerCase().trim(),sf=$('#orderStatusFilter')?.value||'',cf=$('#orderChannelFilter')?.value||'';return (state.orders||[]).filter(o=>(!q||`${o.order_no} ${orderCustomer(o)} ${orderEmail(o)} ${o.customer?.city||''} ${o.sales_channel_label||''} ${commissionRepName(o)} ${o.commission_note||''}`.toLowerCase().includes(q))&&(!sf||o.status===sf)&&(!cf||o.sales_channel===cf))}
function renderOrders(){const opts=Object.entries(state.order_statuses||{}).map(([k,v])=>`<option value="${k}">${esc(v)}</option>`).join(''),channels=['online','advisor','telephone','email','walk_in','marketplace','other'];ws.innerHTML=`<div class="sectionHead"><div><h2>Bestellungen</h2><p>Verkaufsherkunft, Kundenberater, Provision, Zahlung, Fertigung und Versand nachvollziehbar an einem Ort.</p></div><div class="sectionActions"><button class="btn" id="exportOrders">CSV exportieren</button><button class="btn primary" id="reloadOrders">Aktualisieren</button></div></div><div class="salesAttributionNotice"><div><b>Klare Zuordnung statt Schätzung</b><span>Die Herkunft bleibt unverändert. Die Provisionsgutschrift wird separat nach Kundenbeziehung ermittelt.</span></div><strong>V69 · automatische Prüfung</strong></div>
<div class="toolbar"><input class="filterInput" id="orderFilter" placeholder="Bestellnummer, Kunde, Herkunft, Berater …" value="${esc(searchTerm)}"><select class="filterSelect" id="orderStatusFilter"><option value="">Alle Status</option>${opts}</select><select class="filterSelect" id="orderChannelFilter"><option value="">Alle Herkünfte</option>${channels.map(x=>`<option value="${x}">${salesChannelLabel(x)}</option>`).join('')}</select></div><div class="panel tableWrap" id="ordersTable"></div>`;renderOrdersTable();$('#orderFilter').oninput=renderOrdersTable;$('#orderStatusFilter').onchange=renderOrdersTable;$('#orderChannelFilter').onchange=renderOrdersTable;$('#reloadOrders').onclick=()=>load();$('#exportOrders').onclick=exportOrders}
function renderOrdersTable(){const rows=filteredOrders(),host=$('#ordersTable');if(!rows.length){host.innerHTML='<div class="empty"><b>Keine Treffer</b>Für diesen Filter gibt es keine Bestellungen.</div>';return}host.innerHTML=`<table class="dataTable orderAttributionTable"><thead><tr><th>Bestellung</th><th>Kunde</th><th>Datum</th><th>Herkunft</th><th>Status</th><th>Zahlung</th><th>Provision an</th><th>Gesamt</th><th></th></tr></thead><tbody>${rows.map(o=>`<tr><td><button class="rowLink" data-open-order="${esc(o.order_no||o.id)}">${esc(o.order_no||o.id)}</button></td><td class="customerCell"><b>${esc(orderCustomer(o))}</b><small>${esc(orderEmail(o))}</small></td><td>${dt(o.created_at)}</td><td>${salesOriginTag(o)}</td><td>${status(o.status)}</td><td>${pay(o.payment_status||'pending')}</td><td>${commissionCreditTag(o)}</td><td class="money">${money(orderTotal(o))}</td><td><button class="miniBtn" data-open-order="${esc(o.order_no||o.id)}">Öffnen</button></td></tr>`).join('')}</tbody></table>`}
function openOrder(id){const o=(state.orders||[]).find(x=>(x.order_no||x.id)===id);if(!o)return;const c=o.customer||{},tot=o.totals||{},edit=can('edit_orders'),admin=isAdmin(),channels=['online','advisor','telephone','email','walk_in','marketplace','other'];openModal(`<div class="modalHead"><small>BESTELLUNG · VERKAUFSZUORDNUNG</small><h2>${esc(o.order_no||o.id)}</h2><p>${dt(o.created_at)} · ${esc(orderCustomer(o))}</p></div><div class="modalBody"><div class="orderAttributionHero"><div><small>VERKAUFSHERKUNFT</small>${salesOriginTag(o)}</div><div><small>PROVISIONSGUTSCHRIFT</small><strong>${esc(commissionRepName(o))}</strong><span>${esc(attributionLabel(o))}</span></div><div class="attributionReason"><small>BEGRÜNDUNG</small><b>${esc(o.commission_note||'Zuordnung noch nicht dokumentiert')}</b></div></div><div class="detailGrid">
<div class="detailBox"><h4>Kunde</h4><p><b>${esc(c.name||orderCustomer(o))}</b><br>${esc(c.street||'')}<br>${esc(c.zip||'')} ${esc(c.city||'')}<br>${esc(c.email||'')}<br>${esc(c.phone||'')}</p>${o.sales_rep_name?`<div class="customerOwnerBadge"><small>FESTER KUNDENBERATER</small><b>${esc(o.sales_rep_name)}</b></div>`:''}${o.customer_id?`<button class="miniBtn" id="orderEditCustomer">Kundenakte öffnen</button>`:''}</div>
<div class="detailBox"><h4>Auftragsstatus</h4>${edit?`<div class="formGrid"><label>Status<select id="moStatus">${statusOptions(state.order_statuses,o.status)}</select></label><label>Zahlung<select id="moPayment"><option value="pending" ${sel(o.payment_status,'pending')}>offen</option><option value="paid" ${sel(o.payment_status,'paid')}>bezahlt</option><option value="refunded" ${sel(o.payment_status,'refunded')}>erstattet</option><option value="cancelled" ${sel(o.payment_status,'cancelled')}>storniert</option></select></label><label>Versanddienst<input id="moCarrier" value="${esc(o.carrier||'DPD')}"></label><label>Tracking<input id="moTracking" value="${esc(o.tracking||'')}"></label>${admin?`<label>Verkaufsherkunft<select id="moSalesChannel">${channels.map(x=>`<option value="${x}" ${sel(o.sales_channel,x)}>${salesChannelLabel(x)}</option>`).join('')}</select></label><label>Provisionsempfänger<select id="moCommissionRep"><option value="">Nicht zugeordnet</option>${(state.sales_reps||[]).filter(r=>r.status==='active'||r.id===o.commission_sales_rep_id).map(r=>`<option value="${esc(r.id)}" ${sel(o.commission_sales_rep_id||o.sales_rep_id,r.id)}>${esc(r.employee_no||'')} · ${esc(r.name)}</option>`).join('')}</select></label>`:''}</div>`:`<p>Status: <b>${esc(state.order_statuses?.[o.status]||o.status)}</b><br>Zahlung: <b>${o.payment_status==='paid'?'Bezahlt':'Offen'}</b><br>Versanddienst: <b>${esc(o.carrier||'DPD')}</b><br>Tracking: <b>${esc(o.tracking||'–')}</b></p>`}</div>
<div class="detailBox full"><h4>Artikel</h4><table class="detailItems">${(o.items||[]).map(x=>`<tr><td>${Number(x.qty||1)} × ${esc(x.name||x.id)}${x.article_no?`<br><small>Art.-Nr. ${esc(x.article_no)}</small>`:''}${itemMeta(x)?`<br><small>${esc(itemMeta(x))}</small>`:''}</td><td>${money(x.unit_price||x.price||0)}</td><td>${money((x.unit_price||x.price||0)*(x.qty||1))}</td></tr>`).join('')}</table><div class="detailTotals"><div><span>Warenwert</span><b>${money(tot.subtotal||0)}</b></div><div><span>Versand</span><b>${money(tot.shipping||0)}</b></div><div><span>inkl. MwSt.</span><b>${money(tot.vat||0)}</b></div>${Number(tot.shipping_weight_g||0)>0?`<div><span>Versandgewicht</span><b>${weightLabel(tot.shipping_weight_g)}</b></div>`:''}<div class="grand"><span>Gesamt</span><b>${money(tot.gross||orderTotal(o))}</b></div></div></div>
<div class="detailBox full"><h4>Interne Notiz</h4>${edit?`<textarea id="moNote" style="width:100%;min-height:80px;border:1px solid #ded5cb;border-radius:8px;padding:9px">${esc(o.internal_note||'')}</textarea>`:`<p>${esc(o.internal_note||'Keine Notiz').replace(/\n/g,'<br>')}</p>`}</div>
${(o.history||[]).length?`<div class="detailBox full"><h4>Verlauf</h4><div class="history">${[...(o.history||[])].reverse().slice(0,14).map(h=>`<div><time>${dt(h.at)}</time><b>${esc(h.type||'')}</b><span>${esc(h.value||'')}</span></div>`).join('')}</div></div>`:''}
</div><div class="formActions"><button class="btn" id="invoiceDocBtn">Rechnung</button><button class="btn" id="deliveryDocBtn">Lieferschein</button>${edit?'<button class="btn primary" id="saveOrderBtn">Änderungen speichern</button>':''}</div></div>`);
 if($('#orderEditCustomer'))$('#orderEditCustomer').onclick=()=>{closeModal();if(o.customer_id)openCustomer(o.customer_id)};
 $('#invoiceDocBtn').onclick=()=>openDocumentForOrder(o.order_no||o.id,'invoice');$('#deliveryDocBtn').onclick=()=>openDocumentForOrder(o.order_no||o.id,'delivery_note');
 if(edit&&$('#saveOrderBtn'))$('#saveOrderBtn').onclick=async()=>{try{const payload={action:'order_update',id:o.order_no||o.id,status:$('#moStatus').value,payment_status:$('#moPayment').value,carrier:$('#moCarrier').value,tracking:$('#moTracking').value,internal_note:$('#moNote').value};if(admin){payload.sales_channel=$('#moSalesChannel').value;payload.commission_sales_rep_id=$('#moCommissionRep').value}await api(payload);toast('Bestellung und Verkaufszuordnung aktualisiert');closeModal();await load(true)}catch(e){toast(e.message,true)}}
}
function statusOptions(labels,current){return Object.entries(labels||{}).map(([k,v])=>`<option value="${k}" ${sel(current,k)}>${esc(v)}</option>`).join('')}
function sel(a,b){return a===b?'selected':''}

function productionItemSummary(o){const items=o.items||[];if(!items.length)return'–';const first=items.slice(0,2).map(x=>`${Number(x.qty||1)} × ${esc(x.name||x.id)}`).join('<br>');return first+(items.length>2?`<small class="tableSub">+ ${items.length-2} weitere Position(en)</small>`:'')}
function productionStageSelect(o){if(!can('edit_orders'))return status(o.status);return `<select class="productionStatusSelect" data-production-status="${esc(o.order_no||o.id)}">${statusOptions(state.order_statuses,o.status)}</select>`}
function renderProductionLegacyV57(){const stages=[['new','Neu'],['paid','Bezahlt'],['production','Fertigung'],['quality','Qualitätskontrolle'],['packing','Verpacken'],['ready','Versandbereit'],['shipped','Versendet']];const orders=(state.orders||[]).filter(o=>!['complete','cancelled','payment_pending'].includes(o.status));const kpis=[['Offen',orders.filter(o=>['new','paid'].includes(o.status)).length],['In Fertigung',orders.filter(o=>o.status==='production').length],['Qualitätsprüfung',orders.filter(o=>o.status==='quality').length],['Versandbereit',orders.filter(o=>o.status==='ready').length]];ws.innerHTML=`<div class="sectionHead"><div><h2>Produktion</h2><p>Aufträge im Querformat bearbeiten – Status, Artikel, Kunde und Bearbeitung auf einen Blick.</p></div><div class="sectionActions"><button class="btn ${productionViewMode==='table'?'primary':''}" id="productionTableView">▤ Tabelle</button><button class="btn ${productionViewMode==='board'?'primary':''}" id="productionBoardView">▦ Board</button></div></div><div class="productionKpis">${kpis.map(x=>`<div><small>${x[0]}</small><strong>${x[1]}</strong></div>`).join('')}</div>${productionViewMode==='table'?`<div class="panel tableWrap productionLandscape"><table class="dataTable productionTable"><thead><tr><th>Auftrag</th><th>Kunde</th><th>Artikel / Menge</th><th>Zahlung</th><th>Produktionsstatus</th><th>Berater</th><th>Auftrag vom</th><th>Gesamt</th><th>Bearbeiten</th></tr></thead><tbody>${orders.length?orders.map(o=>`<tr><td><button class="rowLink" data-open-order="${esc(o.order_no||o.id)}">${esc(o.order_no||o.id)}</button></td><td class="customerCell"><b>${esc(orderCustomer(o))}</b><small>${esc(o.customer?.company||orderEmail(o)||'')}</small></td><td class="productionItems">${productionItemSummary(o)}</td><td>${pay(o.payment_status||'pending')}</td><td>${productionStageSelect(o)}</td><td>${esc(repName(o.sales_rep_id))}</td><td>${dt(o.created_at)}</td><td class="money">${money(orderTotal(o))}</td><td><button class="miniBtn productionEditBtn" data-open-order="${esc(o.order_no||o.id)}">Bearbeiten</button></td></tr>`).join(''):`<tr><td colspan="9"><div class="empty"><b>Keine Produktionsaufträge</b>Aktuell sind keine offenen Fertigungsaufträge vorhanden.</div></td></tr>`}</tbody></table></div>`:`<div class="kanban">${stages.map(([st,l])=>`<section class="kanCol" data-drop-status="${st}"><div class="kanHead"><b>${l}</b><span>${orders.filter(o=>o.status===st).length}</span></div><div class="kanBody">${orders.filter(o=>o.status===st).map(kanCard).join('')}</div></section>`).join('')}</div>`}`;$('#productionTableView').onclick=()=>{productionViewMode='table';renderProduction()};$('#productionBoardView').onclick=()=>{productionViewMode='board';renderProduction()};if(productionViewMode==='table'&&can('edit_orders'))$$('[data-production-status]').forEach(el=>el.onchange=async()=>{el.disabled=true;try{await api({action:'order_update',id:el.dataset.productionStatus,status:el.value});toast('Produktionsstatus gespeichert');await load(true);if(current==='production')renderProduction()}catch(e){toast(e.message,true);el.disabled=false}});if(productionViewMode==='board'){if(can('edit_orders'))initKanban();else $$('.kanCard').forEach(c=>{c.draggable=false;c.ondblclick=()=>openOrder(c.dataset.orderId)})}}
function kanCard(o){return `<article class="kanCard" draggable="true" data-order-id="${esc(o.order_no||o.id)}"><b>${esc(o.order_no||o.id)}</b><strong>${esc(orderCustomer(o))}</strong><small>${itemCount(o)} Position(en) · ${money(orderTotal(o))}</small><footer><span>${donly(o.created_at)}</span>${pay(o.payment_status||'pending')}</footer></article>`}
function initKanban(){let dragged='';$$('.kanCard').forEach(c=>{c.ondragstart=()=>{dragged=c.dataset.orderId;c.classList.add('dragging')};c.ondragend=()=>{c.classList.remove('dragging');$$('.kanCol').forEach(x=>x.classList.remove('dragOver'))};c.ondblclick=()=>openOrder(c.dataset.orderId)});$$('.kanCol').forEach(col=>{col.ondragover=e=>{e.preventDefault();col.classList.add('dragOver')};col.ondragleave=()=>col.classList.remove('dragOver');col.ondrop=async e=>{e.preventDefault();col.classList.remove('dragOver');if(!dragged)return;try{await api({action:'order_update',id:dragged,status:col.dataset.dropStatus});toast('Produktionsstatus aktualisiert');await load(true)}catch(err){toast(err.message,true)}}})}

function renderPrototypes(){const q=(searchTerm||'').toLowerCase();const rows=(state.prototypes||[]).filter(p=>!q||`${p.reference} ${p.customer?.name||p.fields?.customer_name||''} ${p.project_name||p.fields?.project_name||''}`.toLowerCase().includes(q));ws.innerHTML=`<div class="sectionHead"><div><h2>Prototypen</h2><p>149-€-Projekte von Anfrage bis Fertigstellung steuern.</p></div></div><div class="panel tableWrap">${rows.length?`<table class="dataTable"><thead><tr><th>Projekt</th><th>Kunde</th><th>Bezeichnung</th><th>Status</th><th>Zahlung</th><th>Datum</th><th></th></tr></thead><tbody>${rows.map(p=>`<tr><td><button class="rowLink" data-open-proto="${esc(p.reference||p.id)}">${esc(p.reference||p.id)}</button></td><td>${esc(p.customer?.name||p.fields?.customer_name||'–')}</td><td>${esc(p.project_name||p.fields?.project_name||'–')}</td><td>${status(p.status,state.prototype_statuses)}</td><td>${pay(p.payment_status||'pending')}</td><td>${dt(p.created_at)}</td><td><button class="miniBtn" data-open-proto="${esc(p.reference||p.id)}">Öffnen</button></td></tr>`).join('')}</tbody></table>`:'<div class="empty"><b>Noch keine Prototypen</b>Übermittelte Prototypenanfragen erscheinen automatisch.</div>'}</div>`}
function openPrototype(id){const p=(state.prototypes||[]).find(x=>(x.reference||x.id)===id);if(!p)return;const f=p.fields||{},c=p.customer||{},edit=can('edit_prototypes');openModal(`<div class="modalHead"><small>PROTOTYP-PROJEKT</small><h2>${esc(p.reference||p.id)}</h2><p>${esc(p.project_name||f.project_name||'Sonderanfertigung')} · ${dt(p.created_at)}</p></div><div class="modalBody"><div class="detailGrid"><div class="detailBox"><h4>Kunde</h4><p><b>${esc(c.name||f.customer_name||'–')}</b><br>${esc(c.email||f.email||'')}<br>${esc(c.phone||f.phone||'')}<br>${esc(f.street||'')}<br>${esc(f.zip||'')} ${esc(f.city||'')}</p></div><div class="detailBox"><h4>Bearbeitung</h4>${edit?`<div class="formGrid"><label>Status<select id="mpStatus">${statusOptions(state.prototype_statuses,p.status)}</select></label><label>Tracking<input id="mpTracking" value="${esc(p.tracking||'')}"></label></div>`:`<p>Status: <b>${esc(state.prototype_statuses?.[p.status]||p.status)}</b><br>Tracking: <b>${esc(p.tracking||'–')}</b></p>`}</div><div class="detailBox full"><h4>Projektangaben</h4><p>${esc(p.summary||f.summary||'Keine Zusammenfassung vorhanden.').replace(/\n/g,'<br>')}</p></div>${(p.files||[]).length?`<div class="detailBox full"><h4>Dateien</h4>${p.files.map(x=>`<div>${esc(x.original||x.stored)} · ${Math.round((x.size||0)/1024)} KB</div>`).join('')}</div>`:''}<div class="detailBox full"><h4>Interne Notiz</h4>${edit?`<textarea id="mpNote" style="width:100%;min-height:85px;border:1px solid #ded5cb;border-radius:8px;padding:9px">${esc(p.internal_note||'')}</textarea>`:`<p>${esc(p.internal_note||'Keine Notiz').replace(/\n/g,'<br>')}</p>`}</div></div>${edit?'<div class="formActions"><button class="btn primary" id="saveProtoBtn">Projekt aktualisieren</button></div>':''}</div>`);if(edit&&$('#saveProtoBtn'))$('#saveProtoBtn').onclick=async()=>{try{await api({action:'prototype_update',id:p.reference||p.id,status:$('#mpStatus').value,tracking:$('#mpTracking').value,internal_note:$('#mpNote').value});toast('Prototyp aktualisiert');closeModal();await load(true)}catch(e){toast(e.message,true)}}}
function repName(id){if(!id)return'–';const r=(state.sales_reps||[]).find(x=>x.id===id);if(r)return r.name;if(String(state.current_user?.sales_rep_id||'')===String(id))return state.current_user?.display_name||'Eigener Kundenberater';return'–'}
function salesChannelLabel(v){return ({online:'Online-Shop',advisor:'Kundenberater',telephone:'Telefonleitung',email:'E-Mail',walk_in:'Vor Ort / Abholung',marketplace:'An- & Verkaufen',other:'Sonstiges',shop:'Online-Shop',field:'Kundenberater',phone:'Telefonleitung'})[v]||v||'Sonstiges'}
function commissionRepName(o){return o?.commission_sales_rep_name||repName(o?.commission_sales_rep_id||o?.sales_rep_id)}
function attributionLabel(o){return o?.commission_attribution_label||({returning_customer:'Wiederkehrender Beraterkunde',direct_advisor:'Direkter Kundenberater-Verkauf',house_rotation:'Hausverteilung ohne Beraterkontakt',admin_override:'Manuell geprüft / korrigiert',legacy_assigned:'Bestehende Beraterzuordnung',legacy_unassigned:'Altauftrag ohne eindeutige Zuordnung'})[o?.commission_attribution]||'Noch nicht zugeordnet'}
function salesOriginTag(o){return `<span class="salesOriginTag origin-${esc(o?.sales_channel||'other')}">${esc(o?.sales_channel_label||salesChannelLabel(o?.sales_channel||o?.source))}</span>`}
function commissionCreditTag(o){const n=commissionRepName(o);return `<span class="commissionCreditTag"><b>${esc(n)}</b><small>${esc(attributionLabel(o))}</small></span>`}
function customerOwnerName(c){return c?.sales_rep_name||repName(c?.sales_rep_id)}
function isMyCustomer(c){return !!c?.sales_rep_id&&String(c.sales_rep_id)===String(state.current_user?.sales_rep_id||'')}
function repOptions(current=''){if(!isAdmin()){const rid=state.current_user?.sales_rep_id||current||'';return rid?`<option value="${esc(rid)}" selected>Eigener Kundenberater · ${esc(state.current_user?.display_name||'')}</option>`:'<option value="">Nicht zugeordnet</option>'}return `<option value="">Nicht zugeordnet</option>`+(state.sales_reps||[]).filter(r=>(r.status==='active'||r.id===current)&&!String(r.employee_no||'').startsWith('ZENTRALE-')).map(r=>`<option value="${esc(r.id)}" ${sel(current,r.id)}>${esc(r.employee_no)} · ${esc(r.name)}</option>`).join('')}
function splitCustomerStreet(value){const raw=String(value||'').trim();if(!raw)return{street:'',house:''};const m=raw.match(/^(.*?)[,\s]+(\d+[a-zA-Z]?(?:\s*[\/-]\s*\d+[a-zA-Z]?)?)$/);return m?{street:m[1].trim(),house:m[2].replace(/\s+/g,'')}:{street:raw,house:''}}
function customerStreetValue(){const street=$('#cuStreetName')?.value.trim()||'',house=$('#cuHouseNo')?.value.trim()||'';return [street,house].filter(Boolean).join(' ').trim()}
function customerAddressValue(){return [customerStreetValue(),$('#cuZip')?.value.trim()||'',($('#cuCitySelect')?.value||$('#cuCityManual')?.value||'').trim()].filter(Boolean).join(', ')}
function googleMapsEmbedUrl(address){return 'https://www.google.com/maps?q='+encodeURIComponent(address)+'&output=embed'}
function googleMapsSearchUrl(address){return 'https://www.google.com/maps/search/?api=1&query='+encodeURIComponent(address)}
function customerMapHtml(address,emptyText='Adresse auswählen – die Karte aktualisiert sich automatisch.'){if(!address)return `<div class="customerMapEmpty"><b>Google Maps</b><span>${esc(emptyText)}</span></div>`;return `<iframe title="Google Maps · Kundenadresse" loading="lazy" referrerpolicy="no-referrer-when-downgrade" src="${googleMapsEmbedUrl(address)}"></iframe>`}
function renderCustomers(){const q=(searchTerm||'').toLowerCase();const rows=(state.customers||[]).filter(c=>!q||`${c.name} ${c.email||''} ${c.phone||''} ${c.street||''} ${c.zip||''} ${c.city||''} ${c.company||''} ${customerOwnerName(c)}`.toLowerCase().includes(q));ws.innerHTML=`<div class="sectionHead"><div><h2>Kunden</h2><p>Kundenakte, festen Kundenberater und Bestellhistorie transparent verwalten.</p></div><div class="sectionActions"><b class="muted tiny">${rows.length} Kunden</b><button class="btn" id="customerSales">Produktberatung</button>${can('edit_customers')?'<button class="btn primary" id="newCustomer">+ Kunde anlegen</button>':''}</div></div>${!isAdmin()?'<div class="moduleNotice"><b>Kundenberater-Zuordnung:</b> Wiederkehrende Online-, Telefon- oder E-Mail-Bestellungen deiner bereits betreuten Kunden werden deiner vollen Provisionsabrechnung gutgeschrieben.</div>':''}<div class="customerModuleHint"><div><b>Kundenbeziehung wird geschützt</b><span>Ein bereits zugeordneter Kundenberater bleibt auch bei späteren Onlinekäufen provisionsberechtigt.</span></div><strong>V69 · Kundenstamm</strong></div><div class="panel tableWrap">${rows.length?`<table class="dataTable customerTable"><thead><tr><th>Kunde</th><th>Firma</th><th>Kontakt</th><th>Adresse</th><th>Kundenberater</th><th>Bestellungen</th><th></th></tr></thead><tbody>${rows.map(c=>{const addr=[c.street,c.zip,c.city].filter(Boolean).join(', ');return `<tr class="${isMyCustomer(c)?'myCustomerRow':''}"><td class="customerCell"><b>${esc(c.name||'–')}</b><small>${esc(c.id)}</small>${isMyCustomer(c)?'<span class="myCustomerBadge">Mein Kundenstamm</span>':''}</td><td>${esc(c.company||'–')}</td><td>${esc(c.email||'–')}<br><small>${esc(c.phone||'')}</small></td><td class="customerAddressCell"><b>${esc(c.street||'–')}</b><small>${esc(c.zip||'')} ${esc(c.city||'')}</small>${addr?`<button class="mapMiniBtn" type="button" data-map-customer="${esc(c.id)}">⌖ Maps</button>`:''}</td><td><b>${esc(customerOwnerName(c))}</b>${c.advisor_assigned_at?`<small class="tableSub">seit ${donly(c.advisor_assigned_at)}</small>`:''}</td><td>${(c.orders||[]).length}</td><td class="rowActions">${can('edit_appointments')&&(isAdmin()||isMyCustomer(c))?`<button class="miniBtn primaryMini" data-customer-appointment="${esc(c.id)}">Termin</button>`:''}${can('create_orders')?`<button class="miniBtn" data-new-order="${esc(c.id)}">Bestellung</button>`:''}<button class="miniBtn" data-open-customer="${esc(c.id)}">Akte</button></td></tr>`}).join('')}</tbody></table>`:'<div class="empty"><b>Noch keine Kundenakten</b>Lege den ersten Kunden direkt hier an.</div>'}</div>`;if($('#newCustomer'))$('#newCustomer').onclick=()=>customerModal(null);$('#customerSales').onclick=()=>setView('sales');$$('[data-map-customer]').forEach(btn=>btn.onclick=e=>{e.stopPropagation();const c=(state.customers||[]).find(x=>x.id===btn.dataset.mapCustomer);if(!c)return;const a=[c.street,c.zip,c.city].filter(Boolean).join(', ');if(a)window.open(googleMapsSearchUrl(a),'_blank','noopener')})}
function customerModal(id){if(!can('edit_customers'))return toast('Kundendaten dürfen mit diesem Zugang nicht verändert werden.',true);const c=(state.customers||[]).find(x=>x.id===id)||{},nl=['pending','confirmed'].includes(c.newsletter_status||'none'),split=splitCustomerStreet(c.street||'');openModal(`<div class="modalHead customerModalHead"><small>KUNDENSTAMM · ADRESS-ASSISTENT</small><h2>${id?'Kunde bearbeiten':'Kunde anlegen'}</h2><p>${id?esc(c.id):'Neue Kundenakte · Adresse wird Schritt für Schritt vervollständigt'}</p></div><div class="modalBody customerModalBody">
<section class="customerContactPanel"><div class="customerPanelTitle"><div><small>1 · KONTAKT</small><h3>Stammdaten</h3></div><span>Name, Firma und Erreichbarkeit</span></div><div class="formGrid"><label>Name / Ansprechpartner<input id="cuName" value="${esc(c.name||'')}" required autocomplete="name"></label><label>Firma<input id="cuCompany" value="${esc(c.company||'')}" autocomplete="organization"></label><label>E-Mail · wichtig für Belege & Newsletter<input id="cuEmail" type="email" value="${esc(c.email||'')}" autocomplete="email"></label><label>Telefon<input id="cuPhone" value="${esc(c.phone||'')}" autocomplete="tel"></label>${isAdmin()?`<label class="full">Zuständiger Außendienst<select id="cuRep">${repOptions(c.sales_rep_id||'')}</select></label>`:''}</div></section>
<section class="customerAddressAssistant"><div class="customerPanelTitle"><div><small>2 · ADRESSE</small><h3>PLZ → Ort → Straße</h3></div><span id="cuAddressStatus" class="addressStatus neutral">Bereit</span></div><p class="addressLead">Gib zuerst die 5-stellige Postleitzahl ein. Das Orgaboard lädt passende Orte. Nach der Ortsauswahl werden die Straßen für diesen Ort angeboten.</p><div class="addressWizardGrid"><label><span class="addressStepNo">1</span><b>PLZ</b><input id="cuZip" value="${esc(c.zip||'')}" maxlength="5" inputmode="numeric" autocomplete="postal-code" placeholder="z. B. 24340"><small id="cuZipHint">5 Ziffern eingeben</small></label><label><span class="addressStepNo">2</span><b>Ort auswählen</b><select id="cuCitySelect"><option value="${esc(c.city||'')}">${esc(c.city||'Bitte zuerst PLZ eingeben')}</option></select><input id="cuCityManual" class="addressManualInput" value="${esc(c.city||'')}" autocomplete="address-level2" placeholder="Ort manuell eingeben"><small id="cuCityHint">Ort wird aus der PLZ ermittelt</small></label><label class="addressStreetField"><span class="addressStepNo">3</span><b>Straße auswählen / suchen</b><input id="cuStreetName" value="${esc(split.street)}" list="cuStreetList" autocomplete="off" placeholder="Straße auswählen oder tippen"><datalist id="cuStreetList"></datalist><small id="cuStreetHint">Nach Ortsauswahl werden Straßen geladen</small></label><label class="houseNumberField"><span class="addressStepNo">4</span><b>Hausnr.</b><input id="cuHouseNo" value="${esc(split.house)}" autocomplete="address-line2" placeholder="z. B. 5a"><small>für genaue Position</small></label></div><div class="addressPreview"><div><small>ADRESSVORSCHAU</small><strong id="cuAddressPreview">Noch keine vollständige Adresse</strong></div><button class="btn" type="button" id="cuReloadAddress">Adresse neu laden</button></div></section>
<section class="customerMapPanel"><div class="customerPanelTitle"><div><small>3 · GOOGLE MAPS</small><h3>Standort direkt prüfen</h3></div><a id="cuMapExternal" class="textLink mapExternalBtn" target="_blank" rel="noopener">In Google Maps öffnen ↗</a></div><div id="cuMapBox" class="mapBox customerMapBox">${customerMapHtml([c.street,c.zip,c.city].filter(Boolean).join(', '))}</div><div class="mapPrecision" id="cuMapPrecision"><b>Positionsprüfung:</b> ${c.street&&c.zip&&c.city?'Adresse vorhanden. Mit Hausnummer ist die Position am genauesten.':'PLZ, Ort und Straße auswählen.'}</div></section>
<section class="customerExtraPanel"><div class="formGrid"><label class="full newsletterCheck"><input id="cuNewsletter" type="checkbox" ${nl?'checked':''}><span><b>Newsletter anfragen</b><small>Der Kunde erhält eine Bestätigungs-E-Mail (Double-Opt-In). Ohne Bestätigung wird nichts versendet.</small></span></label><label class="full">Interne Notiz<textarea id="cuNotes">${esc(c.notes||'')}</textarea></label></div></section>
<section class="customerVerifyBox"><div><h3>Kontaktprüfung</h3><p>Telefonbuch kann bei Bedarf separat geöffnet werden. Die Maps-Ansicht aktualisiert sich automatisch mit der eingegebenen Adresse.</p></div><div class="customerVerifyActions"><button class="btn" id="cuPhonebook">Im Telefonbuch prüfen ↗</button>${id?'<button class="btn" id="cuVerified">Als geprüft markieren</button>':''}</div>${c.contact_verified_at?`<div class="verifiedStamp">✓ Zuletzt geprüft: ${dt(c.contact_verified_at)} · ${esc(c.contact_verified_by||'')}</div>`:''}</section>
<div class="formActions customerSaveBar"><button class="btn" id="cancelCustomer">Abbrechen</button><button class="btn primary" id="saveCustomer">Kunde speichern</button></div></div>`);
const citySelect=$('#cuCitySelect'),cityManual=$('#cuCityManual'),streetInput=$('#cuStreetName'),streetList=$('#cuStreetList'),zipInput=$('#cuZip'),statusEl=$('#cuAddressStatus'),preview=$('#cuAddressPreview'),mapBox=$('#cuMapBox'),mapLink=$('#cuMapExternal'),streetHint=$('#cuStreetHint'),zipHint=$('#cuZipHint'),cityHint=$('#cuCityHint'),precision=$('#cuMapPrecision');let streetNames=new Set(),streetToken=0,zipToken=0,mapTimer=0,streetSearchTimer=0,currentCity=c.city||'';
const selectedCity=()=>{const v=citySelect.value;return v==='__manual__'?cityManual.value.trim():v.trim()};
const setStatus=(text,kind='neutral')=>{statusEl.textContent=text;statusEl.className='addressStatus '+kind};
const updatePreviewAndMap=(immediate=false)=>{const a=[customerStreetValue(),zipInput.value.trim(),selectedCity()].filter(Boolean).join(', ');preview.textContent=a||'Noch keine vollständige Adresse';mapLink.href=a?googleMapsSearchUrl(a):'#';mapLink.classList.toggle('disabled',!a);precision.innerHTML=`<b>Positionsprüfung:</b> ${streetInput.value.trim()&&zipInput.value.trim().length===5&&selectedCity()&&$('#cuHouseNo').value.trim()?'Vollständige Adresse – Google Maps zeigt den Zielpunkt.':streetInput.value.trim()&&zipInput.value.trim().length===5&&selectedCity()?'Straße gefunden – Hausnummer ergänzen, damit der Zielpunkt genauer wird.':'PLZ, Ort und Straße vervollständigen.'}`;clearTimeout(mapTimer);const draw=()=>{mapBox.innerHTML=customerMapHtml(a)};if(immediate)draw();else mapTimer=setTimeout(draw,500)};
const renderStreetList=()=>{streetList.innerHTML=[...streetNames].sort((a,b)=>a.localeCompare(b,'de')).map(n=>`<option value="${esc(n)}"></option>`).join('')};
const loadStreetPage=async(page=1,term='',token=streetToken)=>{const d=await api({action:'address_streets',zip:zipInput.value.trim(),city:selectedCity(),term,page});if(token!==streetToken)return null;(d.streets||[]).forEach(x=>streetNames.add(String(x.name||x).trim()));renderStreetList();return d};
const loadStreets=async(reset=true)=>{const zip=zipInput.value.trim(),city=selectedCity();if(zip.length!==5||!city||city==='__manual__')return;if(reset){streetNames=new Set();renderStreetList()}const token=++streetToken;streetHint.textContent='Straßen werden geladen …';setStatus('Straßen laden','loading');try{let total=0,capped=false;for(let page=1;page<=8;page++){const d=await loadStreetPage(page,'',token);if(!d||token!==streetToken)return;const n=(d.streets||[]).length;total+=n;if(n<50)break;if(page===8)capped=true}streetHint.textContent=streetNames.size?`${streetNames.size} Straße${streetNames.size===1?'':'n'} geladen${capped?' · bei Bedarf Anfangsbuchstaben tippen':''}`:'Keine Straßenliste gefunden · Eingabe bleibt manuell möglich';setStatus(streetNames.size?'Straßen bereit':'Manuelle Eingabe','good')}catch(e){streetHint.textContent='Straßendienst nicht erreichbar · Straße kann manuell eingetragen werden';setStatus('Manuell möglich','warn')}};
const loadPlaces=async(force=false)=>{const zip=zipInput.value.replace(/\D/g,'').slice(0,5);zipInput.value=zip;if(zip.length!==5){zipHint.textContent='Bitte 5 Ziffern eingeben';cityHint.textContent='Ort wird aus der PLZ ermittelt';setStatus('PLZ unvollständig','neutral');return}const token=++zipToken;zipHint.textContent='Orte werden gesucht …';setStatus('PLZ prüfen','loading');try{const d=await api({action:'address_localities',zip});if(token!==zipToken)return;const places=(d.places||[]).filter(x=>x&&x.name);const names=[...new Set(places.map(x=>String(x.name).trim()))];const previous=currentCity||selectedCity();citySelect.innerHTML='<option value="">Ort auswählen …</option>'+names.map(n=>`<option value="${esc(n)}">${esc(n)}</option>`).join('')+'<option value="__manual__">Ort manuell eingeben …</option>';if(previous&&names.includes(previous))citySelect.value=previous;else if(names.length===1)citySelect.value=names[0];else if(previous){citySelect.insertAdjacentHTML('beforeend',`<option value="${esc(previous)}">${esc(previous)} · gespeichert</option>`);citySelect.value=previous}else citySelect.value='';currentCity=selectedCity();cityManual.classList.toggle('show',citySelect.value==='__manual__');zipHint.textContent=names.length?`${names.length} Ort${names.length===1?'':'e'} gefunden`:'Kein Ort automatisch gefunden';cityHint.textContent=names.length>1?'Mehrere Orte möglich – bitte auswählen':names.length===1?'Ort automatisch ausgewählt':'Ort manuell eingeben';setStatus(names.length?'Ort gefunden':'Ort manuell','good');if(selectedCity())await loadStreets(true);updatePreviewAndMap(true)}catch(e){citySelect.innerHTML=`<option value="__manual__">Ort manuell eingeben …</option>`;citySelect.value='__manual__';cityManual.classList.add('show');zipHint.textContent='Adressdienst nicht erreichbar';cityHint.textContent='Ort kann weiterhin manuell eingetragen werden';setStatus('Manuelle Eingabe','warn');updatePreviewAndMap(true)}};
zipInput.addEventListener('input',()=>{currentCity='';streetNames=new Set();renderStreetList();clearTimeout(zipInput._tm);zipInput._tm=setTimeout(()=>{if(zipInput.value.replace(/\D/g,'').length===5)loadPlaces()},350);updatePreviewAndMap()});zipInput.addEventListener('blur',()=>{if(zipInput.value.replace(/\D/g,'').length===5)loadPlaces()});citySelect.addEventListener('change',async()=>{cityManual.classList.toggle('show',citySelect.value==='__manual__');currentCity=selectedCity();if(citySelect.value!=='__manual__'&&selectedCity())await loadStreets(true);updatePreviewAndMap(true)});cityManual.addEventListener('input',()=>{currentCity=cityManual.value.trim();updatePreviewAndMap()});streetInput.addEventListener('input',()=>{updatePreviewAndMap();const term=streetInput.value.trim();clearTimeout(streetSearchTimer);if(term.length>=2&&zipInput.value.trim().length===5&&selectedCity())streetSearchTimer=setTimeout(async()=>{const token=streetToken;try{await loadStreetPage(1,term,token);streetHint.textContent=`${streetNames.size} passende/geladene Straßen · auswählen oder weitertippen`}catch(e){}},350)});$('#cuHouseNo').addEventListener('input',()=>updatePreviewAndMap());$('#cuReloadAddress').onclick=()=>loadPlaces(true);$('#cancelCustomer').onclick=closeModal;$('#cuPhonebook').onclick=()=>{const q=[$('#cuName').value,$('#cuCompany').value,selectedCity()].filter(Boolean).join(' ');window.open('https://www.dastelefonbuch.de/Suche/'+encodeURIComponent(q||$('#cuPhone').value),'_blank','noopener')};if($('#cuVerified'))$('#cuVerified').onclick=async()=>{try{const note=prompt('Optionaler Prüfhinweis:','Adresse / Kontaktdaten manuell geprüft')||'';await api({action:'customer_verify',id:c.id,note});toast('Kontaktprüfung gespeichert');closeModal();await load(true);customerModal(c.id)}catch(e){toast(e.message,true)}};$('#saveCustomer').onclick=async()=>{try{const city=selectedCity();if(!$('#cuName').value.trim())return toast('Bitte Name / Ansprechpartner eintragen.',true);if(zipInput.value.trim()&&zipInput.value.trim().length!==5)return toast('Die PLZ muss 5-stellig sein.',true);if(zipInput.value.trim()&&!city)return toast('Bitte einen Ort auswählen oder manuell eintragen.',true);const d=await api({action:'customer_save',id:c.id||'',name:$('#cuName').value,company:$('#cuCompany').value,email:$('#cuEmail').value,phone:$('#cuPhone').value,street:customerStreetValue(),zip:zipInput.value.trim(),city,sales_rep_id:isAdmin()?$('#cuRep').value:(c.sales_rep_id||''),newsletter_opt_in:$('#cuNewsletter').checked,notes:$('#cuNotes').value});toast(d.newsletter_mail_sent?'Kunde gespeichert · Bestätigungsmail versendet':'Kunde gespeichert');closeModal();if(!id&&d.customer?.id)salesCustomerId=d.customer.id;await load(true);if(current==='customers')renderCustomers()}catch(e){toast(e.message,true)}};cityManual.classList.toggle('show',!c.city);if(c.zip&&String(c.zip).length===5)loadPlaces();else updatePreviewAndMap(true)}

function openCustomer(id){const c=(state.customers||[]).find(x=>x.id===id);if(!c)return;const orders=(state.orders||[]).filter(o=>(c.orders||[]).includes(o.order_no||o.id)||(c.email&&orderEmail(o).toLowerCase()===String(c.email).toLowerCase()));const nl={confirmed:'Bestätigt',pending:'Bestätigung offen',unsubscribed:'Abgemeldet',none:'Nicht angemeldet'}[c.newsletter_status||'none']||'Nicht angemeldet',addr=[c.street,c.zip,c.city].filter(Boolean).join(', '),mapUrl=addr?googleMapsSearchUrl(addr):'';openModal(`<div class="modalHead"><small>KUNDENAKTE · STANDORT</small><h2>${esc(c.name||c.email||c.id)}</h2><p>${esc(c.id)}${c.email?' · '+esc(c.email):''}</p></div><div class="modalBody"><div class="customerDetailHero"><section class="customerIdentityCard"><small>KONTAKT & ADRESSE</small><h3>${esc(c.company||c.name||'Kunde')}</h3><p>${c.company?'<b>'+esc(c.name||'')+'</b><br>':''}${esc(c.street||'Keine Straße hinterlegt')}<br>${esc(c.zip||'')} ${esc(c.city||'')}<br><br>${esc(c.email||'Keine E-Mail')}<br>${esc(c.phone||'Kein Telefon')}</p>${mapUrl?`<a class="btn primary customerMapOpen" target="_blank" rel="noopener" href="${mapUrl}">Google Maps öffnen ↗</a>`:''}</section><section class="customerDetailMap"><div class="customerPanelTitle"><div><small>GOOGLE MAPS</small><h3>Adresse auf der Karte</h3></div><span>${addr?'Standortansicht':'Adresse fehlt'}</span></div><div class="mapBox customerMapBox detailMapBox">${customerMapHtml(addr,'Für diese Kundenakte ist noch keine vollständige Adresse hinterlegt.')}</div></section></div><div class="detailGrid customerFactsGrid"><div class="detailBox"><h4>Vertrieb</h4><p>Zuständig: <b>${esc(repName(c.sales_rep_id))}</b><br>Bestellungen: <b>${orders.length}</b><br>Umsatz: <b>${money(orders.reduce((s,o)=>s+orderTotal(o),0))}</b><br>Kunde seit: <b>${donly(c.created_at)}</b></p></div><div class="detailBox"><h4>Newsletter</h4><p>Status: <b>${esc(nl)}</b><br>${c.newsletter_confirmed_at?'Bestätigt: <b>'+dt(c.newsletter_confirmed_at)+'</b><br>':''}${c.newsletter_unsubscribed_at?'Abgemeldet: <b>'+dt(c.newsletter_unsubscribed_at)+'</b>':''}</p></div><div class="detailBox"><h4>Kontaktprüfung</h4><p>${c.contact_verified_at?'✓ Geprüft am <b>'+dt(c.contact_verified_at)+'</b><br>von '+esc(c.contact_verified_by||''):'Noch nicht manuell geprüft.'}</p></div><div class="detailBox"><h4>Adressqualität</h4><p>${c.street&&c.zip&&c.city?'<b>Vollständig hinterlegt</b><br>Google Maps kann direkt verwendet werden.':'<b>Unvollständig</b><br>Bitte PLZ, Ort und Straße ergänzen.'}</p></div><div class="detailBox full"><h4>Bestellungen</h4>${orders.length?orders.map(orderMini).join(''):'<p>Keine Bestellungen zugeordnet.</p>'}</div><div class="detailBox full"><h4>Interne Kundennotiz</h4><p>${esc(c.notes||'Keine Notiz vorhanden.').replace(/\n/g,'<br>')}</p></div></div><div class="formActions">${can('edit_customers')?'<button class="btn" id="editCustomer">Kundendaten bearbeiten</button>':''}<button class="btn orange" id="adviseCustomer">Produktberatung</button>${can('create_orders')?'<button class="btn primary" id="orderCustomerBtn">+ Bestellung</button>':''}</div></div>`);if($('#editCustomer'))$('#editCustomer').onclick=()=>customerModal(c.id);$('#adviseCustomer').onclick=()=>{salesCustomerId=c.id;closeModal();setView('sales')};if(can('create_orders')&&$('#orderCustomerBtn'))$('#orderCustomerBtn').onclick=()=>manualOrderModal(c.id,[],c.sales_rep_id||'')}
function customerLabel(c){return `${c.name||'–'}${c.company?' · '+c.company:''} · ${c.zip||''} ${c.city||''}`}
function activeCatalog(){return (state.catalog||[]).filter(p=>p.status==='active')}
function defaultHookLength(id){return ({std:'12 cm',aal:'12 cm',ultra:'20 cm',kralle:'18 cm',filet:'12 cm',doppel:'12 cm'})[id]||''}
function hookLengths(id){return ({std:['12 cm','14 cm','18 cm','20 cm','24 cm'],aal:['12 cm','14 cm','18 cm','20 cm','24 cm'],ultra:['20 cm','22 cm','24 cm'],kralle:['18 cm','20 cm','24 cm'],filet:['12 cm','14 cm','18 cm','20 cm','24 cm'],doppel:['12 cm','14 cm','18 cm','20 cm','24 cm']})[id]||[]}
function estimatePrice(p,meta={}){if(!p)return 0;if(p.type!=='hook')return Number(p.base||0);const lp={std:{'12 cm':12.90,'14 cm':14.80,'18 cm':15.80,'20 cm':16.80,'24 cm':18.80},aal:{'12 cm':12.90,'14 cm':14.80,'18 cm':15.80,'20 cm':16.80,'24 cm':18.80},kralle:{'18 cm':18.90,'20 cm':19.90,'24 cm':21.90},filet:{'12 cm':15.90,'14 cm':17.80,'18 cm':18.80,'20 cm':19.80,'24 cm':21.80},doppel:{'12 cm':15.90,'14 cm':17.80,'18 cm':18.80,'20 cm':19.80,'24 cm':21.80},ultra:{'20 cm':19.90,'22 cm':23.90,'24 cm':24.90}};let v=Number(lp[p.id]?.[meta.length]??p.base??0);v+=({VA:0,V2A:3.99,V4A:7.99})[meta.material||'VA']||0;if(meta.tip==='extra')v+=2;return Math.round(v*100)/100}
function recommendForNeeds(n){const rec=[];const add=id=>{if((state.catalog||[]).some(p=>p.id===id&&p.status==='active')&&!rec.includes(id))rec.push(id)};const purpose=n.purpose||'fish',fish=n.fish||'forelle',weight=Number(n.weight||2),form=n.form||'whole',priority=n.priority||'standard';if(purpose==='fish'){if(form==='filet')add('filet');else if(fish==='aal')add('aal');else if(weight>5||priority==='heavy')add('ultra');else if(priority==='secure')add('kralle');else add('std');if(fish==='aal')add('lauge-aal-0');else add('lauge-forelle-0');if(fish==='lachs')add('mehl-erle');else add('mehl-buche')}else if(purpose==='meat'){add('fleisch');add('mehl-buche');add('mehl-eiche')}else if(purpose==='smoke'){activeCatalog().filter(p=>String(p.category).toLowerCase().includes('räuchermehl')).slice(0,4).forEach(p=>add(p.id))}else if(purpose==='brine'){activeCatalog().filter(p=>String(p.category).toLowerCase().includes('räucherlauge')).slice(0,4).forEach(p=>add(p.id))}else{activeCatalog().slice(0,4).forEach(p=>add(p.id))}return rec.slice(0,6)}
function renderSalesCustomerResults(q=''){const host=$('#salesCustomerResults');if(!host)return;const x=q.toLowerCase().trim();const rows=(state.customers||[]).filter(c=>!x||`${c.name} ${c.company||''} ${c.phone||''} ${c.email||''} ${c.city||''}`.toLowerCase().includes(x)).slice(0,10);host.innerHTML=rows.length?rows.map(c=>`<button class="customerPick ${salesCustomerId===c.id?'selected':''}" data-pick-customer="${esc(c.id)}"><span><b>${esc(c.name)}</b><small>${esc(c.company||'Privatkunde')} · ${esc(c.phone||c.email||'kein Kontakt')}</small></span><em>${esc(c.zip||'')} ${esc(c.city||'')}</em></button>`).join(''):'<div class="empty compact"><b>Kein Kunde gefunden</b>Du kannst den Kunden direkt neu anlegen.</div>'}
function renderSales(){const c=(state.customers||[]).find(x=>x.id===salesCustomerId)||null;const needs=salesAdvice?.needs||{};const recs=(salesAdvice?.recs||[]).map(id=>(state.catalog||[]).find(p=>p.id===id)).filter(Boolean);ws.innerHTML=`<div class="sectionHead"><div><h2>Produktberatung</h2><p>Kundenbedarf aufnehmen und passende Artikel aus dem Sortiment empfehlen.</p></div><div class="sectionActions">${can('edit_customers')?'<button class="btn" id="salesNewCustomer">+ Kunde</button>':''}${can('create_orders')&&c?'<button class="btn primary" id="salesNewOrder">+ Bestellung für Kunden</button>':''}</div></div>${!isAdmin()?'<div class="moduleNotice"><b>Außendienst:</b> Welche Funktionen verfügbar sind, wird zentral durch die Administration festgelegt.</div>':''}<div class="salesLayout"><section class="panel salesCustomerPanel"><div class="panelTitle"><h3>1. Kunde auswählen</h3><span>Telefon, Name, Firma oder Ort</span></div><div class="panelPad"><input class="bigSearch" id="salesCustomerSearch" placeholder="Kunde suchen …"><div class="customerPickList" id="salesCustomerResults"></div>${c?`<div class="selectedCustomer"><small>AKTUELLER KUNDE</small><h3>${esc(c.name)}</h3><p>${esc(c.street||'')} · ${esc(c.zip||'')} ${esc(c.city||'')}<br>${esc(c.phone||'')} ${c.email?'· '+esc(c.email):''}</p><div><span>Kundenberater: <b>${esc(customerOwnerName(c))}</b></span>${can('edit_customers')?'<button class="miniBtn" id="salesEditCustomer">Kundendaten</button>':''}</div></div>`:''}</div></section><section class="panel salesAdvicePanel"><div class="panelTitle"><h3>2. Bedarf</h3><span>systematisch erfassen</span></div><div class="panelPad"><div class="formGrid"><label>Bereich<select id="advPurpose"><option value="fish" ${sel(needs.purpose||'fish','fish')}>Fisch räuchern</option><option value="meat" ${sel(needs.purpose,'meat')}>Fleisch / Schinken</option><option value="smoke" ${sel(needs.purpose,'smoke')}>Räuchermehl</option><option value="brine" ${sel(needs.purpose,'brine')}>Räucherlauge</option><option value="other" ${sel(needs.purpose,'other')}>Sonstiges</option></select></label><label>Fischart<select id="advFish"><option value="forelle" ${sel(needs.fish||'forelle','forelle')}>Forelle</option><option value="aal" ${sel(needs.fish,'aal')}>Aal</option><option value="lachs" ${sel(needs.fish,'lachs')}>Lachs</option><option value="makrele" ${sel(needs.fish,'makrele')}>Makrele</option><option value="other" ${sel(needs.fish,'other')}>Andere</option></select></label><label>Gewicht pro Stück (kg)<input id="advWeight" type="number" min="0.1" step="0.1" value="${Number(needs.weight||2)}"></label><label>Produktform<select id="advForm"><option value="whole" ${sel(needs.form||'whole','whole')}>Ganzer Fisch / Stück</option><option value="filet" ${sel(needs.form,'filet')}>Filet</option></select></label><label>Priorität<select id="advPriority"><option value="standard" ${sel(needs.priority||'standard','standard')}>Solide Standardlösung</option><option value="secure" ${sel(needs.priority,'secure')}>Besonders sicherer Halt</option><option value="heavy" ${sel(needs.priority,'heavy')}>Schwere Fische / hohe Belastung</option></select></label><label>Materialwunsch<select id="advMaterial"><option value="VA" ${sel(needs.material||'VA','VA')}>VA</option><option value="V2A" ${sel(needs.material,'V2A')}>V2A rostfrei</option><option value="V4A" ${sel(needs.material,'V4A')}>V4A Premium</option></select></label><label>Beratungskanal<select id="advChannel"><option value="phone" ${sel(needs.channel||'phone','phone')}>Telefon</option><option value="field" ${sel(needs.channel,'field')}>Kundenberater vor Ort</option></select></label>${isAdmin()?`<label>Berater<select id="advRep">${repOptions(c?.sales_rep_id||needs.sales_rep_id||'')}</select></label>`:''}<label class="full">Beratungsnotiz<textarea id="advNote" placeholder="Besondere Wünsche, Einsatz, Rückfragen …">${esc(needs.note||'')}</textarea></label></div><div class="formActions"><button class="btn orange" id="runAdvice">Empfehlung ermitteln</button></div></div></section></div>${salesAdvice?`<section class="panel adviceResult"><div class="panelTitle"><h3>3. Empfehlung</h3><span>${recs.length} passende Artikel</span></div><div class="adviceCards">${recs.map(p=>`<article><div class="adviceImage">${p.image?`<img src="../${esc(p.image)}" alt="">`:'◇'}</div><div><small>Art.-Nr. ${esc(p.article_no||'–')}</small><h4>${esc(p.name)}</h4><p>${esc(p.description||p.category||'')}</p><b>ab ${money(p.base)}</b></div></article>`).join('')}</div>${(can('save_consultations')||can('create_orders'))?`<div class="adviceActions">${can('save_consultations')?'<button class="btn" id="saveAdvice">Beratung speichern</button>':''}${can('create_orders')?'<button class="btn primary" id="adviceToOrder">Empfehlung → Bestellung</button>':''}</div>`:''}</section>`:''}${isAdmin()?`<section class="panel recentConsultations"><div class="panelTitle"><h3>Letzte Beratungen</h3><span>${(state.consultations||[]).length} gespeichert</span></div><div class="panelPad">${(state.consultations||[]).slice(0,6).map(x=>{const cu=(state.customers||[]).find(c=>c.id===x.customer_id);return `<div class="consultRow"><b>${esc(x.id)}</b><span>${esc(cu?.name||'ohne Kunde')}</span><small>${dt(x.created_at)}</small></div>`}).join('')||'<div class="empty compact"><b>Noch keine Beratungen</b>Gespeicherte Beratungen erscheinen hier.</div>'}</div></section>`:''}`;renderSalesCustomerResults();$('#salesCustomerSearch').oninput=e=>renderSalesCustomerResults(e.target.value);if($('#salesNewCustomer'))$('#salesNewCustomer').onclick=()=>customerModal(null);if(c){if($('#salesEditCustomer'))$('#salesEditCustomer').onclick=()=>customerModal(c.id);if(can('create_orders')&&$('#salesNewOrder'))$('#salesNewOrder').onclick=()=>manualOrderModal(c.id,[],c.sales_rep_id||'')}$('#runAdvice').onclick=()=>{const n={purpose:$('#advPurpose').value,fish:$('#advFish').value,weight:+$('#advWeight').value,form:$('#advForm').value,priority:$('#advPriority').value,material:$('#advMaterial').value,channel:$('#advChannel').value,sales_rep_id:isAdmin()?$('#advRep').value:(state.current_user?.sales_rep_id||''),note:$('#advNote').value};salesAdvice={needs:n,recs:recommendForNeeds(n)};renderSales()};if(salesAdvice&&(can('save_consultations')||can('create_orders'))){if($('#saveAdvice'))$('#saveAdvice').onclick=async()=>{try{const d=await api({action:'consultation_save',customer_id:c?.id||'',sales_rep_id:isAdmin()?$('#advRep').value:(state.current_user?.sales_rep_id||''),channel:$('#advChannel').value,needs:salesAdvice.needs,recommendations:salesAdvice.recs,notes:$('#advNote').value});salesConsultationId=d.consultation_id;toast('Beratung '+d.consultation_id+' gespeichert');await load(true)}catch(e){toast(e.message,true)}};if($('#adviceToOrder'))$('#adviceToOrder').onclick=async()=>{if(!c)return toast('Bitte zuerst einen Kunden auswählen',true);try{const d=await api({action:'consultation_save',customer_id:c.id,sales_rep_id:isAdmin()?$('#advRep').value:(state.current_user?.sales_rep_id||''),channel:$('#advChannel').value,needs:salesAdvice.needs,recommendations:salesAdvice.recs,notes:$('#advNote').value});salesConsultationId=d.consultation_id;manualOrderModal(c.id,salesAdvice.recs,isAdmin()?$('#advRep').value:(state.current_user?.sales_rep_id||''),d.consultation_id,$('#advChannel').value)}catch(e){toast(e.message,true)}}}}
function manualOrderModal(customerId,presetIds=[],repId='',consultationId='',channel='telephone'){
 const c=(state.customers||[]).find(x=>x.id===customerId);if(!c)return toast('Kunde nicht gefunden',true);
 let cart=presetIds.map(id=>{const p=(state.catalog||[]).find(x=>x.id===id);return p?{id:p.id,qty:1,meta:p.type==='hook'?{length:defaultHookLength(p.id),material:salesAdvice?.needs?.material||'VA',tip:'standard'}:{}}:null}).filter(Boolean);
 const normalized=({field:'advisor',phone:'telephone',shop:'online'})[channel]||channel;
 let opts={rep_id:repId||c.sales_rep_id||(!isAdmin()?(state.current_user?.sales_rep_id||''):''),channel:normalized||'telephone',shipping:'dpd',payment:'Rechnung / Überweisung',paid:false,note:'',consultation_id:consultationId||salesConsultationId||''};
 const draw=()=>{const products=activeCatalog().filter(p=>Number(p.base)>0),sum=cart.reduce((a,x)=>{const p=products.find(z=>z.id===x.id);return a+estimatePrice(p,x.meta)*x.qty},0),owner=customerOwnerName(c),hasOwner=!!c.sales_rep_id;
 openModal(`<div class="modalHead"><small>ORGABOARD VERKAUF · V69</small><h2>Neue Bestellung</h2><p>${esc(c.name)} · Herkunft und Provision werden getrennt dokumentiert.</p></div><div class="modalBody"><div class="orderCustomerStrip"><div><small>KUNDE</small><b>${esc(c.name)}</b><span>${esc(c.street||'')} · ${esc(c.zip||'')} ${esc(c.city||'')}</span></div><button class="miniBtn" id="orderEditCustomer">Kundendaten</button></div>
 <div class="attributionPreview ${hasOwner?'returning':''}"><div><small>KUNDENBEZIEHUNG</small><b>${hasOwner?'Bestehender Kundenberater: '+esc(owner):'Noch kein fester Kundenberater'}</b><span>${hasOwner?'Dieser Kundenberater hat bei Folgekäufen Vorrang und erhält die volle Provision.':'Ohne Beraterkontakt wird die Provisionsgutschrift automatisch abwechselnd Björn Hahne / Jessica Hahne zugeteilt.'}</span></div></div>
 <div class="manualOrderGrid"><section><h3>Artikel hinzufügen</h3><div class="formGrid"><label class="full">Produkt<select id="ordProduct">${products.map(p=>`<option value="${esc(p.id)}">${esc(p.article_no||'–')} · ${esc(p.name)} · ${money(p.base)}</option>`).join('')}</select></label><label>Menge<input id="ordQty" type="number" min="1" max="99" value="1"></label><label>Länge<select id="ordLength"></select></label><label>Material<select id="ordMaterial"><option>VA</option><option>V2A</option><option>V4A</option></select></label><label>Spitze<select id="ordTip"><option value="standard">Standard geschärft</option><option value="extra">Extra scharf</option></select></label></div><button class="btn orange wideBtn" id="addOrderItem">+ Artikel übernehmen</button></section>
 <section><h3>Verkaufszuordnung</h3><div class="formGrid"><label class="full">Verkaufsherkunft<select id="ordChannel"><option value="online" ${sel(opts.channel,'online')}>Online-Shop</option><option value="advisor" ${sel(opts.channel,'advisor')}>Kundenberater</option><option value="telephone" ${sel(opts.channel,'telephone')}>Telefonleitung</option><option value="email" ${sel(opts.channel,'email')}>E-Mail</option><option value="walk_in" ${sel(opts.channel,'walk_in')}>Vor Ort / Abholung</option><option value="other" ${sel(opts.channel,'other')}>Sonstiges</option></select></label><label class="full">Kundenberater / direkter Kontakt<select id="ordRep">${repOptions(opts.rep_id)}</select><small>Bei einem bestehenden Beraterkunden bleibt dessen Kundenberater automatisch provisionsberechtigt.</small></label><label>Übergabe<select id="ordShipping"><option value="dpd" ${sel(opts.shipping,'dpd')}>DPD Versand</option><option value="field_delivery" ${sel(opts.shipping,'field_delivery')}>Übergabe Kundenberater</option><option value="pickup" ${sel(opts.shipping,'pickup')}>Abholung</option></select></label><label>Zahlungsart<select id="ordPayment"><option ${sel(opts.payment,'Rechnung / Überweisung')}>Rechnung / Überweisung</option><option ${sel(opts.payment,'Vorkasse')}>Vorkasse</option><option ${sel(opts.payment,'Bar')}>Bar</option><option ${sel(opts.payment,'Kartenzahlung')}>Kartenzahlung</option></select></label><label class="shopToggle full"><input id="ordPaid" type="checkbox" ${opts.paid?'checked':''}><span><b>Zahlung bereits erfolgt</b><small>Provision wird erst bei bezahltem Auftrag in der Abrechnung wirksam.</small></span></label><label class="full">Interne Auftragsnotiz<textarea id="ordNote">${esc(opts.note)}</textarea></label></div></section></div>
 <div class="orderCart"><div class="panelTitle"><h3>Positionen</h3><span>${cart.reduce((a,x)=>a+x.qty,0)} Stück / Sets</span></div>${cart.length?`<table class="dataTable compactTable"><thead><tr><th>Art.-Nr.</th><th>Artikel</th><th>Variante</th><th>Menge</th><th>Preis ca.</th><th></th></tr></thead><tbody>${cart.map((x,i)=>{const p=products.find(z=>z.id===x.id);return `<tr><td>${esc(p?.article_no||'–')}</td><td><b>${esc(p?.name||x.id)}</b></td><td>${esc(itemMeta(x)||'–')}</td><td>${x.qty}</td><td>${money(estimatePrice(p,x.meta)*x.qty)}</td><td><button class="miniBtn" data-remove-cart="${i}">Entfernen</button></td></tr>`}).join('')}</tbody></table>`:'<div class="empty compact"><b>Noch keine Position</b>Artikel oben auswählen und übernehmen.</div>'}<div class="cartEstimate"><span>Voraussichtlicher Warenwert</span><b>${money(sum)}</b><small>Versand und endgültiger Variantenpreis werden serverseitig berechnet.</small></div></div><div class="formActions"><button class="btn" id="cancelManualOrder">Abbrechen</button><button class="btn primary" id="saveManualOrder" ${cart.length?'':'disabled'}>Bestellung verbindlich erfassen</button></div></div>`);
 const sync=()=>{opts.channel=$('#ordChannel').value;opts.rep_id=$('#ordRep').value;opts.shipping=$('#ordShipping').value;opts.payment=$('#ordPayment').value;opts.paid=$('#ordPaid').checked;opts.note=$('#ordNote').value};
 const updateVariant=()=>{const p=products.find(x=>x.id===$('#ordProduct').value),isHook=p?.type==='hook',lens=hookLengths(p?.id);$('#ordLength').innerHTML=(lens.length?lens:['–']).map(v=>`<option value="${v==='–'?'':v}">${v}</option>`).join('');$('#ordLength').disabled=!isHook;$('#ordMaterial').disabled=!isHook;$('#ordTip').disabled=!isHook};
 $('#ordProduct').onchange=updateVariant;updateVariant();$('#addOrderItem').onclick=()=>{sync();const p=products.find(x=>x.id===$('#ordProduct').value);if(!p)return;const qty=Math.max(1,Math.min(99,+$('#ordQty').value||1)),meta=p.type==='hook'?{length:$('#ordLength').value||defaultHookLength(p.id),material:$('#ordMaterial').value,tip:$('#ordTip').value}:{};cart.push({id:p.id,qty,meta});draw()};$$('[data-remove-cart]').forEach(b=>b.onclick=()=>{sync();cart.splice(+b.dataset.removeCart,1);draw()});$('#cancelManualOrder').onclick=closeModal;$('#orderEditCustomer').onclick=()=>customerModal(c.id);
 $('#saveManualOrder').onclick=async()=>{sync();try{const d=await api({action:'manual_order_create',customer_id:c.id,sales_rep_id:opts.rep_id,consultation_id:opts.consultation_id,channel:opts.channel,shipping_mode:opts.shipping,payment_method:opts.payment,paid:opts.paid,note:opts.note,items:cart});toast('Bestellung '+d.order.order_no+' · Provision: '+(d.attribution?.commission_sales_rep_name||commissionRepName(d.order)));closeModal();await load(true);openOrder(d.order.order_no)}catch(e){toast(e.message,true)}}};
 draw()
}
const TERRITORY_GEOJSON_URL='https://raw.githubusercontent.com/isellsoap/deutschlandGeoJSON/refs/heads/main/2_bundeslaender/4_niedrig.geo.json';
const TERRITORY_COLORS={'01':'#14B8A6','02':'#EF4444','03':'#22C55E','04':'#F97316','05':'#3B82F6','06':'#8B5CF6','07':'#C0265C','08':'#15803D','09':'#0EA5E9','10':'#EAB308','11':'#374151','12':'#B45309','13':'#0F766E','14':'#A21CAF','15':'#65A30D','16':'#EC4899'};
const SALES_ROLE_LABELS={regional_manager:'Regionalmanager',district_manager:'Bezirksmanager',team_leader:'Teamleiter',advisor_active:'Kundenberater Aktiv',advisor_be:'Kundenberater BE'};
const SALES_ROLE_TABS=[['platform','Kundenberater-Plattform'],['territory','Gebiet vergeben'],['regional_manager','Regionalmanager'],['district_manager','Bezirksmanager'],['team_leader','Teamleiter'],['advisor_active','Kundenberater Aktiv'],['advisor_be','Kundenberater BE']];
let salesRepTab='platform';
function salesRoleLabel(v){return SALES_ROLE_LABELS[v]||'Kundenberater Aktiv'}
function publicSalesReps(){return (state.sales_reps||[]).filter(r=>!String(r.employee_no||'').startsWith('ZENTRALE-')&&r.sales_role!=='house')}
function territoryColor(code){return TERRITORY_COLORS[String(code||'').padStart(2,'0')]||'#64748B'}
function territoryOwnerOptions(current=''){return `<option value="">– Weißgebiet / nicht vergeben –</option>${publicSalesReps().filter(r=>r.status==='active'&&r.sales_role!=='advisor_be'&&!(r.sales_role==='advisor_active'&&r.parent_sales_rep_id)).map(r=>`<option value="${esc(r.id)}" ${sel(current,r.id)}>${esc(r.employee_no)} · ${esc(r.name)} · ${esc(salesRoleLabel(r.sales_role))}${r.state_code?' · '+esc(territoryStateName(r.state_code)):''}</option>`).join('')}`}
async function renderTerritoryMiniMap(hostId,selectedCode='',onPick=null){
 const host=typeof hostId==='string'?$(hostId):hostId;if(!host)return;
 host.innerHTML='<div class="territoryMapLoading">Karte wird geladen …</div>';
 try{
  const r=await fetch(TERRITORY_GEOJSON_URL,{cache:'force-cache'});if(!r.ok)throw new Error('Kartendaten nicht verfügbar');
  const geo=await r.json(),features=geo.features||[],all=[];
  const walk=c=>{if(Array.isArray(c)&&typeof c[0]==='number')all.push(c);else if(Array.isArray(c))c.forEach(walk)};features.forEach(f=>walk(f.geometry.coordinates));
  const xs=all.map(p=>p[0]),ys=all.map(p=>p[1]),minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys),W=440,H=520,pad=18;
  const pt=p=>[(p[0]-minX)/(maxX-minX)*(W-pad*2)+pad,H-pad-(p[1]-minY)/(maxY-minY)*(H-pad*2)];
  const ringPath=ring=>ring.map((p,i)=>{const [x,y]=pt(p);return `${i?'L':'M'}${x.toFixed(1)},${y.toFixed(1)}`}).join(' ')+' Z';
  const geomPath=g=>g.type==='Polygon'?g.coordinates.map(ringPath).join(' '):g.coordinates.flatMap(poly=>poly.map(ringPath)).join(' ');
  const codeMap={'DE-SH':'01','DE-HH':'02','DE-NI':'03','DE-HB':'04','DE-NW':'05','DE-HE':'06','DE-RP':'07','DE-BW':'08','DE-BY':'09','DE-SL':'10','DE-BE':'11','DE-BB':'12','DE-MV':'13','DE-SN':'14','DE-ST':'15','DE-TH':'16'};
  let paths='';features.forEach(f=>{const code=codeMap[f.properties?.id]||'',t=(state.sales_territories||[]).find(x=>x.state_code===code),active=Boolean(t?.owner_sales_rep_id),selected=code===selectedCode,color=territoryColor(code);paths+=`<path tabindex="0" role="button" data-mini-territory="${code}" class="territoryShape ${active?'fixed':'white'} ${selected?'selected':''}" style="--territory-color:${color}" d="${geomPath(f.geometry)}"><title>${esc(t?.state_name||f.properties?.name||'')} · ${active?'Festgebiet':'Weißgebiet'}${selected?' · ausgewählt':''}</title></path>`});
  host.innerHTML=`<svg viewBox="0 0 ${W} ${H}" aria-label="Deutschlandkarte der Gebietsbücher">${paths}</svg>`;
  $$('[data-mini-territory]',host).forEach(p=>{const pick=()=>{if(typeof onPick==='function')onPick(p.dataset.miniTerritory)};p.onclick=pick;p.onkeydown=e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();pick()}}});
 }catch(e){const t=(state.sales_territories||[]).find(x=>x.state_code===selectedCode);host.innerHTML=`<div class="territoryMapFallback"><b>${selectedCode?esc(t?.territory_book_no||'FG-'+selectedCode)+' · '+esc(t?.state_name||territoryStateName(selectedCode)):'Deutschlandkarte'}</b><span>${selectedCode?'Gebiet ist ausgewählt. ':''}Kartendaten konnten nicht geladen werden.</span></div>`}
}

function territoryMatchesSearch(t,term=territorySearchTerm){
 term=String(term||'').trim().toLowerCase();
 if(!term)return true;
 const hay=[t?.state_name||'',t?.territory_book_no||'',t?.owner_name||'',t?.state_code||'',t?.owner_sales_rep_id?'festgebiet':'weißgebiet',t?.member_count||'',t?.customer_count||''].join(' ').toLowerCase();
 return hay.includes(term);
}
function territoryFilteredList(){return (state?.sales_territories||[]).filter(t=>territoryMatchesSearch(t)).sort((a,b)=>String(a.territory_book_no||a.state_code).localeCompare(String(b.territory_book_no||b.state_code),'de'));}
function territorySelectedCard(code){territorySelectedCode=String(code||'').padStart(2,'0');renderTerritories();}

function renderAdvisorPlatformTerritoryMap(){const host=$('#platformTerritoryMap');if(!host)return;renderTerritoryMiniMap(host,'',code=>{territoryBookAdminState=code;territoryBookRowsCode='';setView('territorybook')})}

function territoryDetail(code){const t=(state.sales_territories||[]).find(x=>x.state_code===code);if(!t)return;const members=(state.sales_reps||[]).filter(r=>r.state_code===code),owner=(state.sales_reps||[]).find(r=>r.id===t.owner_sales_rep_id);openModal(`<div class="modalHead"><small>${esc(t.territory_book_no)} · ${t.owner_sales_rep_id?'FESTGEBIET':'WEISSGEBIET'}</small><h2>${esc(t.state_name)}</h2><p>${t.owner_sales_rep_id?'Gebietsschutz aktiv':'Noch kein Festgebietsinhaber zugeordnet'}</p></div><div class="modalBody"><div class="territoryModalGrid"><section><label>Festgebietsinhaber<select id="territoryOwner">${territoryOwnerOptions(t.owner_sales_rep_id||'')}</select></label><div class="formActions"><button class="btn primary" id="saveTerritory">Gebiet speichern</button></div><p class="muted tiny">Pro Bundesland ist genau ein Festgebietsinhaber möglich. Eine Freigabe setzt das Gebiet wieder auf Weißgebiet.</p></section><section class="territoryTeamBox"><h3>Team im Gebiet</h3>${members.length?members.map(r=>`<div class="territoryTeamRow"><div><b>${esc(r.name)}</b><small>${esc(r.employee_no)} · ${esc(salesRoleLabel(r.sales_role))}${t.owner_sales_rep_id===r.id?' · FG-Inhaber':''}</small></div>${r.team?.is_team_leader?'<span class="teamLeaderPill">+3 % TL</span>':''}</div>`).join(''):'<div class="empty">Noch kein Außendienstmitarbeiter im Gebiet.</div>'}<button class="btn" id="addTerritoryMember">+ Mitarbeiter im Gebiet</button></section></div></div>`);$('#saveTerritory').onclick=async()=>{try{const d=await api({action:'territory_assign',state_code:code,sales_rep_id:$('#territoryOwner').value});state.sales_territories=d.territories;state.sales_reps=d.sales_reps;toast($('#territoryOwner').value?'Festgebiet vergeben':'Gebiet als Weißgebiet freigegeben');closeModal();renderTerritories()}catch(e){toast(e.message,true)}};$('#addTerritoryMember').onclick=()=>{const parent=t.owner_sales_rep_id||'';closeModal();salesRepModal(null,code,parent)}}
function renderTerritories(){
 const ts=state.sales_territories||[],fixed=ts.filter(t=>t.owner_sales_rep_id).length,white=16-fixed,leaders=publicSalesReps().filter(r=>r.sales_role==='team_leader').length;
 const filtered=territoryFilteredList();
 if(territorySelectedCode && !filtered.some(t=>t.state_code===territorySelectedCode))territorySelectedCode='';
 const selected=(filtered.find(t=>t.state_code===territorySelectedCode)||filtered[0]||null);
 const countLabel=`${filtered.length.toLocaleString('de-DE')} von ${ts.length.toLocaleString('de-DE')} Bundesländern`;
 ws.innerHTML=`<div class="sectionHead"><div><h2>Festgebiete Deutschland</h2><p>16 Bundesländer als geschützte Vertriebsgebiete mit fester Gebietsfarbe, Gebietsbuch und Vertriebsorganisation.</p></div><button class="btn" id="territoryListToggle">Zur Gebietsliste</button></div>
 <section class="panel territoryIntroBand"><div><small>KLARER WORKFLOW</small><h3>Gebiete schneller finden, prüfen und vergeben</h3><p>Suche zuerst nach Bundesland, FG-Nummer oder Inhaber. Danach siehst du Karte, Gebietsbuch und Status in einer logischeren Reihenfolge.</p></div><div class="territoryQuickSteps"><article><b>1</b><span>Suchen</span><small>nach Bundesland, FG oder Inhaber</small></article><article><b>2</b><span>Prüfen</span><small>Status direkt auf Karte und in der Liste</small></article><article><b>3</b><span>Verwalten</span><small>Gebiet öffnen und sauber zuweisen</small></article></div></section>
 <section class="panel territorySearchPanel"><div class="territoryToolbar"><label class="territorySearchWrap"><span>⌕</span><input id="territorySearch" type="search" placeholder="Bundesland, FG-Nummer, Inhaber, Weißgebiet …" value="${esc(territorySearchTerm)}"></label><div class="territoryToolbarMeta"><b>${countLabel}</b><small>${territorySearchTerm?'Suchfilter aktiv · Karte und Liste werden live gefiltert':'Suche über Karte, Gebietsliste und Gebietsinhaber'}</small></div><button class="btn" id="territorySearchClear" ${territorySearchTerm?'':'disabled'}>Suche löschen</button></div>${selected?`<div class="territoryMapSummary"><b>${esc(selected.territory_book_no)} · ${esc(selected.state_name)}</b><span>${selected.owner_sales_rep_id?`Festgebiet · ${esc(selected.owner_name||'Gebietsinhaber hinterlegt')}`:'Weißgebiet · noch nicht vergeben'}</span><small>${Number(selected.member_count||0).toLocaleString('de-DE')} Mitarbeiter · ${Number(selected.customer_count||0).toLocaleString('de-DE')} Kunden</small></div>`:'<div class="territoryMapSummary empty"><b>Kein Bundesland gefunden</b><span>Bitte Suchbegriff anpassen oder Suche zurücksetzen.</span></div>'}</section>
 <div class="metricGrid territoryMetrics">${metric('Festgebiete',fixed,'von 16 Bundesländern')}${metric('Weißgebiete',white,'noch frei')}${metric('Teamleiter',leaders,'Führung im Festgebiet')}${metric('Kundenberater',publicSalesReps().filter(r=>r.status==='active').length,'aktive Vertriebsmitarbeiter')}</div>
 <div class="territoryLayout"><section class="panel territoryMapPanel"><div class="panelTitle"><h3>Deutschlandkarte</h3><span><i class="legendDot fixed"></i> besetzt · Bundeslandfarbe <i class="legendDot white"></i> Weißgebiet</span></div><div id="territoryMap" class="territoryMap"><div class="territoryMapLoading">Bundesländer werden geladen …</div></div><p class="territoryAttribution">Suche filtert Karte und Gebietsliste gleichzeitig. Klick auf ein Bundesland öffnet direkt die Detailverwaltung.</p></section><section class="panel territorySide"><div class="panelTitle"><h3>Gebietsbücher</h3><span>${countLabel}</span></div><div class="territoryStateList">${filtered.length?filtered.map(t=>`<button type="button" data-territory="${t.state_code}" data-territory-select="${t.state_code}" class="territoryStateCard ${t.owner_sales_rep_id?'fixed':'white'} ${territorySelectedCode===t.state_code?'selected':''}" style="--territory-color:${territoryColor(t.state_code)}"><span class="territoryBook">${esc(t.territory_book_no)}</span><i class="territoryColorChip"></i><div><b>${esc(t.state_name)}</b><small>${t.owner_sales_rep_id?esc(t.owner_name||'Gebietsinhaber'):'Weißgebiet · frei'}</small></div><em>${t.member_count||0}</em></button>`).join(''):`<div class="empty"><b>Kein Treffer</b>Für diese Suche wurde kein Bundesland gefunden.</div>`}</div></section></div><section class="panel territoryRules"><h3>Gebietsschutz & Führungslogik</h3><div class="territoryRuleGrid"><div><b>1 · Weißgebiet</b><span>Unbesetzt bleibt das Bundesland neutral. Die feste Landesfarbe ist reserviert, aber noch nicht aktiv.</span></div><div><b>2 · Festgebiet (FG)</b><span>Mit der Vergabe wird die Bundeslandfarbe aktiviert und der Gebietsschutz dokumentiert.</span></div><div><b>3 · Führung</b><span>Regionalmanager, Bezirksmanager, Teamleiter und Kundenberater werden als getrennte Funktionen geführt.</span></div><div><b>4 · Teamleiterbonus</b><span>Ein Kundenberater mit eigenem aktivem Team wird automatisch Teamleiter; der bestehende +3-Prozentpunkte-Bonus bleibt erhalten.</span></div></div></section>`;
 $$('[data-territory]').forEach(b=>b.onclick=()=>{territorySelectedCode=b.dataset.territory;territoryDetail(b.dataset.territory)});
 $$('[data-territory-select]').forEach(b=>b.addEventListener('mouseenter',()=>{territorySelectedCode=b.dataset.territorySelect;renderGermanyTerritoryMap()}));
 $('#territoryListToggle').onclick=()=>$('.territorySide')?.scrollIntoView({behavior:'smooth',block:'start'});
 const searchInput=$('#territorySearch');
 const restoreTerritorySearchFocus=()=>requestAnimationFrame(()=>{const next=$('#territorySearch');if(!next)return;try{next.focus({preventScroll:true})}catch(_){next.focus()}const pos=next.value.length;try{next.setSelectionRange(pos,pos)}catch(_){}});
 const applyTerritorySearch=()=>{if(territorySearchTimer){clearTimeout(territorySearchTimer);territorySearchTimer=0}const rows=territoryFilteredList();if(territorySearchTerm.trim()&&rows.length===1)territorySelectedCode=rows[0].state_code;else if(territorySelectedCode&&!rows.some(r=>r.state_code===territorySelectedCode))territorySelectedCode='';renderTerritories();restoreTerritorySearchFocus()};
 if(searchInput){searchInput.oninput=()=>{territorySearchTerm=searchInput.value;if(territorySearchTimer)clearTimeout(territorySearchTimer);territorySearchTimer=setTimeout(applyTerritorySearch,300)};searchInput.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();if(territorySearchTimer){clearTimeout(territorySearchTimer);territorySearchTimer=0}territorySearchTerm=searchInput.value;const rows=territoryFilteredList();if(rows.length===1){territorySelectedCode=rows[0].state_code;territoryDetail(rows[0].state_code)}else if(rows.length>0){territorySelectedCode=rows[0].state_code;renderTerritories();$('.territoryMapPanel')?.scrollIntoView({behavior:'smooth',block:'start'});restoreTerritorySearchFocus()}}}}
 $('#territorySearchClear').onclick=()=>{if(territorySearchTimer){clearTimeout(territorySearchTimer);territorySearchTimer=0}territorySearchTerm='';territorySelectedCode='';renderTerritories();restoreTerritorySearchFocus()};
 renderGermanyTerritoryMap()
}
async function renderGermanyTerritoryMap(){const host=$('#territoryMap');if(!host)return;try{const r=await fetch(TERRITORY_GEOJSON_URL,{cache:'force-cache'});if(!r.ok)throw new Error('Kartendaten nicht verfügbar');const geo=await r.json(),features=geo.features||[];const all=[];const walk=c=>{if(Array.isArray(c)&&typeof c[0]==='number')all.push(c);else if(Array.isArray(c))c.forEach(walk)};features.forEach(f=>walk(f.geometry.coordinates));const xs=all.map(p=>p[0]),ys=all.map(p=>p[1]),minX=Math.min(...xs),maxX=Math.max(...xs),minY=Math.min(...ys),maxY=Math.max(...ys),W=620,H=760,pad=24;const pt=p=>[(p[0]-minX)/(maxX-minX)*(W-pad*2)+pad,H-pad-(p[1]-minY)/(maxY-minY)*(H-pad*2)];const ringPath=ring=>ring.map((p,i)=>{const [x,y]=pt(p);return `${i?'L':'M'}${x.toFixed(1)},${y.toFixed(1)}`}).join(' ')+' Z';const geomPath=g=>g.type==='Polygon'?g.coordinates.map(ringPath).join(' '):g.coordinates.flatMap(poly=>poly.map(ringPath)).join(' ');const codeMap={'DE-SH':'01','DE-HH':'02','DE-NI':'03','DE-HB':'04','DE-NW':'05','DE-HE':'06','DE-RP':'07','DE-BW':'08','DE-BY':'09','DE-SL':'10','DE-BE':'11','DE-BB':'12','DE-MV':'13','DE-SN':'14','DE-ST':'15','DE-TH':'16'};const visibleCodes=new Set(territoryFilteredList().map(t=>t.state_code));let paths='',labels='';features.forEach(f=>{const code=codeMap[f.properties?.id]||'',t=(state.sales_territories||[]).find(x=>x.state_code===code),active=Boolean(t?.owner_sales_rep_id),color=territoryColor(code),selected=territorySelectedCode===code,dimmed=territorySearchTerm&&!visibleCodes.has(code);const pts=[];const collect=c=>{if(Array.isArray(c)&&typeof c[0]==='number')pts.push(c);else if(Array.isArray(c))c.forEach(collect)};collect(f.geometry.coordinates);const cx=pts.reduce((a,p)=>a+pt(p)[0],0)/Math.max(1,pts.length),cy=pts.reduce((a,p)=>a+pt(p)[1],0)/Math.max(1,pts.length);paths+=`<path tabindex="0" role="button" data-map-territory="${code}" class="territoryShape ${active?'fixed':'white'} ${selected?'selected':''} ${dimmed?'dimmed':''}" style="--territory-color:${color}" d="${geomPath(f.geometry)}"><title>${esc(t?.state_name||f.properties?.name||'')} · ${active?'Festgebiet · '+(t?.owner_name||'besetzt'):'Weißgebiet · frei'}${selected?' · ausgewählt':''}</title></path>`;if(code&&!['02','04','11'].includes(code))labels+=`<text x="${cx.toFixed(0)}" y="${cy.toFixed(0)}" class="territoryMapLabel ${dimmed?'dimmed':''}">${code}</text>`});host.innerHTML=`<svg viewBox="0 0 ${W} ${H}" aria-label="Deutschlandkarte mit 16 Vertriebsgebieten">${paths}${labels}</svg>`;$$('[data-map-territory]',host).forEach(p=>{p.addEventListener('click',()=>{territorySelectedCode=p.dataset.mapTerritory;territoryDetail(p.dataset.mapTerritory)});p.addEventListener('mouseenter',()=>{territorySelectedCode=p.dataset.mapTerritory;renderGermanyTerritoryMap()});p.addEventListener('keydown',e=>{if(e.key==='Enter'||e.key===' '){e.preventDefault();territorySelectedCode=p.dataset.mapTerritory;territoryDetail(p.dataset.mapTerritory)}})})}catch(e){host.innerHTML=`<div class="territoryMapFallback"><b>Deutschlandkarte konnte nicht geladen werden.</b><span>Die 16 Festgebiete bleiben rechts vollständig bedienbar. Ursache: ${esc(e.message)}</span></div>`}}
function territoryStateName(code){return (state.sales_territories||[]).find(t=>t.state_code===code)?.state_name||'–'}
function salesRepRoleBadge(role){const cls={regional_manager:'rm',district_manager:'bm',team_leader:'tl',advisor_active:'ka',advisor_be:'be'}[role]||'ka';return `<span class="salesRoleBadge ${cls}">${esc(salesRoleLabel(role))}</span>`}
function salesRepTabsHtml(){return `<div class="salesRoleTabs">${SALES_ROLE_TABS.map(([k,l])=>`<button type="button" data-sales-tab="${k}" class="${salesRepTab===k?'active':''}">${esc(l)}<em>${k==='territory'?(state.sales_territories||[]).filter(t=>t.owner_sales_rep_id).length:publicSalesReps().filter(r=>r.sales_role===k).length}</em></button>`).join('')}</div>`}
function renderTerritoryAssignmentInsideReps(){const ts=state.sales_territories||[];return `<div class="territoryAssignIntro"><div><b>Festgebiet direkt vergeben</b><span>Die Vergabe hier und im Reiter „Festgebiete Deutschland“ greift auf dieselben Daten zu.</span></div><button class="btn" id="openFullTerritoryMap">Deutschlandkarte öffnen</button></div><div class="territoryAssignGrid">${ts.map(t=>`<article class="territoryAssignCard ${t.owner_sales_rep_id?'occupied':'white'}" style="--territory-color:${territoryColor(t.state_code)}"><div class="territoryAssignHead"><i></i><div><small>${esc(t.territory_book_no)}</small><b>${esc(t.state_name)}</b></div><span>${t.owner_sales_rep_id?'GEBIETSSCHUTZ':'WEISSGEBIET'}</span></div><label>Festgebietsinhaber<select data-inline-territory-owner="${t.state_code}">${territoryOwnerOptions(t.owner_sales_rep_id||'')}</select></label><div class="territoryAssignFoot"><small>${t.member_count||0} Mitarbeiter · ${t.customer_count||0} Kunden</small><button class="miniBtn" data-save-inline-territory="${t.state_code}">Speichern</button></div></article>`).join('')}</div>`}

const TERRITORY_DIR_STATUS={active:'Aktiv',candidate:'Recherche',paused:'Pausiert',archived:'Archiv'};
const TERRITORY_CONTACT_RESULTS={reached:'Erreicht',not_reached:'Nicht erreicht',appointment:'Termin vereinbart',interested:'Interesse',info_sent:'Information gesendet',callback:'Rückruf / Wiedervorlage',no_interest:'Kein Interesse',other:'Sonstiges'};
function territoryDirCategoryLabel(k){return state.territory_directory_categories?.[k]||k||'Sonstiges'}
const TERRITORY_CORE_PACKS=['fishing','meat','hardware','spices','fishery','smoke_bbq','outdoor_trade'];
const TERRITORY_COMPLETE_PACKS=[...TERRITORY_CORE_PACKS,'food','relevant_restaurants'];
const TERRITORY_PACK_LABELS={fishing:'Angelgeschäfte & Angelvereine',meat:'Fleischereien, Metzgereien & Schlachtereien',hardware:'Baumärkte & Gartencenter',spices:'Gewürze, Kräuter & Gewürzmanufakturen',fishery:'Fischhandel & Fischverarbeitung',smoke_bbq:'Räuchern, Smoker, Grill & BBQ',outdoor_trade:'Outdoor, Jagd & relevanter Großhandel',food:'Lebensmittelgeschäfte & Märkte',relevant_restaurants:'Branchennahe Gastronomie',restaurants:'Alle Restaurants'};
function territoryDiscoveryPacks(pack){if(pack==='core')return [...TERRITORY_CORE_PACKS];if(pack==='all')return [...TERRITORY_COMPLETE_PACKS];return [pack]}
function territoryPackLabel(pack){return TERRITORY_PACK_LABELS[pack]||pack}
async function territoryDiscoveryRequest(code,pack,retries=1,onRetry=null){let last=null;for(let attempt=0;attempt<=retries;attempt++){try{return await api({action:'territory_directory_discover',state_code:code,pack})}catch(e){last=e;if(attempt<retries){if(typeof onRetry==='function')onRetry(attempt+1,e);await new Promise(r=>setTimeout(r,1500*(attempt+1)))}}}throw last||new Error('Teilabfrage fehlgeschlagen')}

function advisorPlatformHtml(){const reps=publicSalesReps(),users=state.users||[],ts=state.sales_territories||[],withBook=reps.filter(r=>r.state_code).length,withLogin=reps.filter(r=>r.user_id).length;return `<div class="advisorPlatformHero"><div><small>KUNDENBERATER-PLATTFORM</small><h3>Außendienst komplett steuern</h3><p>Rolle, Führung, Festgebiet, Gebietsbuch, Zugang, Rechte, Kundenstamm, Provision und Kontaktarbeit zentral verwalten.</p></div><div class="advisorPlatformActions"><button class="btn primary" id="platformNewRep">+ Kundenberater</button><button class="btn" id="platformTerritoryBooks">Gebietsbücher verwalten</button><button class="btn" id="platformMap">Deutschlandkarte</button><button class="btn" id="platformCustomers">Kunden</button><button class="btn primary" id="platformAppointments">Terminplaner</button><button class="btn" id="platformCalendar">Vertriebskalender</button><button class="btn" id="platformRights">Rechte-Zentrale</button></div></div><div class="metricGrid">${metric('Kundenberater',reps.length,reps.filter(r=>r.status==='active').length+' aktiv')}${metric('Mit Gebietsbuch',withBook,'von '+reps.length)}${metric('Mit Login',withLogin,'Orgaboard-Zugang')}${metric('Festgebiete',ts.filter(t=>t.owner_sales_rep_id).length,'von 16')}</div><section class="panel tableWrap"><div class="panelTitle"><h3>Kundenberater-Plattform</h3><span>Administrator · Vollsteuerung</span></div>${reps.length?`<table class="dataTable"><thead><tr><th>Mitarbeiter</th><th>Funktion</th><th>Gebietsbuch</th><th>Führung</th><th>Kunden</th><th>Provision</th><th>Zugang</th><th>Aktionen</th></tr></thead><tbody>${reps.map(r=>{const t=ts.find(x=>x.state_code===r.state_code),u=users.find(x=>x.id===r.user_id);return `<tr><td><b>${esc(r.employee_no)} · ${esc(r.name)}</b><br><small>${esc(r.phone||'')} ${r.email?'· '+esc(r.email):''}</small></td><td>${salesRepRoleBadge(r.sales_role)}</td><td>${t?`<b>${esc(t.territory_book_no)}</b><br><span class="territoryMini" style="--territory-color:${territoryColor(r.state_code)}"><i></i>${esc(t.state_name)}</span>`:'<span class="territoryWhitePill">kein Gebiet</span>'}</td><td>${r.parent_name?esc(r.parent_name):'–'}${r.team?.team_size?`<br><small>${r.team.team_size} Teammitglied(er)</small>`:''}</td><td><b>${r.customer_count}</b></td><td><b>${money(r.stats_current?.commission||0)}</b></td><td>${r.user_id?`<span class="status s-paid">Aktiv</span><br><small>${esc(r.username||'')}</small>`:'<span class="status s-pending">Kein Login</span>'}</td><td><div class="rowActions"><button class="miniBtn" data-platform-edit="${esc(r.id)}">Bearbeiten</button>${r.state_code?`<button class="miniBtn" data-platform-book="${esc(r.state_code)}">Gebietsbuch</button>`:''}<button class="miniBtn" data-platform-customers="${esc(r.name)}">Kundenstamm</button><button class="miniBtn" data-platform-stats="${esc(r.id)}">Abrechnung</button><button class="miniBtn" data-platform-rights="${esc(r.user_id||'')}">Rechte</button></div></td></tr>`}).join('')}</tbody></table>`:'<div class="empty"><b>Noch keine Kundenberater</b>Lege den ersten Außendienstmitarbeiter an.</div>'}</section><section class="panel advisorPlatformMapPanel"><div class="panelTitle"><h3>Gebietsbücher als Deutschlandkarte</h3><span>Bundesland anklicken → Gebietsbuch öffnen</span></div><div id="platformTerritoryMap" class="territoryMap advisorPlatformTerritoryMap"><div class="territoryMapLoading">Deutschlandkarte wird geladen …</div></div><div class="territoryMapLegendLine"><span><i class="legendDot fixed"></i> Festgebiet</span><span><i class="legendDot white"></i> Weißgebiet</span></div></section><div class="moduleNotice"><b>Datenschutz & Gebietsschutz:</b> Kundenberater erhalten serverseitig ausschließlich das Gebietsbuch ihres zugeordneten Bundeslandes. Andere Gebietsbücher werden nicht an deren Browser ausgeliefert.</div>`}
function bindAdvisorPlatform(){if($('#platformNewRep'))$('#platformNewRep').onclick=()=>salesRepModal(null,'','','advisor_be');if($('#platformTerritoryBooks'))$('#platformTerritoryBooks').onclick=()=>{territoryBookAdminState='all';territoryBookRowsCode='';setView('territorybook')};if($('#platformMap'))$('#platformMap').onclick=()=>setView('territories');if($('#platformCustomers'))$('#platformCustomers').onclick=()=>{searchTerm='';setView('customers')};if($('#platformAppointments'))$('#platformAppointments').onclick=()=>setView('appointments');if($('#platformCalendar'))$('#platformCalendar').onclick=()=>setView('salescalendar');if($('#platformRights'))$('#platformRights').onclick=()=>setView('rights');$$('[data-platform-edit]').forEach(b=>b.onclick=()=>salesRepModal(b.dataset.platformEdit));$$('[data-platform-book]').forEach(b=>b.onclick=()=>{territoryBookAdminState=b.dataset.platformBook;territoryBookRowsCode='';setView('territorybook')});$$('[data-platform-customers]').forEach(b=>b.onclick=()=>{searchTerm=b.dataset.platformCustomers||'';setView('customers')});$$('[data-platform-stats]').forEach(b=>b.onclick=()=>repStatsModal(b.dataset.platformStats));$$('[data-platform-rights]').forEach(b=>b.onclick=()=>b.dataset.platformRights?rightsModal(b.dataset.platformRights):toast('Noch kein Login verknüpft.',true));renderAdvisorPlatformTerritoryMap()}
function territoryBookScopeCode(){return isAdmin()?(territoryBookAdminState||state.sales_territories?.[0]?.state_code||'01'):(state.territory_book_scope?.state_code||'')}
async function territoryBookLoad(code){if(territoryBookLoading)return;territoryBookLoading=true;try{const d=await api({action:'territory_directory_list',state_code:code});territoryBookRows=d.rows||[];territoryBookRowsCode=d.scope?.state_code||code;state.territory_directory_summary=d.summary||{};if(!isAdmin())state.territory_book_scope=d.scope||state.territory_book_scope}catch(e){toast(e.message,true);territoryBookRows=[];territoryBookRowsCode=code}finally{territoryBookLoading=false;if(current==='territorybook')renderTerritoryBook()}}
function territoryBookFilteredRows(){const q=territoryBookSearch.toLowerCase().trim();return territoryBookRows.filter(r=>{if(territoryBookStatusFilter==='followup'){if(!r.next_follow_up_at)return false}else if(territoryBookStatusFilter!=='all'&&r.status!==territoryBookStatusFilter)return false;if(territoryBookCategoryFilter!=='all'&&r.category!==territoryBookCategoryFilter)return false;return !q||`${r.company} ${r.contact_person||''} ${r.phone||''} ${r.email||''} ${r.street||''} ${r.zip||''} ${r.city||''}`.toLowerCase().includes(q)})}
function territoryPhoneHref(v){return 'tel:'+String(v||'').replace(/[^+\d]/g,'')}
function territoryMailHref(v,company=''){return 'mailto:'+encodeURIComponent(String(v||''))+'?subject='+encodeURIComponent('Räucherhaken24 · Kontakt '+company)}
function territoryMapHref(r){const addr=[r.street,r.zip,r.city].filter(Boolean).join(', ');return addr?'https://www.google.com/maps/search/?api=1&query='+encodeURIComponent(addr):(r.latitude&&r.longitude?'https://www.google.com/maps/search/?api=1&query='+encodeURIComponent(r.latitude+','+r.longitude):'')}
function territoryBookRowHtml(r,admin){const map=territoryMapHref(r),source=r.source==='openstreetmap'?'OSM-Recherche':r.source==='csv'?'CSV-Import':'manuell',phone=(r.phone||r.mobile)?`${r.phone?`<a class="contactDirect phone" href="${territoryPhoneHref(r.phone)}">☎ ${esc(r.phone)}</a>`:''}${r.mobile?`${r.phone?'<br>':''}<a class="contactDirect phone" href="${territoryPhoneHref(r.mobile)}">📱 ${esc(r.mobile)}</a>`:''}`:'<span class="muted">Telefon / Mobil fehlt</span>',mail=r.email?`<a class="contactDirect mail" href="${territoryMailHref(r.email,r.company)}">✉ ${esc(r.email)}</a>`:'<span class="muted">E-Mail fehlt</span>',contact=r.last_contacted_at?`<b>${dt(r.last_contacted_at)}</b><br><small>${esc(TERRITORY_CONTACT_RESULTS[r.last_contact_result]||r.last_contact_result||'')}</small>`:'<span class="status s-pending">Noch nicht kontaktiert</span>',follow=r.next_follow_up_at?`<br><span class="tbFollow">Wiedervorlage ${dt(r.next_follow_up_at)}</span>`:'';return `<tr class="tb-${esc(r.status)}"><td><span class="tbCategory">${esc(r.category_label||territoryDirCategoryLabel(r.category))}</span><br><small>${source}</small></td><td><b>${esc(r.company)}</b><br><small>${esc(TERRITORY_DIR_STATUS[r.status]||r.status)}</small></td><td>${esc(r.street||'–')}<br><b>${esc(r.zip||'')} ${esc(r.city||'')}</b>${map?`<br><a target="_blank" rel="noopener" href="${map}">Maps ↗</a>`:''}</td><td>${esc(r.contact_person||'–')}</td><td>${phone}<br>${mail}</td><td>${contact}${follow}</td><td><div class="rowActions"><button class="miniBtn primaryMini" data-tb-contact="${esc(r.id)}">Kontaktiert / Kalender</button><button class="miniBtn" data-tb-history="${esc(r.id)}">Verlauf</button>${admin?`<button class="miniBtn" data-tb-edit="${esc(r.id)}">Bearbeiten</button>`:''}</div></td></tr>`}
function territoryBookAdminSelectHtml(code){if(!isAdmin())return '';const opts=(state.sales_territories||[]).map(x=>`<option value="${x.state_code}" ${sel(code,x.state_code)}>${esc(x.territory_book_no)} · ${esc(x.state_name)}</option>`).join('');return `<div class="territoryBookAdminSelect"><label>Festgebiet<select id="tbStateSelect">${opts}</select></label><span>Administrator sieht und pflegt alle Gebietsbücher. Kundenberater erhalten serverseitig nur ihr eigenes.</span></div>`}
function territoryBookVisualStats(rows){
 const total=rows.length,withPhone=rows.filter(r=>r.phone||r.mobile).length,withEmail=rows.filter(r=>r.email).length,complete=rows.filter(r=>(r.phone||r.mobile)&&r.email&&r.street&&r.zip&&r.city).length,contacted=rows.filter(r=>r.last_contacted_at).length,followups=rows.filter(r=>r.next_follow_up_at).length,uncontacted=rows.filter(r=>!r.last_contacted_at).length;
 const categoryMap={};rows.forEach(r=>{const key=r.category_label||territoryDirCategoryLabel(r.category);categoryMap[key]=(categoryMap[key]||0)+1});
 const categories=Object.entries(categoryMap).sort((a,b)=>b[1]-a[1]).slice(0,6).map(([label,count])=>({label,count,pct:total?Math.round(count/total*100):0}));
 const cityMap={};rows.forEach(r=>{const key=[r.zip,r.city].filter(Boolean).join(' ').trim()||'Unbekannt';cityMap[key]=(cityMap[key]||0)+1});
 const cities=Object.entries(cityMap).sort((a,b)=>b[1]-a[1]).slice(0,5).map(([label,count])=>({label,count}));
 const upcoming=rows.filter(r=>r.next_follow_up_at).sort((a,b)=>new Date(a.next_follow_up_at)-new Date(b.next_follow_up_at)).slice(0,4);
 return {total,withPhone,withEmail,complete,contacted,followups,uncontacted,categories,cities,upcoming};
}
function territoryBookProgressBar(label,count,total,accent='brown'){const pct=total?Math.max(0,Math.min(100,Math.round(count/total*100))):0;return `<div class="territoryStatBar ${accent}"><div><b>${esc(label)}</b><span>${count} / ${total}</span></div><div class="territoryStatTrack"><i style="width:${pct}%"></i></div></div>`}
function territoryBookCardHtml(r,admin){
 const map=territoryMapHref(r),statusLabel=TERRITORY_DIR_STATUS[r.status]||r.status||'Aktiv',catLabel=r.category_label||territoryDirCategoryLabel(r.category),phoneLabel=r.phone||r.mobile||'',mailLabel=r.email||'',assigned=(state.sales_reps||[]).find(x=>x.id===r.assigned_sales_rep_id),contactState=r.last_contacted_at?`${dt(r.last_contacted_at)} · ${esc(TERRITORY_CONTACT_RESULTS[r.last_contact_result]||r.last_contact_result||'Kontakt dokumentiert')}`:'Noch nicht kontaktiert',follow=r.next_follow_up_at?`<span class="territoryCardFollow">Wiedervorlage ${dt(r.next_follow_up_at)}</span>`:'',source=r.source==='openstreetmap'?'OSM-Recherche':r.source==='csv'?'CSV-Import':'manuell';
 return `<article class="territoryAddressCard status-${esc(r.status)}"><div class="territoryCardTop"><span class="territoryCategoryPill">${esc(catLabel)}</span><span class="territoryStatusPill">${esc(statusLabel)}</span></div><h4>${esc(r.company||'–')}</h4><div class="territoryAddressMeta"><b>${esc(r.street||'Straße fehlt')}</b><span>${esc([r.zip,r.city].filter(Boolean).join(' ')||'Ort fehlt')}</span></div><div class="territoryMiniList"><div><small>Ansprechpartner</small><b>${esc(r.contact_person||'–')}</b></div><div><small>Zuständig</small><b>${assigned?esc(assigned.name):'Gebiet allgemein'}</b></div><div><small>Quelle</small><b>${esc(source)}</b></div></div><div class="territoryContactList">${phoneLabel?`<a class="territoryQuickLink phone" href="${territoryPhoneHref(phoneLabel)}">☎ ${esc(phoneLabel)}</a>`:'<span class="muted">Telefon fehlt</span>'}${mailLabel?`<a class="territoryQuickLink mail" href="${territoryMailHref(mailLabel,r.company)}">✉ ${esc(mailLabel)}</a>`:'<span class="muted">E-Mail fehlt</span>'}${map?`<a class="territoryQuickLink map" target="_blank" rel="noopener" href="${map}">⌖ Google Maps öffnen</a>`:''}</div><div class="territoryCardStatus"><small>Kontaktstatus</small><b>${contactState}</b>${follow}</div><div class="territoryCardActions"><button class="miniBtn primaryMini" data-tb-contact="${esc(r.id)}">Kontaktiert / Kalender</button><button class="miniBtn" data-tb-history="${esc(r.id)}">Verlauf</button>${admin?`<button class="miniBtn" data-tb-edit="${esc(r.id)}">Bearbeiten</button>`:''}</div></article>`
}
function territoryIndustryGuideHtml(rows){
 const listRows=rows||[];const groups={};listRows.forEach(r=>{const label=r.category_label||territoryDirCategoryLabel(r.category)||'Sonstige';const g=groups[label]||(groups[label]={label,count:0,complete:0,withPhone:0,withEmail:0,cities:{},examples:[]});g.count++;if((r.phone||r.mobile)&&r.email&&r.street&&r.zip&&r.city)g.complete++;if(r.phone||r.mobile)g.withPhone++;if(r.email)g.withEmail++;const city=[r.zip,r.city].filter(Boolean).join(' ').trim()||'Unbekannt';g.cities[city]=(g.cities[city]||0)+1;if(r.company&&g.examples.length<3&&!g.examples.includes(r.company))g.examples.push(r.company)});const items=Object.values(groups).sort((a,b)=>b.count-a.count||a.label.localeCompare(b.label)).slice(0,12);
 return `<section class="panel territoryIndustryGuidePanel"><div class="territoryFeaturedHeader"><div><h3>Branchenführer im Gebiet</h3><span>${items.length} Schwerpunktbranchen für dieses Festgebiet</span></div><small>Der Kundenberater sieht direkt, welche Branchen im eigenen Gebiet relevant sind und kann ohne Suchen in die passende Liste springen.</small></div><div class="territoryIndustryGuideGrid">${items.length?items.map(item=>{const topCity=Object.entries(item.cities).sort((a,b)=>b[1]-a[1])[0];const cityLabel=topCity?`${topCity[0]} · ${topCity[1]} Einträge`:'Noch ohne Ortsschwerpunkt';const examples=item.examples.length?item.examples.map(x=>`<span>${esc(x)}</span>`).join(''):'<span>Beispiele folgen nach Datenpflege</span>';return `<article class="territoryIndustryCard"><div class="territoryIndustryHead"><div><h4>${esc(item.label)}</h4><small>${item.count} Adressen im Gebiet</small></div><b>${Math.round(item.count/Math.max(1,listRows.length)*100)}%</b></div><div class="territoryIndustryMeta"><span>Vollständig <b>${item.complete}</b></span><span>Telefon <b>${item.withPhone}</b></span><span>E-Mail <b>${item.withEmail}</b></span></div><div class="territoryIndustryCity">${esc(cityLabel)}</div><div class="territoryIndustryExamples">${examples}</div><div class="territoryIndustryActions"><button class="miniBtn primaryMini" data-tb-quick-category="${esc(item.label)}">Branche öffnen</button></div></article>`}).join(''):`<div class="territoryFeaturedEmpty"><b>Noch keine Branchen vorhanden</b><span>Sobald Adressen im Gebietsbuch liegen, entsteht hier automatisch ein Branchenführer.</span></div>`}</div></section>`
}

function territoryBookGraphicHtml(code,t,rows,sum,admin){
 const territoryRows=territoryBookRows||[],stats=territoryBookVisualStats(territoryRows),territoryName=t.state_name||state.territory_book_scope?.state_name||'',bookNo=t.territory_book_no||('FG-'+code),owner=t.owner_name||'Noch nicht fest vergeben',ownerRole=t.owner_sales_rep_id?'Gebietsverantwortung aktiv':'Weißgebiet / offen',ownerMembers=Number(t.member_count||0),ownerCustomers=Number(t.customer_count||0),featured=(rows.length?rows:territoryRows).slice(0,6),nextActions=stats.upcoming.length?stats.upcoming.map(r=>`<article><b>${esc(r.company)}</b><span>${esc([r.street,r.zip,r.city].filter(Boolean).join(', '))}</span><small>${dt(r.next_follow_up_at)} · ${esc(r.contact_person||'ohne Ansprechpartner')}</small></article>`).join(''):'<div class="territoryFeaturedEmpty"><b>Keine Wiedervorlagen geplant</b><span>Kontakte können direkt aus den Adressen heraus mit Kalender dokumentiert werden.</span></div>',topCities=stats.cities.length?stats.cities.map(c=>`<li><span>${esc(c.label)}</span><b>${c.count}</b></li>`).join(''):'<li><span>Noch keine Ortsdaten</span><b>0</b></li>',topCategories=stats.categories.length?stats.categories.map(c=>territoryBookProgressBar(c.label,c.count,stats.total,'green')).join(''):territoryBookProgressBar('Noch keine Kategorien',0,0,'green'),completeBars=[territoryBookProgressBar('Mit Telefon',stats.withPhone,stats.total,'brown'),territoryBookProgressBar('Mit E-Mail',stats.withEmail,stats.total,'blue'),territoryBookProgressBar('Vollständige Datensätze',stats.complete,stats.total,'green'),territoryBookProgressBar('Bereits kontaktiert',stats.contacted,stats.total,'gold')].join('');
 return `<section class="panel territoryGraphicPanel"><div class="territoryGraphicHero"><div class="territoryGraphicProfile"><small>GEBIETSBUCH · GRAFIKANSICHT</small><h3>${esc(bookNo)} · ${esc(territoryName)}</h3><p>Das Gebietsbuch wird nicht nur als Liste, sondern als echte Arbeitsoberfläche dargestellt: Karte, Kontaktqualität, Schwerpunktbranchen, Wiedervorlagen und direkte Schnellaktionen.</p><div class="territoryGraphicFlow"><article><b>${sum.active||0}</b><span>aktive Geschäfte & Vereine</span></article><article><b>${sum.candidate||0}</b><span>Recherche-Datensätze</span></article><article><b>${stats.contacted}</b><span>bereits kontaktierte Einträge</span></article><article><b>${sum.followups||stats.followups||0}</b><span>offene Wiedervorlagen</span></article></div><div class="territoryGraphicOwner"><div><small>Gebietsverantwortung</small><b>${esc(owner)}</b><span>${esc(ownerRole)}</span></div><div><small>Team / Kunden</small><b>${ownerMembers} Mitarbeiter</b><span>${ownerCustomers} Kunden im Gebiet</span></div><div class="territoryGraphicButtons"><button class="btn primary" id="tbOpenTerritories">Deutschlandkarte</button>${admin?'<button class="btn" id="tbOpenNationalDirectory">Deutschlandweit</button>':''}</div></div></div><div class="territoryBookOverviewMapWrap"><div id="territoryBookMap" class="territoryMap territoryBookOverviewMap"><div class="territoryMapLoading">Gebietskarte wird geladen …</div></div><div class="territoryMapLegendLine"><span><i class="legendDot fixed"></i> Festgebiet</span><span><i class="legendDot white"></i> Weißgebiet</span><span><i class="legendDot" style="background:${territoryColor(code)}"></i> aktuelles Gebiet</span></div></div></div><div class="territoryGraphicInsightGrid"><section class="territoryInsightCard"><div class="panelTitle"><h3>Schwerpunktbranchen</h3><span>nach Anzahl im Gebiet</span></div><div class="territoryStatBars">${topCategories}</div></section><section class="territoryInsightCard"><div class="panelTitle"><h3>Kontaktqualität</h3><span>wie gut das Gebiet bereits pflegbar ist</span></div><div class="territoryStatBars">${completeBars}</div><div class="territoryHealthLine"><div><small>Noch offen</small><b>${stats.uncontacted}</b></div><div><small>Komplett pflegbar</small><b>${stats.complete}</b></div></div></section><section class="territoryInsightCard"><div class="panelTitle"><h3>Orte & Gebietsschwerpunkte</h3><span>wo die meisten Datensätze liegen</span></div><ul class="territoryCityList">${topCities}</ul></section><section class="territoryInsightCard"><div class="panelTitle"><h3>Nächste Wiedervorlagen</h3><span>direkt aus dem Gebietsbuch</span></div><div class="territoryUpcomingList">${nextActions}</div></section></div><div class="territoryFeaturedHeader"><div><h3>Grafische Adresskarten</h3><span>${featured.length} Einträge als schnelle Arbeitskarten</span></div><small>Ideal für Anruf, E-Mail, Maps und Follow-up ohne Tabellenchaos.</small></div><div class="territoryAddressCardGrid">${featured.length?featured.map(r=>territoryBookCardHtml(r,admin)).join(''):`<div class="territoryFeaturedEmpty"><b>Keine Adressen im aktuellen Filter</b><span>Filter anpassen oder neue Datensätze anlegen.</span></div>`}</div></section>`
}


function renderTerritoryBook(){
 if(!can('view_territory_book'))return setView('dashboard');
 const code=territoryBookScopeCode();
 if(!code){ws.innerHTML=`<div class="sectionHead"><div><h2>Mein Gebietsbuch</h2><p>Ihrem Kundenberaterkonto wurde noch kein Festgebiet zugeordnet.</p></div></div><div class="empty"><b>Kein Gebietsbuch</b>Der Administrator muss zuerst unter Kundenberater → Gebiet vergeben ein Bundesland zuordnen.</div>`;return}
 if(territoryBookRowsCode!==code){ws.innerHTML='<div class="loadingState"><div class="spinner"></div><p>Gebietsbuch wird geladen …</p></div>';territoryBookLoad(code);return}
 const t=(state.sales_territories||[]).find(x=>x.state_code===code)||state.territory_directory_summary?.scope||{},rows=territoryBookFilteredRows(),sum=state.territory_directory_summary||{},cats=state.territory_directory_categories||{},admin=isAdmin();
 const adminActions=admin?'<button class="btn" id="tbDiscover">Branchen-Scout</button><button class="btn" id="tbImport">CSV importieren</button><button class="btn primary" id="tbNew">+ Adresse</button>':'';
 const categoryOptions=Object.entries(cats).map(([k,v])=>`<option value="${k}" ${sel(territoryBookCategoryFilter,k)}>${esc(v)}</option>`).join('');
 const table=rows.length?`<table class="dataTable"><thead><tr><th>Branche</th><th>Firma / Verein</th><th>Adresse</th><th>Ansprechpartner</th><th>Telefon / E-Mail</th><th>Kontaktstatus</th><th>Aktionen</th></tr></thead><tbody>${rows.map(r=>territoryBookRowHtml(r,admin)).join('')}</tbody></table>`:`<div class="empty"><b>Keine Adressen in diesem Filter</b>${admin?'Lege eine Adresse an oder starte den Branchen-Scout.':'Der Administrator pflegt die Adressen Ihres Festgebietes.'}</div>`;
 const graphic=territoryBookGraphicHtml(code,t,rows,sum,admin);ws.innerHTML=`<div class="sectionHead"><div><h2>${admin?'Gebietsbuch-Zentrale':'Mein Gebietsbuch'}</h2><p>${esc(t.territory_book_no||'FG-'+code)} · ${esc(t.state_name||state.territory_book_scope?.state_name||'')} · Branchenadressen, Kontaktarbeit und Wiedervorlagen.</p></div><div class="sectionActions">${adminActions}<button class="btn" id="tbPrint">PDF / Drucken</button></div></div>${territoryBookAdminSelectHtml(code)}${territoryBookExecutiveSummaryHtml(code,t,sum)}${graphic}<div class="metricGrid territoryBookMetrics">${metric('Adressen',sum.total||territoryBookRows.length,'im Gebietsbuch')}${metric('Aktiv',sum.active||0,'geprüfte Geschäfte & Vereine')}${metric('Recherche',sum.candidate||0,'noch zu prüfen')}${metric('Wiedervorlagen',sum.followups||0,'geplant')}</div><div class="territoryBookTabs"><button data-tb-status="active" class="${territoryBookStatusFilter==='active'?'active':''}">Aktive Geschäfte & Vereine</button><button data-tb-status="candidate" class="${territoryBookStatusFilter==='candidate'?'active':''}">Recherche</button><button data-tb-status="followup" class="${territoryBookStatusFilter==='followup'?'active':''}">Kalender / Wiedervorlage</button><button data-tb-status="all" class="${territoryBookStatusFilter==='all'?'active':''}">Alle Adressen</button></div><div class="territoryBookToolbar"><input id="tbSearch" type="search" placeholder="Firma, Ansprechpartner, Telefon, PLZ, Ort …" value="${esc(territoryBookSearch)}"><select id="tbCategory"><option value="all">Alle Branchen</option>${categoryOptions}</select><button class="btn" id="tbRefresh">Aktualisieren</button></div><div class="panel tableWrap territoryBookTable">${table}</div><div class="territorySourceNotice"><b>Datenqualität:</b> Automatisch recherchierte öffentliche Einträge werden zunächst als „Recherche“ markiert. Erst nach Prüfung durch den Administrator erscheinen sie als „Aktiv“. Quelle bei OSM-Importen: © OpenStreetMap-Mitwirkende.</div>`;
 bindTerritoryBook(code)
}
function bindTerritoryBook(code){if($('#tbStateSelect'))$('#tbStateSelect').onchange=()=>{territoryBookAdminState=$('#tbStateSelect').value;territoryBookRowsCode='';renderTerritoryBook()};$$('[data-tb-status]').forEach(b=>b.onclick=()=>{territoryBookStatusFilter=b.dataset.tbStatus;renderTerritoryBook()});$('#tbCategory').onchange=()=>{territoryBookCategoryFilter=$('#tbCategory').value;renderTerritoryBook()};$('#tbSearch').oninput=()=>{territoryBookSearch=$('#tbSearch').value;renderTerritoryBook();setTimeout(()=>{$('#tbSearch')?.focus()},0)};$('#tbRefresh').onclick=()=>{territoryBookRowsCode='';renderTerritoryBook()};$('#tbPrint').onclick=()=>window.open('./territory-book-print.php?state_code='+encodeURIComponent(code),'_blank','noopener');if($('#tbNew'))$('#tbNew').onclick=()=>territoryDirectoryModal(null,code);if($('#tbDiscover'))$('#tbDiscover').onclick=()=>territoryDiscoverModal(code);if($('#tbImport'))$('#tbImport').onclick=()=>territoryCsvModal(code);if($('#tbOpenTerritories'))$('#tbOpenTerritories').onclick=()=>setView('territories');if($('#tbOpenNationalDirectory'))$('#tbOpenNationalDirectory').onclick=()=>{territoryBookAdminState='all';territoryBookRowsCode='';setView('territorybook')};const mapHost=$('#territoryBookMap');if(mapHost)renderTerritoryMiniMap(mapHost,code,isAdmin()?(picked=>{territoryBookAdminState=picked;territoryBookRowsCode='';renderTerritoryBook()}):null);$$('[data-tb-contact]').forEach(b=>b.onclick=()=>territoryContactModal(b.dataset.tbContact));$$('[data-tb-history]').forEach(b=>b.onclick=()=>territoryHistoryModal(b.dataset.tbHistory));$$('[data-tb-edit]').forEach(b=>b.onclick=()=>territoryDirectoryModal(b.dataset.tbEdit,code))}
function territoryDirectoryModal(id,code){const r=territoryBookRows.find(x=>x.id===id)||{},cats=state.territory_directory_categories||{},reps=(state.sales_reps||[]).filter(x=>x.status==='active'&&(!x.state_code||x.state_code===code));openModal(`<div class="modalHead"><small>GEBIETSBUCH · ${esc((state.sales_territories||[]).find(x=>x.state_code===code)?.territory_book_no||code)}</small><h2>${id?'Adresse bearbeiten':'Adresse anlegen'}</h2><p>Administrator pflegt vollständige Branchen- und Kontaktdaten.</p></div><div class="modalBody"><div class="formGrid"><label>Branche<select id="tdCat">${Object.entries(cats).map(([k,v])=>`<option value="${k}" ${sel(r.category||'other',k)}>${esc(v)}</option>`).join('')}</select></label><label>Status<select id="tdStatus"><option value="candidate" ${sel(r.status||'active','candidate')}>Recherche · zu prüfen</option><option value="active" ${sel(r.status||'active','active')}>Aktiv / freigegeben</option><option value="paused" ${sel(r.status,'paused')}>Pausiert</option><option value="archived" ${sel(r.status,'archived')}>Archiviert</option></select></label><label class="full">Firma / Verein<input id="tdCompany" value="${esc(r.company||'')}"></label><label>Ansprechpartner<input id="tdContact" value="${esc(r.contact_person||'')}"></label><label>Zuständiger Kundenberater<select id="tdRep"><option value="">Gesamtes Gebiet / nicht einzeln zugewiesen</option>${reps.map(x=>`<option value="${esc(x.id)}" ${sel(r.assigned_sales_rep_id||'',x.id)}>${esc(x.employee_no)} · ${esc(x.name)}</option>`).join('')}</select></label><label>Telefon<input id="tdPhone" value="${esc(r.phone||'')}"></label><label>Mobil / Handy<input id="tdMobile" value="${esc(r.mobile||'')}"></label><label>E-Mail<input id="tdEmail" type="email" value="${esc(r.email||'')}"></label><label>Webseite<input id="tdWebsite" value="${esc(r.website||'')}"></label><label>Straße / Hausnr.<input id="tdStreet" value="${esc(r.street||'')}"></label><label>PLZ<input id="tdZip" value="${esc(r.zip||'')}"></label><label>Ort<input id="tdCity" value="${esc(r.city||'')}"></label><label class="full">Notiz<textarea id="tdNotes">${esc(r.notes||'')}</textarea></label></div><div class="formActions"><button class="btn" id="tdCancel">Abbrechen</button><button class="btn primary" id="tdSave">Speichern</button></div></div>`);$('#tdCancel').onclick=closeModal;$('#tdSave').onclick=async()=>{try{const d=await api({action:'territory_directory_save',id:r.id||'',state_code:code,category:$('#tdCat').value,status:$('#tdStatus').value,company:$('#tdCompany').value,contact_person:$('#tdContact').value,assigned_sales_rep_id:$('#tdRep').value,phone:$('#tdPhone').value,mobile:$('#tdMobile').value,email:$('#tdEmail').value,website:$('#tdWebsite').value,street:$('#tdStreet').value,zip:$('#tdZip').value,city:$('#tdCity').value,notes:$('#tdNotes').value});territoryBookRows=d.rows||[];state.territory_directory_summary=d.summary||{};territoryBookRowsCode=code;toast('Gebietsbuch-Adresse gespeichert');closeModal();renderTerritoryBook()}catch(e){toast(e.message,true)}}}
function localDateTimeValue(d=new Date()){const x=new Date(d.getTime()-d.getTimezoneOffset()*60000);return x.toISOString().slice(0,16)}
function territoryContactModal(id){const r=territoryBookRows.find(x=>x.id===id);if(!r)return;openModal(`<div class="modalHead"><small>KONTAKTIERT · KALENDER</small><h2>${esc(r.company)}</h2><p>${esc([r.street,r.zip,r.city].filter(Boolean).join(', '))}</p></div><div class="modalBody"><div class="contactShortcutGrid">${r.phone?`<a class="contactShortcut" href="${territoryPhoneHref(r.phone)}"><b>☎ Direkt anrufen</b><span>${esc(r.phone)}</span></a>`:''}${r.email?`<a class="contactShortcut" href="${territoryMailHref(r.email,r.company)}"><b>✉ E-Mail schreiben</b><span>${esc(r.email)}</span></a>`:''}</div><div class="formGrid"><label>Kontakt am<input id="tcAt" type="datetime-local" value="${localDateTimeValue()}"></label><label>Kontaktart<select id="tcMethod"><option value="phone">Telefon</option><option value="email">E-Mail</option><option value="visit">Vor-Ort-Besuch</option><option value="whatsapp">WhatsApp</option><option value="letter">Brief/Post</option><option value="other">Sonstiges</option></select></label><label>Ergebnis<select id="tcResult">${Object.entries(TERRITORY_CONTACT_RESULTS).map(([k,v])=>`<option value="${k}">${esc(v)}</option>`).join('')}</select></label><label>Wiedervorlage / Termin<input id="tcFollow" type="datetime-local"></label><label class="full">Gesprächsnotiz<textarea id="tcNotes" placeholder="Gespräch, Interesse, Bedarf, nächster Schritt …"></textarea></label></div><div class="formActions"><button class="btn" id="tcCancel">Abbrechen</button><button class="btn" id="tcSave">Kontakt speichern</button><button class="btn primary" id="tcSaveCal">Speichern + Kalender</button></div></div>`);$('#tcCancel').onclick=closeModal;const save=async(withCal)=>{try{const follow=$('#tcFollow').value,d=await api({action:'territory_directory_contact',id:r.id,contact_at:$('#tcAt').value,method:$('#tcMethod').value,result:$('#tcResult').value,next_follow_up_at:follow,notes:$('#tcNotes').value});territoryBookRows=d.rows||territoryBookRows;state.territory_directory_summary=d.summary||state.territory_directory_summary;if(withCal&&follow)downloadTerritoryIcs(r,follow,$('#tcNotes').value);toast(follow?'Kontakt und Wiedervorlage gespeichert':'Kontakt gespeichert');closeModal();renderTerritoryBook()}catch(e){toast(e.message,true)}};$('#tcSave').onclick=()=>save(false);$('#tcSaveCal').onclick=()=>save(true)}
function downloadTerritoryIcs(r,localValue,note=''){const start=new Date(localValue),end=new Date(start.getTime()+45*60000),fmt=d=>d.toISOString().replace(/[-:]/g,'').replace(/\.\d{3}Z$/,'Z'),escI=v=>String(v||'').replace(/\\/g,'\\\\').replace(/\n/g,'\\n').replace(/,/g,'\\,').replace(/;/g,'\\;'),lines=['BEGIN:VCALENDAR','VERSION:2.0','PRODID:-//Raeucherhaken24//Gebietsbuch V73//DE','BEGIN:VEVENT','UID:'+Date.now()+'@raeucherhaken24.de','DTSTAMP:'+fmt(new Date()),'DTSTART:'+fmt(start),'DTEND:'+fmt(end),'SUMMARY:'+escI('Wiedervorlage: '+r.company),'LOCATION:'+escI([r.street,r.zip,r.city].filter(Boolean).join(', ')),'DESCRIPTION:'+escI(note),'END:VEVENT','END:VCALENDAR'];const blob=new Blob([lines.join('\r\n')],{type:'text/calendar;charset=utf-8'}),url=URL.createObjectURL(blob),a=document.createElement('a');a.href=url;a.download='Gebietsbuch_'+String(r.company||'Termin').replace(/[^a-zA-Z0-9äöüÄÖÜß_-]+/g,'_')+'.ics';document.body.appendChild(a);a.click();a.remove();setTimeout(()=>URL.revokeObjectURL(url),1200)}
async function territoryHistoryModal(id){const r=territoryBookRows.find(x=>x.id===id);if(!r)return;try{const d=await api({action:'territory_directory_history',id});openModal(`<div class="modalHead"><small>KONTAKTVERLAUF</small><h2>${esc(r.company)}</h2><p>${d.history?.length||0} dokumentierte Kontakte</p></div><div class="modalBody"><div class="territoryHistory">${(d.history||[]).map(h=>`<article><time>${dt(h.contact_at)}</time><div><b>${esc(h.method_label)} · ${esc(h.result_label)}</b><span>${esc(h.sales_rep_name||h.user_name||'Orgaboard')}</span><p>${esc(h.notes||'Keine Notiz')}</p>${h.next_follow_up_at?`<small>Wiedervorlage: ${dt(h.next_follow_up_at)}</small>`:''}</div></article>`).join('')||'<div class="empty">Noch kein Kontakt dokumentiert.</div>'}</div></div>`)}catch(e){toast(e.message,true)}}
function territoryCsvModal(code){openModal(`<div class="modalHead"><small>GEBIETSBUCH · CSV-IMPORT</small><h2>Adressliste importieren</h2><p>Bis zu 500 Zeilen pro Import. Semikolon-getrennt.</p></div><div class="modalBody"><div class="moduleNotice"><b>Spalten:</b> Kategorie;Firma;Ansprechpartner;Telefon;Mobil;Email;Webseite;Straße;PLZ;Ort;Notiz</div><textarea id="tbCsv" class="csvImportArea" placeholder="butcher;Fleischerei Muster;Max Mustermann;+49...;+49 Mobil...;info@...;https://...;Musterweg 1;24100;Musterstadt;Erstkontakt"></textarea><div class="formActions"><button class="btn" id="tbCsvCancel">Abbrechen</button><button class="btn primary" id="tbCsvRun">Importieren</button></div></div>`);$('#tbCsvCancel').onclick=closeModal;$('#tbCsvRun').onclick=async()=>{const lines=$('#tbCsv').value.split(/\r?\n/).map(x=>x.trim()).filter(Boolean),rows=lines.map(line=>{const c=line.split(';');return {category:c[0]||'other',company:c[1]||'',contact_person:c[2]||'',phone:c[3]||'',mobile:c[4]||'',email:c[5]||'',website:c[6]||'',street:c[7]||'',zip:c[8]||'',city:c[9]||'',notes:c.slice(10).join(';')}});try{const d=await api({action:'territory_directory_bulk_import',state_code:code,rows});territoryBookRows=d.rows||[];state.territory_directory_summary=d.summary||{};territoryBookRowsCode=code;territoryBookStatusFilter='candidate';toast((d.imported||0)+' Adressen importiert');closeModal();renderTerritoryBook()}catch(e){toast(e.message,true)}}}


/* === V74 · Deutschlandweites Branchen-/Vereinsverzeichnis === */
function territoryBookScopeCode(){return isAdmin()?(territoryBookAdminState||'all'):(state.territory_book_scope?.state_code||'')}
async function territoryBookLoad(code){if(territoryBookLoading)return;territoryBookLoading=true;try{const d=await api({action:'territory_directory_search',state_code:code==='all'?'':code,status:territoryBookStatusFilter,category:territoryBookCategoryFilter,contact:territoryDirectoryContactFilter,q:territoryBookSearch,limit:territoryDirectoryPageSize,offset:territoryDirectoryOffset});territoryBookRows=d.rows||[];territoryBookRowsCode=code;territoryDirectoryTotal=Number(d.total||0);state.territory_directory_summary=d.summary||{};state.territory_directory_national_summary=d.national_summary||state.territory_directory_national_summary||{};if(!isAdmin())state.territory_book_scope=d.scope||state.territory_book_scope}catch(e){toast(e.message,true);territoryBookRows=[];territoryBookRowsCode=code;territoryDirectoryTotal=0}finally{territoryBookLoading=false;if(current==='territorybook')renderTerritoryBook()}}
function territoryBookFilteredRows(){return territoryBookRows}
function territoryBookAdminSelectHtml(code){if(!isAdmin())return '';const opts=(state.sales_territories||[]).map(x=>`<option value="${x.state_code}" ${sel(code,x.state_code)}>${esc(x.territory_book_no)} · ${esc(x.state_name)}</option>`).join('');return `<div class="territoryBookAdminSelect nationalDirectorySelect"><label>Datenbereich<select id="tbStateSelect"><option value="all" ${sel(code,'all')}>Deutschland · alle 16 Gebiete</option>${opts}</select></label><span>Administrator sieht bundesweit alle Datensätze. Kundenberater erhalten serverseitig ausschließlich ihr eigenes Festgebiet.</span></div>`}
function territoryContactQuality(r){const n=((r.phone||r.mobile)?1:0)+(r.email?1:0)+(r.street&&r.zip&&r.city?1:0);return n===3?'<span class="contactQuality good">● vollständig</span>':n===2?'<span class="contactQuality medium">● teilweise</span>':'<span class="contactQuality low">● ergänzen</span>'}
function territoryBookRowHtml(r,admin){const map=territoryMapHref(r),source=r.source==='openstreetmap'?'OSM':r.source==='csv'?'CSV':'manuell',phone=(r.phone||r.mobile)?`${r.phone?`<a class="contactDirect phone" href="${territoryPhoneHref(r.phone)}">☎ ${esc(r.phone)}</a>`:''}${r.mobile?`${r.phone?'<br>':''}<a class="contactDirect phone" href="${territoryPhoneHref(r.mobile)}">📱 ${esc(r.mobile)}</a>`:''}`:'<span class="muted">Telefon / Mobil fehlt</span>',mail=r.email?`<a class="contactDirect mail" href="${territoryMailHref(r.email,r.company)}">✉ ${esc(r.email)}</a>`:'<span class="muted">E-Mail fehlt</span>',contact=r.last_contacted_at?`<b>${dt(r.last_contacted_at)}</b><br><small>${esc(TERRITORY_CONTACT_RESULTS[r.last_contact_result]||r.last_contact_result||'')}</small>`:'<span class="status s-pending">Noch nicht kontaktiert</span>',follow=r.next_follow_up_at?`<br><span class="tbFollow">Wiedervorlage ${dt(r.next_follow_up_at)}</span>`:'',territory=admin?`<b>${esc(r.territory_book_no||('FG-'+(r.state_code||'')))}</b><br><small>${esc(r.state_name||territoryStateName(r.state_code||''))}</small>`:'';return `<tr class="tb-${esc(r.status)}">${admin?`<td>${territory}</td>`:''}<td><span class="tbCategory">${esc(r.category_label||territoryDirCategoryLabel(r.category))}</span><br><small>${source}</small></td><td><b>${esc(r.company)}</b>${r.brand?`<br><small>Marke: ${esc(r.brand)}</small>`:''}<br>${territoryContactQuality(r)}</td><td>${esc(r.street||'–')}<br><b>${esc(r.zip||'')} ${esc(r.city||'')}</b>${map?`<br><a target="_blank" rel="noopener" href="${map}">Maps ↗</a>`:''}</td><td>${esc(r.contact_person||'–')}</td><td>${phone}<br>${mail}</td><td>${contact}${follow}</td><td><div class="rowActions"><button class="miniBtn primaryMini" data-tb-contact="${esc(r.id)}">Kontaktiert / Kalender</button><button class="miniBtn" data-tb-history="${esc(r.id)}">Verlauf</button>${admin?`<button class="miniBtn" data-tb-edit="${esc(r.id)}" data-tb-state="${esc(r.state_code||'')}">Bearbeiten</button>`:''}</div></td></tr>`}
function territoryBookCategoryOverviewHtml(sum){
 const categories=sum?.categories||{},entries=Object.entries(categories).sort((a,b)=>Number(b[1])-Number(a[1])||territoryDirCategoryLabel(a[0]).localeCompare(territoryDirCategoryLabel(b[0]))),total=Number(sum?.total||0);
 return `<section class="panel territoryBookBranchIndex"><div class="territoryFeaturedHeader"><div><h3>Branchen im Gebietsbuch</h3><span>${entries.length} Branchen · ${total.toLocaleString('de-DE')} gefundene Adressen</span></div><small>Alle gefundenen Branchen sind dem Gebietsbuch zugeordnet. Ein Klick öffnet die vollständige Branchenliste.</small></div><div class="territoryBranchIndexGrid">${entries.length?entries.map(([key,count])=>{const pct=total?Math.round(Number(count)/total*100):0;return `<button type="button" class="territoryBranchIndexCard" data-tb-quick-category="${esc(key)}"><span>${esc(territoryDirCategoryLabel(key))}</span><b>${Number(count).toLocaleString('de-DE')}</b><small>${pct}% des Gebietsbuchs</small><i><em style="width:${Math.max(3,pct)}%"></em></i></button>`}).join(''):`<div class="territoryFeaturedEmpty"><b>Noch keine Branchen importiert</b><span>Deutschland-Synchronisierung oder Bundesland-Recherche starten.</span></div>`}</div></section>`
}
function territoryNationalBooksHtml(national){
 const books=national?.states||[];return `<section class="panel territoryBooksNationalPanel"><div class="territoryFeaturedHeader"><div><h3>16 Gebietsbücher · Deutschland</h3><span>Jeder Treffer landet automatisch im richtigen Bundesland-Gebietsbuch</span></div><small>FG-Nummer, Gebietsinhaber, Branchenanzahl, Recherche- und Aktivbestand auf einen Blick.</small></div><div class="territoryBooksNationalGrid">${books.map(b=>{const cats=Object.entries(b.categories||{}).sort((a,c)=>Number(c[1])-Number(a[1])).slice(0,3),owner=b.owner_name||'Weißgebiet / nicht vergeben';return `<button type="button" class="territoryNationalBookCard ${b.owner_sales_rep_id?'owned':'white'}" data-open-territory-book="${esc(b.state_code)}" style="--territory-color:${territoryColor(b.state_code)}"><div class="territoryNationalBookHead"><i></i><div><small>${esc(b.territory_book_no||('FG-'+b.state_code))}</small><b>${esc(b.state_name||'')}</b></div><span>${Number(b.total||0).toLocaleString('de-DE')}</span></div><div class="territoryNationalBookOwner"><small>Gebietsverantwortung</small><b>${esc(owner)}</b></div><div class="territoryNationalBookStats"><span><b>${Number(b.category_count||0)}</b> Branchen</span><span><b>${Number(b.active||0)}</b> aktiv</span><span><b>${Number(b.candidate||0)}</b> Recherche</span></div><div class="territoryNationalBookCats">${cats.length?cats.map(([k,n])=>`<span>${esc(territoryDirCategoryLabel(k))}<b>${Number(n)}</b></span>`).join(''):'<span>Noch keine Branchen importiert</span>'}</div><div class="territoryNationalBookFoot"><span>${Number(b.with_phone||0)} Telefon · ${Number(b.with_email||0)} E-Mail</span><b>Gebietsbuch öffnen →</b></div></button>`}).join('')}</div></section>`
}

function territoryBookExecutiveSummaryHtml(code,t,sum){const total=Number(sum.total||0),active=Number(sum.active||0),candidate=Number(sum.candidate||0),contacted=Number(sum.contacted||0),complete=Number(sum.complete||0),withPhone=Number(sum.with_phone||0),withEmail=Number(sum.with_email||0),quality=total?Math.round(complete/total*100):0,progress=total?Math.round(contacted/total*100):0,activation=total?Math.round(active/total*100):0,territory=state.sales_territories?.find(x=>x.state_code===code)||t||{};return `<section class="territoryExecutive"><div class="territoryExecutiveMain"><div><small>GEBIETSPERFORMANCE</small><h3>${esc(territory.territory_book_no||('FG-'+code))} · ${esc(territory.state_name||'')}</h3><p>Vertriebsreife des Gebietes: Datenqualität, Freigabequote, Kontaktfortschritt und Verantwortlichkeit.</p></div><div class="territoryExecOwner"><small>Verantwortlich</small><b>${esc(territory.owner_name||'Noch nicht vergeben')}</b><span>${territory.owner_sales_rep_id?'Festgebiet aktiv':'Weißgebiet'}</span></div></div><div class="territoryExecGauges">${executiveDonut(activation,'Aktivquote',`${active}/${total} geprüft`,'green')}${executiveDonut(quality,'Datenqualität',`${complete} vollständig`,'blue')}${executiveDonut(progress,'Kontaktfortschritt',`${contacted} kontaktiert`,'gold')}<div class="territoryExecNumbers"><div><small>Recherche</small><b>${candidate}</b></div><div><small>Telefon</small><b>${withPhone}</b></div><div><small>E-Mail</small><b>${withEmail}</b></div><div><small>Follow-ups</small><b>${Number(sum.followups||0)}</b></div></div></div></section>`}

function renderTerritoryBook(){
 if(!can('view_territory_book'))return setView('dashboard');const code=territoryBookScopeCode();if(!code){ws.innerHTML=`<div class="sectionHead"><div><h2>Mein Adressverzeichnis</h2><p>Ihrem Kundenberaterkonto wurde noch kein Festgebiet zugeordnet.</p></div></div><div class="empty"><b>Kein Gebietsbuch</b>Der Administrator muss zuerst ein Festgebiet zuordnen.</div>`;return}
 if(territoryBookRowsCode!==code){ws.innerHTML='<div class="loadingState"><div class="spinner"></div><p>Adressverzeichnis wird geladen …</p></div>';territoryBookLoad(code);return}
 const admin=isAdmin(),all=admin&&code==='all',t=all?{}:((state.sales_territories||[]).find(x=>x.state_code===code)||state.territory_directory_summary?.scope||{}),rows=territoryBookRows,sum=state.territory_directory_summary||{},national=state.territory_directory_national_summary||sum,cats=state.territory_directory_categories||{};
 const adminActions=admin?`${all?'<button class="btn primary" id="tbNationalSync">Deutschland synchronisieren</button>':'<button class="btn" id="tbDiscover">Bundesland recherchieren</button><button class="btn" id="tbImport">CSV importieren</button><button class="btn primary" id="tbNew">+ Adresse</button>'}`:'';
 const categoryOptions=Object.entries(cats).map(([k,v])=>`<option value="${k}" ${sel(territoryBookCategoryFilter,k)}>${esc(v)}</option>`).join('');const start=territoryDirectoryTotal?territoryDirectoryOffset+1:0,end=Math.min(territoryDirectoryOffset+rows.length,territoryDirectoryTotal);
 const table=rows.length?`<table class="dataTable nationalDirectoryTable"><thead><tr>${admin?'<th>Gebiet</th>':''}<th>Branche</th><th>Firma / Verein</th><th>Adresse</th><th>Ansprechpartner</th><th>Telefon / E-Mail</th><th>Kontaktstatus</th><th>Aktionen</th></tr></thead><tbody>${rows.map(r=>territoryBookRowHtml(r,admin)).join('')}</tbody></table>`:`<div class="empty"><b>Keine Treffer</b>Suchfilter anpassen oder ${admin?'die Deutschland-Synchronisierung starten.':'den Administrator um Ergänzung des Gebietsbuchs bitten.'}</div>`;
 const graphic=!all?territoryBookGraphicHtml(code,t,rows,sum,admin):'',industryGuide=!all?territoryBookCategoryOverviewHtml(sum):'',bookOverview=all?territoryNationalBooksHtml(national):'';ws.innerHTML=`<div class="sectionHead"><div><h2>${all?'Deutschland · Branchen & Vereine':admin?'Gebietsbuch-Zentrale':'Mein Adressverzeichnis'}</h2><p>${all?'Bundesweiter Administrator-Datenbestand · alle Fest- und Weißgebiete':esc(t.territory_book_no||'FG-'+code)+' · '+esc(t.state_name||state.territory_book_scope?.state_name||'')} · Geschäfte, Vereine, Gastronomie und branchennaher Handel.</p></div><div class="sectionActions">${adminActions}${!all?'<button class="btn" id="tbPrint">PDF / Drucken</button>':''}</div></div>${territoryBookAdminSelectHtml(code)}${!all?territoryBookExecutiveSummaryHtml(code,t,sum):''}${bookOverview}${graphic}${industryGuide}<div class="metricGrid territoryBookMetrics">${metric('Adressen',all?(national.total||territoryDirectoryTotal):(sum.total||territoryDirectoryTotal),all?'deutschlandweit':'im Gebietsbuch')}${metric('Aktiv',all?(national.active||0):(sum.active||0),'geprüfte Datensätze')}${metric('Mit Telefon',all?(national.with_phone||0):(sum.with_phone||0),'direkt anwählbar')}${metric('Mit E-Mail',all?(national.with_email||0):(sum.with_email||0),'direkt anschreibbar')}${metric('Vollständig',all?(national.complete||0):(sum.complete||0),'Adresse + Telefon + E-Mail')}</div><div class="territoryBookTabs"><button data-tb-status="all" class="${territoryBookStatusFilter==='all'?'active':''}">Alle gefundenen</button><button data-tb-status="active" class="${territoryBookStatusFilter==='active'?'active':''}">Aktiv / freigegeben</button><button data-tb-status="candidate" class="${territoryBookStatusFilter==='candidate'?'active':''}">Recherche / zu prüfen</button><button data-tb-status="followup" class="${territoryBookStatusFilter==='followup'?'active':''}">Wiedervorlagen</button></div><div class="territoryBookToolbar professionalDirectorySearch"><div class="directorySearchMain"><span>⌕</span><input id="tbSearch" type="search" placeholder="Name, Straße, PLZ, Ort, Telefon, E-Mail, Ansprechpartner …" value="${esc(territoryBookSearch)}"><button class="btn primary" id="tbSearchRun">Suchen</button></div><select id="tbCategory"><option value="all">Alle Branchen</option>${categoryOptions}</select><select id="tbContactFilter"><option value="all" ${sel(territoryDirectoryContactFilter,'all')}>Alle Kontaktdaten</option><option value="phone" ${sel(territoryDirectoryContactFilter,'phone')}>Nur mit Telefon</option><option value="email" ${sel(territoryDirectoryContactFilter,'email')}>Nur mit E-Mail</option><option value="complete" ${sel(territoryDirectoryContactFilter,'complete')}>Vollständige Kontakte</option><option value="missing" ${sel(territoryDirectoryContactFilter,'missing')}>Kontaktdaten ergänzen</option></select><button class="btn" id="tbReset">Filter zurücksetzen</button></div><div class="directoryResultBar"><b>${territoryDirectoryTotal.toLocaleString('de-DE')} Treffer</b><span>${start.toLocaleString('de-DE')}–${end.toLocaleString('de-DE')}</span><div><button class="miniBtn" id="tbPrev" ${territoryDirectoryOffset<=0?'disabled':''}>← Zurück</button><button class="miniBtn" id="tbNext" ${end>=territoryDirectoryTotal?'disabled':''}>Weiter →</button></div></div><div class="panel tableWrap territoryBookTable">${table}</div><div class="territorySourceNotice"><b>Datenqualität & Quelle:</b> Öffentliche OSM-Daten können unvollständig oder veraltet sein. Telefonnummer und E-Mail werden nur übernommen, wenn sie öffentlich erfasst sind. Automatische Treffer bleiben bis zur Prüfung „Recherche“. © OpenStreetMap-Mitwirkende · ODbL.</div>`;bindTerritoryBook(code)
}
function bindTerritoryBook(code){if($('#tbStateSelect'))$('#tbStateSelect').onchange=()=>{territoryBookAdminState=$('#tbStateSelect').value;territoryDirectoryOffset=0;territoryBookRowsCode='';renderTerritoryBook()};$$('[data-tb-status]').forEach(b=>b.onclick=()=>{territoryBookStatusFilter=b.dataset.tbStatus;territoryDirectoryOffset=0;territoryBookRowsCode='';renderTerritoryBook()});$('#tbCategory').onchange=()=>{territoryBookCategoryFilter=$('#tbCategory').value;territoryDirectoryOffset=0;territoryBookRowsCode='';renderTerritoryBook()};$('#tbContactFilter').onchange=()=>{territoryDirectoryContactFilter=$('#tbContactFilter').value;territoryDirectoryOffset=0;territoryBookRowsCode='';renderTerritoryBook()};const run=()=>{territoryBookSearch=$('#tbSearch').value.trim();territoryDirectoryOffset=0;territoryBookRowsCode='';renderTerritoryBook()};$('#tbSearchRun').onclick=run;$('#tbSearch').onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();run()}};$('#tbReset').onclick=()=>{territoryBookSearch='';territoryBookCategoryFilter='all';territoryDirectoryContactFilter='all';territoryBookStatusFilter='active';territoryDirectoryOffset=0;territoryBookRowsCode='';renderTerritoryBook()};if($('#tbPrev'))$('#tbPrev').onclick=()=>{territoryDirectoryOffset=Math.max(0,territoryDirectoryOffset-territoryDirectoryPageSize);territoryBookRowsCode='';renderTerritoryBook()};if($('#tbNext'))$('#tbNext').onclick=()=>{territoryDirectoryOffset+=territoryDirectoryPageSize;territoryBookRowsCode='';renderTerritoryBook()};if($('#tbPrint'))$('#tbPrint').onclick=()=>window.open('./territory-book-print.php?state_code='+encodeURIComponent(code),'_blank','noopener');if($('#tbNew'))$('#tbNew').onclick=()=>territoryDirectoryModal(null,code);if($('#tbDiscover'))$('#tbDiscover').onclick=()=>territoryDiscoverModal(code);if($('#tbImport'))$('#tbImport').onclick=()=>territoryCsvModal(code);if($('#tbNationalSync'))$('#tbNationalSync').onclick=territoryNationalSyncModal;if($('#tbOpenTerritories'))$('#tbOpenTerritories').onclick=()=>setView('territories');if($('#tbOpenNationalDirectory'))$('#tbOpenNationalDirectory').onclick=()=>{territoryBookAdminState='all';territoryDirectoryOffset=0;territoryBookRowsCode='';setView('territorybook')};$$('[data-open-territory-book]').forEach(b=>b.onclick=()=>{territoryBookAdminState=b.dataset.openTerritoryBook;territoryBookStatusFilter='all';territoryBookCategoryFilter='all';territoryDirectoryOffset=0;territoryBookRowsCode='';renderTerritoryBook()});const mapHost=$('#territoryBookMap');if(mapHost&&code!=='all')renderTerritoryMiniMap(mapHost,code,isAdmin()?(picked=>{territoryBookAdminState=picked;territoryDirectoryOffset=0;territoryBookRowsCode='';renderTerritoryBook()}):null);$$('[data-tb-quick-category]').forEach(b=>b.onclick=()=>{territoryBookCategoryFilter=b.dataset.tbQuickCategory||'all';territoryBookStatusFilter='all';territoryDirectoryOffset=0;territoryBookRowsCode='';renderTerritoryBook()});$$('[data-tb-contact]').forEach(b=>b.onclick=()=>territoryContactModal(b.dataset.tbContact));$$('[data-tb-history]').forEach(b=>b.onclick=()=>territoryHistoryModal(b.dataset.tbHistory));$$('[data-tb-edit]').forEach(b=>b.onclick=()=>territoryDirectoryModal(b.dataset.tbEdit,b.dataset.tbState||code))}
function territoryDiscoverModal(code){openModal(`<div class="modalHead"><small>BRANCHEN-SCOUT · V83.3 · STABILE TEILABFRAGEN</small><h2>${esc((state.sales_territories||[]).find(x=>x.state_code===code)?.state_name||code)} recherchieren</h2><p>Die Recherche wurde gegen 504-Gateway-Timeouts gehärtet. Große Sammelabfragen werden in kleine Branchenpakete zerlegt und bei einem temporären Fehler einmal automatisch wiederholt.</p></div><div class="modalBody"><div class="formGrid"><label class="full">Recherchepaket<select id="tdPack"><option value="all">Vertrieb komplett · alle Kernbranchen + Lebensmittel + passende Gastronomie</option><option value="core">Branchenkern · 7 getrennte, stabile Teilabfragen</option><option value="fishing">Angelgeschäfte & Angelvereine</option><option value="meat">Fleischereien, Metzgereien, Schlachtereien & Bedarf</option><option value="hardware">Baumärkte, Gartencenter & DIY</option><option value="spices">Gewürzhandel, Kräuter, Gewürzmanufakturen & Gewürzmühlen</option><option value="fishery">Fischhandel & Fischverarbeitung</option><option value="smoke_bbq">Räuchern, Smoker, Grill & BBQ</option><option value="outdoor_trade">Outdoor, Jagd & relevanter Großhandel</option><option value="food">Lebensmittelgeschäfte & Märkte</option><option value="relevant_restaurants">Branchennahe Gastronomie · Fisch, Seafood, BBQ, Grill, regional</option><option value="restaurants">Alle Restaurants · optional, sehr große Abfrage</option></select></label></div><div class="moduleNotice"><b>Neu V83.3:</b> Angelgeschäfte, Baumärkte, Schlachtereien sowie Gewürzläden, Gewürzhersteller, Gewürzmanufakturen, Gewürzmühlen, Kräuteranbieter und Fleischer-/Schlachtereibedarf werden gezielt recherchiert. Treffer bleiben zunächst im Status „Recherche“.</div><pre id="tdDiscLog" class="nationalSyncLog" style="max-height:190px"></pre><div class="formActions"><button class="btn" id="tdDiscCancel">Abbrechen</button><button class="btn primary" id="tdDiscRun">Recherche starten</button></div></div>`);$('#tdDiscCancel').onclick=closeModal;$('#tdDiscRun').onclick=async()=>{const b=$('#tdDiscRun'),selected=$('#tdPack').value,packs=territoryDiscoveryPacks(selected),log=$('#tdDiscLog');b.disabled=true;let found=0,ok=0,failed=0;for(let i=0;i<packs.length;i++){const p=packs[i];b.textContent=`${i+1}/${packs.length} · ${territoryPackLabel(p)}`;try{const d=await territoryDiscoveryRequest(code,p,1,(attempt,e)=>{log.textContent+=`↻ ${territoryPackLabel(p)} · Wiederholung nach Fehler: ${e.message}\n`;log.scrollTop=log.scrollHeight});found+=Number(d.found||0);ok++;log.textContent+=`✓ ${territoryPackLabel(p)} · ${Number(d.found||0).toLocaleString('de-DE')} Treffer\n`}catch(e){failed++;log.textContent+=`✕ ${territoryPackLabel(p)} · ${e.message}\n`}log.scrollTop=log.scrollHeight;if(i<packs.length-1)await new Promise(r=>setTimeout(r,650))}territoryBookRowsCode='';territoryBookStatusFilter='candidate';b.disabled=false;b.textContent='Recherche erneut starten';toast(`${found.toLocaleString('de-DE')} Treffer · ${ok} Teilabfragen OK${failed?' · '+failed+' Fehler':''}`,failed>0&&ok===0);if(ok>0){setTimeout(()=>{closeModal();renderTerritoryBook()},700)}}}
function territoryNationalSyncModal(){if(!isAdmin())return;territoryNationalSyncRunning=false,territorySearchTerm='',territorySelectedCode='';openModal(`<div class="modalHead"><small>DEUTSCHLAND · DATEN-SYNCHRONISIERUNG · V83.3</small><h2>Geschäfte, Vereine & Branchenkontakte importieren</h2><p>Statt einer riesigen Abfrage verarbeitet das Orgaboard jede Branche separat. Ein temporärer Overpass-/Gateway-Fehler wird einmal automatisch wiederholt und stoppt nicht mehr den gesamten Deutschlandlauf.</p></div><div class="modalBody"><div class="formGrid"><label class="full">Umfang<select id="nationalPack"><option value="all">Vertrieb komplett · Kernbranchen + Lebensmittel + passende Gastronomie</option><option value="core">Branchenkern · Angel + Fleisch + Baumarkt + Gewürze + Fisch + BBQ + Outdoor</option><option value="fishing">Nur Angelgeschäfte & Angelvereine</option><option value="meat">Nur Fleischereien, Metzgereien, Schlachtereien & Bedarf</option><option value="hardware">Nur Baumärkte & Gartencenter</option><option value="spices">Nur Gewürze, Kräuter, Manufakturen & Gewürzmühlen</option><option value="fishery">Nur Fischhandel & Fischverarbeitung</option><option value="smoke_bbq">Nur Räuchern / Smoker / Grill / BBQ</option><option value="outdoor_trade">Nur Outdoor / Jagd / relevanter Großhandel</option><option value="food">Lebensmittelgeschäfte & Märkte</option><option value="relevant_restaurants">Nur branchennahe Gastronomie</option><option value="restaurants">Alle Restaurants · optional, langsam</option></select></label></div><div class="moduleNotice"><b>504-Schutz aktiv:</b> Server-Anfragen laufen mit kurzen Zeitlimits und Fallback-Instanz. Bereits importierte OSM-Datensätze werden über ihre OSM-Referenz aktualisiert statt dupliziert. Der Lauf kann deshalb jederzeit neu gestartet werden.</div><div class="nationalSyncProgress"><div><span id="nationalSyncText">Bereit</span><b id="nationalSyncPct">0 %</b></div><progress id="nationalSyncBar" max="16" value="0"></progress><pre id="nationalSyncLog"></pre></div><div class="formActions"><button class="btn" id="nationalSyncCancel">Schließen</button><button class="btn primary" id="nationalSyncStart">Deutschland starten</button></div></div>`);$('#nationalSyncCancel').onclick=()=>{territoryNationalSyncRunning=false,territorySearchTerm='',territorySelectedCode='';closeModal()};$('#nationalSyncStart').onclick=async()=>{const btn=$('#nationalSyncStart'),pack=$('#nationalPack').value,states=[...(state.sales_territories||[])],packs=territoryDiscoveryPacks(pack),steps=states.length*packs.length;btn.disabled=true;territoryNationalSyncRunning=true;$('#nationalSyncBar').max=steps;let ok=0,failed=0,step=0,totalFound=0;for(let i=0;i<states.length&&territoryNationalSyncRunning;i++){const t=states[i];let stateFound=0,stateFailed=0;for(const p of packs){if(!territoryNationalSyncRunning)break;step++;const label=territoryPackLabel(p);$('#nationalSyncText').textContent=`${i+1}/16 · ${t.state_name} · ${label}`;$('#nationalSyncPct').textContent=Math.round((step-1)/steps*100)+' %';try{const d=await territoryDiscoveryRequest(t.state_code,p,1,(attempt,e)=>{$('#nationalSyncLog').textContent+=`↻ ${t.state_name} · ${label} · Wiederholung: ${e.message}\n`});const n=Number(d.found||0);stateFound+=n;totalFound+=n;ok++;$('#nationalSyncLog').textContent+=`→ ${d.territory_book_no||t.territory_book_no||('FG-'+t.state_code)} · ${t.state_name} · ${label}: ${n.toLocaleString('de-DE')} zugeordnet\n`}catch(e){stateFailed++;failed++;$('#nationalSyncLog').textContent+=`✕ ${t.state_name} · ${label}: ${e.message}\n`}$('#nationalSyncBar').value=step;$('#nationalSyncLog').scrollTop=$('#nationalSyncLog').scrollHeight;await new Promise(r=>setTimeout(r,650))}$('#nationalSyncLog').textContent+=`${stateFailed?'⚠':'✓'} ${t.state_name}: ${stateFound.toLocaleString('de-DE')} Treffer${stateFailed?' · '+stateFailed+' Teilfehler':''}\n`;$('#nationalSyncLog').scrollTop=$('#nationalSyncLog').scrollHeight}territoryNationalSyncRunning=false,territorySearchTerm='',territorySelectedCode='';$('#nationalSyncPct').textContent='100 %';$('#nationalSyncText').textContent=`Fertig · ${totalFound.toLocaleString('de-DE')} Treffer · ${ok} Teilabfragen OK · ${failed} Fehler`;btn.textContent='Erneut starten';btn.disabled=false;territoryBookAdminState='all';territoryBookStatusFilter='all';territoryBookRowsCode='';territoryDirectoryOffset=0;toast('Deutschland-Synchronisierung abgeschlossen · Treffer nach 16 Gebietsbüchern zugeordnet')} }


function salesExecutiveOverviewHtml(rows){const territories=state.sales_territories||[],national=state.territory_directory_national_summary||{},active=rows.filter(r=>r.status==='active'),fixed=territories.filter(t=>t.owner_sales_rep_id).length,customers=rows.reduce((s,r)=>s+Number(r.customer_count||0),0),monthRevenue=active.reduce((s,r)=>s+Number(r.stats_current?.gross_goods||0),0),commission=active.reduce((s,r)=>s+Number(r.stats_current?.commission||0)+Number(r.stats_current?.team_leader_bonus||0),0),top=[...active].sort((a,b)=>Number(b.stats_current?.gross_goods||0)-Number(a.stats_current?.gross_goods||0)).slice(0,4);return `<section class="salesExecutive"><div class="salesExecutiveHead"><div><small>VERTRIEB · MANAGEMENTANSICHT</small><h3>Organisation, Gebiete und Leistung</h3><p>Führung, Kundenberater, Festgebiete und Gebietsbuchqualität werden als zusammenhängendes Vertriebsnetz dargestellt.</p></div><div class="salesExecScore"><b>${Math.round(fixed/16*100)}%</b><span>Gebietsabdeckung</span><small>${fixed}/16 Festgebiete vergeben</small></div></div><div class="salesExecKpis"><div><small>Aktive Mitarbeiter</small><b>${active.length}</b><span>${rows.length} insgesamt</span></div><div><small>Kundenstamm</small><b>${customers.toLocaleString('de-DE')}</b><span>zugeordnete Kunden</span></div><div><small>Umsatz Vertrieb</small><b>${money(monthRevenue)}</b><span>aktueller Statistikzeitraum</span></div><div><small>Provision</small><b>${money(commission)}</b><span>inkl. Teamleiterbonus</span></div><div><small>Gebietskontakte</small><b>${Number(national.total||0).toLocaleString('de-DE')}</b><span>${Number(national.active||0).toLocaleString('de-DE')} aktiv</span></div></div>${top.length?`<div class="salesExecTop"><div><b>Top-Berater</b><span>nach Umsatz</span></div>${top.map((r,i)=>`<article><i>${i+1}</i><span><b>${esc(r.name)}</b><small>${esc(salesRoleLabel(r.sales_role))} · ${esc(r.state_code?territoryStateName(r.state_code):'ohne Gebiet')}</small></span><strong>${money(r.stats_current?.gross_goods||0)}</strong></article>`).join('')}</div>`:''}</section>`}

function renderSalesReps(){const rows=publicSalesReps(),active=rows.filter(r=>r.status==='active').length,totalRevenue=rows.reduce((a,r)=>a+Number(r.revenue||0),0),roleRows=['platform','territory'].includes(salesRepTab)?[]:rows.filter(r=>r.sales_role===salesRepTab);if(salesRepTab==='platform'){ws.innerHTML=`<div class="sectionHead"><div><h2>Kundenberater & Vertriebsorganisation</h2><p>Kundenberater-Plattform, Festgebiete, Rollen, Zugänge, Rechte, Gebietsbücher und Provision zentral steuern.</p></div></div><div class="metricGrid repMetrics">${metric('Vertrieb aktiv',active,rows.length+' Mitarbeiter')}${metric('Regionalmanager',rows.filter(r=>r.sales_role==='regional_manager').length,'oberste Vertriebsführung')}${metric('Teamleiter',rows.filter(r=>r.sales_role==='team_leader').length,'+3 % bei aktivem Team')}${metric('Berufseinsteiger',rows.filter(r=>r.sales_role==='advisor_be'&&r.status==='active').length,'Kundenberater BE')}</div>${salesExecutiveOverviewHtml(rows)}${salesRepTabsHtml()}${advisorPlatformHtml()}`;$$('[data-sales-tab]').forEach(b=>b.onclick=()=>{salesRepTab=b.dataset.salesTab;renderSalesReps()});bindAdvisorPlatform();return}ws.innerHTML=`<div class="sectionHead"><div><h2>Kundenberater & Vertriebsorganisation</h2><p>Festgebiete, Führungsrollen, Berufseinsteiger, Kundenstamm und Provision in einer klaren Struktur verwalten.</p></div><button class="btn primary" id="newSalesRep">+ ${salesRepTab==='territory'?'Kundenberater':esc(salesRoleLabel(salesRepTab))}</button></div><div class="metricGrid repMetrics">${metric('Vertrieb aktiv',active,rows.length+' Mitarbeiter')}${metric('Regionalmanager',rows.filter(r=>r.sales_role==='regional_manager').length,'oberste Vertriebsführung')}${metric('Teamleiter',rows.filter(r=>r.sales_role==='team_leader').length,'+3 % bei aktivem Team')}${metric('Berufseinsteiger',rows.filter(r=>r.sales_role==='advisor_be'&&r.status==='active').length,'Kundenberater BE')}</div>${salesExecutiveOverviewHtml(rows)}${salesRepTabsHtml()}${salesRepTab==='territory'?renderTerritoryAssignmentInsideReps():`<div class="roleStructureNotice">${salesRepTab==='regional_manager'?'<b>Regionalmanager</b><span>Übergeordnete Vertriebsführung. Kann Bezirksmanager führen und selbst ein Festgebiet besitzen.</span>':salesRepTab==='district_manager'?'<b>Bezirksmanager</b><span>Berichtet an einen Regionalmanager und führt Teamleiter bzw. operative Vertriebseinheiten.</span>':salesRepTab==='team_leader'?'<b>Teamleiter</b><span>Führt Kundenberater im Außendienst. Bei eigenem aktivem Team greift der bestehende +3-%-Bonus.</span>':salesRepTab==='advisor_be'?'<b>Kundenberater BE · Berufseinsteiger</b><span>Onboarding-Stufe. BE kann einem Teamleiter zugeordnet werden, erhält aber kein eigenes Festgebiet als Hauptinhaber.</span>':'<b>Kundenberater Aktiv</b><span>Voll aktive Kundenberater mit Kundenstamm und provisionsfähigen Verkäufen.</span>'}</div><div class="panel tableWrap">${roleRows.length?`<table class="dataTable"><thead><tr><th>Nr.</th><th>Mitarbeiter</th><th>Rolle</th><th>Festgebiet</th><th>Führungskraft</th><th>Team</th><th>Kunden</th><th>Provision</th><th>Status</th><th></th></tr></thead><tbody>${roleRows.map(r=>`<tr><td><b>${esc(r.employee_no)}</b></td><td><b>${esc(r.name)}</b><br><small>${esc(r.email||'')}</small></td><td>${salesRepRoleBadge(r.sales_role)}</td><td>${r.state_code?`<span class="territoryMini" style="--territory-color:${territoryColor(r.state_code)}"><i></i>${esc(territoryStateName(r.state_code))}</span><br><small>${esc((state.sales_territories||[]).find(t=>t.state_code===r.state_code)?.territory_book_no||'')}</small>`:'<span class="territoryWhitePill">nicht zugeordnet</span>'}</td><td>${r.parent_name?`<b>${esc(r.parent_name)}</b><br><small>${esc(r.parent_role_label||'')}</small>`:'–'}</td><td>${r.team?.team_size?`<b>${r.team.team_size}</b> direkt<br><small>${r.team?.is_team_leader?'TL-Bonus '+Number(r.team.bonus_rate||0)+' %':'Führung'}</small>`:'–'}</td><td>${r.customer_count}</td><td class="money">${money(r.stats_current?.commission||0)}${Number(r.stats_current?.team_leader_bonus||0)>0?`<br><small>inkl. ${money(r.stats_current.team_leader_bonus)} TL</small>`:''}</td><td>${r.status==='active'?'<span class="status s-paid">Aktiv</span>':'<span class="status s-cancelled">Inaktiv</span>'}</td><td><div class="rowActions"><button class="miniBtn" data-rep-stats="${esc(r.id)}">Abrechnung</button><button class="miniBtn" data-rep-rights="${esc(r.user_id||'')}">Rechte</button><button class="miniBtn" data-edit-rep="${esc(r.id)}">Bearbeiten</button></div></td></tr>`).join('')}</tbody></table>`:'<div class="empty"><b>Noch niemand in dieser Funktion</b>Lege den ersten Mitarbeiter dieser Rolle an.</div>'}</div>`}`;$$('[data-sales-tab]').forEach(b=>b.onclick=()=>{salesRepTab=b.dataset.salesTab;renderSalesReps()});$('#newSalesRep').onclick=()=>salesRepModal(null,'','',salesRepTab==='territory'?'advisor_be':salesRepTab);$$('[data-rep-stats]').forEach(b=>b.onclick=()=>repStatsModal(b.dataset.repStats));$$('[data-rep-rights]').forEach(b=>b.onclick=()=>b.dataset.repRights?rightsModal(b.dataset.repRights):toast('Für dieses Konto ist noch kein Login verknüpft.',true));$$('[data-edit-rep]').forEach(b=>b.onclick=()=>salesRepModal(b.dataset.editRep));if($('#openFullTerritoryMap'))$('#openFullTerritoryMap').onclick=()=>setView('territories');$$('[data-save-inline-territory]').forEach(b=>b.onclick=async()=>{const code=b.dataset.saveInlineTerritory,select=$(`[data-inline-territory-owner="${code}"]`);try{const d=await api({action:'territory_assign',state_code:code,sales_rep_id:select.value});state.sales_territories=d.territories;state.sales_reps=d.sales_reps;toast(select.value?'Festgebiet vergeben':'Gebiet als Weißgebiet freigegeben');renderSalesReps()}catch(e){toast(e.message,true)}})}

/* === V73 restore: stable V72 shared modules === */
function salesRepParentOptions(current='',role='advisor_active',selfId=''){const reps=publicSalesReps().filter(x=>x.status==='active'&&x.id!==selfId);const allowed=x=>{if(role==='regional_manager')return false;if(role==='district_manager')return x.sales_role==='regional_manager';if(role==='team_leader')return ['regional_manager','district_manager'].includes(x.sales_role);return ['regional_manager','district_manager','team_leader','advisor_active','advisor_be'].includes(x.sales_role)};return `<option value="">${role==='regional_manager'?'Keine · Regionalmanager ist oberste Führungsebene':'– keine Führungskraft zugeordnet –'}</option>`+reps.filter(allowed).map(x=>`<option value="${esc(x.id)}" data-state="${esc(x.state_code||'')}" data-role="${esc(x.sales_role||'advisor_active')}" ${sel(current,x.id)}>${esc(x.employee_no)} · ${esc(x.name)} · ${esc(salesRoleLabel(x.sales_role))}${x.state_code?' · '+esc(territoryStateName(x.state_code)):''}</option>`).join('')}
function salesRepModal(id,presetState='',presetParent='',presetRole=''){
 const r=(state.sales_reps||[]).find(x=>x.id===id)||{},territories=state.sales_territories||[];let stateCode=presetState||r.state_code||'',parentId=presetParent||r.parent_sales_rep_id||'',role=presetRole||r.sales_role||'advisor_active';
 openModal(`<div class="modalHead"><small>VERTRIEBSORGANISATION</small><h2>${id?'Mitarbeiter bearbeiten':'Vertriebsmitarbeiter anlegen'}</h2><p>${id?esc(r.employee_no):'Mitarbeiternummer wird automatisch vergeben, wenn leer.'} <span class="orgaboardVersionPill">V76 AKTIV</span></p></div><div class="modalBody"><div class="formGrid"><label>Mitarbeiternummer<input id="srNo" value="${esc(r.employee_no||'')}" placeholder="leer = automatisch"></label><label>Name<input id="srName" value="${esc(r.name||'')}"></label><label>E-Mail<input id="srEmail" type="email" value="${esc(r.email||'')}"></label><label>Telefon<input id="srPhone" value="${esc(r.phone||'')}"></label><label>Funktion<select id="srRole">${Object.entries(SALES_ROLE_LABELS).map(([k,l])=>`<option value="${k}" ${sel(role,k)}>${esc(l)}</option>`).join('')}</select></label><label>Bundesland / primäres Gebiet<select id="srState"><option value="">– noch kein Gebiet –</option>${territories.map(t=>`<option value="${t.state_code}" ${sel(stateCode,t.state_code)}>${esc(t.state_name)} · ${esc(t.territory_book_no)} · ${t.owner_sales_rep_id?'Festgebiet':'Weißgebiet'}</option>`).join('')}</select></label><label class="full">Übergeordnete Führungskraft<select id="srParent">${salesRepParentOptions(parentId,role,id||'')}</select><small id="srParentHelp"></small></label><label>Status<select id="srStatus"><option value="active" ${sel(r.status||'active','active')}>Aktiv</option><option value="inactive" ${sel(r.status,'inactive')}>Inaktiv</option></select></label><label class="full">Interne Notiz<textarea id="srNotes">${esc(r.notes||'')}</textarea></label></div><div id="srRoleNotice" class="moduleNotice compact"></div>
 <section class="repTerritoryPreview"><div class="panelTitle"><h3>Gebietsbuch & Karte</h3><span id="srTerritoryMeta">Gebiet auswählen</span></div><div class="repTerritoryPreviewGrid"><div id="srMiniMap" class="territoryMap miniTerritoryMap"><div class="territoryMapLoading">Karte wird geladen …</div></div><div class="repTerritoryInfo"><div id="srTerritoryFacts"></div><div class="repTerritoryActions"><button type="button" class="btn primary" id="srOpenBook">Gebietsbuch öffnen</button><button type="button" class="btn" id="srOpenMap">Deutschlandkarte öffnen</button></div></div></div></section>
 <div id="srSaveStatus" class="repSaveStatus" role="status" aria-live="polite"></div><div class="formActions"><button type="button" class="btn" id="cancelRep">Abbrechen</button><button type="button" class="btn primary" id="saveRep">Mitarbeiter speichern</button></div></div>`);
 const saveStatus=(msg,type='')=>{const box=$('#srSaveStatus');if(!box)return;box.textContent=msg||'';box.className='repSaveStatus'+(type?' '+type:'')};
 const syncTerritoryPreview=()=>{stateCode=$('#srState').value;const t=territories.find(x=>x.state_code===stateCode),book=$('#srOpenBook');if(t){$('#srTerritoryMeta').textContent=`${t.territory_book_no} · ${t.state_name}`;$('#srTerritoryFacts').innerHTML=`<b>${esc(t.territory_book_no)} · ${esc(t.state_name)}</b><span>${t.owner_sales_rep_id?'Festgebiet · Gebietsschutz aktiv':'Weißgebiet · noch frei'}</span><small>${t.member_count||0} Mitarbeiter · ${t.customer_count||0} Kunden</small>`;book.disabled=false}else{$('#srTerritoryMeta').textContent='Noch kein Gebiet ausgewählt';$('#srTerritoryFacts').innerHTML='<b>Kein Gebietsbuch zugeordnet</b><span>Wähle oben ein Bundesland aus.</span>';book.disabled=true}renderTerritoryMiniMap($('#srMiniMap'),stateCode,code=>{if(!$('#srState').disabled){$('#srState').value=code;syncTerritoryPreview()}})};
 const syncParent=()=>{const p=$('#srParent').selectedOptions[0],pc=p?.dataset.state||'',pr=p?.dataset.role||'';const sameTeam=['advisor_active','advisor_be'].includes(role)&&['team_leader','advisor_active','advisor_be'].includes(pr);if(sameTeam&&pc){$('#srState').value=pc;$('#srState').disabled=true;$('#srParentHelp').textContent='Gebiet wird vom Teamleiter/Team übernommen.'}else{$('#srState').disabled=false;$('#srParentHelp').textContent=role==='advisor_be'?'BE sollte einem Teamleiter zugeordnet werden.':''}syncTerritoryPreview()};
 const refreshRole=()=>{role=$('#srRole').value;const old=$('#srParent').value;$('#srParent').innerHTML=salesRepParentOptions(old,role,id||'');$('#srParent').disabled=role==='regional_manager';const notices={regional_manager:'Regionalmanager ist die oberste Führungsebene. Er kann Bezirksmanager führen und optional selbst ein Festgebiet besitzen.',district_manager:'Bezirksmanager kann einem Regionalmanager zugeordnet werden und führt Teamleiter bzw. operative Einheiten.',team_leader:'Teamleiter führt Kundenberater. Der +3-%-Bonus greift nach bestehender Regel, sobald ein aktives Team vorhanden ist.',advisor_active:'Aktiver Kundenberater. Wird ihm selbst ein weiterer Kundenberater zugeordnet, wird er automatisch Teamleiter.',advisor_be:'Berufseinsteiger. Für BE ist die Zuordnung zu einem Teamleiter empfohlen; ein eigenes Festgebiet als Hauptinhaber ist gesperrt.'};$('#srRoleNotice').innerHTML='<b>'+esc(salesRoleLabel(role))+':</b> '+esc(notices[role]||'');syncParent()};
 $('#srRole').onchange=refreshRole;$('#srParent').onchange=syncParent;$('#srState').onchange=syncTerritoryPreview;refreshRole();
 $('#cancelRep').onclick=closeModal;
 $('#srOpenMap').onclick=()=>{closeModal();setView('territories')};
 $('#srOpenBook').onclick=()=>{const code=$('#srState')?.value||stateCode;if(!code)return;territoryBookAdminState=code;territoryBookRowsCode='';closeModal();setView('territorybook')};
 $('#saveRep').onclick=async()=>{
  const b=$('#saveRep'),cancel=$('#cancelRep');saveStatus('Datenbankverbindung wird geprüft …');b.disabled=true;b.textContent='Prüfe …';
  try{
    const payload={action:'sales_rep_save',id:r.id||'',employee_no:$('#srNo').value.trim(),name:$('#srName').value.trim(),email:$('#srEmail').value.trim(),phone:$('#srPhone').value.trim(),sales_role:$('#srRole').value,state_code:$('#srState').value,parent_sales_rep_id:$('#srParent').disabled?'':$('#srParent').value,status:$('#srStatus').value,notes:$('#srNotes').value};
    if(!payload.name)throw new Error('Bitte einen Namen eintragen.');
    const probe=await api({action:'sales_rep_save_probe',id:r.id||''});
    saveStatus(`Datenbank bereit · Schema ${probe.schema_version||'–'} · speichert jetzt …`,'working');b.textContent='Speichert …';
    const d=await api(payload);
    if(Array.isArray(d.sales_reps))state.sales_reps=d.sales_reps;else if(d.sales_rep){const i=(state.sales_reps||[]).findIndex(x=>x.id===d.sales_rep.id);if(i>=0)state.sales_reps[i]=d.sales_rep;else(state.sales_reps||(state.sales_reps=[])).push(d.sales_rep)}
    if(Array.isArray(d.territories))state.sales_territories=d.territories;
    saveStatus(`✓ Dauerhaft gespeichert · ${d.sales_rep?.employee_no||payload.employee_no||''} · ${salesRoleLabel(d.sales_rep?.sales_role||payload.sales_role)} · Server V74.2.1`,'success');
    b.textContent='Gespeichert ✓';b.disabled=false;cancel.textContent='Schließen';
    toast('Mitarbeiter dauerhaft gespeichert');
    if(current==='salesreps')renderSalesReps();
  }catch(e){saveStatus('Speichern fehlgeschlagen: '+(e.message||'Unbekannter Fehler'),'error');toast(e.message||'Speichern nicht möglich.',true);b.disabled=false;b.textContent='Erneut speichern'}
}
}

function renderDealers(){const rows=state.dealers||[];ws.innerHTML=`<div class="sectionHead"><div><h2>Händler</h2><p>B2B-Kunden, zuständigen Außendienst und verbindlichen 14-Tage-Besuchsrhythmus zentral verwalten.</p></div><button class="btn primary" id="newDealer">+ Händler anlegen</button></div><div class="moduleNotice"><b>Besuchsstandard:</b> Nach jedem erledigten Händlerbesuch wird automatisch ein neuer Termin in 14 Tagen geplant.</div><div class="panel tableWrap">${rows.length?`<table class="dataTable"><thead><tr><th>Händler</th><th>Kontakt / Adresse</th><th>Außendienst</th><th>Letzter Besuch</th><th>Nächster Besuch</th><th>Stufe</th><th></th></tr></thead><tbody>${rows.map(h=>`<tr><td class="customerCell"><b>${esc(h.company)}</b><small>${esc(h.id)}</small></td><td>${esc(h.contact||'')}<br><small>${esc(h.street||'')} · ${esc(h.zip||'')} ${esc(h.city||'')}<br>${esc(h.email||'')}</small></td><td>${esc(repName(h.sales_rep_id)||'Nicht zugeordnet')}</td><td>${h.last_visit_at?donly(h.last_visit_at):'–'}</td><td><b>${h.next_visit_at?donly(h.next_visit_at):'–'}</b></td><td>${esc(h.tier)} · ${Number(h.discount||0)} %</td><td><button class="miniBtn" data-open-dealer="${esc(h.id)}">Bearbeiten</button></td></tr>`).join('')}</tbody></table>`:'<div class="empty"><b>Noch keine Händler</b>Lege B2B-Kunden mit Rabattstufe an.</div>'}</div>`;$('#newDealer').onclick=()=>dealerModal(null)}
function dealerModal(id){const h=(state.dealers||[]).find(x=>x.id===id)||{},customers=state.customers||[],reps=state.sales_reps||[];const today=(h.last_visit_at?String(h.last_visit_at).slice(0,10):new Date().toISOString().slice(0,10));openModal(`<div class="modalHead"><small>HÄNDLER · 14-TAGE-BESUCHSRHYTHMUS</small><h2>${id?'Händler bearbeiten':'Händler anlegen'}</h2><p>Der Folgetermin wird automatisch 14 Tage nach dem letzten Besuch geplant.</p></div><div class="modalBody"><div class="formGrid"><label>Firma<input id="hdCompany" value="${esc(h.company||'')}"></label><label>Ansprechpartner<input id="hdContact" value="${esc(h.contact||'')}"></label><label>E-Mail<input id="hdEmail" type="email" value="${esc(h.email||'')}"></label><label>Telefon<input id="hdPhone" value="${esc(h.phone||'')}"></label><label class="full">Straße / Hausnummer<input id="hdStreet" value="${esc(h.street||'')}"></label><label>PLZ<input id="hdZip" value="${esc(h.zip||'')}"></label><label>Ort<input id="hdCity" value="${esc(h.city||'')}"></label><label class="full">Mit Kundenakte verknüpfen<select id="hdCustomer"><option value="">Automatisch über Firma/E-Mail zuordnen</option>${customers.map(c=>`<option value="${esc(c.id)}" ${sel(h.customer_id||'',c.id)}>${esc(c.company||c.name)} · ${esc(c.city||'')} · ${esc(c.email||'')}</option>`).join('')}</select></label><label class="full">Zuständiger Außendienst<select id="hdRep"><option value="">Nicht zugeordnet</option>${reps.map(r=>`<option value="${esc(r.id)}" ${sel(h.sales_rep_id||'',r.id)}>${esc(r.employee_no)} · ${esc(r.name)}</option>`).join('')}</select></label><label>Letzter persönlicher Besuch<input id="hdLastVisit" type="date" value="${today}"></label><label>Besuchsintervall<input value="14 Tage" disabled></label><label>Stufe<select id="hdTier"><option ${sel(h.tier||'Bronze','Bronze')}>Bronze</option><option ${sel(h.tier,'Silber')}>Silber</option><option ${sel(h.tier,'Gold')}>Gold</option></select></label><label>Status<select id="hdStatus"><option value="active" ${sel(h.status||'active','active')}>Aktiv</option><option value="blocked" ${sel(h.status,'blocked')}>Gesperrt</option></select></label><label class="full">Notiz zum letzten Besuch<textarea id="hdVisitNote">${esc(h.last_visit_note||'')}</textarea></label></div><div class="formActions"><button class="btn primary" id="saveDealer">Speichern & Folgetermin planen</button></div></div>`);$('#saveDealer').onclick=async()=>{try{await api({action:'dealer_save',id:h.id||'',company:$('#hdCompany').value,contact:$('#hdContact').value,email:$('#hdEmail').value,phone:$('#hdPhone').value,street:$('#hdStreet').value,zip:$('#hdZip').value,city:$('#hdCity').value,customer_id:$('#hdCustomer').value,sales_rep_id:$('#hdRep').value,last_visit_at:$('#hdLastVisit').value,last_visit_note:$('#hdVisitNote').value,tier:$('#hdTier').value,status:$('#hdStatus').value});toast('Händler gespeichert · Folgetermin in 14 Tagen geplant');closeModal();dealerCalendarLoaded=false;await load(true);if(current==='dealers')renderDealers()}catch(e){toast(e.message,true)}}}
function bindQuickProductControls(){if(!can('edit_products'))return;$$('[data-quick-online]').forEach(cb=>cb.onchange=async()=>{const id=cb.dataset.quickOnline,old=!cb.checked;cb.disabled=true;try{const r=await api({action:'product_quick_update',id,shop_visible:cb.checked});const i=(state.catalog||[]).findIndex(p=>p.id===id);if(i>=0)state.catalog[i]={...state.catalog[i],...(r.product||{})};const span=cb.closest('.quickOnline')?.querySelector('span');if(span)span.textContent=cb.checked?'Online':'Offline';toast(cb.checked?'Artikel ist jetzt online':'Artikel wurde aus dem Shop genommen')}catch(e){cb.checked=old;toast(e.message,true)}finally{cb.disabled=false}});const bindWeight=(attr,key,label)=>{$$(`[${attr}]`).forEach(btn=>btn.onclick=async()=>{const id=btn.getAttribute(attr),wrap=btn.closest('.quickPrice'),inp=wrap?.querySelector('input'),val=Math.max(0,Math.round(Number(inp?.value||0)));if(!Number.isFinite(val))return toast('Bitte einen gültigen Gewichtswert eingeben.',true);btn.disabled=true;try{const r=await api({action:'product_quick_update',id,[key]:val});const i=(state.catalog||[]).findIndex(p=>p.id===id);if(i>=0)state.catalog[i]={...state.catalog[i],...(r.product||{})};if(inp)inp.value=String(r.product?.[key]??val);toast(label+' gespeichert')}catch(e){toast(e.message,true)}finally{btn.disabled=false}})};$$('[data-quick-price-save]').forEach(btn=>btn.onclick=async()=>{const id=btn.dataset.quickPriceSave,wrap=btn.closest('.quickPrice'),inp=wrap?.querySelector('input'),val=Number(inp?.value||0);if(!Number.isFinite(val)||val<0)return toast('Bitte einen gültigen Preis eingeben.',true);btn.disabled=true;try{const r=await api({action:'product_quick_update',id,base:val});const i=(state.catalog||[]).findIndex(p=>p.id===id);if(i>=0)state.catalog[i]={...state.catalog[i],...(r.product||{})};if(inp)inp.value=Number(r.product?.base??val).toFixed(2);toast('Preis gespeichert')}catch(e){toast(e.message,true)}finally{btn.disabled=false}});bindWeight('data-quick-product-weight-save','product_weight_g','Produktgewicht');bindWeight('data-quick-shipping-weight-save','shipping_weight_g','Versandgewicht');$$('[data-quick-price], [data-quick-product-weight], [data-quick-shipping-weight]').forEach(inp=>inp.onkeydown=e=>{if(e.key==='Enter'){e.preventDefault();inp.closest('.quickPrice')?.querySelector('button')?.click()}})}
function legacyRenderProductsV83_1(){const q=(productSearchTerm||searchTerm||'').toLowerCase().trim();const rows=(state.catalog||[]).filter(p=>!q||`${p.name} ${p.sku||''} ${p.article_no||''} ${p.barcode||''} ${p.category||''} ${p.id}`.toLowerCase().includes(q));const inv=Object.fromEntries((state.inventory||[]).map(x=>[x.id,x]));const qw=(p,key,inputAttr,saveAttr)=>can('edit_products')?`<div class="quickPrice weightQuick"><input ${inputAttr}="${esc(p.id)}" type="number" min="0" step="1" value="${Number(p[key]||0)}"><button class="quickPriceSave" ${saveAttr}="${esc(p.id)}" title="Gewicht speichern">✓</button></div>`:`<span>${weightLabel(p[key])}</span>`;ws.innerHTML=`<div class="sectionHead"><div><h2>Artikel & Produkte</h2><p>Professionelle Schnellsuche, Barcode-Scanner, Preis, Gewichte und Shopstatus in einer Ansicht.</p></div><div class="sectionActions"><b class="muted tiny">${rows.length} Artikel</b>${can('edit_products')?'<button class="btn primary" id="newProduct">+ Neuen Artikel anlegen</button>':''}</div></div>${productSearchToolbar(productSearchTerm)}${!can('edit_products')?'<div class="moduleNotice"><b>Lesemodus:</b> Produktdaten und Preise können eingesehen, aber nicht verändert werden.</div>':'<div class="moduleNotice"><b>Schnellbearbeitung:</b> Suche nach Artikel, Art.-Nr., SKU oder Barcode/EAN. Preis und Gewichte direkt eintragen und mit ✓ speichern; Online-Haken ohne Bearbeitungsdialog setzen.</div>'}<div class="panel tableWrap">${rows.length?`<table class="dataTable"><thead><tr><th>Bild</th><th>Art.-Nr.</th><th>Barcode / EAN</th><th>Artikel</th><th>SKU</th><th>Kategorie</th><th>Preis</th><th>Produktgewicht</th><th>Versandgewicht</th><th>Bestand</th><th>Online</th><th>Bearbeiten</th></tr></thead><tbody>${rows.map(p=>`<tr><td>${p.image?`<img class="productThumb" src="../${esc(p.image)}" alt="">`:'<div class="productThumb emptyThumb">◇</div>'}</td><td><b>${esc(p.article_no||'–')}</b></td><td>${esc(p.barcode||'–')}</td><td class="customerCell"><b>${esc(p.name)}</b><small>${esc(p.id)}</small></td><td>${esc(p.sku||'–')}</td><td>${esc(p.category||'–')}</td><td>${can('edit_products')?`<div class="quickPrice"><input data-quick-price="${esc(p.id)}" type="number" min="0" step="0.01" value="${Number(p.base||0).toFixed(2)}"><button class="quickPriceSave" data-quick-price-save="${esc(p.id)}">✓</button></div>`:`<span class="money">${money(p.base)}</span>`}</td><td>${qw(p,'product_weight_g','data-quick-product-weight','data-quick-product-weight-save')}</td><td>${qw(p,'shipping_weight_g','data-quick-shipping-weight','data-quick-shipping-weight-save')}</td><td>${p.type==='prototype'?'–':Number(inv[p.id]?.stock??p.stock??0)}</td><td>${can('edit_products')&&p.type!=='prototype'?`<label class="quickOnline"><input type="checkbox" data-quick-online="${esc(p.id)}" ${p.shop_visible&&p.status==='active'?'checked':''}><span>${p.shop_visible&&p.status==='active'?'Online':'Offline'}</span></label>`:(p.shop_visible&&p.status==='active'?'<span class="status s-paid">Online</span>':'<span class="status s-cancelled">Nicht sichtbar</span>')}</td><td>${can('edit_products')?`<button class="miniBtn" data-open-product="${esc(p.id)}">Bearbeiten</button>`:'<span class="muted tiny">Nur Ansicht</span>'}</td></tr>`).join('')}</tbody></table>`:'<div class="empty"><b>Keine Produkte gefunden</b>Suchbegriff oder Barcode prüfen.</div>'}</div>`;if(can('edit_products')&&$('#newProduct'))$('#newProduct').onclick=()=>productModal(null);bindQuickProductControls();bindProductSearch(renderProducts)}

function ensureWarehouse(){
 if(!state) return {locations:[],suppliers:[],packaging:[],purchase_orders:[],movements:[],tasks:[]};
 const base=(state.warehouse&&typeof state.warehouse==='object')?state.warehouse:{};
 const defaults={
  locations:[
   {id:'LOC-INBOUND',name:'Wareneingang',type:'inbound',description:'Anlieferung, Prüfung und erste Erfassung'},
   {id:'LOC-MAIN',name:'Hauptlager',type:'storage',description:'Regallager für laufende Bestände'},
   {id:'LOC-PICK',name:'Kommissionierung',type:'picking',description:'Bereitstellung für Fertigung und Versand'},
   {id:'LOC-SHIP',name:'Versandzone',type:'shipping',description:'Verpacken, Etikettieren und Versand'},
   {id:'LOC-RET',name:'Retouren / QS',type:'quality',description:'Prüfung, Nacharbeit und Rückläufer'}
  ],
  suppliers:[],
  packaging:[
   {id:'PACK-BOX-S',name:'Karton klein',stock:0,minimum:20,unit:'Stück',location_id:'LOC-SHIP',supplier_id:'',notes:''},
   {id:'PACK-FILL',name:'Füllmaterial',stock:0,minimum:10,unit:'Beutel',location_id:'LOC-SHIP',supplier_id:'',notes:''},
   {id:'PACK-LABEL',name:'Versandetiketten',stock:0,minimum:50,unit:'Stück',location_id:'LOC-SHIP',supplier_id:'',notes:''}
  ],
  purchase_orders:[],movements:[],tasks:[]
 };
 state.warehouse={
  locations:Array.isArray(base.locations)&&base.locations.length?base.locations:defaults.locations,
  suppliers:Array.isArray(base.suppliers)?base.suppliers:defaults.suppliers,
  packaging:Array.isArray(base.packaging)&&base.packaging.length?base.packaging:defaults.packaging,
  purchase_orders:Array.isArray(base.purchase_orders)?base.purchase_orders:defaults.purchase_orders,
  movements:Array.isArray(base.movements)?base.movements:defaults.movements,
  tasks:Array.isArray(base.tasks)?base.tasks:defaults.tasks,
 };
 return state.warehouse;
}
function warehouseLocationName(id){const wh=ensureWarehouse();return (wh.locations.find(x=>x.id===id)||{}).name||'–'}
function warehouseSupplierName(id){const wh=ensureWarehouse();return (wh.suppliers.find(x=>x.id===id)||{}).name||'–'}
function inventoryHealth(stock,minimum){stock=Number(stock||0);minimum=Number(minimum||0);const ratio=minimum>0?Math.max(0,Math.min(100,Math.round(stock/(minimum*2)*100))):100;if(stock<=0)return{label:'leer',className:'critical',ratio};if(minimum>0&&stock<=minimum)return{label:'kritisch',className:'low',ratio};if(minimum>0&&stock<=minimum*1.5)return{label:'beobachten',className:'watch',ratio};return{label:'stabil',className:'good',ratio};}
function warehouseLowInventory(){return (state.inventory||[]).map(x=>({...x,health:inventoryHealth(x.stock,x.minimum)})).filter(x=>Number(x.stock)<=Number(x.minimum)).sort((a,b)=>(a.stock-a.minimum)-(b.stock-b.minimum))}
function warehouseLowPackaging(){const wh=ensureWarehouse();return (wh.packaging||[]).map(x=>({...x,health:inventoryHealth(x.stock,x.minimum)})).filter(x=>Number(x.stock)<=Number(x.minimum)).sort((a,b)=>(a.stock-a.minimum)-(b.stock-b.minimum))}
function warehouseOpenPurchases(){const wh=ensureWarehouse();return (wh.purchase_orders||[]).filter(x=>!['closed','received','cancelled'].includes(String(x.status||'')))}
function warehouseSuggestions(){return warehouseLowInventory().map(x=>({type:'inventory',id:x.id,name:x.name,qty:Math.max(Number(x.minimum||0)*2-Number(x.stock||0),1),unit:x.unit||'Stück'})).concat(warehouseLowPackaging().map(x=>({type:'packaging',id:x.id,name:x.name,qty:Math.max(Number(x.minimum||0)*2-Number(x.stock||0),1),unit:x.unit||'Stück'}))) }
function warehouseMovementIcon(kind){return({receipt:'↗',issue:'↘',reservation:'⤴',correction_plus:'＋',correction_minus:'－'}[kind]||'•')}
function warehouseMovementText(m){const delta=Number(m.delta||0);return `${delta>0?'+':''}${delta} ${m.target==='packaging'?'Packmittel':'Bestand'}`}
function warehouseStockRows(){const q=(warehouseSearchTerm||'').toLowerCase().trim();const rows=(state.inventory||[]).map(x=>({...x,health:inventoryHealth(x.stock,x.minimum)})).filter(x=>warehouseFilter==='low'?Number(x.stock)<=Number(x.minimum):true).filter(x=>!q||`${x.id} ${x.name} ${x.unit}`.toLowerCase().includes(q));return rows.sort((a,b)=>a.name.localeCompare(b.name,'de'))}
function warehouseModuleTabs(active){const tabs=[['cockpit','Lager-Cockpit'],['stock','Bestände'],['procurement','Beschaffung'],['locations','Lagerorte'],['packaging','Packmittel'],['movements','Bewegungen']];return `<div class="moduleTabs">${tabs.map(([k,l])=>`<button class="moduleTab ${active===k?'active':''}" data-inv-module="${k}">${l}</button>`).join('')}</div>`}

function warehouseCoverageMetrics(){const wh=ensureWarehouse(),moves=wh.movements||[],cut=Date.now()-30*86400000,issue={};moves.forEach(m=>{const ts=Date.parse(m.created_at||'');if(ts>=cut&&m.target==='inventory'&&Number(m.delta||0)<0)issue[m.item_id]=(issue[m.item_id]||0)+Math.abs(Number(m.delta||0));});return (state.inventory||[]).map(x=>{const monthly=issue[x.id]||0,daily=monthly/30,days=daily>0?Math.round(Number(x.stock||0)/daily):null,pressure=Number(x.minimum||0)>0?Math.max(0,Math.min(200,Math.round((Number(x.minimum||0)-Number(x.stock||0))/Number(x.minimum||0)*100+100))):0;return {...x,monthly_issue:monthly,days_cover:days,pressure};}).sort((a,b)=>(a.days_cover??99999)-(b.days_cover??99999))}
function warehouseAnalyticsHtml(){const metrics=warehouseCoverageMetrics(),withUse=metrics.filter(x=>x.monthly_issue>0),avgDays=withUse.length?Math.round(withUse.reduce((s,x)=>s+Math.min(x.days_cover||0,365),0)/withUse.length):null,critical=metrics.filter(x=>Number(x.stock)<=Number(x.minimum)),fast=withUse.sort((a,b)=>b.monthly_issue-a.monthly_issue).slice(0,6),coverage=metrics.slice(0,6);return `<div class="warehouseAnalytics"><section class="panel"><div class="panelTitle"><h3>Bestands-Ampel</h3><span>automatisch</span></div><div class="trafficSummary"><div class="traffic green"><i></i><b>${metrics.filter(x=>Number(x.stock)>Number(x.minimum)*1.5).length}</b><span>stabil</span></div><div class="traffic yellow"><i></i><b>${metrics.filter(x=>Number(x.stock)>Number(x.minimum)&&Number(x.stock)<=Number(x.minimum)*1.5).length}</b><span>beobachten</span></div><div class="traffic red"><i></i><b>${critical.length}</b><span>kritisch</span></div></div><div class="warehouseAnalyticMeta"><span>Ø Reichweite bei Verbrauch <b>${avgDays===null?'noch ohne Bewegungsdaten':avgDays+' Tage'}</b></span><span>30-Tage-Verbrauch erfasst bei <b>${withUse.length}/${metrics.length}</b> Artikeln</span></div></section><section class="panel"><div class="panelTitle"><h3>Verbrauch · Top Positionen</h3><span>letzte 30 Tage</span></div>${fast.length?executiveMiniBars(fast.map(x=>({label:x.name,value:x.monthly_issue,display:`${x.monthly_issue} ${x.unit||'Stk.'}`}))):'<div class="empty"><b>Noch keine Verbrauchsdaten</b>Nach ersten Lagerentnahmen entsteht hier automatisch eine Verbrauchsanalyse.</div>'}</section><section class="panel"><div class="panelTitle"><h3>Reichweite / Nachkaufdruck</h3><span>kritisch zuerst</span></div><div class="coverageList">${coverage.map(x=>`<div class="coverageRow"><span><b>${esc(x.name)}</b><small>${x.days_cover===null?'keine Verbrauchsbasis':x.days_cover+' Tage Reichweite'} · Bestand ${x.stock}</small></span><div class="coverageMeter"><i style="width:${Math.min(100,x.minimum?Number(x.stock)/Math.max(1,Number(x.minimum)*2)*100:100)}%"></i></div><strong>${Number(x.stock)<=Number(x.minimum)?'NACHKAUF':'OK'}</strong></div>`).join('')||'<div class="empty">Keine Lagerartikel.</div>'}</div></section></div>`}

function renderInventory(){const wh=ensureWarehouse(),rows=state.inventory||[],low=warehouseLowInventory(),lowPack=warehouseLowPackaging(),po=warehouseOpenPurchases(),moves=(wh.movements||[]).slice(0,12),coverage=rows.length?Math.round(((rows.length-low.length)/rows.length)*100):100,totalUnits=rows.reduce((s,x)=>s+Number(x.stock||0),0),locationCount=(wh.locations||[]).length,packCount=(wh.packaging||[]).length,labels={cockpit:'Operativer Leitstand mit Warnungen, Nachkauf und letzten Buchungen.',stock:'Artikelbestand, Mindestbestand und direkte Lagerbuchung.',procurement:'Beschaffungsvorschläge, Lieferanten und offene Bestellungen.',locations:'Lagerorte, Funktionszonen und organisatorische Struktur.',packaging:'Versandmaterial, Packmittel und deren Bestände.',movements:'Alle dokumentierten Lagerbewegungen mit Verantwortlichkeit.'};
 let body='';
 if(inventoryModule==='stock'){
   const stockRows=warehouseStockRows();
   body=`<section class="panel softPanel"><div class="panelTitle"><h3>Bestandsliste</h3><span>${stockRows.length} Positionen</span></div><div class="toolbar warehouseToolbar"><input class="filterInput" id="warehouseSearch" placeholder="Artikelname, ID oder Einheit …" value="${esc(warehouseSearchTerm)}"><select class="filterSelect" id="warehouseFilter"><option value="all" ${sel(warehouseFilter,'all')}>Alle</option><option value="low" ${sel(warehouseFilter,'low')}>Nur kritisch</option></select><button class="btn" id="warehouseStockExport">CSV</button></div><div class="tableWrap"><table class="dataTable warehouseTable"><thead><tr><th>Artikel</th><th>Bestand</th><th>Meldebestand</th><th>Status</th><th>Direkt pflegen</th><th>Buchen</th></tr></thead><tbody>${stockRows.map(x=>`<tr><td><b>${esc(x.name)}</b><br><small>${esc(x.id)} · ${esc(x.unit||'Stück')}</small></td><td><strong>${Number(x.stock||0)}</strong></td><td>${Number(x.minimum||0)}</td><td><div class="stockHealth ${x.health.className}"><i style="width:${x.health.ratio}%"></i></div><span class="warehouseTag ${x.health.className}">${x.health.label}</span></td><td>${can('edit_inventory')?`<div class="stockEdit compact"><input type="number" min="0" id="stock-${esc(x.id)}" value="${Number(x.stock||0)}"><input type="number" min="0" id="min-${esc(x.id)}" value="${Number(x.minimum||0)}"><button class="miniBtn" data-save-stock="${esc(x.id)}">Speichern</button></div>`:'<span class="muted tiny">Nur Ansicht</span>'}</td><td>${can('edit_inventory')?`<button class="miniBtn" data-book-stock="${esc(x.id)}">Bewegung</button>`:'—'}</td></tr>`).join('')||`<tr><td colspan="6"><div class="empty"><b>Keine Bestände</b>Noch keine Lagerartikel vorhanden.</div></td></tr>`}</tbody></table></div></section>`;
 } else if(inventoryModule==='procurement'){
   const sugg=warehouseSuggestions();
   body=`<div class="warehouseGrid two"><section class="panel softPanel"><div class="panelTitle"><h3>Nachkaufbedarf</h3><span>${sugg.length} Vorschläge</span></div><div class="stackList">${sugg.map(x=>`<div class="stackItem"><div><b>${esc(x.name)}</b><small>${esc(x.id)} · Sollmenge ${x.qty} ${esc(x.unit)}</small></div><div class="stackActions"><button class="miniBtn" data-new-po="${esc(x.id)}" data-po-type="${esc(x.type)}">Bestellung anlegen</button></div></div>`).join('')||'<div class="empty">Aktuell keine Nachkaufvorschläge.</div>'}</div></section><section class="panel softPanel"><div class="panelTitle"><h3>Lieferanten</h3><span>${(wh.suppliers||[]).length} Kontakte</span></div><div class="stackList">${(wh.suppliers||[]).map(x=>`<div class="stackItem"><div><b>${esc(x.name)}</b><small>${esc(x.email||'')} · ${esc(x.phone||'')} · Lieferzeit ${esc(x.lead_time_days||'–')} Tage</small></div><div class="stackActions"><button class="miniBtn" data-edit-supplier="${esc(x.id)}">Bearbeiten</button></div></div>`).join('')||'<div class="empty">Noch keine Lieferanten hinterlegt.</div>'}</div><div class="formActions"><button class="btn" id="warehouseAddSupplier">Lieferant anlegen</button></div></section></div><section class="panel softPanel"><div class="panelTitle"><h3>Offene Bestellungen</h3><span>${po.length} offen</span></div><div class="stackList">${po.map(x=>`<div class="stackItem"><div><b>${esc(x.title||x.id)}</b><small>${esc(warehouseSupplierName(x.supplier_id))} · ETA ${esc(x.eta||'offen')} · ${esc(x.status||'draft')}</small><small>${(x.lines||[]).map(y=>esc(y)).join(' · ')}</small></div><div class="stackActions"><button class="miniBtn" data-edit-po="${esc(x.id)}">Bearbeiten</button></div></div>`).join('')||'<div class="empty">Keine offenen Einkaufsbestellungen.</div>'}</div><div class="formActions"><button class="btn primary" id="warehouseNewPO">Neue Bestellung</button></div></section>`;
 } else if(inventoryModule==='locations'){
   body=`<div class="warehouseGrid three">${(wh.locations||[]).map(loc=>{const packHere=(wh.packaging||[]).filter(x=>x.location_id===loc.id).length;return `<article class="warehouseCard"><div class="warehouseCardHead"><div><small>${esc(loc.type||'zone').toUpperCase()}</small><h3>${esc(loc.name)}</h3></div><span class="warehouseTag neutral">${packHere} Packmittel</span></div><p>${esc(loc.description||'')}</p><div class="miniStatsRow"><div><small>Packmittel</small><strong>${packHere}</strong></div><div><small>Letzte Bewegung</small><strong>${esc((moves.find(m=>m.location_id===loc.id)||{}).created_at?donly((moves.find(m=>m.location_id===loc.id)||{}).created_at):'–')}</strong></div></div><div class="formActions"><button class="miniBtn" data-edit-location="${esc(loc.id)}">Bearbeiten</button></div></article>`}).join('')}</div><div class="formActions"><button class="btn primary" id="warehouseAddLocation">Lagerort anlegen</button></div>`;
 } else if(inventoryModule==='packaging'){
   const pack=wh.packaging||[];
   body=`<section class="panel softPanel"><div class="panelTitle"><h3>Packmittel & Versandmaterial</h3><span>${pack.length} Positionen</span></div><div class="tableWrap"><table class="dataTable warehouseTable"><thead><tr><th>Artikel</th><th>Bestand</th><th>Meldebestand</th><th>Lagerort</th><th>Lieferant</th><th>Status</th><th></th></tr></thead><tbody>${pack.map(x=>{const health=inventoryHealth(x.stock,x.minimum);return `<tr><td><b>${esc(x.name)}</b><br><small>${esc(x.id)} · ${esc(x.unit||'Stück')}</small></td><td>${Number(x.stock||0)}</td><td>${Number(x.minimum||0)}</td><td>${esc(warehouseLocationName(x.location_id))}</td><td>${esc(warehouseSupplierName(x.supplier_id))}</td><td><div class="stockHealth ${health.className}"><i style="width:${health.ratio}%"></i></div><span class="warehouseTag ${health.className}">${health.label}</span></td><td><div class="rowActions"><button class="miniBtn" data-edit-packaging="${esc(x.id)}">Bearbeiten</button><button class="miniBtn" data-book-packaging="${esc(x.id)}">Buchen</button></div></td></tr>`}).join('')||`<tr><td colspan="7"><div class="empty"><b>Keine Packmittel</b>Lege Kartons, Etiketten und Verbrauchsmaterial an.</div></td></tr>`}</tbody></table></div><div class="formActions"><button class="btn primary" id="warehouseAddPackaging">Packmittel anlegen</button></div></section>`;
 } else if(inventoryModule==='movements'){
   body=`<section class="panel softPanel"><div class="panelTitle"><h3>Lagerbewegungen</h3><span>${(wh.movements||[]).length} Einträge</span></div><div class="movementList">${(wh.movements||[]).map(m=>`<div class="movementItem"><div class="movementIcon ${Number(m.delta||0)>=0?'plus':'minus'}">${warehouseMovementIcon(m.kind)}</div><div><b>${esc(m.item_name||m.item_id)}</b><small>${dt(m.created_at)} · ${esc(m.user_name||'')}</small><small>${esc(m.reason||'ohne Grund')} · ${esc(warehouseLocationName(m.location_id))}</small>${m.note?`<small>${esc(m.note)}</small>`:''}</div><div class="movementValue ${Number(m.delta||0)>=0?'plus':'minus'}">${warehouseMovementText(m)}</div></div>`).join('')||'<div class="empty"><b>Noch keine Bewegungen</b>Wareneingänge und Entnahmen erscheinen hier automatisch.</div>'}</div></section>`;
 } else {
   body=`<div class="warehouseGrid two"><section class="panel softPanel executiveHero"><div class="panelTitle"><h3>Lager-Cockpit</h3><span>operativ</span></div><div class="warehouseStatGrid">${metric('Artikelbestand',rows.length,'aktive Lagerpositionen')}${metric('Packmittel',packCount,'Versandmaterial')}${metric('Warnungen',low.length+lowPack.length,'sofort prüfen',low.length+lowPack.length?'attn':'')}${metric('Offene Bestellungen',po.length,'Beschaffung')}</div><div class="miniStatsRow"><div><small>Bestandsabdeckung</small><strong>${coverage}%</strong></div><div><small>Lagereinheiten</small><strong>${totalUnits}</strong></div><div><small>Lagerorte</small><strong>${locationCount}</strong></div></div><p class="muted">Das Lager ist jetzt nicht mehr nur eine nackte Bestandsliste. Du bekommst einen echten Leitstand mit Beschaffung, Lagerorten, Packmitteln und Bewegungsprotokoll.</p></section><section class="panel softPanel"><div class="panelTitle"><h3>Kritische Positionen</h3><span>${low.length+lowPack.length} kritisch</span></div><div class="stackList">${low.slice(0,6).map(x=>`<div class="stackItem"><div><b>${esc(x.name)}</b><small>${x.stock} ${esc(x.unit||'Stück')} · Minimum ${x.minimum}</small></div><div class="stackActions"><span class="warehouseTag ${x.health.className}">${x.health.label}</span><button class="miniBtn" data-book-stock="${esc(x.id)}">Buchen</button></div></div>`).join('')}${lowPack.slice(0,4).map(x=>`<div class="stackItem"><div><b>${esc(x.name)}</b><small>Packmittel · ${x.stock} ${esc(x.unit||'Stück')} · Minimum ${x.minimum}</small></div><div class="stackActions"><span class="warehouseTag ${x.health.className}">${x.health.label}</span><button class="miniBtn" data-book-packaging="${esc(x.id)}">Buchen</button></div></div>`).join('')||(!low.length?'<div class="empty">Keine kritischen Bestände.</div>':'')}</div></section></div><div class="warehouseGrid two"><section class="panel softPanel"><div class="panelTitle"><h3>Beschaffung als Nächstes</h3><span>${po.length} offen</span></div><div class="stackList">${po.slice(0,5).map(x=>`<div class="stackItem"><div><b>${esc(x.title||x.id)}</b><small>${esc(warehouseSupplierName(x.supplier_id))} · ETA ${esc(x.eta||'offen')} · ${esc(x.status||'draft')}</small></div><div class="stackActions"><button class="miniBtn" data-edit-po="${esc(x.id)}">Öffnen</button></div></div>`).join('')||'<div class="empty">Keine offenen Einkaufsbestellungen.</div>'}</div><div class="formActions"><button class="btn" id="warehouseNewPOFromCockpit">Neue Bestellung</button></div></section><section class="panel softPanel"><div class="panelTitle"><h3>Letzte Bewegungen</h3><span>${moves.length} zuletzt</span></div><div class="movementList compact">${moves.map(m=>`<div class="movementItem"><div class="movementIcon ${Number(m.delta||0)>=0?'plus':'minus'}">${warehouseMovementIcon(m.kind)}</div><div><b>${esc(m.item_name||m.item_id)}</b><small>${dt(m.created_at)} · ${esc(m.user_name||'')}</small></div><div class="movementValue ${Number(m.delta||0)>=0?'plus':'minus'}">${warehouseMovementText(m)}</div></div>`).join('')||'<div class="empty">Noch keine Bewegungen erfasst.</div>'}</div></section></div>`;
 }
 ws.innerHTML=`<div class="sectionHead"><div><h2>Lager & Logistik</h2><p>${labels[inventoryModule]||labels.cockpit}</p></div><div class="sectionActions">${can('edit_inventory')?'<button class="btn" id="inventoryReload">Aktualisieren</button>':''}<button class="btn primary" id="inventoryGoCockpit">Leitstand</button></div></div><div class="warehouseHero">${metric('Bestände',rows.length,'Lagerartikel')}${metric('Kritisch',low.length,'unter Mindestbestand',low.length?'attn':'')}${metric('Packmittel kritisch',lowPack.length,'Versandmaterial',lowPack.length?'attn':'')}${metric('Bewegungen',moves.length,'zuletzt protokolliert')}</div>${warehouseModuleTabs(inventoryModule)}${inventoryModule==='cockpit'?warehouseAnalyticsHtml():''}<div class="warehouseShell">${body}</div>`;
 bindInventory();
}
function bindInventory(){if($('#inventoryReload'))$('#inventoryReload').onclick=()=>load(true);if($('#inventoryGoCockpit'))$('#inventoryGoCockpit').onclick=()=>{inventoryModule='cockpit';localStorage.setItem('rh24-inventory-module',inventoryModule);renderInventory()};$$('[data-inv-module]').forEach(b=>b.onclick=()=>{inventoryModule=b.dataset.invModule;localStorage.setItem('rh24-inventory-module',inventoryModule);renderInventory()});if($('#warehouseSearch'))$('#warehouseSearch').oninput=e=>{warehouseSearchTerm=e.target.value;renderInventory()};if($('#warehouseFilter'))$('#warehouseFilter').onchange=e=>{warehouseFilter=e.target.value;renderInventory()};if($('#warehouseStockExport'))$('#warehouseStockExport').onclick=()=>{const rows=warehouseStockRows();const csv=['Artikel;ID;Bestand;Meldebestand;Einheit'];rows.forEach(x=>csv.push([x.name,x.id,x.stock,x.minimum,x.unit||''].map(v=>`"${String(v).replaceAll('"','""')}"`).join(';')));const blob=new Blob(['\ufeff'+csv.join('\n')],{type:'text/csv;charset=utf-8'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='rh24-lagerbestand-v84.csv';a.click();URL.revokeObjectURL(a.href)};
 $$('[data-save-stock]').forEach(b=>b.onclick=async()=>{const id=b.dataset.saveStock;try{const d=await api({action:'inventory_update',id,stock:+$('#stock-'+CSS.escape(id)).value,minimum:+$('#min-'+CSS.escape(id)).value});const i=(state.inventory||[]).findIndex(x=>x.id===id);if(i>=0&&d.inventory)state.inventory[i]=d.inventory;toast('Bestand gespeichert');renderInventory()}catch(e){toast(e.message,true)}});
 $$('[data-book-stock]').forEach(b=>b.onclick=()=>inventoryBookModal(b.dataset.bookStock,'inventory'));
 $$('[data-book-packaging]').forEach(b=>b.onclick=()=>inventoryBookModal(b.dataset.bookPackaging,'packaging'));
 $$('[data-edit-location]').forEach(b=>b.onclick=()=>warehouseLocationModal(b.dataset.editLocation));if($('#warehouseAddLocation'))$('#warehouseAddLocation').onclick=()=>warehouseLocationModal('');
 $$('[data-edit-supplier]').forEach(b=>b.onclick=()=>warehouseSupplierModal(b.dataset.editSupplier));if($('#warehouseAddSupplier'))$('#warehouseAddSupplier').onclick=()=>warehouseSupplierModal('');
 $$('[data-edit-packaging]').forEach(b=>b.onclick=()=>warehousePackagingModal(b.dataset.editPackaging));if($('#warehouseAddPackaging'))$('#warehouseAddPackaging').onclick=()=>warehousePackagingModal('');
 $$('[data-edit-po]').forEach(b=>b.onclick=()=>warehousePurchaseOrderModal(b.dataset.editPo));if($('#warehouseNewPO'))$('#warehouseNewPO').onclick=()=>warehousePurchaseOrderModal('');if($('#warehouseNewPOFromCockpit'))$('#warehouseNewPOFromCockpit').onclick=()=>warehousePurchaseOrderModal('');$$('[data-new-po]').forEach(b=>b.onclick=()=>warehousePurchaseOrderModal('',{id:b.dataset.newPo,type:b.dataset.poType}));}
async function saveWarehouseState(message='Lagerdaten gespeichert'){try{const d=await api({action:'warehouse_save',warehouse:state.warehouse});state.warehouse=d.warehouse||state.warehouse;toast(message);renderInventory()}catch(e){toast(e.message,true)}}
function inventoryBookModal(id,target='inventory'){const wh=ensureWarehouse();const item=target==='packaging'?(wh.packaging||[]).find(x=>x.id===id):(state.inventory||[]).find(x=>x.id===id);if(!item)return;openModal(`<div class="modalHead"><small>LAGERBUCHUNG</small><h2>${esc(item.name||id)}</h2><p>${target==='packaging'?'Packmittel':'Lagerartikel'} · aktueller Bestand ${Number(item.stock||0)} ${esc(item.unit||'Stück')}</p></div><div class="modalBody"><div class="formGrid"><label>Buchungsart<select id="wbKind"><option value="receipt">Wareneingang (+)</option><option value="issue">Entnahme / Verbrauch (-)</option><option value="correction_plus">Korrektur (+)</option><option value="correction_minus">Korrektur (-)</option></select></label><label>Menge<input id="wbQty" type="number" min="1" step="1" value="1"></label><label>Lagerort<select id="wbLocation"><option value="">ohne Zuordnung</option>${(wh.locations||[]).map(l=>`<option value="${esc(l.id)}">${esc(l.name)}</option>`).join('')}</select></label><label class="full">Grund<input id="wbReason" placeholder="z. B. Wareneingang Lieferant, Kommissionierung, Inventur"></label><label class="full">Notiz<textarea id="wbNote" placeholder="Optionale interne Notiz"></textarea></label></div><div class="formActions"><button class="btn" id="wbCancel">Abbrechen</button><button class="btn primary" id="wbSave">Buchen</button></div></div>`);$('#wbCancel').onclick=closeModal;$('#wbSave').onclick=async()=>{const qty=Math.max(1,parseInt($('#wbQty').value||'1',10));const kind=$('#wbKind').value;const delta=(kind==='receipt'||kind==='correction_plus'?qty:-qty);try{const d=await api({action:'warehouse_stock_book',target,id,delta,kind,location_id:$('#wbLocation').value,reason:$('#wbReason').value,note:$('#wbNote').value});if(d.inventory){const i=(state.inventory||[]).findIndex(x=>x.id===id);if(i>=0)state.inventory[i]=d.inventory}else if(d.packaging){const p=(state.warehouse.packaging||[]).findIndex(x=>x.id===id);if(p>=0)state.warehouse.packaging[p]=d.packaging}state.warehouse=d.warehouse||state.warehouse;closeModal();toast('Lagerbewegung gespeichert');renderInventory()}catch(e){toast(e.message,true)}}}
function warehouseLocationModal(id=''){const wh=ensureWarehouse();const row=(wh.locations||[]).find(x=>x.id===id)||{id:'',name:'',type:'storage',description:''};openModal(`<div class="modalHead"><small>LAGERORT</small><h2>${id?'Lagerort bearbeiten':'Neuen Lagerort anlegen'}</h2></div><div class="modalBody"><div class="formGrid"><label>ID / Kürzel<input id="wlId" value="${esc(row.id)}" placeholder="LOC-MAIN"></label><label>Bezeichnung<input id="wlName" value="${esc(row.name)}"></label><label>Typ<select id="wlType"><option value="inbound" ${sel(row.type,'inbound')}>Wareneingang</option><option value="storage" ${sel(row.type,'storage')}>Lager</option><option value="picking" ${sel(row.type,'picking')}>Kommissionierung</option><option value="shipping" ${sel(row.type,'shipping')}>Versand</option><option value="quality" ${sel(row.type,'quality')}>QS / Retouren</option></select></label><label class="full">Beschreibung<textarea id="wlDesc">${esc(row.description||'')}</textarea></label></div><div class="formActions"><button class="btn" id="wlCancel">Abbrechen</button><button class="btn primary" id="wlSave">Speichern</button></div></div>`);$('#wlCancel').onclick=closeModal;$('#wlSave').onclick=()=>{const payload={id:($('#wlId').value||'').trim()||('LOC-'+Math.random().toString(16).slice(2,6).toUpperCase()),name:$('#wlName').value.trim(),type:$('#wlType').value,description:$('#wlDesc').value.trim()};if(!payload.name)return toast('Bezeichnung fehlt',true);const idx=wh.locations.findIndex(x=>x.id===id||x.id===payload.id);if(idx>=0)wh.locations[idx]=payload;else wh.locations.unshift(payload);closeModal();saveWarehouseState('Lagerort gespeichert')}}
function warehouseSupplierModal(id=''){const wh=ensureWarehouse();const row=(wh.suppliers||[]).find(x=>x.id===id)||{id:'',name:'',email:'',phone:'',lead_time_days:'',notes:''};openModal(`<div class="modalHead"><small>LIEFERANT</small><h2>${id?'Lieferant bearbeiten':'Lieferant anlegen'}</h2></div><div class="modalBody"><div class="formGrid"><label>ID / Kürzel<input id="wsId" value="${esc(row.id)}" placeholder="SUP-01"></label><label>Name<input id="wsName" value="${esc(row.name)}"></label><label>E-Mail<input id="wsMail" value="${esc(row.email||'')}"></label><label>Telefon<input id="wsPhone" value="${esc(row.phone||'')}"></label><label>Lieferzeit (Tage)<input id="wsLead" type="number" min="0" value="${esc(row.lead_time_days||'')}"></label><label class="full">Notiz<textarea id="wsNotes">${esc(row.notes||'')}</textarea></label></div><div class="formActions"><button class="btn" id="wsCancel">Abbrechen</button><button class="btn primary" id="wsSave">Speichern</button></div></div>`);$('#wsCancel').onclick=closeModal;$('#wsSave').onclick=()=>{const payload={id:($('#wsId').value||'').trim()||('SUP-'+Math.random().toString(16).slice(2,6).toUpperCase()),name:$('#wsName').value.trim(),email:$('#wsMail').value.trim(),phone:$('#wsPhone').value.trim(),lead_time_days:$('#wsLead').value.trim(),notes:$('#wsNotes').value.trim()};if(!payload.name)return toast('Lieferantenname fehlt',true);const idx=wh.suppliers.findIndex(x=>x.id===id||x.id===payload.id);if(idx>=0)wh.suppliers[idx]=payload;else wh.suppliers.unshift(payload);closeModal();saveWarehouseState('Lieferant gespeichert') }}
function warehousePackagingModal(id=''){const wh=ensureWarehouse();const row=(wh.packaging||[]).find(x=>x.id===id)||{id:'',name:'',stock:0,minimum:0,unit:'Stück',location_id:'LOC-SHIP',supplier_id:'',notes:''};openModal(`<div class="modalHead"><small>PACKMITTEL</small><h2>${id?'Packmittel bearbeiten':'Packmittel anlegen'}</h2></div><div class="modalBody"><div class="formGrid"><label>ID<input id="wpId" value="${esc(row.id)}"></label><label>Bezeichnung<input id="wpName" value="${esc(row.name)}"></label><label>Bestand<input id="wpStock" type="number" min="0" value="${Number(row.stock||0)}"></label><label>Meldebestand<input id="wpMin" type="number" min="0" value="${Number(row.minimum||0)}"></label><label>Einheit<input id="wpUnit" value="${esc(row.unit||'Stück')}"></label><label>Lagerort<select id="wpLoc">${(wh.locations||[]).map(l=>`<option value="${esc(l.id)}" ${sel(row.location_id||'',l.id)}>${esc(l.name)}</option>`).join('')}</select></label><label>Lieferant<select id="wpSup"><option value="">Keiner</option>${(wh.suppliers||[]).map(s=>`<option value="${esc(s.id)}" ${sel(row.supplier_id||'',s.id)}>${esc(s.name)}</option>`).join('')}</select></label><label class="full">Notiz<textarea id="wpNotes">${esc(row.notes||'')}</textarea></label></div><div class="formActions"><button class="btn" id="wpCancel">Abbrechen</button><button class="btn primary" id="wpSave">Speichern</button></div></div>`);$('#wpCancel').onclick=closeModal;$('#wpSave').onclick=()=>{const payload={id:($('#wpId').value||'').trim()||('PACK-'+Math.random().toString(16).slice(2,6).toUpperCase()),name:$('#wpName').value.trim(),stock:+$('#wpStock').value||0,minimum:+$('#wpMin').value||0,unit:$('#wpUnit').value.trim()||'Stück',location_id:$('#wpLoc').value,supplier_id:$('#wpSup').value,notes:$('#wpNotes').value.trim()};if(!payload.name)return toast('Bezeichnung fehlt',true);const idx=wh.packaging.findIndex(x=>x.id===id||x.id===payload.id);if(idx>=0)wh.packaging[idx]=payload;else wh.packaging.unshift(payload);closeModal();saveWarehouseState('Packmittel gespeichert') }}
function warehousePurchaseOrderModal(id='',suggested=null){const wh=ensureWarehouse();const row=(wh.purchase_orders||[]).find(x=>x.id===id)||{id:'',title:'',supplier_id:'',status:'draft',eta:'',lines:[],note:''};const prefill=suggested?warehouseSuggestions().find(x=>x.id===suggested.id&&x.type===suggested.type):null;const lineText=row.lines&&row.lines.length?row.lines.join('\n'):(prefill?`${prefill.name} | ${prefill.qty} ${prefill.unit}`:'');openModal(`<div class="modalHead"><small>EINKAUFSBESTELLUNG</small><h2>${id?'Bestellung bearbeiten':'Neue Bestellung'}</h2></div><div class="modalBody"><div class="formGrid"><label>Titel<input id="wpoTitle" value="${esc(row.title||(prefill?('Nachkauf '+prefill.name):''))}"></label><label>Lieferant<select id="wpoSup"><option value="">Bitte wählen</option>${(wh.suppliers||[]).map(s=>`<option value="${esc(s.id)}" ${sel(row.supplier_id||'',s.id)}>${esc(s.name)}</option>`).join('')}</select></label><label>Status<select id="wpoStatus"><option value="draft" ${sel(row.status,'draft')}>Entwurf</option><option value="ordered" ${sel(row.status,'ordered')}>Bestellt</option><option value="partial" ${sel(row.status,'partial')}>Teilgeliefert</option><option value="received" ${sel(row.status,'received')}>Geliefert</option><option value="cancelled" ${sel(row.status,'cancelled')}>Storniert</option></select></label><label>ETA<input id="wpoEta" type="date" value="${esc(row.eta||'')}"></label><label class="full">Positionen (eine Zeile pro Position)<textarea id="wpoLines" placeholder="Artikel | Menge">${esc(lineText)}</textarea></label><label class="full">Notiz<textarea id="wpoNote">${esc(row.note||'')}</textarea></label></div><div class="formActions"><button class="btn" id="wpoCancel">Abbrechen</button><button class="btn primary" id="wpoSave">Speichern</button></div></div>`);$('#wpoCancel').onclick=closeModal;$('#wpoSave').onclick=()=>{const lines=$('#wpoLines').value.split(/\n+/).map(x=>x.trim()).filter(Boolean);const payload={id:row.id||('PO-'+Math.random().toString(16).slice(2,8).toUpperCase()),title:$('#wpoTitle').value.trim()||('Bestellung '+new Date().toLocaleDateString('de-DE')),supplier_id:$('#wpoSup').value,status:$('#wpoStatus').value,eta:$('#wpoEta').value,lines,note:$('#wpoNote').value.trim(),updated_at:new Date().toISOString(),created_at:row.created_at||new Date().toISOString()};const idx=wh.purchase_orders.findIndex(x=>x.id===payload.id);if(idx>=0)wh.purchase_orders[idx]=payload;else wh.purchase_orders.unshift(payload);closeModal();saveWarehouseState('Einkaufsbestellung gespeichert')}}

function renderShipping(){const rows=(state.orders||[]).filter(o=>['ready','shipped','packing','production','quality'].includes(o.status));const dhl=shippingIntegration('DHL'),dpd=shippingIntegration('DPD');ws.innerHTML=`<div class="sectionHead"><div><h2>Versand</h2><p>DHL und DPD vorbereitet: Sendungsdaten, Tracking und Versandlabel zentral bearbeiten und prüfen.</p></div>${isAdmin()?'<div class="sectionActions"><button class="btn" id="goShippingSettings">DHL / DPD einrichten</button></div>':''}</div><div class="shippingIntegrationStrip"><article>${carrierBadge('DHL')}<div><b>DHL Paket</b>${integrationStatus(dhl)}<small>${esc(dhl.last_message||'Zugangsdaten können unter Einstellungen hinterlegt werden.')}</small></div>${isAdmin()?'<button class="miniBtn" data-test-carrier="DHL">Live prüfen</button>':''}</article><article>${carrierBadge('DPD')}<div><b>DPD</b>${integrationStatus(dpd)}<small>${esc(dpd.last_message||'Zugangsdaten können unter Einstellungen hinterlegt werden.')}</small></div>${isAdmin()?'<button class="miniBtn" data-test-carrier="DPD">Live prüfen</button>':''}</article></div><div class="moduleNotice"><b>Versandlabel-Vorschau:</b> Versanddaten bearbeiten, offizielles PDF/PNG-Label hinterlegen und anschließend direkt im Orgaboard-Fenster ansehen. Die Schnittstellen sind so vorbereitet, dass nach Eingabe deiner DHL-/DPD-Zugangsdaten eine Live-Verbindungsprüfung möglich ist.</div><div class="panel tableWrap">${rows.length?`<table class="dataTable"><thead><tr><th>Bestellung</th><th>Kunde</th><th>Ort</th><th>Status</th><th>Versandgewicht</th><th>Dienstleister</th><th>Tracking</th><th>Label</th><th></th></tr></thead><tbody>${rows.map(o=>{const lm=shippingLabelMeta(o.order_no||o.id);return `<tr><td><button class="rowLink" data-open-order="${esc(o.order_no||o.id)}">${esc(o.order_no||o.id)}</button></td><td>${esc(orderCustomer(o))}</td><td>${esc(o.customer?.zip||'')} ${esc(o.customer?.city||'')}</td><td>${status(o.status)}</td><td>${weightLabel(o.totals?.shipping_weight_g||0)}</td><td>${carrierBadge(o.carrier||'DPD')}</td><td>${esc(o.tracking||lm?.tracking_no||'–')}</td><td>${lm?.has_label?'<span class="status s-paid">Label vorhanden</span>':'<span class="status s-production">Vorbereiten</span>'}</td><td><button class="miniBtn" data-shipping-edit="${esc(o.order_no||o.id)}">Versand bearbeiten</button></td></tr>`}).join('')}</tbody></table>`:'<div class="empty"><b>Nichts im Versand</b></div>'}</div>`;if($('#goShippingSettings'))$('#goShippingSettings').onclick=()=>setView('settings');$$('[data-shipping-edit]').forEach(b=>b.onclick=()=>shippingModal(b.dataset.shippingEdit));$$('[data-test-carrier]').forEach(b=>b.onclick=()=>testCarrier(b.dataset.testCarrier))}

async function testCarrier(carrier){try{toast(carrier+' Verbindung wird geprüft …');const d=await api({action:'shipping_connection_test',carrier});toast(d.message||carrier+' Verbindung steht');await load(true);if(current==='shipping')renderShipping();if(current==='settings')renderSettings()}catch(e){toast(e.message,true);await load(true);if(current==='shipping')renderShipping();if(current==='settings')renderSettings()}}
async function shippingModal(orderNo){const o=(state.orders||[]).find(x=>(x.order_no||x.id)===orderNo);if(!o)return;const lm=shippingLabelMeta(orderNo);const carrier=String(o.carrier||lm?.carrier||state.settings?.shipping_default_carrier||'DPD').toUpperCase();openModal(`<div class="modalHead"><small>VERSAND · ${esc(orderNo)}</small><h2>Versand bearbeiten</h2><p>${esc(orderCustomer(o))} · ${esc(o.customer?.zip||'')} ${esc(o.customer?.city||'')}</p></div><div class="modalBody shippingEditor"><div class="shippingEditorGrid"><section><div class="formGrid"><label>Dienstleister<select id="shipCarrier"><option value="DPD" ${sel(carrier,'DPD')}>DPD</option><option value="DHL" ${sel(carrier,'DHL')}>DHL</option></select></label><label>Trackingnummer<input id="shipTracking" value="${esc(o.tracking||lm?.tracking_no||'')}" placeholder="nach Labelerstellung"></label><label class="full">Versandgewicht<input value="${esc(weightLabel(o.totals?.shipping_weight_g||0))}" disabled></label><label class="full">Empfänger<input value="${esc(orderCustomer(o))}" disabled></label><label class="full">Adresse<input value="${esc([o.customer?.street,[o.customer?.zip,o.customer?.city].filter(Boolean).join(' ')].filter(Boolean).join(', '))}" disabled></label><label class="full shippingFilePicker"><b>Offizielles Versandlabel hinterlegen</b><span>PDF, PNG oder JPG · wird danach hier im Fenster angezeigt</span><input id="shipLabelFile" type="file" accept="application/pdf,image/png,image/jpeg"></label></div><div class="formActions"><button class="btn" id="shipCancel">Schließen</button><button class="btn primary" id="shipSave">Versanddaten speichern</button></div></section><section><div class="shippingPreviewHead"><b>Label-Vorschau</b><button class="miniBtn" id="shipOpenLabel" ${lm?.has_label?'':'disabled'}>Label groß öffnen</button></div><div id="shippingLabelPreview">${shippingDraftHtml(o,carrier,o.tracking||lm?.tracking_no||'')}</div></section></div></div>`);$('#shipCancel').onclick=closeModal;$('#shipCarrier').onchange=()=>{if(!shippingLabelMeta(orderNo)?.has_label)$('#shippingLabelPreview').innerHTML=shippingDraftHtml(o,$('#shipCarrier').value,$('#shipTracking').value)};$('#shipTracking').oninput=()=>{if(!shippingLabelMeta(orderNo)?.has_label)$('#shippingLabelPreview').innerHTML=shippingDraftHtml(o,$('#shipCarrier').value,$('#shipTracking').value)};const file=$('#shipLabelFile');file.onchange=()=>{const f=file.files?.[0];if(!f)return;const url=URL.createObjectURL(f);$('#shippingLabelPreview').innerHTML=f.type==='application/pdf'?`<iframe class="labelFrame" src="${url}" title="Versandlabel"></iframe>`:`<img class="labelImage" src="${url}" alt="Versandlabel">`;$('#shipOpenLabel').disabled=false;$('#shipOpenLabel').onclick=()=>window.open(url,'_blank')};if(lm?.has_label){try{const d=await api({action:'shipping_label_get',order_no:orderNo});const url=dataUrlToBlobUrl(d.label.label_data,d.label.label_mime);if(url){$('#shippingLabelPreview').innerHTML=d.label.label_mime==='application/pdf'?`<iframe class="labelFrame" src="${url}" title="Versandlabel"></iframe>`:`<img class="labelImage" src="${url}" alt="Versandlabel">`;$('#shipOpenLabel').onclick=()=>window.open(url,'_blank')}}catch(e){}}$('#shipSave').onclick=async()=>{try{const f=file.files?.[0];let data='',mime='';if(f){mime=f.type;data=await new Promise((res,rej)=>{const r=new FileReader();r.onload=()=>res(String(r.result||''));r.onerror=rej;r.readAsDataURL(f)})}const d=await api({action:'shipping_label_save',order_no:orderNo,carrier:$('#shipCarrier').value,tracking_no:$('#shipTracking').value,label_mime:mime,label_data:data,shipping_weight_g:Number(o.totals?.shipping_weight_g||0),customer:o.customer||{}});toast('Versanddaten gespeichert');state.shipping_labels=d.shipping_labels||state.shipping_labels;await load(true);closeModal();renderShipping()}catch(e){toast(e.message,true)}}}

function renderDocuments(){const rows=(state.orders||[]).filter(o=>o.status!=='cancelled');ws.innerHTML=`<div class="sectionHead"><div><h2>Rechnungen & Lieferscheine</h2><p>Dokumente je Bestellung erzeugen, einsehen und ${can('edit_documents')?'versioniert bearbeiten':'im Lesemodus öffnen'}.</p></div></div><div class="moduleNotice"><b>Änderungsschutz:</b> Dokumentänderungen werden versioniert und im Änderungsprotokoll erfasst. Bereits ausgegebene Rechnungen werden nicht still überschrieben; jede Bearbeitung erzeugt eine neue Version.</div><div class="panel tableWrap">${rows.length?`<table class="dataTable"><thead><tr><th>Bestellung</th><th>Kunde</th><th>Rechnung</th><th>Lieferschein</th><th>Gesamt</th><th></th></tr></thead><tbody>${rows.map(o=>{const inv=existingDocument(o.order_no||o.id,'invoice'),del=existingDocument(o.order_no||o.id,'delivery_note');return `<tr><td><b>${esc(o.order_no||o.id)}</b><br><small>${donly(o.created_at)}</small></td><td>${esc(orderCustomer(o))}</td><td>${inv?`<b>${esc(inv.document_no)}</b><br><small>${esc(documentStatusLabel(inv.status))} · V${inv.version_no}</small>`:'<span class="muted">noch nicht erzeugt</span>'}</td><td>${del?`<b>${esc(del.document_no)}</b><br><small>${esc(documentStatusLabel(del.status))} · V${del.version_no}</small>`:'<span class="muted">noch nicht erzeugt</span>'}</td><td class="money">${money(orderTotal(o))}</td><td class="rowActions"><button class="miniBtn" data-doc-order="${esc(o.order_no||o.id)}" data-doc-type="invoice">Rechnung</button><button class="miniBtn" data-doc-order="${esc(o.order_no||o.id)}" data-doc-type="delivery_note">Lieferschein</button></td></tr>`}).join('')}</tbody></table>`:'<div class="empty"><b>Noch keine Bestellungen</b></div>'}</div>`}
async function openDocumentForOrder(orderNo,type){try{const d=await api({action:'document_get',order_no:orderNo,type});const idx=(state.documents||[]).findIndex(x=>x.id===d.document.id);if(idx>=0)state.documents[idx]=d.document;else(state.documents||(state.documents=[])).unshift(d.document);documentModal(d.document)}catch(e){toast(e.message,true)}}
function documentModal(doc){const p=doc.payload||{},c=p.customer||{},items=p.items||[],tot=p.totals||{},edit=can('edit_documents');const isInvoice=doc.document_type==='invoice';openModal(`<div class="modalHead"><small>${isInvoice?'RECHNUNG':'LIEFERSCHEIN'} · VERSION ${doc.version_no}</small><h2>${esc(doc.document_no)}</h2><p>Bestellung ${esc(doc.order_no)} · ${esc(documentStatusLabel(doc.status))}</p></div><div class="modalBody">${doc.status==='issued'?'<div class="moduleNotice"><b>Bereits ausgegeben:</b> Eine Bearbeitung erzeugt eine neue protokollierte Dokumentversion. Für steuerlich relevante Korrekturen sollten Storno-/Korrekturbelege verwendet werden.</div>':''}<div class="detailGrid">
<div class="detailBox"><h4>Empfänger</h4>${edit?`<div class="formGrid"><label class="full">Name<input id="docName" value="${esc(c.name||'')}"></label><label class="full">Firma<input id="docCompany" value="${esc(c.company||'')}"></label><label class="full">Straße<input id="docStreet" value="${esc(c.street||'')}"></label><label>PLZ<input id="docZip" value="${esc(c.zip||'')}"></label><label>Ort<input id="docCity" value="${esc(c.city||'')}"></label><label class="full">E-Mail<input id="docEmail" value="${esc(c.email||'')}"></label></div>`:`<p><b>${esc(c.name||'')}</b><br>${esc(c.company||'')}<br>${esc(c.street||'')}<br>${esc(c.zip||'')} ${esc(c.city||'')}<br>${esc(c.email||'')}</p>`}</div>
<div class="detailBox"><h4>Dokumentstatus</h4><p>Nummer: <b>${esc(doc.document_no)}</b><br>Status: <b>${esc(documentStatusLabel(doc.status))}</b><br>Version: <b>${doc.version_no}</b><br>Ausgegeben: <b>${doc.issued_at?donly(doc.issued_at):'noch nicht'}</b></p></div>
<div class="detailBox full"><h4>Positionen</h4><div id="docItems">${items.map((x,i)=>edit?`<div class="docItemEdit" data-doc-row="${i}"><input class="docArt" value="${esc(x.article_no||'')}" placeholder="Art.-Nr."><input class="docName" value="${esc(x.name||x.id||'')}" placeholder="Bezeichnung"><input class="docQty" type="number" min="0" step="1" value="${Number(x.qty||1)}"><input class="docPrice" type="number" min="0" step="0.01" value="${Number(x.unit_price||0)}"></div>`:`<div class="docItemRead"><span><b>${Number(x.qty||1)} × ${esc(x.name||x.id||'')}</b><small>${x.article_no?'Art.-Nr. '+esc(x.article_no):''}</small></span>${isInvoice?`<strong>${money((x.unit_price||0)*(x.qty||1))}</strong>`:''}</div>`).join('')}</div></div>
${isInvoice?`<div class="detailBox full"><h4>Summen</h4>${edit?`<div class="formGrid"><label>Warenwert<input id="docSubtotal" type="number" step="0.01" value="${Number(tot.subtotal||0)}"></label><label>Versand<input id="docShipping" type="number" step="0.01" value="${Number(tot.shipping||0)}"></label><label>MwSt.<input id="docVat" type="number" step="0.01" value="${Number(tot.vat||0)}"></label><label>Gesamt<input id="docGross" type="number" step="0.01" value="${Number(tot.gross||0)}"></label></div>`:`<div class="detailTotals"><div><span>Warenwert</span><b>${money(tot.subtotal||0)}</b></div><div><span>Versand</span><b>${money(tot.shipping||0)}</b></div><div><span>MwSt.</span><b>${money(tot.vat||0)}</b></div><div class="grand"><span>Gesamt</span><b>${money(tot.gross||0)}</b></div></div>`}</div>`:''}
<div class="detailBox full"><h4>Dokumentnotiz</h4>${edit?`<textarea id="docNote">${esc(p.document_note||'')}</textarea>`:`<p>${esc(p.document_note||'Keine Notiz').replace(/\n/g,'<br>')}</p>`}</div>
</div><div class="formActions"><button class="btn" id="docPrint">Druckansicht</button>${edit?`<button class="btn" id="docSave">Version speichern</button>${doc.status!=='issued'?'<button class="btn primary" id="docIssue">Als ausgegeben markieren</button>':''}${doc.status!=='cancelled'?'<button class="btn danger" id="docCancel">Stornieren</button>':''}`:''}</div></div>`);
 $('#docPrint').onclick=()=>printDocument(doc,collectDocumentPayload(doc,false));
 if(edit&&$('#docSave'))$('#docSave').onclick=async()=>{try{const payload=collectDocumentPayload(doc,true);const note=prompt('Kurzer Änderungsgrund für das Protokoll:','Bearbeitung im Orgaboard')||'Bearbeitung im Orgaboard';const d=await api({action:'document_save',id:doc.id,payload,change_note:note});toast('Dokumentversion gespeichert');closeModal();await load(true);documentModal(d.document)}catch(e){toast(e.message,true)}};
 if(edit&&$('#docIssue'))$('#docIssue').onclick=async()=>{try{await api({action:'document_status',id:doc.id,status:'issued'});toast('Dokument als ausgegeben markiert');closeModal();await load(true)}catch(e){toast(e.message,true)}};
 if(edit&&$('#docCancel'))$('#docCancel').onclick=async()=>{if(!confirm('Dokument wirklich stornieren?'))return;try{await api({action:'document_status',id:doc.id,status:'cancelled'});toast('Dokument storniert');closeModal();await load(true)}catch(e){toast(e.message,true)}}
}
function collectDocumentPayload(doc,fromFields=true){const p=JSON.parse(JSON.stringify(doc.payload||{}));if(!fromFields||!isAdmin())return p;p.customer={...(p.customer||{}),name:$('#docName').value,company:$('#docCompany').value,street:$('#docStreet').value,zip:$('#docZip').value,city:$('#docCity').value,email:$('#docEmail').value};p.items=$$('[data-doc-row]').map(r=>({...(p.items?.[+r.dataset.docRow]||{}),article_no:$('.docArt',r).value,name:$('.docName',r).value,qty:+$('.docQty',r).value,unit_price:+$('.docPrice',r).value,line_total:+$('.docQty',r).value*+$('.docPrice',r).value}));if(doc.document_type==='invoice')p.totals={...(p.totals||{}),subtotal:+$('#docSubtotal').value,shipping:+$('#docShipping').value,vat:+$('#docVat').value,gross:+$('#docGross').value};p.document_note=$('#docNote').value;return p}
function printDocument(doc,payload){const c=payload.customer||{},items=payload.items||[],tot=payload.totals||{},isInvoice=doc.document_type==='invoice',w=window.open('','_blank','width=900,height=900');if(!w)return toast('Pop-up wurde blockiert',true);w.document.write(`<!doctype html><html><head><meta charset="utf-8"><title>${isInvoice?'Rechnung':'Lieferschein'} ${esc(doc.document_no)}</title><style>body{font-family:Arial,sans-serif;color:#222;padding:45px;max-width:820px;margin:auto}header{border-bottom:3px solid #70461f;padding-bottom:18px;margin-bottom:28px}h1{margin:0;color:#5b3a1e}small{color:#777}.cols{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin:25px 0}table{width:100%;border-collapse:collapse;margin-top:25px}th,td{text-align:left;padding:10px 8px;border-bottom:1px solid #ddd;font-size:13px}td:last-child,th:last-child{text-align:right}.tot{width:320px;margin:20px 0 0 auto}.tot div{display:flex;justify-content:space-between;padding:4px}.grand{font-size:17px;font-weight:bold;border-top:2px solid #333;margin-top:6px;padding-top:9px!important}.note{margin-top:30px;font-size:12px}@media print{button{display:none}}</style></head><body><header><h1>Räucherhaken24</h1><small>${isInvoice?'Rechnung':'Lieferschein'}</small></header><h2>${esc(doc.document_no)}</h2><div class="cols"><div><b>Empfänger</b><br>${esc(c.name||'')}<br>${esc(c.company||'')}<br>${esc(c.street||'')}<br>${esc(c.zip||'')} ${esc(c.city||'')}</div><div><b>Bestellung</b><br>${esc(doc.order_no)}<br><br><b>Datum</b><br>${donly(doc.created_at||new Date().toISOString())}</div></div><table><thead><tr><th>Art.-Nr.</th><th>Position</th><th>Menge</th>${isInvoice?'<th>Einzel</th><th>Gesamt</th>':''}</tr></thead><tbody>${items.map(x=>`<tr><td>${esc(x.article_no||'–')}</td><td>${esc(x.name||x.id||'')}</td><td>${Number(x.qty||1)}</td>${isInvoice?`<td>${money(x.unit_price||0)}</td><td>${money((x.unit_price||0)*(x.qty||1))}</td>`:''}</tr>`).join('')}</tbody></table>${isInvoice?`<div class="tot"><div><span>Warenwert</span><b>${money(tot.subtotal||0)}</b></div><div><span>Versand</span><b>${money(tot.shipping||0)}</b></div><div><span>MwSt.</span><b>${money(tot.vat||0)}</b></div><div class="grand"><span>Gesamt</span><b>${money(tot.gross||0)}</b></div></div>`:''}<p class="note">${esc(payload.document_note||'')}</p><button onclick="print()">Drucken</button></body></html>`);w.document.close()}

function renderMessages(){const msgs=state.messages||[],uid=state.current_user?.id,unread=msgs.filter(m=>m.recipient_user_id===uid&&!m.read_at).length;const users=(state.message_users||[]).filter(u=>u.id!==uid&&(isAdmin()||u.role==='admin'));const threads=[];msgs.forEach(m=>{if(!threads.some(x=>x.thread_id===m.thread_id))threads.push(m)});ws.innerHTML=`<div class="sectionHead"><div><h2>Nachrichten</h2><p>Interne Kommunikation zwischen Administration und Außendienst.</p></div><button class="btn primary" id="newMessage">+ Neue Nachricht</button></div><div class="metricGrid compactMetrics">${metric('Ungelesen',unread,'neue Nachrichten')}${metric('Unterhaltungen',threads.length,'Nachrichtenverläufe')}${metric('Kontakte',users.length,'erreichbare Benutzer')}</div><div class="panel tableWrap">${threads.length?`<table class="dataTable"><thead><tr><th>Betreff</th><th>Mit</th><th>Letzte Nachricht</th><th>Status</th><th></th></tr></thead><tbody>${threads.map(m=>{const other=m.sender_user_id===uid?m.recipient_name:m.sender_name;const hasUnread=msgs.some(x=>x.thread_id===m.thread_id&&x.recipient_user_id===uid&&!x.read_at);return `<tr><td><b>${esc(m.subject||'Ohne Betreff')}</b></td><td>${esc(other||'–')}</td><td>${dt(m.created_at)}</td><td>${hasUnread?'<span class="status s-payment_pending">Neu</span>':'<span class="status s-paid">Gelesen</span>'}</td><td><button class="miniBtn" data-thread="${esc(m.thread_id)}">Öffnen</button></td></tr>`}).join('')}</tbody></table>`:'<div class="empty"><b>Noch keine Nachrichten</b>Starte die erste interne Unterhaltung.</div>'}</div>`;$('#newMessage').onclick=()=>messageCompose(users)}
function messageCompose(users,threadId='',recipientId='',subject=''){openModal(`<div class="modalHead"><small>INTERNE NACHRICHT</small><h2>Nachricht senden</h2></div><div class="modalBody"><div class="formGrid"><label class="full">Empfänger<select id="msgRecipient">${users.map(u=>`<option value="${esc(u.id)}" ${sel(recipientId,u.id)}>${esc(u.display_name)} · ${u.role==='admin'?'Administrator':u.role==='production'?'Produktion':'Außendienst'}</option>`).join('')}</select></label><label class="full">Betreff<input id="msgSubject" value="${esc(subject)}"></label><label class="full">Nachricht<textarea id="msgBody" style="min-height:160px"></textarea></label></div><div class="formActions"><button class="btn primary" id="sendMessage">Senden</button></div></div>`);$('#sendMessage').onclick=async()=>{try{await api({action:'message_send',recipient_user_id:$('#msgRecipient').value,subject:$('#msgSubject').value,body:$('#msgBody').value,thread_id:threadId});toast('Nachricht gesendet');closeModal();await load(true);renderMessages()}catch(e){toast(e.message,true)}}}
async function openMessageThread(threadId){const msgs=(state.messages||[]).filter(m=>m.thread_id===threadId).sort((a,b)=>new Date(a.created_at)-new Date(b.created_at)),uid=state.current_user?.id;if(!msgs.length)return;for(const m of msgs.filter(m=>m.recipient_user_id===uid&&!m.read_at)){try{await api({action:'message_read',id:m.id});m.read_at=new Date().toISOString()}catch{}}const last=msgs[msgs.length-1],otherId=last.sender_user_id===uid?last.recipient_user_id:last.sender_user_id,users=(state.message_users||[]).filter(u=>u.id!==uid&&(isAdmin()||u.role==='admin'));openModal(`<div class="modalHead"><small>NACHRICHTENVERLAUF</small><h2>${esc(last.subject||'Unterhaltung')}</h2></div><div class="modalBody"><div class="messageThread">${msgs.map(m=>`<article class="${m.sender_user_id===uid?'mine':'theirs'}"><small>${esc(m.sender_name||'')} · ${dt(m.created_at)}</small><p>${esc(m.body).replace(/\n/g,'<br>')}</p></article>`).join('')}</div><div class="formActions"><button class="btn primary" id="replyMessage">Antworten</button></div></div>`);$('#replyMessage').onclick=()=>messageCompose(users,threadId,otherId,last.subject||'')}

function calcScenario(cost,fee,margin,vat){const den=1-(fee+margin)/100;if(den<=.05)return null;const net=cost/den,gross=net*(1+vat/100);return {net,gross,profit:net*(1-fee/100)-cost,margin}}
function roundSalesPrice(v){if(!Number.isFinite(v)||v<=0)return 0;const x=Math.ceil(v*10)/10;return Math.max(.05,Math.round((x-.05)*100)/100)}
function renderCalculatorHelp(){if(!isAdmin())return setView('dashboard');ws.innerHTML=`
<div class="sectionHead"><div><h2>Kalkulator-Hilfe</h2><p>Jedes Eingabefeld verständlich erklärt – mit Praxisbeispielen und typischen Fehlern.</p></div><button class="btn primary" id="goCalculator">Zum Produkt-Kalkulator</button></div>
<div class="guideHero"><div><small>PREISKALKULATION</small><h3>So entsteht ein Verkaufspreis, der nicht nur Kosten deckt.</h3><p>Trage die Kosten immer für genau die Verkaufseinheit ein, die der Kunde später kauft – zum Beispiel 1 Stück, ein 10er-Set oder 500 g.</p></div><div class="guideRule"><b>Grundregel</b><span>Nicht Materialkosten × beliebiger Faktor rechnen. Arbeitszeit, Verpackung, Gemeinkosten, Gebühren und Zielmarge gehören mit hinein.</span></div></div>
<div class="calcGuideGrid">
${calcGuideCard('1','Produkt','Wähle den Artikel, den du kalkulieren willst. Die gespeicherte Kalkulation wird diesem Artikel zugeordnet.','Beispiel: Art.-Nr. 10001 · Räucherhaken Standard','Nicht mehrere verschiedene Produkte in einer Kalkulation vermischen.')}
${calcGuideCard('2','Materialkosten (€)','Alle direkt verbrauchten Materialien für eine Verkaufseinheit: Draht, Gewürzmischung, Beutel, Etikettbestandteile, Lot usw.','Wenn ein 10er-Set in der Herstellung 2,50 € Material verbraucht, trägst du 2,50 ein.','Nur den Einkaufspreis eines Rohstoffs einzutragen, obwohl mehrere Materialien verbraucht werden.')}
${calcGuideCard('3','Arbeitszeit (Min.)','Die reale Arbeitszeit für eine Verkaufseinheit. Bei Serienfertigung Gesamtzeit durch die produzierte Stückzahl teilen.','100 Haken brauchen 120 Minuten → 1 Haken = 1,2 Minuten. Für ein 10er-Set = 12 Minuten.','Arbeitszeit mit 0 anzusetzen, weil du selbst produzierst. Deine Zeit ist ein Kostenfaktor.')}
${calcGuideCard('4','Interner Stundensatz (€)','Der Wert einer Arbeitsstunde im Betrieb. Er ist nicht dein Kunden-Stundensatz, sondern ein interner Kalkulationswert.','32,00 € bedeutet: 30 Minuten Arbeitszeit werden mit 16,00 € angesetzt.','Nur den gewünschten privaten Stundenlohn verwenden und betriebliche Belastungen ignorieren.')}
${calcGuideCard('5','Verpackung (€)','Direkte Verpackung pro Verkaufseinheit: Beutel, Kartonanteil, Etikett, Füllmaterial oder Produktverpackung.','Produktbeutel 0,22 € + Etikett 0,08 € = 0,30 €.','Versandkosten hier doppelt einrechnen, obwohl sie separat dem Kunden berechnet werden.')}
${calcGuideCard('6','Sonstige Kosten (€)','Direkte Stückkosten, die in kein anderes Feld passen: externe Bearbeitung, Prüfkosten, Verbrauchsmittel oder Lizenzanteile.','0,15 € Schleifmittel + 0,10 € Reinigungsmittel = 0,25 €.','Allgemeine Miete oder Strom pauschal hier eintragen – dafür sind Gemeinkosten gedacht.')}
${calcGuideCard('7','Gemeinkosten (%)','Deckt allgemeine Betriebskosten ab, die nicht sauber einem einzelnen Artikel zugeordnet werden: Strom, Werkzeugverschleiß, Software, Büro, Lager, Telefon usw.','12 % bedeutet: Auf 10,00 € direkte Kosten kommen 1,20 € Gemeinkosten.','Gemeinkosten auf 0 setzen. Dann wirkt der Artikel profitabel, obwohl der Betrieb Kosten trägt.')}
${calcGuideCard('8','Verkaufs-/Zahlungsgebühr (%)','Prozentuale Kosten des Verkaufs: Zahlungsanbieter, Marktplatz- oder Transaktionsgebühren.','Bei durchschnittlich 2,5 % Gebühren trägst du 2,5 ein.','Eine feste Versandpauschale als Prozentwert eintragen.')}
${calcGuideCard('9','Zielmarge (%)','Der Anteil des Nettoverkaufspreises, der nach kalkulierten Kosten und Verkaufsgebühren als Gewinnbeitrag übrig bleiben soll.','45 % ist im Orgaboard als kräftige Zielmarge voreingestellt. Prüfe immer, ob der Marktpreis das zulässt.','Marge mit Aufschlag verwechseln: 45 % Marge ist nicht dasselbe wie 45 % Aufschlag auf die Kosten.')}
${calcGuideCard('10','MwSt. (%)','Der gesetzliche Umsatzsteuersatz, der auf den Nettoverkaufspreis aufgeschlagen wird.','Bei regulär 19 % bleibt 19 eingetragen.','Die Umsatzsteuer als Gewinn behandeln – sie gehört wirtschaftlich nicht dir.')}
</div>
<section class="panel panelPad calcFormula"><div><small>BERECHNUNGSWEG</small><h3>Was das Orgaboard im Hintergrund rechnet</h3></div><ol><li><b>Arbeitskosten</b> = Arbeitszeit ÷ 60 × interner Stundensatz</li><li><b>Direkte Kosten</b> = Material + Arbeitskosten + Verpackung + Sonstiges</li><li><b>Herstell-/Vollkosten</b> = direkte Kosten + Gemeinkosten</li><li><b>Nettoverkaufspreis</b> wird so berechnet, dass Gebühren und Zielmarge im Verkaufspreis enthalten sind.</li><li><b>Bruttopreis</b> = Nettoverkaufspreis + MwSt.; anschließend erfolgt eine verkaufsfreundliche Rundung.</li></ol><div class="formulaWarning"><b>Wichtig:</b> Der Rechner liefert einen betriebswirtschaftlichen Zielpreis, aber keinen Beweis, dass der Markt diesen Preis akzeptiert. Danach immer Wettbewerbsniveau, Produktqualität und gewünschte Positionierung prüfen.</div></section>
<section class="panel panelPad exampleCalc"><div class="panelTitle plain"><h3>Kurzes Praxisbeispiel</h3><span>nur zur Erklärung</span></div><div class="exampleSteps"><div><span>Material</span><b>2,50 €</b></div><div><span>Arbeitszeit</span><b>12 Min.</b></div><div><span>Stundensatz</span><b>32,00 €</b></div><div><span>Verpackung</span><b>0,30 €</b></div><div><span>Gemeinkosten</span><b>12 %</b></div><div><span>Zielmarge</span><b>45 %</b></div></div><p>Das Beispiel zeigt nur, welche Art von Werten in die Felder gehört. Für echte Preise trägst du deine tatsächlichen Kosten des jeweiligen Artikels ein.</p></section>`;$('#goCalculator').onclick=()=>setView('calculator')}
function calcGuideCard(no,title,what,example,mistake){return `<article class="calcGuideCard"><div class="guideNo">${no}</div><div><h3>${title}</h3><p>${what}</p><div class="guideExample"><b>Beispiel</b><span>${example}</span></div><div class="guideMistake"><b>Typischer Fehler</b><span>${mistake}</span></div></div></article>`}

let deferredInstallPrompt=null;
window.addEventListener('beforeinstallprompt',e=>{e.preventDefault();deferredInstallPrompt=e});
window.addEventListener('appinstalled',()=>{deferredInstallPrompt=null;toast('Orgaboard-App wurde installiert')});
function isStandaloneApp(){return window.matchMedia('(display-mode: standalone)').matches||window.navigator.standalone===true}
function renderMobileApp(){const standalone=isStandaloneApp(),ios=/iphone|ipad|ipod/i.test(navigator.userAgent);ws.innerHTML=`<div class="sectionHead"><div><h2>Orgaboard Handy-App</h2><p>Das komplette Räucherhaken24-Orgaboard als installierbare App auf Smartphone oder Tablet.</p></div></div><div class="mobileAppHero"><div class="appIconPreview"><img src="assets/icons/icon-192.png" alt="Räucherhaken24 Orgaboard"></div><div><small>RÄUCHERHAKEN24</small><h3>Orgaboard Mobile</h3><p>Greift auf dieselben Benutzer, Kunden, Bestellungen und MariaDB-Daten zu wie die Desktop-Version. Es gibt keine zweite Datenbank und keine Synchronisationskopie.</p><div class="appStatus ${standalone?'installed':''}">${standalone?'✓ Diese Seite läuft bereits als installierte App':'Browser-Version erkannt · Installation möglich'}</div></div></div><div class="appInstallGrid"><section class="panel panelPad"><h3>Auf iPhone / iPad installieren</h3><ol class="installSteps"><li>Orgaboard in <b>Safari</b> öffnen.</li><li>Unten bzw. oben auf <b>Teilen</b> tippen.</li><li><b>„Zum Home-Bildschirm“</b> auswählen.</li><li>Name „RH24 Orgaboard“ bestätigen und <b>Hinzufügen</b> tippen.</li></ol><div class="moduleNotice"><b>iPhone-Hinweis:</b> Apple zeigt keinen automatischen Installationsdialog. Die Installation erfolgt immer über das Teilen-Menü.</div></section><section class="panel panelPad"><h3>Auf Android installieren</h3><ol class="installSteps"><li>Orgaboard in <b>Chrome</b> öffnen.</li><li>Auf <b>App installieren</b> tippen oder Chrome-Menü öffnen.</li><li>Installation bestätigen.</li><li>Danach startet das Orgaboard im eigenen App-Fenster.</li></ol><button class="btn primary fullBtn" id="installAppBtn" ${standalone?'disabled':''}>${standalone?'App ist installiert':'App jetzt installieren'}</button><p class="muted tiny" id="installHint"></p></section></div><section class="panel panelPad appSecurity"><h3>Sicherheitskonzept der App</h3><div class="securityGrid"><div><b>Keine Kundendaten offline</b><span>Der Service Worker speichert ausschließlich statische App-Dateien wie CSS, JavaScript und Icons. Kunden-, Bestell- und Rechnungsdaten werden nicht offline gecacht.</span></div><div><b>Gleiche Rechte wie im Orgaboard</b><span>Björn und Jessica behalten Administratorrechte. Außendienstkonten behalten ihre eingeschränkten Rechte auch in der Handy-App.</span></div><div><b>Eine Datenbank</b><span>Desktop und Handy arbeiten direkt mit derselben MariaDB. Änderungen sind daher sofort überall aktuell.</span></div><div><b>HTTPS erforderlich</b><span>Die Installation und sichere Anmeldung funktionieren produktiv nur über die verschlüsselte HTTPS-Adresse.</span></div></div></section>`;const b=$('#installAppBtn'),h=$('#installHint');if(!b)return;if(standalone){h.textContent='Keine weitere Installation notwendig.';return}b.onclick=async()=>{if(deferredInstallPrompt){deferredInstallPrompt.prompt();await deferredInstallPrompt.userChoice;deferredInstallPrompt=null}else{h.textContent=ios?'Auf iPhone bitte Safari → Teilen → Zum Home-Bildschirm verwenden.':'Falls kein Dialog erscheint: Browser-Menü öffnen und „App installieren“ bzw. „Zum Startbildschirm hinzufügen“ wählen.'}}}

function renderCalculator(){if(!isAdmin())return setView('dashboard');const products=(state.catalog||[]).filter(p=>p.type!=='prototype'),profiles=state.cost_profiles||[];ws.innerHTML=`<div class="sectionHead"><div><h2>Produkt-Kalkulator</h2><p>Herstellkosten, Gemeinkosten, Gebühren und Zielmarge in einen belastbaren Verkaufspreis übersetzen.</p></div></div><div class="moduleNotice"><b>Standard „Gut verdienen“:</b> 45 % Zielmarge auf den Nettoverkaufspreis, zuzüglich kalkulierter Verkaufsgebühren und Gemeinkosten. Alle Werte sind frei anpassbar.</div><div class="calcLayout"><section class="panel panelPad"><h3>Kostendaten</h3><div class="formGrid"><label class="full">Produkt<select id="calcProduct">${products.map(p=>`<option value="${esc(p.id)}">${esc(p.article_no||'–')} · ${esc(p.name)}</option>`).join('')}</select></label><label>Materialkosten (€)<input id="calcMaterial" type="number" step="0.01" min="0" value="0"></label><label>Arbeitszeit (Min.)<input id="calcMinutes" type="number" step="1" min="0" value="0"></label><label>Interner Stundensatz (€)<input id="calcHourly" type="number" step="0.01" min="0" value="32"></label><label>Verpackung (€)<input id="calcPackaging" type="number" step="0.01" min="0" value="0"></label><label>Sonstige Kosten (€)<input id="calcOther" type="number" step="0.01" min="0" value="0"></label><label>Gemeinkosten (%)<input id="calcOverhead" type="number" step="0.1" min="0" value="12"></label><label>Verkaufs-/Zahlungsgebühr (%)<input id="calcFee" type="number" step="0.1" min="0" value="2.5"></label><label>Zielmarge (%)<input id="calcMargin" type="number" step="0.1" min="0" value="45"></label><label>MwSt. (%)<input id="calcVat" type="number" step="0.1" min="0" value="19"></label></div><div class="formActions"><button class="btn orange" id="runCalc">Neu berechnen</button><button class="btn" id="saveCalc">Kalkulation speichern</button></div></section><section class="panel panelPad"><h3>Ergebnis</h3><div id="calcResult"></div></section></div>`;const loadProfile=()=>{const pr=profiles.find(x=>x.product_id===$('#calcProduct').value)||{};$('#calcMaterial').value=pr.material_cost??0;$('#calcMinutes').value=pr.labor_minutes??0;$('#calcHourly').value=pr.labor_hourly_rate??32;$('#calcPackaging').value=pr.packaging_cost??0;$('#calcOther').value=pr.other_cost??0;$('#calcOverhead').value=pr.overhead_percent??12;$('#calcFee').value=pr.selling_fee_percent??2.5;$('#calcMargin').value=pr.target_margin_percent??45;$('#calcVat').value=pr.vat_percent??19;calculateNow()};const values=()=>({material:+$('#calcMaterial').value,minutes:+$('#calcMinutes').value,hourly:+$('#calcHourly').value,packaging:+$('#calcPackaging').value,other:+$('#calcOther').value,overhead:+$('#calcOverhead').value,fee:+$('#calcFee').value,margin:+$('#calcMargin').value,vat:+$('#calcVat').value});const calculateNow=()=>{const v=values(),labor=v.minutes/60*v.hourly,direct=v.material+labor+v.packaging+v.other,cost=direct*(1+v.overhead/100),sc35=calcScenario(cost,v.fee,35,v.vat),sc45=calcScenario(cost,v.fee,v.margin,v.vat),sc55=calcScenario(cost,v.fee,55,v.vat),recommended=roundSalesPrice(sc45?.gross||0);$('#calcResult').innerHTML=`<div class="calcCost"><span>Direkte Kosten</span><b>${money(direct)}</b><span>inkl. Gemeinkosten</span><b>${money(cost)}</b></div><div class="priceTiers">${[['Solide',sc35],['Gut verdienen',sc45],['Stark',sc55]].map(([l,s])=>s?`<article class="${l==='Gut verdienen'?'recommended':''}"><small>${l}</small><strong>${money(roundSalesPrice(s.gross))}</strong><span>Gewinn netto ca. ${money(s.profit)}</span></article>`:'').join('')}</div><div class="recommendedPrice"><span>Empfohlener Bruttopreis</span><strong id="calcRecommended" data-value="${recommended}">${money(recommended)}</strong><small>auf verkaufsfreundliche Endung gerundet</small></div><button class="btn primary fullBtn" id="applyCalcPrice">Preis auf Produkt übernehmen</button>`;$('#applyCalcPrice').onclick=async()=>{if(!confirm('Empfohlenen Preis als Basispreis im Artikelstamm übernehmen?'))return;try{await api({action:'cost_price_apply',product_id:$('#calcProduct').value,price:+$('#calcRecommended').dataset.value});toast('Produktpreis aktualisiert');await load(true)}catch(e){toast(e.message,true)}}};$('#calcProduct').onchange=loadProfile;$('#runCalc').onclick=calculateNow;$('#saveCalc').onclick=async()=>{const v=values();try{const d=await api({action:'cost_profile_save',product_id:$('#calcProduct').value,material_cost:v.material,labor_minutes:v.minutes,labor_hourly_rate:v.hourly,packaging_cost:v.packaging,other_cost:v.other,overhead_percent:v.overhead,selling_fee_percent:v.fee,target_margin_percent:v.margin,vat_percent:v.vat});toast('Kalkulation gespeichert · Empfehlung '+money(d.recommended_gross));await load(true)}catch(e){toast(e.message,true)}};loadProfile()}

function userRoleLabel(role){return role==='admin'?'Administrator':role==='production'?'Produktion':'Außendienst'}
function userRoleBadge(role){return role==='admin'?'<span class="status s-paid">Administrator</span>':role==='production'?'<span class="status s-ready">Produktion</span>':'<span class="status s-production">Außendienst</span>'}
function roleDefaultPerms(role){return role==='production'?defaultProductionPerms:defaultFieldPerms}
function renderAdministration(){if(!isAdmin())return setView('dashboard');const users=state.users||[],activity=state.activity||[],mails=state.mail_log||[];ws.innerHTML=`<div class="sectionHead"><div><h2>Administration</h2><p>Benutzer, E-Mail-Zugänge, Rollen und sämtliche protokollierten Änderungen kontrollieren.</p></div><div class="sectionActions"><button class="btn" id="openRightsCenter">Zugriffsrechte</button><button class="btn primary" id="newUser">+ Benutzer anlegen</button></div></div><div class="adminSummary">${metric('Administratoren',users.filter(u=>u.role==='admin'&&u.status==='active').length,'Vollzugriff')}${metric('Produktions-Logins',users.filter(u=>u.role==='production'&&u.status==='active').length,'Fertigung · kein Außendienst')}${metric('Außendienst-Logins',users.filter(u=>u.role==='field_sales'&&u.status==='active').length,'Vertrieb · Provision')}${metric('Protokolleinträge',activity.length,'letzte Aktivitäten')}</div><div class="adminColumns"><section class="panel"><div class="panelTitle"><h3>Benutzer & Zugänge</h3><span>klar getrennte Rollen</span></div><div class="tableWrap"><table class="dataTable"><thead><tr><th>Benutzer</th><th>Rolle</th><th>Zuordnung</th><th>Willkommensmail</th><th>Letzter Login</th><th>Status</th><th></th></tr></thead><tbody>${users.map(u=>`<tr><td><b>${esc(u.display_name)}</b><br><small>${esc(u.username)} · ${esc(u.email||'')}</small></td><td>${userRoleBadge(u.role)}</td><td>${u.role==='field_sales'?esc(repName(u.sales_rep_id)):u.role==='production'?'<b>Produktionsteam</b>':'–'}</td><td>${u.welcome_sent_at?dt(u.welcome_sent_at):'–'}</td><td>${dt(u.last_login)}</td><td>${u.status==='active'?'Aktiv':'Inaktiv'}</td><td><div class="rowActions"><button class="miniBtn" data-user="${esc(u.id)}">Bearbeiten</button>${u.role!=='admin'?`<button class="miniBtn" data-admin-rights="${esc(u.id)}">Rechte</button><button class="miniBtn" data-resend-welcome="${esc(u.id)}">Mail senden</button>`:''}</div></td></tr>`).join('')}</tbody></table></div></section><section class="panel"><div class="panelTitle"><h3>System-E-Mails</h3><span>Versandstatus</span></div><div class="auditList">${mails.slice(0,80).map(m=>`<article><time>${dt(m.created_at)}</time><div><b>${esc(m.recipient)}</b><span>${esc(m.mail_type)} · ${m.status==='sent'?'versendet':'FEHLER'} · ${esc(m.subject)}</span></div></article>`).join('')||'<div class="empty">Noch keine System-E-Mails.</div>'}</div></section><section class="panel adminWide"><div class="panelTitle"><h3>Änderungsprotokoll</h3><span>nachvollziehbar</span></div><div class="auditList">${activity.slice(0,150).map(a=>`<article><time>${dt(a.created_at)}</time><div><b>${esc(a.actor)}</b><span>${esc(a.action_name)} · ${esc(a.entity_type)} ${esc(a.entity_id)}</span></div></article>`).join('')||'<div class="empty">Noch keine Protokolleinträge.</div>'}</div></section></div>`;$('#newUser').onclick=()=>userModal(null);$('#openRightsCenter').onclick=()=>setView('rights');$$('[data-admin-rights]').forEach(b=>b.onclick=()=>rightsModal(b.dataset.adminRights));$$('[data-resend-welcome]').forEach(b=>b.onclick=async()=>{if(!confirm('Begrüßungs- und Einrichtungslink erneut per E-Mail senden?'))return;try{await api({action:'welcome_resend',id:b.dataset.resendWelcome});toast('Begrüßungsmail versendet');await load(true);renderAdministration()}catch(e){toast(e.message,true)}})}
function userModal(id,forcedRole=''){const u=(state.users||[]).find(x=>x.id===id)||{},reps=state.sales_reps||[],initialRole=forcedRole||u.role||'field_sales',perms=Array.isArray(u.permissions)?u.permissions:roleDefaultPerms(initialRole);openModal(`<div class="modalHead"><small>BENUTZER & RECHTE</small><h2>${id?'Benutzer bearbeiten':initialRole==='production'?'Produktionsmitarbeiter anlegen':'Benutzer anlegen'}</h2><p>${initialRole==='production'?'Eigenes Orgaboard-Konto für die Fertigung · keine Außendienst-Zuordnung.':''}</p></div><div class="modalBody"><div class="formGrid"><label>Benutzername<input id="usrName" value="${esc(u.username||'')}" placeholder="vorname.nachname"></label><label>Anzeigename<input id="usrDisplay" value="${esc(u.display_name||'')}"></label><label>E-Mail<input id="usrEmail" type="email" value="${esc(u.email||'')}"></label><label>Rolle<select id="usrRole"><option value="admin" ${sel(initialRole,'admin')}>Administrator · Vollzugriff</option><option value="production" ${sel(initialRole,'production')}>Produktion · Fertigung, kein Außendienst</option><option value="field_sales" ${sel(initialRole,'field_sales')}>Außendienst · Vertrieb & Provision</option></select></label><label class="full" id="usrRepLabel">Außendienst-Zuordnung<select id="usrRep"><option value="">Keine</option>${reps.map(r=>`<option value="${esc(r.id)}" ${sel(u.sales_rep_id||'',r.id)}>${esc(r.employee_no)} · ${esc(r.name)}</option>`).join('')}</select></label><label>Status<select id="usrStatus"><option value="active" ${sel(u.status||'active','active')}>Aktiv</option><option value="inactive" ${sel(u.status,'inactive')}>Inaktiv</option></select></label><label>Neues / temporäres Passwort<input id="usrPassword" type="password" minlength="12" placeholder="${id?'leer = unverändert':'mindestens 12 Zeichen'}"></label></div><div class="roleExplain" id="usrRoleExplain"></div><div class="formActions"><button class="btn" id="usrRights" ${id&&initialRole!=='admin'?'':'disabled'}>Detaillierte Rechte</button><button class="btn primary" id="saveUser">Benutzer speichern</button></div></div>`);const syncRole=()=>{const role=$('#usrRole').value,isField=role==='field_sales';$('#usrRep').disabled=!isField;$('#usrRepLabel').style.opacity=isField?'1':'.45';if(!isField)$('#usrRep').value='';$('#usrRoleExplain').innerHTML=role==='production'?'<b>Produktionsrolle</b><span>Produktion ansehen/bearbeiten, Lager ansehen und Nachrichten nutzen. Keine Kundendatenpflege, keine Zahlungen, keine Provision, kein Außendienst.</span>':role==='field_sales'?'<b>Außendienstrolle</b><span>Vertrieb, Kundendaten und persönliche Statistik. Produktions- und Administrationsrechte sind getrennt.</span>':'<b>Administrator</b><span>Vollzugriff auf alle Bereiche.</span>'};$('#usrRole').onchange=syncRole;syncRole();$('#usrRights').onclick=()=>{closeModal();rightsModal(u.id)};$('#saveUser').onclick=async()=>{try{const role=$('#usrRole').value;const sendPerms=id&&role===initialRole?perms:roleDefaultPerms(role);await api({action:'user_save',id:u.id||'',username:$('#usrName').value,display_name:$('#usrDisplay').value,email:$('#usrEmail').value,role,sales_rep_id:role==='field_sales'?$('#usrRep').value:'',status:$('#usrStatus').value,password:$('#usrPassword').value,permissions:sendPerms});toast(role==='production'?'Produktionsmitarbeiter gespeichert':'Benutzer gespeichert');closeModal();await load(true);if(current==='production_staff')renderProductionStaff();else renderAdministration()}catch(e){toast(e.message,true)}}}
function renderProfile(){const u=state.current_user||{};ws.innerHTML=`<div class="sectionHead"><div><h2>Mein Zugang</h2><p>Eigenes Orgaboard-Passwort verwalten.</p></div></div><div class="settingsGrid"><section class="panel settingsPanel"><h3>${esc(u.display_name||'Benutzer')}</h3><p class="muted">${esc(u.username||'')} · ${u.role==='admin'?'Administrator':u.role==='production'?'Produktion':'Außendienst'}</p>${u.must_change_password?'<div class="moduleNotice"><b>Empfehlung:</b> Bitte das gemeinsame Startpasswort jetzt durch ein persönliches Passwort ersetzen.</div>':''}<div class="formGrid"><label class="full">Aktuelles Passwort<input id="ownPwCurrent" type="password"></label><label class="full">Neues Passwort<input id="ownPwNew" type="password" minlength="12"></label></div><div class="formActions"><button class="btn primary" id="ownPwSave">Passwort ändern</button></div></section></div>`;$('#ownPwSave').onclick=async()=>{try{await api({action:'password_change',current:$('#ownPwCurrent').value,new:$('#ownPwNew').value});toast('Passwort geändert');$('#ownPwCurrent').value=$('#ownPwNew').value='';state.current_user.must_change_password=false}catch(e){toast(e.message,true)}}}
function renderReviews(){const rows=state.reviews||[];ws.innerHTML=`<div class="sectionHead"><div><h2>Bewertungen</h2><p>Freigabe, Antwort und Moderation für serverseitig eingehende Bewertungen.</p></div></div><div class="moduleNotice"><b>Aktueller Stand:</b> Die vorhandenen Shop-Bewertungen aus V31 arbeiten noch clientseitig. Neue serverseitige Bewertungen können hier bereits verwaltet werden; die vollständige Umstellung des öffentlichen Bewertungsmoduls ist ein eigener Integrationsschritt.</div><div class="panel">${rows.length?rows.map(r=>`<div class="orderMini"><b>${'★'.repeat(Math.max(1,Math.min(5,+r.rating||5)))}</b><span class="customer"><strong>${esc(r.name||'Kunde')}</strong><small>${esc(r.text||'')}</small></span><span>${esc(r.status||'new')}</span><button class="miniBtn" data-review="${esc(r.id)}">Bearbeiten</button></div>`).join(''):'<div class="empty"><b>Keine serverseitigen Bewertungen</b>Dieser Bereich ist vorbereitet.</div>'}</div>`}
function contentBody(c){if(c&&c.body_data&&typeof c.body_data==='object')return c.body_data;try{return JSON.parse(c?.body||'{}')}catch(e){return {text:c?.body||''}}}
function contentTextList(v){return Array.isArray(v)?v.join('\n'):String(v||'')}
function renderContent(){const all=state.content||[],q=($('#contentSearch')?.value||contentSearchTerm||'').toLowerCase().trim(),type=contentTypeFilter||'all';let rows=all.filter(c=>(type==='all'||c.type===type)&&(!q||`${c.title} ${c.type} ${c.body||''}`.toLowerCase().includes(q)));const recipes=all.filter(c=>c.type==='Rezept').length,guides=all.filter(c=>c.type==='Anleitung').length,advice=all.filter(c=>c.type==='Ratgeber').length;ws.innerHTML=`<div class="sectionHead"><div><h2>Rezepte & Inhalte</h2><p>Alle Räucherhaken24-Rezepte und redaktionellen Inhalte zentral verwalten, drucken und als PDF sichern.</p></div>${can('edit_content')?'<button class="btn primary" id="newContent">+ Inhalt anlegen</button>':''}</div><div class="metricGrid compactMetrics">${metric('Rezepte',recipes,'aus dem Shop übernommen')}${metric('Anleitungen',guides,'Arbeitsabläufe')}${metric('Ratgeber',advice,'Wissen & Grundlagen')}${metric('Gesamt',all.length,'Inhalte im Orgaboard')}</div><div class="contentToolbar"><div class="productSearchBox"><span>⌕</span><input id="contentSearch" type="search" value="${esc(contentSearchTerm||'')}" placeholder="Rezept oder Inhalt suchen …"></div><select id="contentTypeFilter"><option value="all">Alle Inhalte</option><option value="Rezept" ${type==='Rezept'?'selected':''}>Rezepte</option><option value="Anleitung" ${type==='Anleitung'?'selected':''}>Anleitungen</option><option value="Ratgeber" ${type==='Ratgeber'?'selected':''}>Ratgeber</option></select><button class="btn" id="printContentList">⎙ Liste drucken / PDF</button></div><div class="moduleNotice"><b>V59:</b> Die 120 öffentlichen Räucherrezepte sowie die wichtigsten Fisch-, Pökel- und Schinken-Inhalte sind jetzt direkt im Orgaboard enthalten. PDF erfolgt über den Druckdialog („Als PDF sichern“).</div><div class="panel tableWrap">${rows.length?`<table class="dataTable contentTable"><thead><tr><th>Titel</th><th>Typ</th><th>Schwerpunkt</th><th>Status</th><th>Geändert</th><th>Drucken / PDF</th><th>Bearbeiten</th></tr></thead><tbody>${rows.map(c=>{const b=contentBody(c),focus=b.food||b.method||b.source||'–';return `<tr><td><b>${esc(c.title)}</b><small>${esc(b.intro||b.text||'')}</small></td><td><span class="contentType ${String(c.type).toLowerCase()}">${esc(c.type)}</span></td><td>${esc(focus)}</td><td>${c.status==='published'?'<span class="status s-paid">Veröffentlicht</span>':'<span class="status s-open">Entwurf</span>'}</td><td>${dt(c.updated_at)}</td><td><div class="inlineActions"><button class="miniBtn" data-print-content="${esc(c.id)}" title="Drucken">⎙</button><button class="miniBtn" data-pdf-content="${esc(c.id)}" title="Als PDF sichern">PDF</button></div></td><td>${can('edit_content')?`<button class="miniBtn" data-content="${esc(c.id)}">Bearbeiten</button>`:'<span class="muted tiny">Nur Ansicht</span>'}</td></tr>`}).join('')}</tbody></table>`:'<div class="empty"><b>Keine Inhalte gefunden</b>Suchbegriff oder Filter ändern.</div>'}</div>`;if($('#newContent'))$('#newContent').onclick=()=>contentModal(null);$('#contentSearch').oninput=e=>{contentSearchTerm=e.target.value;renderContent()};$('#contentTypeFilter').onchange=e=>{contentTypeFilter=e.target.value;renderContent()};$('#printContentList').onclick=()=>printContentOverview(rows);$$('[data-content]').forEach(b=>b.onclick=()=>contentModal(b.dataset.content));$$('[data-print-content]').forEach(b=>b.onclick=()=>printContentItem(b.dataset.printContent,false));$$('[data-pdf-content]').forEach(b=>b.onclick=()=>printContentItem(b.dataset.pdfContent,true))}
function contentPrintableHtml(c){const b=contentBody(c),list=(title,arr,ordered=false)=>Array.isArray(arr)&&arr.length?`<h2>${title}</h2><${ordered?'ol':'ul'}>${arr.map(x=>`<li>${esc(x)}</li>`).join('')}</${ordered?'ol':'ul'}>`:'';const sections=Array.isArray(b.sections)?`<h2>Inhalte</h2>${b.sections.map(x=>`<section><h3>${esc(x.title||'')}</h3><p>${esc(x.text||'')}</p></section>`).join('')}`:'';return `<article><div class="printBrand">Räucherhaken24 · ${esc(c.type)}</div><h1>${esc(c.title)}</h1>${b.intro?`<p class="lead">${esc(b.intro)}</p>`:''}<div class="printMeta">${b.food?`<span><b>Produkt:</b> ${esc(b.food)}</span>`:''}${b.method?`<span><b>Methode:</b> ${esc(b.method)}</span>`:''}${b.wood?`<span><b>Rauch/Holz:</b> ${esc(b.wood)}</span>`:''}${b.difficulty?`<span><b>Schwierigkeit:</b> ${esc(b.difficulty)}</span>`:''}</div>${list('Zutaten',b.ingredients,false)}${list('Zubereitung',b.steps,true)}${sections}${b.text?`<div class="bodyText">${esc(b.text).replace(/\n/g,'<br>')}</div>`:''}${b.tip?`<div class="tip"><b>Tipp:</b> ${esc(b.tip)}</div>`:''}${b.warning?`<div class="warning"><b>Hinweis:</b> ${esc(b.warning)}</div>`:''}<footer>Räucherhaken24 · Ausdruck aus dem Orgaboard · ${new Date().toLocaleDateString('de-DE')}</footer></article>`}
function openPrintWindow(title,body,auto=true){const w=window.open('','_blank','width=980,height=800');if(!w)return toast('Popup wurde blockiert. Bitte Popups für das Orgaboard erlauben.',true);w.document.write(`<!doctype html><html lang="de"><head><meta charset="utf-8"><title>${esc(title)}</title><style>@page{size:A4;margin:16mm}body{font:14px/1.5 -apple-system,BlinkMacSystemFont,"Segoe UI",Arial,sans-serif;color:#1e1e1e}article{max-width:760px;margin:auto}.printBrand{font-size:11px;letter-spacing:.12em;text-transform:uppercase;color:#8b4a22;font-weight:800;border-bottom:2px solid #8b4a22;padding-bottom:8px}h1{font-size:28px;margin:18px 0 8px}h2{font-size:17px;margin-top:24px}h3{font-size:15px}.lead{font-size:16px;color:#555}.printMeta{display:flex;flex-wrap:wrap;gap:8px 18px;padding:12px;background:#f5f1eb;border-radius:8px}.tip,.warning{padding:12px 14px;margin-top:18px;border-left:4px solid #b06a2c;background:#faf6ef}.warning{border-color:#9d352e;background:#fff4f2}li{margin:6px 0}footer{margin-top:34px;padding-top:10px;border-top:1px solid #ddd;color:#777;font-size:11px}@media print{button{display:none}}</style></head><body>${body}</body></html>`);w.document.close();if(auto)setTimeout(()=>{w.focus();w.print()},250)}
function printContentItem(id,pdf=false){const c=(state.content||[]).find(x=>x.id===id);if(!c)return;openPrintWindow((pdf?'PDF · ':'')+c.title,contentPrintableHtml(c),true);if(pdf)toast('Im Druckdialog „Als PDF sichern“ auswählen.')}
function printContentOverview(rows){const body=`<article><div class="printBrand">Räucherhaken24 · Orgaboard</div><h1>Rezepte & Inhalte</h1><p class="lead">${rows.length} Inhalte</p><table style="width:100%;border-collapse:collapse"><thead><tr><th style="text-align:left;border-bottom:2px solid #333;padding:7px">Titel</th><th style="text-align:left;border-bottom:2px solid #333;padding:7px">Typ</th><th style="text-align:left;border-bottom:2px solid #333;padding:7px">Status</th></tr></thead><tbody>${rows.map(c=>`<tr><td style="padding:7px;border-bottom:1px solid #ddd">${esc(c.title)}</td><td style="padding:7px;border-bottom:1px solid #ddd">${esc(c.type)}</td><td style="padding:7px;border-bottom:1px solid #ddd">${esc(c.status)}</td></tr>`).join('')}</tbody></table></article>`;openPrintWindow('Rezepte & Inhalte',body,true)}
function contentModal(id){const c=(state.content||[]).find(x=>x.id===id)||{},b=contentBody(c);openModal(`<div class="modalHead"><small>CONTENT · V59</small><h2>${id?'Inhalt bearbeiten':'Inhalt anlegen'}</h2><p>Rezepte, Anleitungen und Ratgeber strukturiert pflegen.</p></div><div class="modalBody"><div class="formGrid"><label class="full">Titel<input id="ctTitle" value="${esc(c.title||'')}"></label><label>Typ<select id="ctType"><option ${sel(c.type||'Rezept','Rezept')}>Rezept</option><option ${sel(c.type,'Anleitung')}>Anleitung</option><option ${sel(c.type,'Ratgeber')}>Ratgeber</option></select></label><label>Status<select id="ctStatus"><option value="draft" ${sel(c.status||'draft','draft')}>Entwurf</option><option value="published" ${sel(c.status,'published')}>Veröffentlicht</option></select></label><label class="full">Kurzbeschreibung<textarea id="ctIntro">${esc(b.intro||b.text||'')}</textarea></label><label>Produkt / Lebensmittel<input id="ctFood" value="${esc(b.food||'')}"></label><label>Methode<input id="ctMethod" value="${esc(b.method||'')}"></label><label>Räucherholz / Aroma<input id="ctWood" value="${esc(b.wood||'')}"></label><label>Schwierigkeit<input id="ctDifficulty" value="${esc(b.difficulty||'')}"></label><label class="full">Zutaten · eine Zeile pro Zutat<textarea id="ctIngredients">${esc(contentTextList(b.ingredients))}</textarea></label><label class="full">Zubereitung / Schritte · eine Zeile pro Schritt<textarea id="ctSteps">${esc(contentTextList(b.steps))}</textarea></label><label class="full">Tipp<textarea id="ctTip">${esc(b.tip||'')}</textarea></label><label class="full">Hinweis / Sicherheit<textarea id="ctWarning">${esc(b.warning||'')}</textarea></label></div><div class="formActions"><button class="btn" id="ctPrint">⎙ Drucken / PDF</button><button class="btn primary" id="saveContent">Speichern</button></div></div>`);$('#ctPrint').onclick=()=>{const tmp={...c,title:$('#ctTitle').value||'Inhalt',type:$('#ctType').value,body_data:{intro:$('#ctIntro').value,food:$('#ctFood').value,method:$('#ctMethod').value,wood:$('#ctWood').value,difficulty:$('#ctDifficulty').value,ingredients:$('#ctIngredients').value.split('\n').map(x=>x.trim()).filter(Boolean),steps:$('#ctSteps').value.split('\n').map(x=>x.trim()).filter(Boolean),tip:$('#ctTip').value,warning:$('#ctWarning').value}};openPrintWindow(tmp.title,contentPrintableHtml(tmp),true)};$('#saveContent').onclick=async()=>{try{const body={kind:$('#ctType').value==='Rezept'?'recipe':'guide',intro:$('#ctIntro').value,food:$('#ctFood').value,method:$('#ctMethod').value,wood:$('#ctWood').value,difficulty:$('#ctDifficulty').value,ingredients:$('#ctIngredients').value.split('\n').map(x=>x.trim()).filter(Boolean),steps:$('#ctSteps').value.split('\n').map(x=>x.trim()).filter(Boolean),tip:$('#ctTip').value,warning:$('#ctWarning').value,source:'Orgaboard'};await api({action:'content_save',id:c.id||'',title:$('#ctTitle').value,type:$('#ctType').value,status:$('#ctStatus').value,body:JSON.stringify(body)});toast('Inhalt gespeichert');closeModal();await load(true);renderContent()}catch(e){toast(e.message,true)}}}

function renderAI(){const questions=state.ai?.questions||[];const max=Math.max(1,...questions.map(x=>+x.count||0));ws.innerHTML=`<div class="sectionHead"><div><h2>Smoky / KI</h2><p>Häufige Beratungsfragen als Grundlage für Produkte, Inhalte und Conversion.</p></div></div><div class="moduleNotice"><b>Datenschutzfreundlich vorbereitet:</b> Dieses Modul soll nur aggregierte Fragekategorien speichern. Die aktuelle Smoky-Version V31 protokolliert Gespräche noch nicht serverseitig.</div><div class="panel panelPad"><h3 style="margin-top:0;font-size:13px">Häufige Themen</h3>${questions.length?`<div class="statBars">${questions.map(x=>`<div class="statRow"><span>${esc(x.label)}</span><div class="statTrack"><i style="width:${(+x.count/max)*100}%"></i></div><b>${+x.count}</b></div>`).join('')}</div>`:'<div class="empty"><b>Noch keine aggregierten KI-Daten</b>Smoky arbeitet aktuell ohne serverseitige Gesprächsprotokollierung.</div>'}</div>`}
function renderNewsletter(){if(!isAdmin())return setView('dashboard');const sum=state.newsletter_summary||{},rows=state.newsletter_campaigns||[],defaultEmail=state.settings?.system_email||'';ws.innerHTML=`<div class="sectionHead"><div><h2>Newsletter</h2><p>Kundenbindung mit Double-Opt-In. Versendet wird ausschließlich an bestätigte Empfänger.</p></div></div><div class="metricGrid compactMetrics">${metric('Bestätigt',sum.confirmed||0,'dürfen Newsletter erhalten')}${metric('Bestätigung offen',sum.pending||0,'Double-Opt-In ausstehend')}${metric('Abgemeldet',sum.unsubscribed||0,'werden nicht angeschrieben')}</div><div class="newsletterLayout"><section class="panel panelPad"><div class="panelTitle plain"><h3>Newsletter erstellen</h3><span>max. 150 Empfänger über STRATO-Mail</span></div><div class="formGrid"><label class="full">Betreff<input id="nlSubject" maxlength="220" placeholder="z. B. Neue Räucherideen für den Herbst"></label><label class="full">Nachricht<textarea id="nlBody" style="min-height:240px" maxlength="12000" placeholder="Schreibe hier deine Nachricht …"></textarea></label><label class="full">Test-E-Mail<input id="nlTestEmail" type="email" value="${esc(defaultEmail)}"></label></div><div class="moduleNotice"><b>Rechtssicherer Ablauf:</b> Ein Kunde wird erst nach Bestätigung der Double-Opt-In-Mail in den Verteiler aufgenommen. Jeder Newsletter enthält automatisch einen Abmeldelink.</div><div class="formActions"><button class="btn" id="nlTest">Test senden</button><button class="btn primary" id="nlSend">An ${Number(sum.confirmed||0)} bestätigte Empfänger senden</button></div></section><section class="panel tableWrap"><div class="panelTitle"><h3>Versandhistorie</h3><span>${rows.length} Kampagnen</span></div>${rows.length?`<table class="dataTable"><thead><tr><th>Datum</th><th>Betreff</th><th>Empfänger</th><th>Versendet</th><th>Fehler</th><th>Status</th></tr></thead><tbody>${rows.map(r=>`<tr><td>${dt(r.created_at)}</td><td><b>${esc(r.subject)}</b></td><td>${r.recipient_count}</td><td>${r.sent_count}</td><td>${r.failed_count}</td><td>${esc(r.status)}</td></tr>`).join('')}</tbody></table>`:'<div class="empty"><b>Noch kein Newsletter versendet</b>Bestätigte Newsletter-Kunden erscheinen automatisch im Verteiler.</div>'}</section></div>`;$('#nlTest').onclick=async()=>{try{const d=await api({action:'newsletter_test',email:$('#nlTestEmail').value,subject:$('#nlSubject').value,body:$('#nlBody').value});toast(d.sent?'Testmail versendet':'Testmail konnte nicht versendet werden',!d.sent)}catch(e){toast(e.message,true)}};$('#nlSend').onclick=async()=>{if(!confirm(`Newsletter wirklich an ${Number(sum.confirmed||0)} bestätigte Empfänger senden?`))return;try{const d=await api({action:'newsletter_send',subject:$('#nlSubject').value,body:$('#nlBody').value});toast(`Newsletter: ${d.sent} versendet${d.failed?' · '+d.failed+' Fehler':''}`,d.failed>0);await load(true);renderNewsletter()}catch(e){toast(e.message,true)}}}

function marketStatusLabel(s){return ({pending:'Zahlung offen',active:'Aktiv',expired:'Abgelaufen',cancelled:'Storniert',published:'Online',paused:'Pausiert',sold:'Erledigt',rejected:'Abgelehnt'})[s]||s||'–'}
function renderMarketplace(){
 const m=state.marketplace||{stats:{},users:[],listings:[],reports:[]},st=m.stats||{},users=m.users||[],listings=m.listings||[],reports=m.reports||[];
 const pending=listings.filter(x=>x.status==='pending'),published=listings.filter(x=>x.status==='published'),openReports=reports.filter(x=>x.status==='open');
 const allowed=['Räucheröfen & Räucherschränke','Smoker','Grills','Räucherhaken & Halter','Räucherzubehör','Grill- & BBQ-Zubehör','Thermometer & Messtechnik','Räucherholz, Chunks & Räuchermehl','Brennstoff- & Feuerzubehör','Ersatzteile & Werkzeuge'];
 const policyBadge=x=>allowed.includes(x.category)?'<span class="status s-paid">Kategorie OK</span>':'<span class="status s-cancelled">NICHT ZULÄSSIG</span>';
 ws.innerHTML=`<div class="sectionHead"><div><h2>An- & Verkaufen</h2><p>Jahreszugänge, Mitglieder, Anzeigenfreigaben und Meldungen zentral verwalten.</p></div><div class="sectionActions"><a class="btn" href="../ankauf-verkauf.php" target="_blank" rel="noopener">Marktplatz öffnen ↗</a><a class="btn" href="../marktplatz-agb.html" target="_blank" rel="noopener">Marktplatz-AGB ↗</a><button class="btn primary" id="marketReload">Aktualisieren</button></div></div>
 <div class="moduleNotice" style="border-left:5px solid #b7372f"><b>V61 · Strikte Freigaberegel:</b> Nur Räucher-, Smoker- und Grilltechnik sowie passendes Zubehör. <b>Lebensmittel und verzehrbare Ware dürfen niemals freigegeben werden.</b> Räucherhaken24 stellt ausschließlich die Plattform bereit.</div>
 <div class="marketAdminPrice"><div><small>JAHRESZUGANG</small><strong>19,99 €</strong><span>inkl. MwSt. · 1 Jahr · maximal 10 aktive Anzeigen</span></div><p>Neue Registrierungen erzeugen automatisch einen Auftrag. Sobald die Zahlung in <b>Bestellungen</b> auf „Bezahlt“ gestellt wird, wird der Marktplatz-Zugang automatisch für 365 Tage freigeschaltet.</p></div>
 <div class="metricGrid marketMetrics">${metric('Mitglieder',st.users||0,'registrierte Konten')}${metric('Aktive Zugänge',st.active_members||0,'Jahreszugang freigeschaltet')}${metric('Zahlung offen',st.pending_members||0,'noch nicht freigeschaltet',st.pending_members?'attn':'')}${metric('Prüfung offen',st.pending_listings||0,'Anzeigen freigeben',st.pending_listings?'attn':'')}${metric('Online',st.published_listings||0,'veröffentlichte Anzeigen')}${metric('Meldungen',st.open_reports||0,'offene Prüfmeldungen',st.open_reports?'attn':'')}</div>
 <div class="marketAdminGrid"><section class="panel"><div class="panelTitle"><div><h3>Anzeigen zur Prüfung</h3><span>${pending.length} offen</span></div></div>${pending.length?`<div class="tableWrap"><table class="dataTable marketAdminTable"><thead><tr><th>Anzeige</th><th>Verkäufer</th><th>Regelcheck</th><th>Region</th><th>Preis</th><th>Aktion</th></tr></thead><tbody>${pending.map(x=>`<tr><td style="min-width:300px"><b>${esc(x.title)}</b><small>${esc(x.category)}</small><div style="font-size:11px;color:#6e6258;line-height:1.4;margin-top:5px;max-width:480px">${esc((x.description||'').slice(0,260))}${(x.description||'').length>260?' …':''}</div></td><td>${esc(x.seller_name||'–')}<small>${esc(x.seller_email||'')}</small></td><td>${policyBadge(x)}<small>${x.kind==='wanted'?'Gesucht':x.kind==='gift'?'Verschenken':'Verkaufen'}</small></td><td>${esc(x.zip)} ${esc(x.city)}</td><td>${x.kind==='gift'?'Kostenlos':money(x.price)}</td><td><div class="inlineActions"><button class="miniBtn primaryMini" data-market-listing="${esc(x.id)}" data-market-status="published">Freigeben</button><button class="miniBtn" data-market-listing="${esc(x.id)}" data-market-status="rejected">Ablehnen</button></div></td></tr>`).join('')}</tbody></table></div>`:'<div class="empty"><b>Keine offenen Anzeigen</b>Aktuell wartet keine Anzeige auf Freigabe.</div>'}</section>
 <section class="panel"><div class="panelTitle"><div><h3>Offene Meldungen</h3><span>${openReports.length}</span></div></div>${openReports.length?`<div class="marketReportList">${openReports.map(r=>`<article><div><b>${esc(r.listing_title||r.listing_id)}</b><span>${esc(r.reason)}</span><small>${esc(r.details||'')} · ${dt(r.created_at)}</small>${r.reporter_email?`<small>Meldung von: ${esc(r.reporter_name||'–')} · ${esc(r.reporter_email)}</small>`:''}</div><button class="miniBtn" data-market-report="${r.id}">Erledigt</button></article>`).join('')}</div>`:'<div class="empty"><b>Keine Meldungen</b>Es liegen keine offenen Nutzerhinweise vor.</div>'}</section></div>
 <section class="panel" style="margin-top:16px"><div class="panelTitle"><div><h3>Mitglieder & Jahreszugänge</h3><span>${users.length} Konten</span></div></div><div class="tableWrap"><table class="dataTable marketAdminTable"><thead><tr><th>Mitglied</th><th>E-Mail</th><th>Marktplatz-AGB</th><th>Jahreszugang</th><th>Gültig bis</th><th>Auftrag</th><th>Konto</th><th>Aktion</th></tr></thead><tbody>${users.map(u=>`<tr><td><b>${esc(u.display_name)}</b><small>${esc(u.zip||'')} ${esc(u.city||'')}</small></td><td>${u.email_verified_at?'<span class="status s-paid">Bestätigt</span>':'<span class="status s-open">Offen</span>'}</td><td>${u.terms_version==='2026-08-24-v61'?'<span class="status s-paid">V61 bestätigt</span>':'<span class="status s-open">Neu bestätigen</span>'}</td><td><span class="marketMembership ${esc(u.membership_status)}">${esc(marketStatusLabel(u.membership_status))}</span></td><td>${u.membership_expires_at?donly(u.membership_expires_at):'–'}</td><td>${esc(u.membership_order_no||'–')}</td><td>${u.status==='active'?'<span class="status s-paid">Aktiv</span>':'<span class="status s-cancelled">Gesperrt</span>'}</td><td><div class="inlineActions">${u.membership_status!=='active'?`<button class="miniBtn primaryMini" data-market-activate="${esc(u.id)}">1 Jahr freischalten</button>`:''}<button class="miniBtn" data-market-user="${esc(u.id)}" data-market-user-status="${u.status==='active'?'banned':'active'}">${u.status==='active'?'Sperren':'Entsperren'}</button></div></td></tr>`).join('')}</tbody></table></div></section>
 <section class="panel" style="margin-top:16px"><div class="panelTitle"><div><h3>Veröffentlichte Anzeigen</h3><span>${published.length} online</span></div></div>${published.length?`<div class="tableWrap"><table class="dataTable marketAdminTable"><thead><tr><th>Anzeige</th><th>Verkäufer</th><th>Regelcheck</th><th>Region</th><th>Aufrufe</th><th>Aktion</th></tr></thead><tbody>${published.map(x=>`<tr><td><b>${esc(x.title)}</b><small>${esc(x.category)}</small></td><td>${esc(x.seller_name||'–')}</td><td>${policyBadge(x)}</td><td>${esc(x.zip)} ${esc(x.city)}</td><td>${Number(x.views||0)}</td><td><button class="miniBtn" data-market-listing="${esc(x.id)}" data-market-status="paused">Pausieren</button></td></tr>`).join('')}</tbody></table></div>`:'<div class="empty">Noch keine veröffentlichten Anzeigen.</div>'}</section>`;
 if($('#marketReload'))$('#marketReload').onclick=async()=>{await load(true);renderMarketplace()};
 $$('[data-market-listing]').forEach(b=>b.onclick=async()=>{if(b.dataset.marketStatus==='published'&&!confirm('Anzeige geprüft? Keine Lebensmittel, nur Räucher-/Smoker-/Grilltechnik oder Zubehör?'))return;try{const d=await api({action:'market_listing_moderate',id:b.dataset.marketListing,status:b.dataset.marketStatus});state.marketplace=d.marketplace;toast('Marktplatz-Anzeige aktualisiert');renderMarketplace()}catch(e){toast(e.message,true)}});
 $$('[data-market-activate]').forEach(b=>b.onclick=async()=>{if(!confirm('Jahreszugang für 365 Tage freischalten?'))return;try{const d=await api({action:'market_membership_activate',user_id:b.dataset.marketActivate});state.marketplace=d.marketplace;toast('Jahreszugang freigeschaltet');renderMarketplace()}catch(e){toast(e.message,true)}});
 $$('[data-market-user]').forEach(b=>b.onclick=async()=>{try{const d=await api({action:'market_user_status',user_id:b.dataset.marketUser,status:b.dataset.marketUserStatus});state.marketplace=d.marketplace;toast('Mitgliedskonto aktualisiert');renderMarketplace()}catch(e){toast(e.message,true)}});
 $$('[data-market-report]').forEach(b=>b.onclick=async()=>{try{const d=await api({action:'market_report_close',report_id:+b.dataset.marketReport});state.marketplace=d.marketplace;toast('Meldung abgeschlossen');renderMarketplace()}catch(e){toast(e.message,true)}})
}

function renderSettings(){const s=state.settings||{};ws.innerHTML=`<div class="sectionHead"><div><h2>Einstellungen</h2><p>Shopparameter und Admin-Sicherheit verwalten.</p></div></div><div class="settingsGrid"><section class="panel settingsPanel"><h3>Shopparameter</h3><div class="formGrid"><label class="full">Shopname<input id="setName" value="${esc(s.shop_name||'Räucherhaken24')}"></label><label>Versandfrei ab (€)<input id="setThreshold" type="number" step="0.01" value="${Number(s.shipping_threshold??39)}"></label><label>Versandkosten (€)<input id="setShipping" type="number" step="0.01" value="${Number(s.shipping_cost??9.5)}"></label><label>MwSt. (%)<input id="setVat" type="number" step="1" value="${Number(s.vat_rate??19)}"></label></div><div class="formActions"><button class="btn primary" id="saveSettings">Speichern</button></div></section><section class="panel settingsPanel"><h3>Admin-Passwort ändern</h3><div class="formGrid"><label class="full">Aktuelles Passwort<input id="pwCurrent" type="password"></label><label class="full">Neues Passwort<input id="pwNew" type="password" minlength="10"></label></div><div class="formActions"><button class="btn" id="changePassword">Passwort ändern</button></div><p class="muted tiny">Mindestens 10 Zeichen; besser 14+ Zeichen.</p></section></div>`;$('#saveSettings').onclick=async()=>{try{await api({action:'settings_save',shop_name:$('#setName').value,shipping_threshold:+$('#setThreshold').value,shipping_cost:+$('#setShipping').value,vat_rate:+$('#setVat').value});toast('Einstellungen gespeichert');await load(true)}catch(e){toast(e.message,true)}};$('#changePassword').onclick=async()=>{try{await api({action:'password_change',current:$('#pwCurrent').value,new:$('#pwNew').value});toast('Admin-Passwort geändert');$('#pwCurrent').value=$('#pwNew').value=''}catch(e){toast(e.message,true)}}}

function openModal(html){$('#modalContent').innerHTML=html;$('#modalBackdrop').classList.add('open')}
function closeModal(){$('#modalBackdrop').classList.remove('open');$('#modalContent').innerHTML=''}
function exportOrders(){const rows=filteredOrders(),lines=[['Bestellnummer','Datum','Kunde','E-Mail','Herkunft','Fester Kundenberater','Provisionsgutschrift an','Zuordnungsgrund','Status','Zahlung','Gesamt'].join(';')];rows.forEach(o=>lines.push([o.order_no||o.id,donly(o.created_at),orderCustomer(o),orderEmail(o),o.sales_channel_label||salesChannelLabel(o.sales_channel||o.source),o.sales_rep_name||repName(o.sales_rep_id),commissionRepName(o),o.commission_note||attributionLabel(o),state.order_statuses[o.status]||o.status,o.payment_status,money(orderTotal(o)).replace(' €','')].map(v=>'"'+String(v??'').replaceAll('"','""')+'"').join(';')));const blob=new Blob(['\ufeff'+lines.join('\n')],{type:'text/csv;charset=utf-8'}),a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download='raeucherhaken24-bestellungen-v69-mit-verkaufsherkunft.csv';a.click();URL.revokeObjectURL(a.href)}
function printInvoice(o){const c=o.customer||{},t=o.totals||{},items=o.items||[],w=window.open('','_blank','width=900,height=900');if(!w)return toast('Pop-up wurde blockiert',true);w.document.write(`<!doctype html><html><head><meta charset="utf-8"><title>Rechnung ${esc(o.order_no||o.id)}</title><style>body{font-family:Arial,sans-serif;color:#222;padding:45px;max-width:800px;margin:auto}header{border-bottom:3px solid #70461f;padding-bottom:18px;margin-bottom:28px}h1{margin:0;color:#5b3a1e}small{color:#777}section{display:grid;grid-template-columns:1fr 1fr;gap:20px;margin:25px 0}table{width:100%;border-collapse:collapse;margin-top:25px}th,td{text-align:left;padding:10px 8px;border-bottom:1px solid #ddd;font-size:13px}th{font-size:11px;color:#666}td:last-child,th:last-child{text-align:right}.tot{width:320px;margin:20px 0 0 auto}.tot div{display:flex;justify-content:space-between;padding:4px}.grand{font-size:17px;font-weight:bold;border-top:2px solid #333;margin-top:6px;padding-top:9px!important}.note{margin-top:35px;font-size:11px;color:#666}@media print{button{display:none}}</style></head><body><header><h1>Räucherhaken24</h1><small>Rechnungs-/Bestellbeleg</small></header><h2>Beleg ${esc(o.order_no||o.id)}</h2><section><div><b>Rechnung an</b><br>${esc(c.name||'')}<br>${esc(c.street||'')}<br>${esc(c.zip||'')} ${esc(c.city||'')}<br>${esc(c.email||'')}</div><div><b>Bestelldatum</b><br>${donly(o.created_at)}<br><br><b>Zahlungsstatus</b><br>${o.payment_status==='paid'?'Bezahlt':'Offen'}</div></section><table><thead><tr><th>Position</th><th>Menge</th><th>Einzel</th><th>Gesamt</th></tr></thead><tbody>${items.map(x=>`<tr><td>${esc(x.name||x.id)}${itemMeta(x)?'<br><small>'+esc(itemMeta(x))+'</small>':''}</td><td>${Number(x.qty||1)}</td><td>${money(x.unit_price||0)}</td><td>${money((x.unit_price||0)*(x.qty||1))}</td></tr>`).join('')}</tbody></table><div class="tot"><div><span>Warenwert</span><b>${money(t.subtotal||0)}</b></div><div><span>Versand</span><b>${money(t.shipping||0)}</b></div><div><span>inkl. 19 % MwSt.</span><b>${money(t.vat||0)}</b></div><div class="grand"><span>Gesamt</span><b>${money(t.gross||orderTotal(o))}</b></div></div><p class="note">Dieser Ausdruck wird aus den gespeicherten Bestelldaten erzeugt. Vor produktivem Einsatz als steuerliche Rechnung müssen Pflichtangaben und fortlaufende Rechnungsnummern abschließend eingerichtet werden.</p><button onclick="print()">Drucken</button></body></html>`);w.document.close()}


function periodLabel(p){if(!p)return'aktueller Monat';const [y,m]=p.split('-');return new Date(+y,+m-1,1).toLocaleDateString('de-DE',{month:'long',year:'numeric'})}
function commissionScaleHtml(){return `<div class="commissionScale progressive"><div><b>0–10.000 €</b><span>5 %</span><small>nur auf diesen Umsatzbereich</small></div><div><b>10.000–20.000 €</b><span>7,5 %</span><small>nur auf diesen Umsatzbereich</small></div><div><b>20.000–30.000 €</b><span>10 %</span><small>nur auf diesen Umsatzbereich</small></div><div><b>30.000–50.000 €</b><span>12,5 %</span><small>nur auf diesen Umsatzbereich</small></div><div><b>50.000–75.000 €</b><span>15 %</span><small>nur auf diesen Umsatzbereich</small></div><div><b>ab 75.000 €</b><span>20 %</span><small>nur auf den Mehrumsatz</small></div></div>`}
function salesStatsPanel(st,title='Verkaufsstatistik'){st=st||{};const next=st.next_threshold,eff=Number(st.effective_rate||0),credits=st.credited_orders||[];return `<div class="sectionHead miniSection"><div><h2>${esc(title)}</h2><p>Volle progressive Provision auf alle bezahlten Aufträge, die dir fachlich gutgeschrieben wurden – auch bei späteren Onlinekäufen deiner Kunden.</p></div></div><div class="metricGrid repMetrics">${metric('Nettoumsatz',money(st.net_revenue||0),'voll provisionsfähig')}${metric('Provision',money(st.commission||0),'effektiv '+eff.toLocaleString('de-DE',{maximumFractionDigits:2})+' %')}${metric('Gutgeschriebene Aufträge',st.order_count||0,'im Zeitraum')}${metric('Verkaufte Einheiten',st.item_qty||0,'Stück / Sets')}</div>${next?`<div class="moduleNotice"><b>Nächste Umsatzstufe:</b> Noch ${money(st.gap_to_next||0)} bis ${money(next)}. Der höhere Satz gilt progressiv für den zusätzlichen Umsatzbereich.</div>`:`<div class="moduleNotice goodNotice"><b>Spitzenstufe erreicht:</b> Umsatz oberhalb von 75.000 € wird mit 20 % vergütet.</div>`}${commissionScaleHtml()}<section class="panel commissionLedger"><div class="panelTitle"><h3>Provisionsgutschriften</h3><span>${credits.length} Aufträge</span></div>${credits.length?`<div class="tableWrap"><table class="dataTable"><thead><tr><th>Datum</th><th>Bestellung</th><th>Kunde</th><th>Herkunft</th><th>Gutschrift-Grund</th><th>Nettoumsatz</th><th>Provisionsanteil</th></tr></thead><tbody>${credits.map(x=>`<tr class="${x.sales_channel==='online'?'onlineCreditRow':''}"><td>${donly(x.created_at)}</td><td><b>${esc(x.order_no)}</b></td><td>${esc(x.customer_name||'–')}</td><td><span class="salesOriginTag origin-${esc(x.sales_channel||'other')}">${esc(x.sales_channel_label||salesChannelLabel(x.sales_channel))}</span></td><td><b>${x.sales_channel==='online'?'Online gekauft · Provisionsgutschrift':esc(x.commission_attribution_label||'Provisionsgutschrift')}</b><small class="tableSub">${esc(x.commission_note||'')}</small></td><td class="money">${money(x.net||0)}</td><td class="money commissionMoney">${money(x.commission_credit||0)}</td></tr>`).join('')}</tbody></table></div>`:'<div class="empty"><b>Noch keine Provisionsgutschriften</b>Es werden nur bezahlte, nicht stornierte und dir zugeordnete Aufträge berücksichtigt.</div>'}</section><div class="panel tableWrap" style="margin-top:18px">${(st.breakdown||[]).length?`<table class="dataTable"><thead><tr><th>Art.-Nr.</th><th>Produkt</th><th>Menge</th><th>Nettoumsatz</th></tr></thead><tbody>${st.breakdown.map(x=>`<tr><td><b>${esc(x.article_no||'–')}</b></td><td>${esc(x.name)}</td><td>${Number(x.qty||0)}</td><td class="money">${money(x.net||0)}</td></tr>`).join('')}</tbody></table>`:'<div class="empty"><b>Noch kein provisionsfähiger Umsatz</b>Es werden nur bezahlte und nicht stornierte Aufträge berücksichtigt.</div>'}</div>`}
function earningsTier(net){net=Math.max(0,Number(net)||0);if(net>=75000)return{rate:20,from:75000,to:null,label:'20 % Spitzenstufe'};if(net>=50000)return{rate:15,from:50000,to:75000,label:'15 % Platin'};if(net>=30000)return{rate:12.5,from:30000,to:50000,label:'12,5 % Gold'};if(net>=20000)return{rate:10,from:20000,to:30000,label:'10 % Silber'};if(net>=10000)return{rate:7.5,from:10000,to:20000,label:'7,5 % Bronze'};return{rate:5,from:0,to:10000,label:'5 % Starter'}}
function earningsCommission(net){net=Math.max(0,Number(net)||0);const tiers=[{from:0,to:10000,rate:.05},{from:10000,to:20000,rate:.075},{from:20000,to:30000,rate:.10},{from:30000,to:50000,rate:.125},{from:50000,to:75000,rate:.15},{from:75000,to:Infinity,rate:.20}];let total=0;for(const t of tiers){if(net<=t.from)break;const part=Math.min(net,t.to)-t.from;if(part>0)total+=part*t.rate;}return total}
function revenueForCommission(target){target=Math.max(0,Number(target)||0);if(target<=0)return 0;const tiers=[{from:0,to:10000,rate:.05},{from:10000,to:20000,rate:.075},{from:20000,to:30000,rate:.10},{from:30000,to:50000,rate:.125},{from:50000,to:75000,rate:.15},{from:75000,to:Infinity,rate:.20}];let remaining=target;for(const t of tiers){const cap=t.to===Infinity?Infinity:(t.to-t.from)*t.rate;if(remaining<=cap||cap===Infinity)return t.from+remaining/t.rate;remaining-=cap;}return 0}
function businessDaysRemaining(){const d=new Date(),end=new Date(d.getFullYear(),d.getMonth()+1,0),x=new Date(d.getFullYear(),d.getMonth(),d.getDate());let n=0;for(;x<=end;x.setDate(x.getDate()+1)){const wd=x.getDay();if(wd!==0&&wd!==6)n++}return Math.max(1,n)}
function earningStatsForSelected(){if(!isAdmin())return state.my_sales_stats||{};const reps=state.sales_reps||[];if(!earningsRepId&&reps.length)earningsRepId=reps[0].id;return reps.find(r=>r.id===earningsRepId)?.stats_current||{period:'',net_revenue:0,commission:0,commission_rate:7.5,order_count:0,item_qty:0}}
function earningRepForSelected(){if(!isAdmin())return{name:state.current_user?.display_name||'Außendienst',id:state.current_user?.sales_rep_id||''};const reps=state.sales_reps||[];if(!earningsRepId&&reps.length)earningsRepId=reps[0].id;return reps.find(r=>r.id===earningsRepId)||{name:'Planung ohne Mitarbeiter',id:''}}
function renderEarnings(){if(!can('view_earnings_calculator'))return setView('dashboard');const st=earningStatsForSelected(),rep=earningRepForSelected(),cur=Number(st.net_revenue||0),days=businessDaysRemaining();const repPicker=isAdmin()?`<label class="earnRepPicker">Mitarbeiter<select id="earnRepSelect"><option value="">Planung ohne Mitarbeiter</option>${(state.sales_reps||[]).map(r=>`<option value="${esc(r.id)}" ${sel(earningsRepId,r.id)}>${esc(r.employee_no)} · ${esc(r.name)}</option>`).join('')}</select></label>`:'';ws.innerHTML=`<div class="sectionHead"><div><h2>${isAdmin()?'Verdienst- & Provisionsrechner':'Mein Verdienst- & Provisionsrechner'}</h2><p>Monatsverdienst planen, Provisionsstufen simulieren und Verkaufsziele rückwärts berechnen.</p></div>${repPicker}</div><div class="earningsHero"><div><small>AKTUELLER MONAT · ${esc(periodLabel(st.period))}</small><h3>${esc(rep.name||'Außendienst')}</h3><p>Der Ist-Umsatz kommt automatisch aus bezahlten, nicht stornierten Aufträgen. Eigene Planwerte verändern keine Bestellungen und keine Abrechnung.</p></div><div class="earningsHeroMetric"><span>Aktuell provisionsfähig</span><strong>${money(cur)}</strong><small>${Number(st.commission_rate||7.5).toLocaleString('de-DE')} % · ${money(st.commission||0)} Provision</small></div></div><div class="earningsLayout"><section class="panel panelPad"><div class="panelTitle plain"><h3>1. Monatsplanung</h3><span>Deine Annahmen</span></div><div class="formGrid earnForm"><label>Ist-Nettoumsatz (€)<input id="earnCurrent" type="number" step="0.01" value="${cur.toFixed(2)}" ${isAdmin()?'':'readonly'}></label><label>Geplanter zusätzlicher Nettoumsatz (€)<input id="earnExtra" type="number" min="0" step="100" value="0"></label><label>Grundvergütung / Fixum brutto (€)<input id="earnBase" type="number" min="0" step="50" value="0"></label><label>Zusätzlicher steuerpflichtiger Bonus (€)<input id="earnBonus" type="number" min="0" step="25" value="0"></label><label>Kosten-/Spesen-Erstattung (€)<input id="earnReimburse" type="number" min="0" step="25" value="0"></label><label>Eigene nicht erstattete Kosten (€)<input id="earnOwnCosts" type="number" min="0" step="25" value="0"></label><label>Persönlicher Plan-Abzug (%)<input id="earnDeduct" type="number" min="0" max="80" step="1" value="35"></label><label>Verbleibende Verkaufstage<input id="earnDays" type="number" min="1" max="31" value="${days}"></label><label>Ø Netto-Auftragswert (€)<input id="earnAvgOrder" type="number" min="1" step="10" value="250"></label><label>Ziel-Nettoumsatz (€)<input id="earnTargetRevenue" type="number" min="0" step="500" value="${Math.max(15000,Math.ceil(cur/5000)*5000||15000)}"></label><label>Wunsch-Provision (€)<input id="earnTargetCommission" type="number" min="0" step="100" value="2000"></label><label>Gewünschte Auszahlung ca. (€)<input id="earnTargetPayout" type="number" min="0" step="100" value="3000"></label></div><div class="moduleNotice"><b>Wichtig:</b> „Persönlicher Plan-Abzug“ ist nur deine eigene Planungsannahme für Steuern/Sozialabgaben. Das ist keine Lohnabrechnung und ersetzt keine steuerliche Berechnung.</div></section><section class="panel panelPad"><div class="panelTitle plain"><h3>2. Ergebnis</h3><span>Live berechnet</span></div><div id="earnResult"></div></section></div><section class="panel panelPad earningsScenarios"><div class="panelTitle plain"><h3>3. Umsatz-Szenarien</h3><span>Was bringt welche Stufe?</span></div><div id="earnScenarios"></div></section><section class="panel panelPad earningsExplain"><h3>So liest du den Rechner</h3><div class="securityGrid"><div><b>Nettoumsatz entscheidet</b><span>Die Provisionsstufe richtet sich nach dem bezahlten Nettowarenumsatz ohne Versandkosten im laufenden Kalendermonat.</span></div><div><b>Progressive Staffel</b><span>Jeder Prozentsatz gilt nur für den jeweiligen Umsatzbereich. Eine höhere Stufe erhöht nicht rückwirkend die Provision auf den gesamten Monatsumsatz.</span></div><div><b>Ziel rückwärts rechnen</b><span>Der Rechner zeigt, welchen Nettoumsatz du mindestens brauchst, um eine gewünschte Provision oder eine grobe Wunschauszahlung zu erreichen.</span></div><div><b>Planung ≠ Abrechnung</b><span>Fixum, Bonus, Erstattungen und Plan-Abzug sind frei wählbare Szenariowerte. Verbindlich ist nur die später tatsächlich vereinbarte Vergütung.</span></div></div></section>`;
 if($('#earnRepSelect'))$('#earnRepSelect').onchange=e=>{earningsRepId=e.target.value;renderEarnings()};['earnCurrent','earnExtra','earnBase','earnBonus','earnReimburse','earnOwnCosts','earnDeduct','earnDays','earnAvgOrder','earnTargetRevenue','earnTargetCommission','earnTargetPayout'].forEach(id=>{const el=$('#'+id);if(el)el.oninput=calculateEarnings});calculateEarnings()}
function calculateEarnings(){const val=id=>Number($('#'+id)?.value||0),current=Math.max(0,val('earnCurrent')),extra=Math.max(0,val('earnExtra')),projected=current+extra,base=Math.max(0,val('earnBase')),bonus=Math.max(0,val('earnBonus')),reimburse=Math.max(0,val('earnReimburse')),ownCosts=Math.max(0,val('earnOwnCosts')),deduct=Math.min(80,Math.max(0,val('earnDeduct')))/100,days=Math.max(1,val('earnDays')||1),avg=Math.max(1,val('earnAvgOrder')||1),targetRevenue=Math.max(0,val('earnTargetRevenue')),targetCommission=Math.max(0,val('earnTargetCommission')),targetPayout=Math.max(0,val('earnTargetPayout'));const tier=earningsTier(projected),commission=earningsCommission(projected),currentCommission=earningsCommission(current),taxableGross=base+bonus+commission,estimatedPayout=Math.max(0,taxableGross*(1-deduct)+reimburse-ownCosts),yearGross=taxableGross*12,yearPayout=estimatedPayout*12;const next=[10000,20000,30000,50000,75000].find(x=>projected<x)||null,nextGap=next?next-projected:0,targetGap=Math.max(0,targetRevenue-current),daily=targetGap/days,orders=Math.ceil(targetGap/avg),revForCommission=revenueForCommission(targetCommission);const neededTaxable=deduct>=.999?Infinity:Math.max(0,(targetPayout-reimburse+ownCosts)/(1-deduct)),neededCommission=Math.max(0,neededTaxable-base-bonus),revForPayout=Number.isFinite(neededTaxable)?revenueForCommission(neededCommission):Infinity;const result=$('#earnResult');if(result)result.innerHTML=`<div class="earnMetricGrid"><article><small>Prognose Nettoumsatz</small><strong>${money(projected)}</strong><span>${money(extra)} zusätzlich geplant</span></article><article class="accent"><small>Provisionsstufe</small><strong>${tier.label}</strong><span>${money(commission)} Provision</span></article><article><small>Mehr-Provision ggü. heute</small><strong>${money(Math.max(0,commission-currentCommission))}</strong><span>bei Erreichen der Planung</span></article><article><small>Brutto-Verdienst geplant</small><strong>${money(taxableGross)}</strong><span>Fixum + Bonus + Provision</span></article><article class="green"><small>Auszahlung grob geplant</small><strong>${money(estimatedPayout)}</strong><span>nach ${Math.round(deduct*100)} % Plan-Abzug + Erstattung − Eigenkosten</span></article><article><small>Jahreshochrechnung</small><strong>${money(yearGross)}</strong><span>brutto · Auszahlung ca. ${money(yearPayout)}</span></article></div>${next?`<div class="earnProgress"><div><b>Nächste Stufe: ${money(next)}</b><span>Noch ${money(nextGap)}</span></div><progress max="${next}" value="${Math.min(projected,next)}"></progress></div>`:`<div class="moduleNotice goodNotice"><b>Spitzenstufe erreicht:</b> Umsatz oberhalb von 75.000 € wird mit 20 % vergütet.</div>`}<div class="earnGoalGrid"><div><b>Umsatzziel ${money(targetRevenue)}</b><span>Noch ${money(targetGap)} · ${money(daily)} je Verkaufstag · ca. ${orders} Auftrag/Aufträge bei Ø ${money(avg)}</span></div><div><b>Wunsch-Provision ${money(targetCommission)}</b><span>Mindestens ca. ${money(revForCommission)} provisionsfähiger Nettoumsatz erforderlich.</span></div><div><b>Wunschauszahlung ca. ${money(targetPayout)}</b><span>${Number.isFinite(revForPayout)?`Ca. ${money(revForPayout)} Nettoumsatz nötig – unter deinen eingegebenen Planannahmen.`:'Mit 100 % Plan-Abzug nicht berechenbar.'}</span></div></div>`;const scenarios=$('#earnScenarios');if(scenarios){const targets=[10000,20000,30000,50000,75000,100000];scenarios.innerHTML=`<div class="tableWrap"><table class="dataTable earningsTable"><thead><tr><th>Nettoumsatz</th><th>Stufe</th><th>Provision</th><th>Brutto inkl. Fixum/Bonus</th><th>Auszahlung grob</th><th>Ø Tagesumsatz (${days} Tage)</th></tr></thead><tbody>${targets.map(n=>{const t=earningsTier(n),c=earningsCommission(n),gross=base+bonus+c,pay=Math.max(0,gross*(1-deduct)+reimburse-ownCosts);return `<tr class="${projected>=n?'reached':''}"><td><b>${money(n)}</b></td><td>${t.label}</td><td class="money"><b>${money(c)}</b></td><td class="money">${money(gross)}</td><td class="money">${money(pay)}</td><td class="money">${money(n/days)}</td></tr>`}).join('')}</tbody></table></div>`}}

function renderMyStats(){if(!can('view_own_stats'))return setView('dashboard');ws.innerHTML=salesStatsPanel(state.my_sales_stats||{},'Meine Verkaufs- & Provisionsstatistik')}
function repStatsModal(id){const r=(state.sales_reps||[]).find(x=>x.id===id);if(!r)return;openModal(`<div class="modalHead"><small>KUNDENBERATER · PROVISIONSABRECHNUNG</small><h2>${esc(r.name)}</h2><p>${esc(r.employee_no)} · ${esc(r.territory||'kein Gebiet')}</p></div><div class="modalBody">${salesStatsPanel(r.stats_current||{},'Aktueller Monat')}</div>`)}

/* === /restore === */
function renderSalesRepsLegacyV69(){if(!isAdmin())return setView('dashboard');const rows=state.sales_reps||[],active=rows.filter(r=>r.status==='active').length,totalNet=rows.reduce((a,r)=>a+Number(r.stats_current?.net_revenue||0),0),totalCommission=rows.reduce((a,r)=>a+Number(r.stats_current?.commission||0),0);ws.innerHTML=`<div class="sectionHead"><div><h2>Kundenberater & Provision</h2><p>Kundenstamm, direkte Verkäufe und automatische Provisionsgutschriften aus Online-, Telefon- und E-Mail-Bestellungen zentral prüfen.</p></div><button class="btn primary" id="newSalesRep">+ Kundenberater</button></div><div class="metricGrid repMetrics">${metric('Aktive Konten',active,rows.length+' insgesamt')}${metric('Nettoumsatz Monat',money(totalNet),'gutgeschrieben · bezahlt')}${metric('Provision aktuell',money(totalCommission),'volle progressive Staffel')}${metric('Feste Kundenbeziehungen',rows.reduce((a,r)=>a+Number(r.customer_count||0),0),'Kundenstamm')}</div><div class="moduleNotice"><b>V69 Zuordnungsregel:</b> Bestehende Kundenbeziehungen haben Vorrang. Folgekäufe werden dem bisherigen Kundenberater voll gutgeschrieben. Neukäufe ohne Beraterkontakt laufen abwechselnd auf Björn Hahne und Jessica Hahne, ohne den Kunden dadurch dauerhaft einem Berater zuzuordnen.</div>${commissionScaleHtml()}<div class="panel tableWrap" style="margin-top:18px">${rows.length?`<table class="dataTable"><thead><tr><th>Nr.</th><th>Kundenberater / Konto</th><th>Login</th><th>Gebiet</th><th>Feste Kunden</th><th>Gutgeschr. Netto</th><th>Aktueller Satz</th><th>Provision</th><th>Status</th><th></th></tr></thead><tbody>${rows.map(r=>{const st=r.stats_current||{},house=String(r.employee_no||'').startsWith('ZENTRALE-');return `<tr class="${house?'houseCommissionRow':''}"><td><b>${esc(r.employee_no)}</b></td><td><b>${esc(r.name)}</b>${house?'<br><span class="houseAccountBadge">Hausverteilung</span>':''}<br><small>${esc(r.email||'')}</small></td><td>${esc(r.username||'–')}</td><td>${esc(r.territory||'–')}</td><td>${r.customer_count}</td><td class="money">${money(st.net_revenue||0)}</td><td><b>${Number(st.commission_rate||0).toLocaleString('de-DE')} %</b></td><td class="money"><b>${money(st.commission||0)}</b></td><td>${r.status==='active'?'<span class="status s-paid">Aktiv</span>':'<span class="status s-cancelled">Inaktiv</span>'}</td><td><div class="rowActions"><button class="miniBtn" data-rep-stats="${esc(r.id)}">Abrechnung</button><button class="miniBtn" data-rep-rights="${esc(r.user_id||'')}">Rechte</button><button class="miniBtn" data-edit-rep="${esc(r.id)}">Bearbeiten</button></div></td></tr>`}).join('')}</tbody></table>`:'<div class="empty"><b>Noch keine Kundenberater</b>Lege den ersten Kundenberater an.</div>'}</div>`;$('#newSalesRep').onclick=()=>salesRepModal(null);$$('[data-rep-stats]').forEach(b=>b.onclick=()=>repStatsModal(b.dataset.repStats));$$('[data-rep-rights]').forEach(b=>b.onclick=()=>b.dataset.repRights?rightsModal(b.dataset.repRights):toast('Für dieses Konto ist noch kein Login verknüpft.',true));$$('[data-edit-rep]').forEach(b=>b.onclick=()=>salesRepModal(b.dataset.editRep))}
function salesRepModalLegacyV69(id){const r=(state.sales_reps||[]).find(x=>x.id===id)||{},isNew=!id;openModal(`<div class="modalHead"><small>KUNDENBERATER</small><h2>${isNew?'Kundenberater anlegen':'Kundenberater bearbeiten'}</h2><p>${isNew?'Beim Speichern wird automatisch ein persönlicher Orgaboard-Zugang angelegt und per E-Mail versendet.':esc(r.employee_no||'')}</p></div><div class="modalBody"><div class="formGrid"><label>Mitarbeiternummer<input id="srNo" value="${esc(r.employee_no||'')}" placeholder="leer = automatisch"></label><label>Name<input id="srName" value="${esc(r.name||'')}" required></label><label>E-Mail<input id="srEmail" type="email" value="${esc(r.email||'')}" required></label><label>Telefon<input id="srPhone" value="${esc(r.phone||'')}"></label><label class="full">Vertriebsgebiet<input id="srTerritory" value="${esc(r.territory||'')}" placeholder="z. B. Eckernförde / Rendsburg"></label><label>Status<select id="srStatus"><option value="active" ${sel(r.status||'active','active')}>Aktiv</option><option value="inactive" ${sel(r.status,'inactive')}>Inaktiv</option></select></label><label class="full">Interne Notiz<textarea id="srNotes">${esc(r.notes||'')}</textarea></label><label class="shopToggle full"><input id="srWelcome" type="checkbox" ${isNew?'checked':''}><span><b>${isNew?'Begrüßungsmail mit Zugang automatisch senden':'Begrüßungs-/Zugangsmail erneut senden'}</b><small>Enthält Benutzername, persönlichen Passwort-Einrichtungslink und Link zur Handy-App. Das Passwort selbst wird aus Sicherheitsgründen nicht per E-Mail versendet.</small></span></label></div><div class="moduleNotice"><b>Provision wird automatisch berechnet.</b> Ein manueller Provisionssatz ist nicht erforderlich. Grundlage ist die festgelegte Monatsstaffel auf bezahlten Nettowarenumsatz ohne Versand.</div><div class="formActions"><button class="btn" id="cancelRep">Abbrechen</button><button class="btn primary" id="saveRep">Kundenberater speichern</button></div></div>`);$('#cancelRep').onclick=closeModal;$('#saveRep').onclick=async()=>{try{const d=await api({action:'sales_rep_save',id:r.id||'',employee_no:$('#srNo').value,name:$('#srName').value,email:$('#srEmail').value,phone:$('#srPhone').value,territory:$('#srTerritory').value,status:$('#srStatus').value,notes:$('#srNotes').value,send_welcome:$('#srWelcome').checked});closeModal();await load(true);const user=d.user?.username?` · Benutzer: ${d.user.username}`:'';if(d.mail_requested&&!d.mail_sent)toast('Mitarbeiter gespeichert'+user+' · E-Mail konnte nicht versendet werden.',true);else toast('Mitarbeiter gespeichert'+user+(d.mail_sent?' · Begrüßungsmail versendet':''));renderSalesReps()}catch(e){toast(e.message,true)}}}
function permissionGroups(){const cat=state.permission_catalog||{},groups={};Object.entries(cat).forEach(([key,v])=>{(groups[v.group]??=[]).push([key,v.label])});return groups}
function rightsModal(id){const u=(state.users||[]).find(x=>x.id===id);if(!u)return toast('Benutzer nicht gefunden',true);if(u.role==='admin')return openModal(`<div class="modalHead"><small>ZUGRIFFSRECHTE</small><h2>${esc(u.display_name)}</h2></div><div class="modalBody"><div class="moduleNotice goodNotice"><b>Administrator · Vollzugriff</b> Dieses Konto besitzt automatisch alle Rechte.</div></div>`);const defaults=roleDefaultPerms(u.role),selected=new Set(Array.isArray(u.permissions)?u.permissions:defaults),groups=permissionGroups();openModal(`<div class="modalHead"><small>ZUGRIFFSRECHTE · ${esc(userRoleLabel(u.role).toUpperCase())}</small><h2>${esc(u.display_name)}</h2><p>${esc(u.username)} · ${u.role==='production'?'Produktion ist organisatorisch vollständig vom Außendienst getrennt.':'Rechteänderungen greifen bei der nächsten Datenaktualisierung.'}</p></div><div class="modalBody"><div class="rightsGrid">${Object.entries(groups).map(([g,items])=>`<section class="rightsGroup"><h3>${esc(g)}</h3>${items.map(([k,l])=>`<label class="permissionCheck"><input type="checkbox" value="${esc(k)}" ${selected.has(k)?'checked':''}><span><b>${esc(l)}</b><small>${esc(k)}</small></span></label>`).join('')}</section>`).join('')}</div><div class="formActions"><button class="btn" id="rightsDefault">Standard ${esc(userRoleLabel(u.role))}</button><button class="btn primary" id="rightsSave">Rechte speichern</button></div></div>`);$('#rightsDefault').onclick=()=>{$$('.permissionCheck input').forEach(x=>x.checked=defaults.includes(x.value))};$('#rightsSave').onclick=async()=>{const permissions=$$('.permissionCheck input:checked').map(x=>x.value);try{await api({action:'user_permissions_save',id:u.id,permissions});toast('Zugriffsrechte gespeichert');closeModal();await load(true);renderRights()}catch(e){toast(e.message,true)}}}
function renderRights(){if(!isAdmin())return setView('dashboard');const users=(state.users||[]).filter(u=>u.role!=='admin');ws.innerHTML=`<div class="sectionHead"><div><h2>Zugriffsrechte-Zentrale</h2><p>Produktion und Außendienst besitzen getrennte Standardprofile. Rechte können pro Person erweitert oder reduziert werden.</p></div></div><div class="rightsRoleSummary"><div><b>Produktion</b><span>Fertigung steuern · Produkte/Lager ansehen · keine Provision · kein Außendienst</span></div><div><b>Außendienst</b><span>Kunden & Vertrieb · persönliche Statistik · keine Produktionssteuerung</span></div></div><div class="panel tableWrap">${users.length?`<table class="dataTable"><thead><tr><th>Mitarbeiter</th><th>Rolle</th><th>Benutzername</th><th>Zuordnung</th><th>Freigegebene Rechte</th><th>Status</th><th></th></tr></thead><tbody>${users.map(u=>{const ps=Array.isArray(u.permissions)?u.permissions:roleDefaultPerms(u.role);return `<tr><td><b>${esc(u.display_name)}</b><br><small>${esc(u.email||'')}</small></td><td>${userRoleBadge(u.role)}</td><td>${esc(u.username)}</td><td>${u.role==='production'?'Produktionsteam':esc(repName(u.sales_rep_id))}</td><td><b>${ps.length}</b> Berechtigungen</td><td>${u.status==='active'?'<span class="status s-paid">Aktiv</span>':'<span class="status s-cancelled">Inaktiv</span>'}</td><td><button class="btn primary smallBtn" data-rights-user="${esc(u.id)}">Rechte verwalten</button></td></tr>`}).join('')}</tbody></table>`:'<div class="empty"><b>Noch keine Mitarbeiter-Logins</b></div>'}</div>`;$$('[data-rights-user]').forEach(b=>b.onclick=()=>rightsModal(b.dataset.rightsUser))}

function renderAdministration(){if(!isAdmin())return setView('dashboard');const users=state.users||[],activity=state.activity||[],mails=state.mail_log||[];ws.innerHTML=`<div class="sectionHead"><div><h2>Administration</h2><p>Benutzer, E-Mail-Zugänge, Rollen und sämtliche protokollierten Änderungen kontrollieren.</p></div><div class="sectionActions"><button class="btn" id="openRightsCenter">Zugriffsrechte</button><button class="btn primary" id="newUser">+ Benutzer anlegen</button></div></div><div class="adminSummary">${metric('Administratoren',users.filter(u=>u.role==='admin'&&u.status==='active').length,'Vollzugriff')}${metric('Außendienst-Logins',users.filter(u=>u.role==='field_sales'&&u.status==='active').length,'individuelle Rechte')}${metric('E-Mails protokolliert',mails.length,'letzte Systemmails')}${metric('Protokolleinträge',activity.length,'letzte Aktivitäten')}</div><div class="adminColumns"><section class="panel"><div class="panelTitle"><h3>Benutzer & Zugänge</h3><span>rollenbasiert</span></div><div class="tableWrap"><table class="dataTable"><thead><tr><th>Benutzer</th><th>Rolle</th><th>Außendienst</th><th>Willkommensmail</th><th>Letzter Login</th><th>Status</th><th></th></tr></thead><tbody>${users.map(u=>`<tr><td><b>${esc(u.display_name)}</b><br><small>${esc(u.username)} · ${esc(u.email||'')}</small></td><td>${u.role==='admin'?'<span class="status s-paid">Administrator</span>':'<span class="status s-production">Außendienst</span>'}</td><td>${esc(repName(u.sales_rep_id))}</td><td>${u.welcome_sent_at?dt(u.welcome_sent_at):'–'}</td><td>${dt(u.last_login)}</td><td>${u.status==='active'?'Aktiv':'Inaktiv'}</td><td><div class="rowActions"><button class="miniBtn" data-user="${esc(u.id)}">Bearbeiten</button>${u.role==='field_sales'?`<button class="miniBtn" data-admin-rights="${esc(u.id)}">Rechte</button><button class="miniBtn" data-resend-welcome="${esc(u.id)}">Mail senden</button>`:''}</div></td></tr>`).join('')}</tbody></table></div></section><section class="panel"><div class="panelTitle"><h3>System-E-Mails</h3><span>Versandstatus</span></div><div class="auditList">${mails.slice(0,80).map(m=>`<article><time>${dt(m.created_at)}</time><div><b>${esc(m.recipient)}</b><span>${esc(m.mail_type)} · ${m.status==='sent'?'versendet':'FEHLER'} · ${esc(m.subject)}</span></div></article>`).join('')||'<div class="empty">Noch keine System-E-Mails.</div>'}</div></section><section class="panel adminWide"><div class="panelTitle"><h3>Änderungsprotokoll</h3><span>nachvollziehbar</span></div><div class="auditList">${activity.slice(0,150).map(a=>`<article><time>${dt(a.created_at)}</time><div><b>${esc(a.actor)}</b><span>${esc(a.action_name)} · ${esc(a.entity_type)} ${esc(a.entity_id)}</span></div></article>`).join('')||'<div class="empty">Noch keine Protokolleinträge.</div>'}</div></section></div>`;$('#newUser').onclick=()=>userModal(null);$('#openRightsCenter').onclick=()=>setView('rights');$$('[data-admin-rights]').forEach(b=>b.onclick=()=>rightsModal(b.dataset.adminRights));$$('[data-resend-welcome]').forEach(b=>b.onclick=async()=>{if(!confirm('Begrüßungs- und Einrichtungslink erneut per E-Mail senden?'))return;try{await api({action:'welcome_resend',id:b.dataset.resendWelcome});toast('Begrüßungsmail versendet');await load(true);renderAdministration()}catch(e){toast(e.message,true)}})}
function userModal(id){const u=(state.users||[]).find(x=>x.id===id)||{},reps=state.sales_reps||[],perms=Array.isArray(u.permissions)?u.permissions:defaultFieldPerms;openModal(`<div class="modalHead"><small>BENUTZER & RECHTE</small><h2>${id?'Benutzer bearbeiten':'Benutzer anlegen'}</h2></div><div class="modalBody"><div class="formGrid"><label>Benutzername<input id="usrName" value="${esc(u.username||'')}" placeholder="vorname.nachname"></label><label>Anzeigename<input id="usrDisplay" value="${esc(u.display_name||'')}"></label><label>E-Mail<input id="usrEmail" type="email" value="${esc(u.email||'')}"></label><label>Rolle<select id="usrRole"><option value="admin" ${sel(u.role||'field_sales','admin')}>Administrator · Vollzugriff</option><option value="field_sales" ${sel(u.role||'field_sales','field_sales')}>Außendienst · individuelle Rechte</option></select></label><label class="full">Außendienst-Zuordnung<select id="usrRep"><option value="">Keine</option>${reps.map(r=>`<option value="${esc(r.id)}" ${sel(u.sales_rep_id||'',r.id)}>${esc(r.employee_no)} · ${esc(r.name)}</option>`).join('')}</select></label><label>Status<select id="usrStatus"><option value="active" ${sel(u.status||'active','active')}>Aktiv</option><option value="inactive" ${sel(u.status,'inactive')}>Inaktiv</option></select></label><label>Neues / temporäres Passwort<input id="usrPassword" type="password" minlength="12" placeholder="${id?'leer = unverändert':'mindestens 12 Zeichen'}"></label></div><div class="roleExplain"><b>Zugriffsrechte</b><span>Für Außendienstkonten werden die detaillierten Rechte in der Zugriffsrechte-Zentrale verwaltet. Administratoren besitzen immer Vollzugriff.</span></div><div class="formActions"><button class="btn" id="usrRights" ${id&&u.role==='field_sales'?'':'disabled'}>Detaillierte Rechte</button><button class="btn primary" id="saveUser">Benutzer speichern</button></div></div>`);$('#usrRole').onchange=()=>{$('#usrRep').disabled=$('#usrRole').value==='admin'};$('#usrRole').onchange();$('#usrRights').onclick=()=>{closeModal();rightsModal(u.id)};$('#saveUser').onclick=async()=>{try{await api({action:'user_save',id:u.id||'',username:$('#usrName').value,display_name:$('#usrDisplay').value,email:$('#usrEmail').value,role:$('#usrRole').value,sales_rep_id:$('#usrRep').value,status:$('#usrStatus').value,password:$('#usrPassword').value,permissions:perms});toast('Benutzer gespeichert');closeModal();await load(true);renderAdministration()}catch(e){toast(e.message,true)}}}
function renderSettings(){const s=state.settings||{};ws.innerHTML=`<div class="sectionHead"><div><h2>Einstellungen</h2><p>Shopparameter, System-E-Mail und Admin-Sicherheit verwalten.</p></div></div><div class="settingsGrid"><section class="panel settingsPanel"><h3>Shopparameter</h3><div class="formGrid"><label class="full">Shopname<input id="setName" value="${esc(s.shop_name||'Räucherhaken24')}"></label><label>Versandfrei ab (€)<input id="setThreshold" type="number" step="0.01" value="${Number(s.shipping_threshold??39)}"></label><label>Versandkosten (€)<input id="setShipping" type="number" step="0.01" value="${Number(s.shipping_cost??7)}"></label><label>MwSt. (%)<input id="setVat" type="number" step="1" value="${Number(s.vat_rate??19)}"></label><label class="full">System-E-Mail für Begrüßung & Passwort-Reset<input id="setSystemEmail" type="email" value="${esc(s.system_email||'service@raeucherhaken24.com')}"></label><label>Newsletter-Absendername<input id="setNewsletterName" value="${esc(s.newsletter_sender_name||'Räucherhaken24')}"></label><label>Newsletter-Antwortadresse<input id="setNewsletterReply" type="email" value="${esc(s.newsletter_reply_to||s.system_email||'service@raeucherhaken24.com')}"></label></div><div class="formActions"><button class="btn primary" id="saveSettings">Speichern</button></div></section><section class="panel settingsPanel"><h3>Eigenes Passwort ändern</h3><div class="formGrid"><label class="full">Aktuelles Passwort<input id="pwCurrent" type="password"></label><label class="full">Neues Passwort<input id="pwNew" type="password" minlength="12"></label></div><div class="formActions"><button class="btn" id="changePassword">Passwort ändern</button></div><p class="muted tiny">Mindestens 12 Zeichen; besser 14+ Zeichen.</p></section></div>`;$('#saveSettings').onclick=async()=>{try{await api({action:'settings_save',shop_name:$('#setName').value,shipping_threshold:+$('#setThreshold').value,shipping_cost:+$('#setShipping').value,vat_rate:+$('#setVat').value,system_email:$('#setSystemEmail').value});toast('Einstellungen gespeichert');await load(true)}catch(e){toast(e.message,true)}};$('#changePassword').onclick=async()=>{try{await api({action:'password_change',current:$('#pwCurrent').value,new:$('#pwNew').value});toast('Passwort geändert');$('#pwCurrent').value=$('#pwNew').value=''}catch(e){toast(e.message,true)}}}


function starIcons(n,total=6){n=Math.max(0,Math.min(total,Number(n)||0));return `<span class="starRow" aria-label="${n} von ${total} Sternen">${Array.from({length:total},(_,i)=>`<i class="${i<n?'earned':''}">★</i>`).join('')}</span>`}
function starLevelName(n){return ['Start','Sternstufe 1','Sternstufe 2','Sternstufe 3','Sternstufe 4','Sternstufe 5','Champion'][Math.max(0,Math.min(6,Number(n)||0))]}
function currentUserLeaderboard(){return (state.leaderboard||[]).find(r=>r.sales_rep_id===state.current_user?.sales_rep_id)}
function maybeCelebrateStars(){if(isAdmin()||!can('view_star_stats'))return;const st=state.my_sales_stats||{},n=Number(st.stars||0);if(n<1)return;const key=`rh24-star-celebration-${state.current_user?.id||'user'}-${st.period||''}`;let prev=0;try{prev=Number(localStorage.getItem(key)||0)}catch(e){}if(n>prev){launchConfetti();toast(`Glückwunsch! ${n}. Stern erreicht ★`);try{localStorage.setItem(key,String(n))}catch(e){}}}
function launchConfetti(){const c=document.createElement('canvas');c.className='confettiCanvas';document.body.appendChild(c);const x=c.getContext('2d');const fit=()=>{c.width=innerWidth*devicePixelRatio;c.height=innerHeight*devicePixelRatio;c.style.width=innerWidth+'px';c.style.height=innerHeight+'px';x.setTransform(devicePixelRatio,0,0,devicePixelRatio,0,0)};fit();const parts=Array.from({length:180},()=>({x:innerWidth/2+(Math.random()-.5)*80,y:innerHeight*.25,vx:(Math.random()-.5)*13,vy:-Math.random()*10-2,g:.18+Math.random()*.08,r:3+Math.random()*4,a:Math.random()*Math.PI*2,vr:(Math.random()-.5)*.3,h:Math.floor(Math.random()*360)}));let frames=0;function tick(){x.clearRect(0,0,innerWidth,innerHeight);parts.forEach(p=>{p.x+=p.vx;p.y+=p.vy;p.vy+=p.g;p.a+=p.vr;x.save();x.translate(p.x,p.y);x.rotate(p.a);x.fillStyle=`hsl(${p.h} 75% 55%)`;x.fillRect(-p.r,-p.r*.55,p.r*2,p.r*1.1);x.restore()});frames++;if(frames<210&&parts.some(p=>p.y<innerHeight+30))requestAnimationFrame(tick);else c.remove()}requestAnimationFrame(tick)}
async function ensureLeaderboardData(){
 if(state?._leaderboardLoaded)return true;
 ws.innerHTML='<div class="loadingState"><div class="spinner"></div><p>Rangliste wird berechnet …</p></div>';
 try{const d=await api({action:'leaderboard_get'});state.leaderboard=d.leaderboard||[];state.my_star_year=d.my_star_year||{};state._leaderboardLoaded=true;return true}catch(e){ws.innerHTML=`<div class="panel panelPad"><h2>Rangliste konnte nicht geladen werden</h2><p class="muted">${esc(e.message)}</p></div>`;return false}
}
function renderLeaderboard(){if(!state?._leaderboardLoaded){ensureLeaderboardData().then(ok=>{if(ok&&current==='leaderboard')renderLeaderboard()});return;}if(!can('view_leaderboard'))return setView('dashboard');const rows=state.leaderboard||[],mine=currentUserLeaderboard(),year=state.my_star_year||{},ths=state.star_thresholds||[];ws.innerHTML=`<div class="sectionHead"><div><h2>Mitarbeiter-Rangliste</h2><p>Monatliche Rangliste nach aktuell verdienter Provision. Bei Gleichstand entscheidet der höhere provisionsfähige Nettoumsatz.</p></div><span class="periodPill">${esc(periodLabel(state.my_sales_stats?.period||new Date().toISOString().slice(0,7)))}</span></div>${mine?`<div class="leaderSelf"><div><small>DEIN AKTUELLER PLATZ</small><strong>#${mine.rank}</strong><span>${money(mine.commission)} Provision · ${money(mine.net_revenue)} Nettoumsatz</span></div><div>${starIcons(mine.stars)}<b>${starLevelName(mine.stars)}</b></div></div>`:''}<div class="podiumGrid">${rows.slice(0,3).map((r,i)=>`<article class="podium p${i+1}"><span class="medal">${['1','2','3'][i]}</span><small>PLATZ ${i+1}</small><h3>${esc(r.name)}</h3><strong>${money(r.commission)}</strong><p>${money(r.net_revenue)} Netto · ${Number(r.commission_rate).toLocaleString('de-DE')} %</p>${starIcons(r.stars)}<span class="yearStars">${r.year_stars} Jahressterne</span></article>`).join('')}</div><section class="panel tableWrap"><table class="dataTable leaderboardTable"><thead><tr><th>Rang</th><th>Mitarbeiter</th><th>Sterne Monat</th><th>Nettoumsatz</th><th>Provision</th><th>Jahressterne</th></tr></thead><tbody>${rows.map(r=>`<tr class="${r.sales_rep_id===state.current_user?.sales_rep_id?'isMe':''}"><td><b>#${r.rank}</b></td><td><b>${esc(r.name)}</b><br><small>${esc(r.employee_no)}</small></td><td>${starIcons(r.stars)}</td><td class="money">${money(r.net_revenue)}</td><td class="money"><b>${money(r.commission)}</b></td><td><b>${r.year_stars}</b></td></tr>`).join('')}</tbody></table></section>${can('view_star_stats')&&!isAdmin()?starYearHtml(year,ths):`<section class="panel panelPad starRules"><div class="panelTitle plain"><h3>6 monatliche Sternstufen</h3><span>vom Admin einstellbar</span></div>${starThresholdHtml(ths)}</section>`}`;maybeCelebrateStars()}
function starThresholdHtml(ths){return `<div class="starThresholds">${(ths||[]).map((v,i)=>`<div><span>${starIcons(i+1)}</span><b>${i+1}. Stern</b><small>ab ${money(v)} Nettoumsatz</small></div>`).join('')}</div>`}
function starYearHtml(year,ths){const months=year?.months||[];return `<section class="panel panelPad starYear"><div class="panelTitle plain"><h3>Sternstatistik ${year?.year||new Date().getFullYear()}</h3><span>${Number(year?.total_stars||0)} Sterne im laufenden Jahr</span></div><p class="muted tiny">Die Monatswertung startet jeden Januar neu. Im Orgaboard wird für Mitarbeiter ausschließlich das laufende Kalenderjahr angezeigt.</p><div class="starMonthGrid">${months.map(m=>`<article><small>${new Date(m.period+'-01T12:00:00').toLocaleDateString('de-DE',{month:'short'})}</small>${starIcons(m.stars)}<b>${m.stars} Stern${m.stars===1?'':'e'}</b><span>${money(m.net_revenue)} Netto</span></article>`).join('')}</div><div class="starRulesInline"><b>Monatliche Ziele</b>${starThresholdHtml(ths)}</div></section>`}
function salesCalendarMonth(year,month,events){const first=new Date(year,month,1),last=new Date(year,month+1,0),start=(first.getDay()+6)%7;const cells=[];for(let i=0;i<start;i++)cells.push('<span class="calBlank"></span>');for(let d=1;d<=last.getDate();d++){const iso=`${year}-${String(month+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`,ev=events.filter(e=>e.date===iso);cells.push(`<article class="calDay ${ev.length?'hasEvent':''}"><b>${d}</b>${ev.map(e=>`<i class="${e.type}" title="${esc(e.title)}"></i>`).join('')}</article>`)}return `<section class="panel panelPad calendarMonth"><div class="panelTitle plain"><h3>${first.toLocaleDateString('de-DE',{month:'long',year:'numeric'})}</h3><span>Vertriebskalender</span></div><div class="calWeek"><b>Mo</b><b>Di</b><b>Mi</b><b>Do</b><b>Fr</b><b>Sa</b><b>So</b></div><div class="calGrid">${cells.join('')}</div></section>`}
async function ensureDealerCalendarData(){if(dealerCalendarLoaded)return true;if(!can('view_dealer_visits')){dealerCalendarLoaded=true;state.dealer_calendar={dealers:[]};return true}try{const d=await api({action:'dealer_calendar_get'});state.dealer_calendar=d.dealer_calendar||{dealers:[]};dealerCalendarLoaded=true;return true}catch(e){toast(e.message,true);return false}}
function dealerDueClass(d){if(!d)return'';const x=new Date(String(d).slice(0,10)+'T12:00:00'),t=new Date();t.setHours(12,0,0,0);const days=Math.round((x-t)/86400000);return days<0?'overdue':days===0?'today':days<=3?'soon':''}
function dealerCalendarMonth(y,m,rows){const first=new Date(y,m,1),days=new Date(y,m+1,0).getDate(),lead=(first.getDay()+6)%7,names=['Mo','Di','Mi','Do','Fr','Sa','So'];let html=`<section class="panel dealerMonth"><div class="panelTitle"><h3>${first.toLocaleDateString('de-DE',{month:'long',year:'numeric'})}</h3><span>Händlerbesuche</span></div><div class="dealerCalendarGrid">${names.map(n=>`<b class="dayName">${n}</b>`).join('')}${Array.from({length:lead},()=>'<i class="blank"></i>').join('')}`;for(let d=1;d<=days;d++){const iso=`${y}-${String(m+1).padStart(2,'0')}-${String(d).padStart(2,'0')}`,hits=rows.filter(r=>String(r.next_visit_at||'').slice(0,10)===iso);html+=`<div class="dealerDay"><strong>${d}</strong>${hits.map(r=>`<button class="dealerVisitChip ${dealerDueClass(r.next_visit_at)}" data-dealer-visit="${esc(r.id)}"><b>${esc(r.company)}</b><small>${esc(r.city||'')}</small></button>`).join('')}</div>`}return html+'</div></section>'}
function dealerVisitModal(id){const rows=state.dealer_calendar?.dealers||[],d=rows.find(x=>x.id===id);if(!d)return;const h=d.purchase_history||{},prods=(h.products||[]),orders=h.orders||[],addr=[d.street,d.zip,d.city].filter(Boolean).join(', '),mapUrl='https://www.google.com/maps/search/?api=1&query='+encodeURIComponent(addr||d.company),cust=(state.customers||[]).find(c=>c.id===d.customer_id);openModal(`<div class="modalHead"><small>HÄNDLERBESUCH · 14-TAGE-RHYTHMUS</small><h2>${esc(d.company)}</h2><p>${esc(d.contact||'')} · ${esc(d.phone||'')} · ${esc(d.email||'')}</p></div><div class="modalBody"><div class="dealerVisitHero"><div><small>NÄCHSTER BESUCH</small><strong>${d.next_visit_at?donly(d.next_visit_at):'nicht geplant'}</strong><span>${esc(d.street||'')} · ${esc(d.zip||'')} ${esc(d.city||'')}</span></div><div><small>LETZTER EINKAUF</small><strong>${h.last_purchase_at?donly(h.last_purchase_at):'noch keiner'}</strong><span>${money(h.total_net||0)} Netto historisch</span></div></div><div class="formActions dealerQuickActions"><a class="btn" href="${mapUrl}" target="_blank" rel="noopener">Route in Google Maps</a>${cust&&can('create_orders')?`<button class="btn primary" id="dealerReorder">Nachbestellung erfassen</button>`:''}</div><section class="panel panelPad"><div class="panelTitle plain"><h3>Nachbestellung ansprechen</h3><span>aus bisherigen Käufen</span></div>${prods.length?`<div class="repeatProductGrid">${prods.slice(0,8).map(p=>`<article class="${p.repeat_prompt?'suggested':''}"><small>Art.-Nr. ${esc(p.article_no||'–')}</small><b>${esc(p.name)}</b><span>${p.qty} insgesamt · ${p.order_count} Bestellung(en)</span><em>zuletzt ${p.last_qty} · ${p.days_since_last} Tage her</em>${p.repeat_prompt?'<mark>Nachbestellung ansprechen</mark>':''}</article>`).join('')}</div>`:'<div class="empty compact"><b>Noch keine Kaufhistorie</b>Nach der ersten bezahlten Bestellung erscheinen hier Wiederbestell-Hinweise.</div>'}</section><section class="panel tableWrap"><div class="panelTitle"><h3>Vergangene Einkäufe</h3><span>${orders.length} letzte Bestellungen</span></div>${orders.length?`<table class="dataTable"><thead><tr><th>Datum</th><th>Bestellung</th><th>Artikel</th><th>Gesamt</th></tr></thead><tbody>${orders.map(o=>`<tr><td>${donly(o.created_at)}</td><td><b>${esc(o.order_no)}</b></td><td>${o.items.map(i=>`${esc(i.article_no||'–')} · ${esc(i.name)} × ${i.qty}`).join('<br>')}</td><td class="money">${money(o.gross||0)}</td></tr>`).join('')}</tbody></table>`:'<div class="empty">Noch keine bezahlten Bestellungen.</div>'}</section><section class="panel panelPad"><div class="panelTitle plain"><h3>Besuchsverlauf</h3><span>${(d.visits||[]).length} Einträge</span></div><div class="eventTimeline">${(d.visits||[]).slice(0,8).map(v=>`<article><time>${v.completed_at?donly(v.completed_at):donly(v.planned_at)}</time><i class="statement"></i><div><b>${esc(v.outcome||v.status)}</b><span>${esc(v.notes||'Keine Notiz')}</span></div></article>`).join('')||'<div class="empty compact"><b>Noch kein Besuch protokolliert</b></div>'}</div></section>${can('manage_dealer_visits')?`<section class="panel panelPad"><div class="panelTitle plain"><h3>Besuch dokumentieren</h3><span>Folgetermin automatisch +14 Tage</span></div><div class="formGrid"><label>Ergebnis<select id="dvOutcome"><option>Besuch durchgeführt</option><option>Nachbestellung besprochen</option><option>Bestellung aufgenommen</option><option>Kein Bedarf</option><option>Nicht angetroffen</option></select></label><label>Besuchstag<input id="dvCompleted" type="date" value="${new Date().toISOString().slice(0,10)}"></label><label class="full">Gesprächsnotiz<textarea id="dvNotes" placeholder="Bedarf, Rückmeldung, gewünschte Produkte, nächster Gesprächspunkt …"></textarea></label></div><div class="formActions"><button class="btn" id="dvReschedule">Termin verschieben</button><button class="btn primary" id="dvComplete">Besuch erledigt · +14 Tage planen</button></div></section>`:''}</div>`);if($('#dealerReorder'))$('#dealerReorder').onclick=()=>{const ids=prods.filter(p=>p.repeat_prompt&&p.id).slice(0,5).map(p=>p.id);closeModal();manualOrderModal(cust.id,ids,d.sales_rep_id||cust.sales_rep_id||'')};if($('#dvComplete'))$('#dvComplete').onclick=async()=>{try{const r=await api({action:'dealer_visit_complete',dealer_id:d.id,completed_at:$('#dvCompleted').value,outcome:$('#dvOutcome').value,notes:$('#dvNotes').value});state.dealer_calendar=r.dealer_calendar;toast('Besuch gespeichert · nächster Termin in 14 Tagen');closeModal();renderSalesCalendar()}catch(e){toast(e.message,true)}};if($('#dvReschedule'))$('#dvReschedule').onclick=()=>{const nd=prompt('Neues Besuchsdatum (JJJJ-MM-TT):',String(d.next_visit_at||'').slice(0,10));if(!nd)return;api({action:'dealer_visit_reschedule',dealer_id:d.id,next_visit_at:nd}).then(r=>{state.dealer_calendar=r.dealer_calendar;toast('Termin verschoben');closeModal();renderSalesCalendar()}).catch(e=>toast(e.message,true))}}


/* V76 · Terminplaner */
function appointmentLocalKey(v){if(!v)return'';const d=new Date(v);if(Number.isNaN(d.getTime()))return String(v).slice(0,10);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`}
function appointmentInputValue(v){if(!v)return'';const d=new Date(v);if(Number.isNaN(d.getTime()))return String(v).slice(0,16);return `${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}T${String(d.getHours()).padStart(2,'0')}:${String(d.getMinutes()).padStart(2,'0')}`}
function appointmentMonthLabel(period){const [y,m]=String(period||'').split('-').map(Number);return new Date(y,m-1,1).toLocaleDateString('de-DE',{month:'long',year:'numeric'})}
function appointmentTime(v){if(!v)return'–';const d=new Date(v);return Number.isNaN(d.getTime())?'–':d.toLocaleTimeString('de-DE',{hour:'2-digit',minute:'2-digit'})}
function appointmentStatusClass(s){return ({scheduled:'planned',confirmed:'confirmed',completed:'completed',cancelled:'cancelled',no_show:'noshow'})[s]||'planned'}
function appointmentPriorityLabel(p){return ({low:'Niedrig',normal:'Normal',high:'Hoch',urgent:'Dringend'})[p]||'Normal'}
function appointmentMapUrl(a){return a?.address?'https://www.google.com/maps/search/?api=1&query='+encodeURIComponent(a.address):''}
function appointmentsOn(date){return (appointmentData?.rows||[]).filter(a=>appointmentLocalKey(a.starts_at)===date).sort((a,b)=>String(a.starts_at).localeCompare(String(b.starts_at)))}
function appointmentFilteredRows(){const q=appointmentSearch.trim().toLowerCase();return (appointmentData?.rows||[]).filter(a=>!q||`${a.title} ${a.customer_name||''} ${a.customer_company||''} ${a.contact_name||''} ${a.address||''} ${a.phone||''} ${a.email||''} ${a.type_label||''} ${a.status_label||''}`.toLowerCase().includes(q))}
async function appointmentLoad(period=appointmentPeriod,rep=appointmentRepId){if(appointmentLoading)return;appointmentLoading=true;try{const d=await api({action:'appointment_get',period,sales_rep_id:rep});appointmentData=d.appointments;appointmentPeriod=appointmentData.period||period;appointmentRepId=appointmentData.rep?.id||rep||''}catch(e){toast(e.message,true);appointmentData={period,rep:{id:rep},reps:[],rows:[],stats:{},types:{},statuses:{},locations:{}}}finally{appointmentLoading=false;if(current==='appointments')renderAppointments()}}
function appointmentEventChip(a,compact=false){return `<button type="button" class="appointmentChip ${appointmentStatusClass(a.status)} priority-${esc(a.priority||'normal')}" data-appointment-open="${esc(a.id)}"><time>${appointmentTime(a.starts_at)}</time><span>${esc(a.title)}</span>${compact?'':`<small>${esc(a.contact_name||a.customer_name||a.location_label||'')}</small>`}</button>`}
function appointmentMonthGrid(){const [y,m]=appointmentPeriod.split('-').map(Number),first=new Date(y,m-1,1),startOffset=(first.getDay()+6)%7,start=new Date(y,m-1,1-startOffset),today=new Date().toISOString().slice(0,10),cells=[];for(let i=0;i<42;i++){const d=new Date(start);d.setDate(start.getDate()+i);const key=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`,events=appointmentsOn(key),other=d.getMonth()!==m-1;cells.push(`<article class="appointmentDay ${other?'otherMonth':''} ${key===today?'today':''} ${key===appointmentSelectedDate?'selected':''}" data-appointment-day="${key}"><header><button type="button" data-appointment-day-pick="${key}">${d.getDate()}</button>${events.length?`<em>${events.length}</em>`:''}</header><div>${events.slice(0,3).map(a=>appointmentEventChip(a,true)).join('')}${events.length>3?`<button type="button" class="appointmentMore" data-appointment-day-pick="${key}">+ ${events.length-3} weitere</button>`:''}</div></article>`)}return `<div class="appointmentWeekHead">${['Mo','Di','Mi','Do','Fr','Sa','So'].map(x=>`<span>${x}</span>`).join('')}</div><div class="appointmentMonthGrid">${cells.join('')}</div>`}
function appointmentWeekGrid(){const base=new Date(appointmentSelectedDate+'T12:00:00'),offset=(base.getDay()+6)%7,start=new Date(base);start.setDate(base.getDate()-offset);const cols=[];for(let i=0;i<7;i++){const d=new Date(start);d.setDate(start.getDate()+i);const key=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`,events=appointmentsOn(key);cols.push(`<section class="appointmentWeekDay ${key===appointmentSelectedDate?'selected':''}"><button class="appointmentWeekDate" data-appointment-day-pick="${key}"><small>${d.toLocaleDateString('de-DE',{weekday:'short'})}</small><b>${d.getDate()}</b><span>${d.toLocaleDateString('de-DE',{month:'short'})}</span></button><div class="appointmentWeekEvents">${events.length?events.map(a=>appointmentEventChip(a)).join(''):'<button class="appointmentEmptySlot" data-appointment-new-date="'+key+'">+ Termin</button>'}</div></section>`)}return `<div class="appointmentWeekGrid">${cols.join('')}</div>`}
function appointmentAgenda(){const rows=appointmentFilteredRows().filter(a=>a.status!=='cancelled').sort((a,b)=>String(a.starts_at).localeCompare(String(b.starts_at)));return rows.length?`<div class="appointmentAgenda">${rows.map(a=>`<article><div class="appointmentAgendaDate"><b>${new Date(a.starts_at).toLocaleDateString('de-DE',{day:'2-digit',month:'2-digit'})}</b><span>${appointmentTime(a.starts_at)}</span></div><div class="appointmentAgendaMain"><span class="appointmentStatus ${appointmentStatusClass(a.status)}">${esc(a.status_label)}</span><h4>${esc(a.title)}</h4><p>${esc(a.contact_name||a.customer_name||'')} ${a.address?'· '+esc(a.address):''}</p></div><div class="appointmentAgendaActions">${a.location_mode==='visit'&&a.address?`<a class="miniBtn" href="${appointmentMapUrl(a)}" target="_blank" rel="noopener">Maps</a>`:''}<button class="miniBtn primaryMini" data-appointment-open="${esc(a.id)}">Öffnen</button></div></article>`).join('')}</div>`:'<div class="empty"><b>Keine Termine gefunden</b>Erstelle einen neuen Termin oder passe die Suche an.</div>'}
function appointmentDayPanel(){const rows=appointmentsOn(appointmentSelectedDate);const date=new Date(appointmentSelectedDate+'T12:00:00');return `<section class="panel appointmentDayPanel"><div class="panelTitle"><div><h3>${date.toLocaleDateString('de-DE',{weekday:'long',day:'2-digit',month:'long'})}</h3><span>${rows.length} Termin(e)</span></div><div class="sectionActions"><button class="btn" id="appointmentDayRoute">Tagesroute planen</button><button class="btn primary" id="appointmentDayNew">+ Termin an diesem Tag</button></div></div><div class="appointmentDayAgenda">${rows.length?rows.map(a=>`<article class="${appointmentStatusClass(a.status)}"><time>${appointmentTime(a.starts_at)}<small>bis ${appointmentTime(a.ends_at)}</small></time><div><span>${esc(a.type_label)}</span><b>${esc(a.title)}</b><small>${esc(a.contact_name||a.customer_name||'')}${a.address?' · '+esc(a.address):''}</small></div><button class="miniBtn" data-appointment-open="${esc(a.id)}">Details</button></article>`).join(''):'<div class="empty compact"><b>Noch frei</b>Für diesen Tag ist noch kein Termin geplant.</div>'}</div></section>`}
function renderAppointments(){if(!can('view_appointments'))return setView('dashboard');if(!appointmentData||appointmentData.period!==appointmentPeriod||(isAdmin()&&appointmentRepId&&appointmentData.rep?.id!==appointmentRepId)){ws.innerHTML='<div class="loadingState"><div class="spinner"></div><p>Terminplaner wird geladen …</p></div>';appointmentLoad();return}const st=appointmentData.stats||{},rep=appointmentData.rep||{},reps=appointmentData.reps||[],calendar=appointmentView==='month'?appointmentMonthGrid():appointmentView==='week'?appointmentWeekGrid():appointmentAgenda();const repPicker=isAdmin()?`<label class="appointmentRepPicker">Kundenberater<select id="appointmentRepSelect">${reps.map(r=>`<option value="${esc(r.id)}" ${sel(rep.id,r.id)}>${esc(r.employee_no)} · ${esc(r.name)}</option>`).join('')}</select></label>`:'';ws.innerHTML=`<div class="sectionHead appointmentHead"><div><small class="appointmentKicker">KUNDENBERATER · TERMINMANAGEMENT</small><h2>Professioneller Terminplaner</h2><p>Kundentermine erzeugen, aus dem Kundenstamm übernehmen, im Kalender steuern und direkt mit Google Maps und Fahrtenbuch verbinden.</p></div><div class="sectionActions">${repPicker}<button class="btn" id="appointmentPrint">Drucken / PDF</button>${can('edit_appointments')?'<button class="btn" id="appointmentFromCustomer">Aus Kundenstamm</button><button class="btn primary" id="appointmentNew">+ Neuer Termin</button>':''}</div></div><section class="appointmentHero"><div><span>PLANUNG FÜR</span><h3>${esc(rep.name||'Kundenberater')}</h3><p>${appointmentMonthLabel(appointmentPeriod)} · Kundenstamm, Termine und Außendienst in einer Ansicht.</p></div><div class="appointmentHeroStats"><article><small>Heute</small><strong>${Number(st.today||0)}</strong></article><article><small>Nächste 7 Tage</small><strong>${Number(st.next7||0)}</strong></article><article><small>Offen</small><strong>${Number(st.open||0)}</strong></article><article><small>Bestätigt</small><strong>${Number(st.confirmed||0)}</strong></article></div></section><div class="appointmentToolbar panel"><div class="appointmentPeriodNav"><button class="iconBtn" id="appointmentPrev">‹</button><button class="btn" id="appointmentToday">Heute</button><strong>${appointmentMonthLabel(appointmentPeriod)}</strong><button class="iconBtn" id="appointmentNext">›</button></div><label class="appointmentSearch"><span>⌕</span><input id="appointmentSearch" type="search" value="${esc(appointmentSearch)}" placeholder="Termin, Kunde, Firma, Ort, Telefon …"></label><div class="appointmentViewTabs"><button data-appointment-view="month" class="${appointmentView==='month'?'active':''}">Monat</button><button data-appointment-view="week" class="${appointmentView==='week'?'active':''}">Woche</button><button data-appointment-view="agenda" class="${appointmentView==='agenda'?'active':''}">Agenda</button></div></div><div class="appointmentLayout"><section class="panel appointmentCalendarPanel"><div id="appointmentCalendar">${calendar}</div></section>${appointmentView!=='agenda'?appointmentDayPanel():`<section class="panel appointmentRouteBox"><div class="panelTitle"><h3>Tagesroute</h3><span>Fahrtenbuch-Verknüpfung</span></div><div class="panelPad"><p>Vor-Ort-Termine aus dem Terminplaner werden automatisch als planbare Stopps im Fahrtenbuch bereitgestellt.</p><button class="btn primary" id="appointmentToTriplog">Route im Fahrtenbuch planen</button></div></section>`}</div><div class="moduleNotice"><b>Kundenberater-Datenschutz:</b> Die Kundensuche im Terminplaner wird serverseitig auf den Kundenstamm des ausgewählten Beraters beschränkt. Administratoren können den Berater oben wechseln.</div>`;bindAppointments()}
function bindAppointments(){if($('#appointmentRepSelect'))$('#appointmentRepSelect').onchange=e=>{appointmentRepId=e.target.value;appointmentData=null;appointmentLoad(appointmentPeriod,appointmentRepId)};$('#appointmentPrev').onclick=()=>{const d=new Date(appointmentPeriod+'-01T12:00:00');d.setMonth(d.getMonth()-1);appointmentPeriod=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`;appointmentSelectedDate=appointmentPeriod+'-01';appointmentData=null;appointmentLoad()};$('#appointmentNext').onclick=()=>{const d=new Date(appointmentPeriod+'-01T12:00:00');d.setMonth(d.getMonth()+1);appointmentPeriod=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}`;appointmentSelectedDate=appointmentPeriod+'-01';appointmentData=null;appointmentLoad()};$('#appointmentToday').onclick=()=>{appointmentSelectedDate=new Date().toISOString().slice(0,10);appointmentPeriod=appointmentSelectedDate.slice(0,7);appointmentData=null;appointmentLoad()};$$('[data-appointment-view]').forEach(b=>b.onclick=()=>{appointmentView=b.dataset.appointmentView;renderAppointments()});$$('[data-appointment-day-pick]').forEach(b=>b.onclick=()=>{appointmentSelectedDate=b.dataset.appointmentDayPick;renderAppointments()});$$('[data-appointment-new-date]').forEach(b=>b.onclick=()=>{appointmentSelectedDate=b.dataset.appointmentNewDate;appointmentModal(null)});$$('[data-appointment-open]').forEach(b=>b.onclick=e=>{e.stopPropagation();appointmentDetail(b.dataset.appointmentOpen)});if($('#appointmentDayRoute'))$('#appointmentDayRoute').onclick=()=>{tripRouteDate=appointmentSelectedDate;tripLogData=null;setView('triplog')};if($('#appointmentDayNew'))$('#appointmentDayNew').onclick=()=>appointmentModal(null);if($('#appointmentNew'))$('#appointmentNew').onclick=()=>appointmentModal(null);if($('#appointmentFromCustomer'))$('#appointmentFromCustomer').onclick=()=>appointmentModal(null,'',true);$('#appointmentPrint').onclick=()=>window.open(`./terminplaner-print.php?period=${encodeURIComponent(appointmentPeriod)}&sales_rep_id=${encodeURIComponent(appointmentData?.rep?.id||'')}`,'_blank','noopener');const search=$('#appointmentSearch');if(search)search.oninput=()=>{appointmentSearch=search.value;const pos=search.selectionStart;clearTimeout(appointmentSearchTimer);appointmentSearchTimer=setTimeout(()=>{renderAppointments();setTimeout(()=>{const x=$('#appointmentSearch');if(x){x.focus();try{x.setSelectionRange(pos,pos)}catch(e){}}},0)},240)};if($('#appointmentToTriplog'))$('#appointmentToTriplog').onclick=()=>{tripRouteDate=appointmentSelectedDate;tripLogData=null;setView('triplog')}}
async function appointmentCustomerSearch(term='',renderInto='#appointmentCustomerResults'){const host=$(renderInto);if(!host)return;host.innerHTML='<div class="appointmentCustomerLoading">Kundenstamm wird durchsucht …</div>';try{const d=await api({action:'appointment_customer_search',sales_rep_id:appointmentData?.rep?.id||appointmentRepId||'',term});const rows=d.customers||[];host.innerHTML=rows.length?rows.map(c=>`<button type="button" class="appointmentCustomerPick" data-appointment-customer="${esc(c.id)}"><span><b>${esc(c.name)}</b><small>${esc(c.company||'Privatkunde')} · ${esc(c.phone||c.email||'kein Kontakt')}</small></span><em>${esc(c.zip||'')} ${esc(c.city||'')}</em></button>`).join(''):'<div class="empty compact"><b>Kein Kunde gefunden</b>Nur Kunden aus dem Kundenstamm dieses Beraters werden angezeigt.</div>';$$('[data-appointment-customer]',host).forEach(b=>b.onclick=()=>appointmentApplyCustomer(rows.find(c=>c.id===b.dataset.appointmentCustomer)))}catch(e){host.innerHTML=`<div class="empty compact"><b>Suche fehlgeschlagen</b>${esc(e.message)}</div>`}}
function appointmentApplyCustomer(c){if(!c)return;const hid=$('#apCustomerId');if(hid)hid.value=c.id;$('#apCustomerSelected').innerHTML=`<b>${esc(c.name)}</b><span>${esc(c.company||'Privatkunde')} · ${esc(c.phone||c.email||'')}</span><small>${esc(c.street||'')} · ${esc(c.zip||'')} ${esc(c.city||'')}</small>`;if($('#apTitle')&&!$('#apTitle').value.trim())$('#apTitle').value='Termin · '+c.name;if($('#apContact'))$('#apContact').value=c.name||'';if($('#apPhone'))$('#apPhone').value=c.phone||'';if($('#apEmail'))$('#apEmail').value=c.email||'';if($('#apAddress'))$('#apAddress').value=[c.street,[c.zip,c.city].filter(Boolean).join(' ')].filter(Boolean).join(', ');$('#appointmentCustomerResults').innerHTML='';}
function appointmentModal(id,customerId='',focusCustomer=false){if(!can('edit_appointments'))return toast('Keine Berechtigung zum Bearbeiten von Terminen.',true);const r=(appointmentData?.rows||[]).find(x=>x.id===id)||{},known=(state.customers||[]).find(c=>c.id===(customerId||r.customer_id))||null;let date=appointmentSelectedDate||new Date().toISOString().slice(0,10),start=r.starts_at?appointmentInputValue(r.starts_at):date+'T10:00',end=r.ends_at?appointmentInputValue(r.ends_at):date+'T11:00';openModal(`<div class="modalHead appointmentModalHead"><small>TERMINPLANER · ${id?'TERMIN BEARBEITEN':'NEUER TERMIN'}</small><h2>${id?'Termin bearbeiten':'Termin erstellen'}</h2><p>Freien Termin anlegen oder einen Kunden aus dem persönlichen Kundenstamm übernehmen.</p></div><div class="modalBody"><input type="hidden" id="apCustomerId" value="${esc(customerId||r.customer_id||'')}"><section class="appointmentCustomerSearchBox"><div class="panelTitle plain"><h3>Kunde aus Kundenstamm</h3><span>optional · serverseitig auf Berater beschränkt</span></div><label class="appointmentSearch"><span>⌕</span><input id="apCustomerSearch" type="search" placeholder="Name, Firma, Telefon, E-Mail, PLZ oder Ort …"></label><div id="apCustomerSelected" class="appointmentCustomerSelected">${known?`<b>${esc(known.name)}</b><span>${esc(known.company||'Privatkunde')} · ${esc(known.phone||known.email||'')}</span><small>${esc(known.street||'')} · ${esc(known.zip||'')} ${esc(known.city||'')}</small>`:'<b>Freier Termin</b><span>Noch kein Kunde verknüpft.</span>'}</div><div id="appointmentCustomerResults" class="appointmentCustomerResults"></div></section><div class="formGrid appointmentForm"><label class="full">Terminbezeichnung<input id="apTitle" value="${esc(r.title||'')}"></label><label>Terminart<select id="apType">${Object.entries(appointmentData?.types||{}).map(([k,v])=>`<option value="${k}" ${sel(r.appointment_type||'consultation',k)}>${esc(v)}</option>`).join('')}</select></label><label>Status<select id="apStatus">${Object.entries(appointmentData?.statuses||{}).map(([k,v])=>`<option value="${k}" ${sel(r.status||'scheduled',k)}>${esc(v)}</option>`).join('')}</select></label><label>Beginn<input id="apStart" type="datetime-local" value="${esc(start)}"></label><label>Ende<input id="apEnd" type="datetime-local" value="${esc(end)}"></label><label>Termin findet statt<select id="apLocation">${Object.entries(appointmentData?.locations||{}).map(([k,v])=>`<option value="${k}" ${sel(r.location_mode||'visit',k)}>${esc(v)}</option>`).join('')}</select></label><label>Priorität<select id="apPriority"><option value="low" ${sel(r.priority,'low')}>Niedrig</option><option value="normal" ${sel(r.priority||'normal','normal')}>Normal</option><option value="high" ${sel(r.priority,'high')}>Hoch</option><option value="urgent" ${sel(r.priority,'urgent')}>Dringend</option></select></label><label class="full">Adresse / Treffpunkt<input id="apAddress" value="${esc(r.address||'')}"></label><label>Ansprechpartner<input id="apContact" value="${esc(r.contact_name||known?.name||'')}"></label><label>Telefon<input id="apPhone" value="${esc(r.phone||known?.phone||'')}"></label><label>E-Mail<input id="apEmail" type="email" value="${esc(r.email||known?.email||'')}"></label><label>Erinnerung<select id="apReminder"><option value="0" ${sel(String(r.reminder_minutes??60),'0')}>Keine</option><option value="15" ${sel(String(r.reminder_minutes??60),'15')}>15 Minuten vorher</option><option value="30" ${sel(String(r.reminder_minutes??60),'30')}>30 Minuten vorher</option><option value="60" ${sel(String(r.reminder_minutes??60),'60')}>1 Stunde vorher</option><option value="1440" ${sel(String(r.reminder_minutes??60),'1440')}>1 Tag vorher</option></select></label><label class="full">Terminnotiz<textarea id="apNotes" placeholder="Ziel des Termins, Produkte, Rückfragen, Besonderheiten …">${esc(r.notes||'')}</textarea></label></div><div id="appointmentSaveHint" class="repSaveStatus" aria-live="polite"></div><div class="formActions"><button class="btn" id="apCancel">Abbrechen</button><button class="btn" id="apMaps">Adresse in Maps</button><button class="btn primary" id="apSave">Termin speichern</button></div></div>`);if(known)appointmentApplyCustomer(known);const q=$('#apCustomerSearch');q.oninput=()=>{clearTimeout(appointmentCustomerSearchTimer);appointmentCustomerSearchTimer=setTimeout(()=>appointmentCustomerSearch(q.value),280)};q.onfocus=()=>{if(!q.value)appointmentCustomerSearch('')};if(focusCustomer)setTimeout(()=>q.focus(),50);$('#apCancel').onclick=closeModal;$('#apMaps').onclick=()=>{const a=$('#apAddress').value.trim();if(a)window.open('https://www.google.com/maps/search/?api=1&query='+encodeURIComponent(a),'_blank','noopener');else toast('Bitte zuerst eine Adresse eingeben.',true)};$('#apSave').onclick=async()=>{const btn=$('#apSave'),hint=$('#appointmentSaveHint');btn.disabled=true;btn.textContent='Speichert …';try{const starts=$('#apStart').value,resp=await api({action:'appointment_save',id:r.id||'',sales_rep_id:appointmentData?.rep?.id||appointmentRepId||'',customer_id:$('#apCustomerId').value,title:$('#apTitle').value,appointment_type:$('#apType').value,status:$('#apStatus').value,starts_at:starts,ends_at:$('#apEnd').value,location_mode:$('#apLocation').value,address:$('#apAddress').value,contact_name:$('#apContact').value,phone:$('#apPhone').value,email:$('#apEmail').value,priority:$('#apPriority').value,reminder_minutes:+$('#apReminder').value,notes:$('#apNotes').value});appointmentPeriod=starts.slice(0,7);appointmentSelectedDate=starts.slice(0,10);appointmentData=resp.appointments;hint.textContent=resp.warning?'✓ Gespeichert · '+resp.warning:'✓ Termin dauerhaft gespeichert';hint.className='repSaveStatus success';toast(resp.warning?'Termin gespeichert · Überschneidung beachten':'Termin gespeichert',Boolean(resp.warning));setTimeout(()=>{closeModal();if(current==='appointments')renderAppointments()},700)}catch(e){hint.textContent=e.message;hint.className='repSaveStatus error';btn.disabled=false;btn.textContent='Termin speichern'}}}
function appointmentDetail(id){const a=(appointmentData?.rows||[]).find(x=>x.id===id);if(!a)return;const maps=appointmentMapUrl(a);openModal(`<div class="modalHead"><small>${esc(a.type_label)} · ${esc(a.status_label)}</small><h2>${esc(a.title)}</h2><p>${new Date(a.starts_at).toLocaleDateString('de-DE',{weekday:'long',day:'2-digit',month:'long',year:'numeric'})} · ${appointmentTime(a.starts_at)}–${appointmentTime(a.ends_at)} Uhr</p></div><div class="modalBody"><div class="appointmentDetailGrid"><section><h3>Kontakt</h3><b>${esc(a.contact_name||a.customer_name||'–')}</b><span>${esc(a.phone||'')}</span><span>${esc(a.email||'')}</span>${a.customer_company?`<small>${esc(a.customer_company)}</small>`:''}</section><section><h3>Ort</h3><b>${esc(a.location_label||'')}</b><span>${esc(a.address||'–')}</span>${maps?`<a href="${maps}" target="_blank" rel="noopener">In Google Maps öffnen ↗</a>`:''}</section><section><h3>Planung</h3><b>${esc(appointmentPriorityLabel(a.priority))} Priorität</b><span>Erinnerung ${Number(a.reminder_minutes||0)?Number(a.reminder_minutes)>=1440?'1 Tag vorher':Number(a.reminder_minutes)+' Minuten vorher':'aus'}</span><small>${esc(a.notes||'Keine Notiz')}</small></section></div><div class="appointmentDetailActions"><a class="btn" href="./termin-ics.php?id=${encodeURIComponent(a.id)}" target="_blank" rel="noopener">Kalenderdatei (.ics)</a>${maps?`<a class="btn" href="${maps}" target="_blank" rel="noopener">Google Maps</a>`:''}<button class="btn" id="appointmentTripRoute">Im Fahrtenbuch planen</button>${can('edit_appointments')?`<button class="btn" id="appointmentEdit">Bearbeiten</button>${a.status!=='confirmed'?'<button class="btn" id="appointmentConfirm">Bestätigen</button>':''}${!['completed','cancelled'].includes(a.status)?'<button class="btn primary" id="appointmentComplete">Erledigt</button>':''}<button class="btn" id="appointmentFollow">Folgetermin</button>${a.status!=='completed'?'<button class="btn danger" id="appointmentCancel">Absagen</button><button class="btn danger" id="appointmentDelete">Termin löschen</button>':''}`:''}</div></div>`);$('#appointmentTripRoute').onclick=()=>{tripRouteDate=appointmentLocalKey(a.starts_at);tripLogData=null;closeModal();setView('triplog')};if($('#appointmentEdit'))$('#appointmentEdit').onclick=()=>{closeModal();appointmentModal(a.id)};if($('#appointmentConfirm'))$('#appointmentConfirm').onclick=()=>appointmentSetStatus(a.id,'confirmed');if($('#appointmentComplete'))$('#appointmentComplete').onclick=()=>appointmentSetStatus(a.id,'completed');if($('#appointmentCancel'))$('#appointmentCancel').onclick=()=>{const reason=prompt('Grund der Absage (optional):','')??'';appointmentSetStatus(a.id,'cancelled',reason)};if($('#appointmentDelete'))$('#appointmentDelete').onclick=async()=>{if(!confirm('Diesen Termin wirklich löschen?'))return;try{const d=await api({action:'appointment_delete',id:a.id});appointmentData=d.appointments;closeModal();toast('Termin gelöscht');renderAppointments()}catch(e){toast(e.message,true)}};if($('#appointmentFollow'))$('#appointmentFollow').onclick=()=>{const d=new Date(a.starts_at);d.setDate(d.getDate()+14);appointmentSelectedDate=`${d.getFullYear()}-${String(d.getMonth()+1).padStart(2,'0')}-${String(d.getDate()).padStart(2,'0')}`;appointmentPeriod=appointmentSelectedDate.slice(0,7);closeModal();appointmentModal(null,a.customer_id||'')}}
async function appointmentSetStatus(id,status,reason=''){try{const d=await api({action:'appointment_status',id,status,reason});appointmentData=d.appointments;closeModal();toast('Terminstatus aktualisiert');renderAppointments()}catch(e){toast(e.message,true)}}


/* V75 · Fahrtenbuch & Routenplanung */
function tripTypeLabel(v){return {business:'Geschäftlich',private:'Privat',commute:'Wohnung ↔ Betriebsstätte'}[v]||v}
function tripStatusBadge(r){return r.status==='finalized'?`<span class="status s-paid">Abgeschlossen</span>${r.hash_ok===false?'<span class="tripHashWarn">Prüfsumme abweichend</span>':''}`:'<span class="status s-pending">Entwurf</span>'}
function tripGoogleMapsUrl(origin,destination,stops=[]){const p=new URLSearchParams({api:'1',origin,destination,travelmode:'driving'});if(stops.length)p.set('waypoints',stops.map(x=>x.address||x).join('|'));return 'https://www.google.com/maps/dir/?'+p.toString()}
async function loadTripLog(force=false){if(tripLogLoading)return false;if(tripLogData&&!force)return true;tripLogLoading=true;try{const d=await api({action:'triplog_get',period:tripLogPeriod,sales_rep_id:tripLogRepId});tripLogData=d.triplog||{};tripLogRepId=tripLogData.rep?.id||tripLogRepId;return true}catch(e){toast(e.message,true);return false}finally{tripLogLoading=false}}
function tripAppointmentsForDate(){return (tripLogData?.appointments||[]).filter(a=>String(a.at||'').slice(0,10)===tripRouteDate)}
function renderTripLog(){
 if(!can('view_triplog'))return setView('dashboard');if(!tripLogData){ws.innerHTML='<div class="loadingState"><div class="spinner"></div><p>Fahrtenbuch wird geladen …</p></div>';loadTripLog().then(ok=>{if(ok&&current==='triplog')renderTripLog()});return}
 const d=tripLogData,stats=d.stats||{},rows=d.rows||[],vehicles=d.vehicles||[],appointments=tripAppointmentsForDate(),rep=d.rep||{},activeVehicle=vehicles.find(v=>v.status==='active')||vehicles[0],base=activeVehicle?.base_address||'',maps=Boolean(d.maps?.configured),finalPct=rows.length?Math.round((Number(stats.finalized||0)/rows.length)*100):100;
 const repSelect=isAdmin()?`<label class="tripTopSelect">Kundenberater<select id="tripRepSelect">${(d.reps||[]).filter(x=>x.status==='active').map(x=>`<option value="${esc(x.id)}" ${sel(rep.id,x.id)}>${esc(x.employee_no)} · ${esc(x.name)}</option>`).join('')}</select></label>`:'';
 ws.innerHTML=`<div class="sectionHead"><div><h2>Fahrtenbuch & Routenplanung</h2><p>Termine bündeln, Route mit Google Maps planen und jede tatsächlich gefahrene Strecke revisionssicher dokumentieren.</p></div><div class="sectionActions">${repSelect}<button class="btn" id="tripPrint">Monat drucken / PDF</button><button class="btn" id="tripCsv">CSV-Export</button><button class="btn orange" id="tripElster">ELSTER-Unterlagen</button><button class="btn primary" id="tripNew">+ Fahrt erfassen</button></div></div>
 <section class="tripHero panel"><div><small>FAHRTENBUCH · ${esc(rep.employee_no||'')}</small><h3>${esc(rep.name||'Kundenberater')}</h3><p>Abgeschlossene Fahrten sind gesperrt. Nachträgliche Änderungen sind nur als protokollierte Korrektur mit Begründung möglich.</p></div><div class="tripHeroState"><strong>${finalPct}%</strong><span>abgeschlossen</span><small>${Number(stats.drafts||0)} Entwürfe · ${Number(stats.hash_warnings||0)} Prüfwarnungen</small></div></section>
 <div class="metricGrid tripMetrics">${metric('Geschäftlich',Number(stats.business_km||0).toLocaleString('de-DE',{maximumFractionDigits:1})+' km','im ausgewählten Monat')}${metric('Privat',Number(stats.private_km||0).toLocaleString('de-DE',{maximumFractionDigits:1})+' km','im ausgewählten Monat')}${metric('Wohnung/Betrieb',Number(stats.commute_km||0).toLocaleString('de-DE',{maximumFractionDigits:1})+' km','separat ausgewiesen')}${metric('Fahrten',stats.trips||0,`${stats.finalized||0} abgeschlossen`)}${metric('Termine heute',appointments.length,'für die Routenplanung')}${metric('Fahrzeuge',vehicles.filter(v=>v.status==='active').length,'aktive Fahrzeuge')}</div>
 <div class="tripLayout"><section class="panel tripRoutePanel"><div class="panelTitle"><div><h3>Termine & Tagesroute</h3><span>Termine aus Händlerplan und Gebietsbuch-Wiedervorlagen</span></div><span class="mapsStatus ${maps?'connected':'pending'}"><i></i>${maps?'Google Routes aktiv':'Google Maps Basis'}</span></div><div class="tripRouteControls"><label>Tag<input id="tripRouteDate" type="date" value="${esc(tripRouteDate)}"></label><label>Fahrzeug<select id="tripRouteVehicle">${vehicles.map(v=>`<option value="${esc(v.id)}" ${sel(activeVehicle?.id,v.id)}>${esc(v.label)}${v.license_plate?' · '+esc(v.license_plate):''}</option>`).join('')}</select></label><label>Start<input id="tripRouteStart" value="${esc(base)}" placeholder="Startadresse"></label><label>Ziel<input id="tripRouteEnd" value="${esc(base)}" placeholder="Endadresse"></label><label>Planung<select id="tripRouteMode"><option value="time">Termintreu · Reihenfolge beibehalten</option><option value="optimize">Google-optimiert · flexible Termine</option></select></label></div>${!vehicles.length?'<div class="moduleNotice"><b>Noch kein Fahrzeug angelegt.</b> Lege zuerst ein Fahrzeug mit Start-/Heimatadresse an.</div>':''}<div class="tripAppointments">${appointments.length?appointments.map((a,i)=>`<article><label class="tripStopCheck"><input type="checkbox" data-trip-stop="${i}" checked><span></span></label><time>${String(a.at||'').slice(11,16)||'–'}</time><div><b>${esc(a.title)}</b><small>${esc(a.source)} · ${esc(a.address)}</small></div><button class="miniBtn" data-trip-from-appointment="${i}">Fahrt anlegen</button></article>`).join(''):'<div class="empty compact"><b>Keine Termine an diesem Tag</b>Du kannst trotzdem eine Fahrt manuell erfassen.</div>'}</div><div class="tripRouteActions"><button class="btn" id="tripOpenMaps" ${appointments.length?'':'disabled'}>Route in Google Maps öffnen</button><button class="btn primary" id="tripOptimize" ${appointments.length?'':'disabled'}>${maps?'Route berechnen / optimieren':'Google Routes API einrichten'}</button>${isAdmin()?'<button class="btn" id="tripMapsSetup">Google Maps einrichten</button>':''}</div><div id="tripRouteResult" class="tripRouteResult">${tripRouteResult?tripRouteResultHtml(tripRouteResult):'<span>Wähle einen Tag und plane die Route. Bei festen Uhrzeiten immer „Termintreu“ verwenden.</span>'}</div><div class="tripMapFrame"><iframe title="Google Maps" loading="lazy" referrerpolicy="no-referrer-when-downgrade" src="https://www.google.com/maps?q=${encodeURIComponent(appointments[0]?.address||base||'Deutschland')}&output=embed"></iframe></div></section>
 <section class="panel tripSide"><div class="panelTitle"><h3>Fahrzeuge</h3><span>Startadresse & Kilometerbasis</span></div><div class="tripVehicleList">${vehicles.length?vehicles.map(v=>`<button class="tripVehicleCard" data-trip-vehicle="${esc(v.id)}"><span>${esc(v.label)}</span><b>${esc(v.license_plate||'ohne Kennzeichen')}</b><small>${esc(v.base_address||'keine Basisadresse')} · Start ${Number(v.odometer_start||0).toLocaleString('de-DE',{maximumFractionDigits:1})} km</small></button>`).join(''):'<div class="empty compact"><b>Kein Fahrzeug</b>Fahrzeug jetzt anlegen.</div>'}</div>${can('edit_triplog')?'<button class="btn wideBtn" id="tripVehicleNew">+ Fahrzeug anlegen</button>':''}<div class="tripCompliance"><b>Finanzamt-Logik</b><span>Datum, Kilometerstände, Ziel, Reisezweck und Geschäftspartner werden je Fahrt geführt.</span><span>Abgeschlossene Fahrten erhalten eine Prüfsumme.</span><span>Korrekturen werden mit Grund und Zeitstempel protokolliert.</span></div></section></div>
 <section class="panel tripBookPanel"><div class="panelTitle"><div><h3>Fahrtenbuch ${esc(d.period||tripLogPeriod)}</h3><span>laufend · fortlaufend · prüfbar</span></div><label>Monat<input id="tripPeriod" type="month" value="${esc(d.period||tripLogPeriod)}"></label></div>${rows.length?`<div class="tableWrap"><table class="dataTable tripTable"><thead><tr><th>Datum</th><th>Fahrzeug</th><th>Art</th><th>Strecke</th><th>Zweck / Partner</th><th>Km-Stand</th><th>km</th><th>Status</th><th></th></tr></thead><tbody>${rows.map(r=>`<tr><td><b>${donly(r.trip_date)}</b><br><small>${esc(String(r.start_time||'').slice(0,5))}${r.end_time?'–'+esc(String(r.end_time).slice(0,5)):''}</small></td><td>${esc(r.vehicle_label||'–')}<br><small>${esc(r.license_plate||'')}</small></td><td>${esc(tripTypeLabel(r.trip_type))}</td><td><b>${esc(r.start_address)}</b><br><span>→ ${esc(r.destination_address)}</span>${r.detour_reason?`<br><small>Umweg: ${esc(r.detour_reason)}</small>`:''}</td><td>${esc(r.purpose||'–')}<br><small>${esc(r.business_partner||'')}</small></td><td>${Number(r.start_odometer).toLocaleString('de-DE',{minimumFractionDigits:1})}<br>→ ${Number(r.end_odometer).toLocaleString('de-DE',{minimumFractionDigits:1})}</td><td><b>${Number(r.distance_km).toLocaleString('de-DE',{minimumFractionDigits:1})}</b></td><td>${tripStatusBadge(r)}</td><td><div class="rowActions">${r.status==='draft'?`<button class="miniBtn" data-trip-edit="${esc(r.id)}">Bearbeiten</button><button class="miniBtn primaryMini" data-trip-finalize="${esc(r.id)}">Abschließen</button><button class="miniBtn" data-trip-delete="${esc(r.id)}">Löschen</button>`:`<button class="miniBtn" data-trip-correct="${esc(r.id)}">Korrektur</button>`}</div></td></tr>`).join('')}</tbody></table></div>`:'<div class="empty"><b>Noch keine Fahrten in diesem Monat</b>Neue Fahrten können manuell oder direkt aus einem Termin angelegt werden.</div>'}</section>
 ${(d.revisions||[]).length?`<section class="panel tripRevisionPanel"><div class="panelTitle"><h3>Änderungsprotokoll</h3><span>${d.revisions.length} protokollierte Korrekturen</span></div><div class="tripRevisionList">${d.revisions.slice(0,20).map(x=>`<article><time>${dt(x.changed_at)}</time><b>${esc(x.trip_id)} · Version ${x.version_no}</b><span>${esc(x.change_reason)}</span><small>${esc(x.changed_by_name||'System')}</small></article>`).join('')}</div></section>`:''}
 <div class="moduleNotice"><b>ELSTER / Finanzamt:</b> Für Fahrtenbücher gibt es keine eigene direkte ELSTER-Fahrtenbuch-Übertragung im Orgaboard. Das Modul erstellt deshalb druckbare PDF-Unterlagen und CSV-Auszüge und öffnet die offizielle ELSTER-Belegnachreichung. Die Anerkennung hängt von vollständiger, zeitnaher und nachvollziehbarer tatsächlicher Führung ab.</div>`;
 bindTripLog()
}
function tripRouteResultHtml(r){return `<div><b>${Number(r.distance_km||0).toLocaleString('de-DE',{maximumFractionDigits:1})} km · ca. ${Number(r.duration_minutes||0).toLocaleString('de-DE')} Min.</b><span>${r.stops?.length||0} Termin(e) · ${r.optimized?'Google-optimiert':'termintreu'}</span></div>${r.stops?.length?`<ol>${r.stops.map(s=>`<li>${esc(s.title||s.address)}<small>${esc(s.address)}</small></li>`).join('')}</ol>`:''}${r.google_maps_url?`<a class="btn primary" target="_blank" rel="noopener" href="${esc(r.google_maps_url)}">In Google Maps starten</a>`:''}`}
function tripSelectedStops(){const all=tripAppointmentsForDate();return $$('[data-trip-stop]').filter(x=>x.checked).map(x=>all[Number(x.dataset.tripStop)]).filter(Boolean)}
function bindTripLog(){
 if($('#tripRepSelect'))$('#tripRepSelect').onchange=()=>{tripLogRepId=$('#tripRepSelect').value;tripLogData=null;tripRouteResult=null;renderTripLog()};
 $('#tripPeriod').onchange=()=>{tripLogPeriod=$('#tripPeriod').value;tripLogData=null;renderTripLog()};$('#tripRouteDate').onchange=()=>{tripRouteDate=$('#tripRouteDate').value;tripRouteResult=null;renderTripLog()};
 $('#tripNew').onclick=()=>tripModal(null);$('#tripPrint').onclick=()=>window.open(`./fahrtenbuch-print.php?period=${encodeURIComponent(tripLogData.period||tripLogPeriod)}&sales_rep_id=${encodeURIComponent(tripLogData.rep?.id||'')}`,'_blank','noopener');$('#tripCsv').onclick=()=>window.open(`./fahrtenbuch-export.php?format=csv&period=${encodeURIComponent(tripLogData.period||tripLogPeriod)}&sales_rep_id=${encodeURIComponent(tripLogData.rep?.id||'')}`,'_blank','noopener');$('#tripElster').onclick=()=>window.open(`./fahrtenbuch-print.php?elster=1&period=${encodeURIComponent(tripLogData.period||tripLogPeriod)}&sales_rep_id=${encodeURIComponent(tripLogData.rep?.id||'')}`,'_blank','noopener');
 if($('#tripVehicleNew'))$('#tripVehicleNew').onclick=()=>tripVehicleModal(null);$$('[data-trip-vehicle]').forEach(b=>b.onclick=()=>tripVehicleModal(b.dataset.tripVehicle));$$('[data-trip-from-appointment]').forEach(b=>b.onclick=()=>tripModal(null,tripAppointmentsForDate()[Number(b.dataset.tripFromAppointment)]));
 $$('[data-trip-edit]').forEach(b=>b.onclick=()=>tripModal(b.dataset.tripEdit));$$('[data-trip-correct]').forEach(b=>b.onclick=()=>tripCorrectionModal(b.dataset.tripCorrect));$$('[data-trip-finalize]').forEach(b=>b.onclick=()=>tripFinalize(b.dataset.tripFinalize));$$('[data-trip-delete]').forEach(b=>b.onclick=()=>tripDelete(b.dataset.tripDelete));
 $('#tripOpenMaps').onclick=()=>{const stops=tripSelectedStops(),origin=$('#tripRouteStart').value.trim(),destination=$('#tripRouteEnd').value.trim();if(!origin||!destination||!stops.length)return toast('Start, Ziel und mindestens einen Termin auswählen.',true);window.open(tripGoogleMapsUrl(origin,destination,stops),'_blank','noopener')};
 $('#tripOptimize').onclick=async()=>{if(!tripLogData.maps?.configured){if(isAdmin())return tripMapsSetupModal();return toast('Google Routes API muss vom Administrator eingerichtet werden. Google Maps kann trotzdem geöffnet werden.',true)}const stops=tripSelectedStops(),origin=$('#tripRouteStart').value.trim(),destination=$('#tripRouteEnd').value.trim();if(!origin||!destination||!stops.length)return toast('Start, Ziel und mindestens einen Termin auswählen.',true);const btn=$('#tripOptimize');btn.disabled=true;btn.textContent='Route wird berechnet …';try{const r=await api({action:'trip_route_optimize',origin,destination,stops,optimize:$('#tripRouteMode').value==='optimize'});tripRouteResult={...(r.route||{}),optimized:$('#tripRouteMode').value==='optimize'};$('#tripRouteResult').innerHTML=tripRouteResultHtml(tripRouteResult)}catch(e){toast(e.message,true)}finally{btn.disabled=false;btn.textContent='Route berechnen / optimieren'}};
 if($('#tripMapsSetup'))$('#tripMapsSetup').onclick=tripMapsSetupModal;
}
function tripModal(id,appointment=null){const r=(tripLogData?.rows||[]).find(x=>x.id===id)||{},vehicles=tripLogData?.vehicles||[],v=vehicles.find(x=>x.id===r.vehicle_id)||vehicles.find(x=>x.status==='active')||vehicles[0],last=v?Math.max(Number(v.odometer_start||0),...(tripLogData.rows||[]).filter(x=>x.vehicle_id===v.id&&x.id!==id).map(x=>Number(x.end_odometer||0))):0,start=appointment?(v?.base_address||''):(r.start_address||v?.base_address||''),dest=appointment?.address||r.destination_address||'',purpose=appointment?`Kundentermin · ${appointment.source}`:(r.purpose||''),partner=appointment?.partner||r.business_partner||'';openModal(`<div class="modalHead"><small>FAHRTENBUCH · ${id?'ENTWURF BEARBEITEN':'NEUE FAHRT'}</small><h2>${id?'Fahrt bearbeiten':'Fahrt erfassen'}</h2><p>Tatsächlich gefahrene Strecke dokumentieren – geplante Route nicht automatisch als Fahrt übernehmen.</p></div><div class="modalBody"><div class="formGrid"><label>Fahrzeug<select id="tlVehicle">${vehicles.map(x=>`<option value="${esc(x.id)}" ${sel(v?.id,x.id)}>${esc(x.label)} ${x.license_plate?'· '+esc(x.license_plate):''}</option>`).join('')}</select></label><label>Fahrtdatum<input id="tlDate" type="date" value="${esc(r.trip_date||appointment?.at?.slice(0,10)||new Date().toISOString().slice(0,10))}"></label><label>Startzeit<input id="tlStartTime" type="time" value="${esc(String(r.start_time||'').slice(0,5))}"></label><label>Endzeit<input id="tlEndTime" type="time" value="${esc(String(r.end_time||'').slice(0,5))}"></label><label>Fahrtart<select id="tlType"><option value="business" ${sel(r.trip_type||'business','business')}>Geschäftlich</option><option value="private" ${sel(r.trip_type,'private')}>Privat</option><option value="commute" ${sel(r.trip_type,'commute')}>Wohnung ↔ Betriebsstätte</option></select></label><label>Start-Kilometerstand<input id="tlStartOdo" type="number" step="0.1" value="${Number(r.start_odometer??last).toFixed(1)}"></label><label>End-Kilometerstand<input id="tlEndOdo" type="number" step="0.1" value="${Number(r.end_odometer??last).toFixed(1)}"></label><label class="full">Startadresse<input id="tlStart" value="${esc(start)}"></label><label class="full">Zieladresse<input id="tlDest" value="${esc(dest)}"></label><label>Reisezweck<input id="tlPurpose" value="${esc(purpose)}" placeholder="z. B. Kundenberatung, Händlerbesuch"></label><label>Geschäftspartner<input id="tlPartner" value="${esc(partner)}" placeholder="Name / Firma"></label><label class="full">Umweg / Besonderheit<input id="tlDetour" value="${esc(r.detour_reason||'')}" placeholder="Nur ausfüllen, wenn ein Umweg gefahren wurde"></label></div><div class="tripDistancePreview" id="tripDistancePreview"></div><div class="formActions"><button class="btn" id="tlMaps">Strecke in Google Maps</button><button class="btn" id="tlCancel">Abbrechen</button><button class="btn primary" id="tlSave">Als Entwurf speichern</button></div></div>`);const preview=()=>{const a=Number($('#tlStartOdo').value||0),b=Number($('#tlEndOdo').value||0);$('#tripDistancePreview').innerHTML=b>=a?`Erfasste Strecke: <b>${(b-a).toLocaleString('de-DE',{minimumFractionDigits:1,maximumFractionDigits:1})} km</b>`:'<span class="errorText">Endkilometerstand liegt unter Start.</span>'};$('#tlStartOdo').oninput=$('#tlEndOdo').oninput=preview;preview();$('#tlCancel').onclick=closeModal;$('#tlMaps').onclick=()=>{const a=$('#tlStart').value.trim(),b=$('#tlDest').value.trim();if(a&&b)window.open(tripGoogleMapsUrl(a,b),'_blank','noopener')};$('#tlSave').onclick=async()=>{try{const x=await api({action:'trip_save',id:r.id||'',sales_rep_id:tripLogData.rep?.id||'',period:tripLogPeriod,vehicle_id:$('#tlVehicle').value,trip_date:$('#tlDate').value,start_time:$('#tlStartTime').value,end_time:$('#tlEndTime').value,trip_type:$('#tlType').value,start_address:$('#tlStart').value,destination_address:$('#tlDest').value,purpose:$('#tlPurpose').value,business_partner:$('#tlPartner').value,start_odometer:+$('#tlStartOdo').value,end_odometer:+$('#tlEndOdo').value,detour_reason:$('#tlDetour').value,appointment_type:appointment?.type||r.appointment_type||'',appointment_ref:appointment?.ref||r.appointment_ref||''});tripLogData=x.triplog;toast('Fahrt als Entwurf gespeichert');closeModal();renderTripLog()}catch(e){toast(e.message,true)}}}
function tripVehicleModal(id){const r=(tripLogData?.vehicles||[]).find(x=>x.id===id)||{};openModal(`<div class="modalHead"><small>FAHRTENBUCH · FAHRZEUG</small><h2>${id?'Fahrzeug bearbeiten':'Fahrzeug anlegen'}</h2><p>Die Basisadresse wird als Start/Ziel der Tagesroute vorgeschlagen.</p></div><div class="modalBody"><div class="formGrid"><label>Bezeichnung<input id="tvLabel" value="${esc(r.label||'')}" placeholder="z. B. Außendienstfahrzeug"></label><label>Kennzeichen<input id="tvPlate" value="${esc(r.license_plate||'')}"></label><label class="full">Hersteller / Modell<input id="tvModel" value="${esc(r.make_model||'')}"></label><label class="full">Basis-/Startadresse<input id="tvBase" value="${esc(r.base_address||'')}" placeholder="Straße, PLZ Ort"></label><label>Ausgangs-Kilometerstand<input id="tvOdo" type="number" step="0.1" min="0" value="${Number(r.odometer_start||0)}"></label><label>Status<select id="tvStatus"><option value="active" ${sel(r.status||'active','active')}>Aktiv</option><option value="inactive" ${sel(r.status,'inactive')}>Inaktiv</option></select></label></div><div class="formActions"><button class="btn" id="tvCancel">Abbrechen</button><button class="btn primary" id="tvSave">Fahrzeug speichern</button></div></div>`);$('#tvCancel').onclick=closeModal;$('#tvSave').onclick=async()=>{try{const x=await api({action:'trip_vehicle_save',id:r.id||'',sales_rep_id:tripLogData.rep?.id||'',period:tripLogPeriod,label:$('#tvLabel').value,license_plate:$('#tvPlate').value,make_model:$('#tvModel').value,base_address:$('#tvBase').value,odometer_start:+$('#tvOdo').value,status:$('#tvStatus').value});tripLogData=x.triplog;toast('Fahrzeug gespeichert');closeModal();renderTripLog()}catch(e){toast(e.message,true)}}}
function tripMapsSetupModal(){openModal(`<div class="modalHead"><small>GOOGLE MAPS PLATFORM</small><h2>Routenoptimierung einrichten</h2><p>Der API-Key bleibt serverseitig verschlüsselt und wird nicht an den Browser zurückgegeben.</p></div><div class="modalBody"><div class="formGrid"><label class="full">Google Maps Platform API-Key<input id="tmApiKey" type="password" placeholder="${tripLogData?.maps?.configured?'bereits gespeichert · leer lassen zum Beibehalten':'API-Key eingeben'}"></label></div><div class="moduleNotice"><b>Routes API erforderlich:</b> Für echte Stoppreihenfolge-Optimierung muss in der Google Cloud die Routes API freigeschaltet und abgerechnet werden. Ohne Key bleibt der direkte Google-Maps-Routenlink verfügbar.</div><div class="formActions"><button class="btn" id="tmCancel">Abbrechen</button><button class="btn primary" id="tmSave">API-Key speichern</button></div></div>`);$('#tmCancel').onclick=closeModal;$('#tmSave').onclick=async()=>{try{const d=await api({action:'trip_maps_config_save',api_key:$('#tmApiKey').value});tripLogData.maps=d.maps;toast('Google Routes API gespeichert');closeModal();renderTripLog()}catch(e){toast(e.message,true)}}}
async function tripFinalize(id){if(!confirm('Fahrt wirklich abschließen? Danach ist eine Änderung nur noch als protokollierte Korrektur möglich.'))return;try{const d=await api({action:'trip_finalize',id});tripLogData=d.triplog;toast('Fahrt abgeschlossen und mit Prüfsumme gesichert');renderTripLog()}catch(e){toast(e.message,true)}}
async function tripDelete(id){if(!confirm('Entwurf löschen?'))return;try{const d=await api({action:'trip_delete',id});tripLogData=d.triplog;toast('Entwurf gelöscht');renderTripLog()}catch(e){toast(e.message,true)}}
function tripCorrectionModal(id){const r=(tripLogData?.rows||[]).find(x=>x.id===id);if(!r)return;openModal(`<div class="modalHead"><small>FAHRTENBUCH · PROTOKOLLIERTE KORREKTUR</small><h2>Abgeschlossene Fahrt korrigieren</h2><p>Der alte Stand bleibt im Änderungsprotokoll erhalten.</p></div><div class="modalBody"><div class="formGrid"><label>Startzeit<input id="tcStartTime" type="time" value="${esc(String(r.start_time||'').slice(0,5))}"></label><label>Endzeit<input id="tcEndTime" type="time" value="${esc(String(r.end_time||'').slice(0,5))}"></label><label>Fahrtart<select id="tcType"><option value="business" ${sel(r.trip_type,'business')}>Geschäftlich</option><option value="private" ${sel(r.trip_type,'private')}>Privat</option><option value="commute" ${sel(r.trip_type,'commute')}>Wohnung ↔ Betriebsstätte</option></select></label><label>Start-Km<input id="tcStartOdo" type="number" step="0.1" value="${Number(r.start_odometer)}"></label><label>End-Km<input id="tcEndOdo" type="number" step="0.1" value="${Number(r.end_odometer)}"></label><label class="full">Startadresse<input id="tcStart" value="${esc(r.start_address)}"></label><label class="full">Zieladresse<input id="tcDest" value="${esc(r.destination_address)}"></label><label>Reisezweck<input id="tcPurpose" value="${esc(r.purpose||'')}"></label><label>Geschäftspartner<input id="tcPartner" value="${esc(r.business_partner||'')}"></label><label class="full">Umweg<input id="tcDetour" value="${esc(r.detour_reason||'')}"></label><label class="full">Korrekturgrund<input id="tcReason" placeholder="Pflichtfeld · warum wird der abgeschlossene Eintrag geändert?"></label></div><div class="formActions"><button class="btn" id="tcCancel">Abbrechen</button><button class="btn primary" id="tcSave">Korrektur protokolliert speichern</button></div></div>`);$('#tcCancel').onclick=closeModal;$('#tcSave').onclick=async()=>{try{const d=await api({action:'trip_correct',id,reason:$('#tcReason').value,start_time:$('#tcStartTime').value,end_time:$('#tcEndTime').value,trip_type:$('#tcType').value,start_address:$('#tcStart').value,destination_address:$('#tcDest').value,purpose:$('#tcPurpose').value,business_partner:$('#tcPartner').value,start_odometer:+$('#tcStartOdo').value,end_odometer:+$('#tcEndOdo').value,detour_reason:$('#tcDetour').value});tripLogData=d.triplog;toast('Korrektur mit Änderungsnachweis gespeichert');closeModal();renderTripLog()}catch(e){toast(e.message,true)}}}

function renderSalesCalendar(){if(!can('view_sales_calendar'))return setView('dashboard');if(!dealerCalendarLoaded){ws.innerHTML='<div class="loadingState"><div class="spinner"></div><p>Händler-Touren werden geladen …</p></div>';ensureDealerCalendarData().then(ok=>{if(ok&&current==='salescalendar')renderSalesCalendar()});return}const data=state.sales_calendar||{},events=data.events||[],rows=state.dealer_calendar?.dealers||[],now=new Date(),next=new Date(now.getFullYear(),now.getMonth()+1,1),today=new Date();today.setHours(12,0,0,0);const due=rows.map(r=>({...r,_days:r.next_visit_at?Math.round((new Date(String(r.next_visit_at).slice(0,10)+'T12:00:00')-today)/86400000):9999})).sort((a,b)=>a._days-b._days),urgent=due.filter(r=>r._days<=14);ws.innerHTML=`<div class="sectionHead"><div><h2>Vertriebskalender & Händler-Touren</h2><p>Jeder zugeordnete Händler wird im 14-Tage-Rhythmus erneut besucht. Kaufhistorie und Wiederbestell-Hinweise sind direkt am Termin verfügbar.</p></div><span class="periodPill">${rows.length} Händler im Besuchsplan</span></div><div class="calendarHero"><div><small>PROVISIONSABRECHNUNG</small><strong>${Number(data.statement_day||27)}.</strong><span>jeden Monats</span></div><div><small>GEPLANTE AUSZAHLUNG</small><strong>${Number(data.payout_day||1)}.</strong><span>des Folgemonats</span></div><div><small>HÄNDLER-RHYTHMUS</small><strong>14</strong><span>Tage zwischen Besuchen</span></div></div>${can('view_dealer_visits')?`<div class="dealerRouteSummary">${metric('Überfällig',due.filter(r=>r._days<0).length,'sofort einplanen')}${metric('Heute',due.filter(r=>r._days===0).length,'heutige Händlerbesuche')}${metric('Nächste 14 Tage',urgent.filter(r=>r._days>=0).length,'anstehende Termine')}${metric('Mit Kaufhistorie',rows.filter(r=>r.purchase_history?.orders?.length).length,'für Nachbestellung nutzbar')}</div><div class="calendarTwo dealerCalendarTwo">${dealerCalendarMonth(now.getFullYear(),now.getMonth(),rows)}${dealerCalendarMonth(next.getFullYear(),next.getMonth(),rows)}</div><section class="panel tableWrap"><div class="panelTitle"><h3>Besuchsagenda</h3><span>nach Fälligkeit sortiert</span></div>${due.length?`<table class="dataTable"><thead><tr><th>Fällig</th><th>Händler</th><th>Ort</th><th>Letzter Einkauf</th><th>Wiederbestellung</th><th></th></tr></thead><tbody>${due.map(r=>{const h=r.purchase_history||{},p=(h.products||[]).filter(x=>x.repeat_prompt).slice(0,2);return `<tr class="${r._days<0?'rowOverdue':''}"><td><b>${r.next_visit_at?donly(r.next_visit_at):'–'}</b><br><small>${r._days<0?Math.abs(r._days)+' Tage überfällig':r._days===0?'heute':r._days+' Tage'}</small></td><td><b>${esc(r.company)}</b><br><small>${esc(r.contact||'')}</small></td><td>${esc(r.zip||'')} ${esc(r.city||'')}</td><td>${h.last_purchase_at?donly(h.last_purchase_at):'–'}</td><td>${p.length?p.map(x=>esc(x.name)).join('<br>'):'–'}</td><td><button class="btn primary smallBtn" data-dealer-visit="${esc(r.id)}">Termin öffnen</button></td></tr>`}).join('')}</tbody></table>`:'<div class="empty"><b>Noch keine Händler zugeordnet</b>Administratoren können im Reiter Händler Adresse, Kundenakte und Außendienstmitarbeiter zuweisen.</div>'}</section>`:'<div class="moduleNotice"><b>Händler-Touren nicht freigegeben.</b> Die Administration kann dieses Recht in der Zugriffsrechte-Zentrale aktivieren.</div>'}<section class="panel panelPad"><div class="panelTitle plain"><h3>Nächste Provisions- & Auszahlungstermine</h3><span>automatisch fortgeschrieben</span></div><div class="eventTimeline">${events.filter(e=>new Date(e.date+'T23:59:59')>=new Date()).slice(0,8).map(e=>`<article><time>${new Date(e.date+'T12:00:00').toLocaleDateString('de-DE',{weekday:'short',day:'2-digit',month:'2-digit',year:'numeric'})}</time><i class="${e.type}"></i><div><b>${esc(e.title)}</b><span>${esc(e.description)}</span></div></article>`).join('')}</div></section>`;$$('[data-dealer-visit]').forEach(b=>b.onclick=()=>dealerVisitModal(b.dataset.dealerVisit))}
function renderMyStats(){if(!can('view_own_stats'))return setView('dashboard');const st=state.my_sales_stats||{};ws.innerHTML=salesStatsPanel(st,'Meine Verkaufs- & Provisionsstatistik')+(can('view_star_stats')?`<section class="panel panelPad starMini"><div class="panelTitle plain"><h3>Meine Monatssterne</h3><span>${Number(st.stars||0)} von 6</span></div>${starIcons(st.stars||0)}${st.next_star_threshold?`<p>Noch <b>${money(st.gap_to_next_star||0)}</b> Nettoumsatz bis zum nächsten Stern.</p>`:'<p><b>6 von 6 Sternen erreicht – Champion!</b></p>'}</section>`:'');maybeCelebrateStars()}
function renderSettings(){const s=state.settings||{},ths=(state.star_thresholds||[15000,20000,30000,40000,50000,75000]),dhl=shippingIntegration('DHL'),dpd=shippingIntegration('DPD');const stored=(i,k)=>i.credentials?.[k]?'gespeichert – leer lassen zum Beibehalten':'';ws.innerHTML=`<div class="sectionHead"><div><h2>Einstellungen</h2><p>Shopparameter, Vertrieb, DHL/DPD-Schnittstellen und Sicherheit verwalten.</p></div></div><div class="settingsGrid"><section class="panel settingsPanel"><h3>Shopparameter</h3><div class="formGrid"><label class="full">Shopname<input id="setName" value="${esc(s.shop_name||'Räucherhaken24')}"></label><label>Versandfrei ab (€)<input id="setThreshold" type="number" step="0.01" value="${Number(s.shipping_threshold??39)}"></label><label>Versandkosten (€)<input id="setShipping" type="number" step="0.01" value="${Number(s.shipping_cost??7)}"></label><label>MwSt. (%)<input id="setVat" type="number" step="1" value="${Number(s.vat_rate??19)}"></label><label>Standard-Versender<select id="setDefaultCarrier"><option value="DPD" ${sel(s.shipping_default_carrier||'DPD','DPD')}>DPD</option><option value="DHL" ${sel(s.shipping_default_carrier,'DHL')}>DHL</option></select></label><label class="full">System-E-Mail<input id="setSystemEmail" type="email" value="${esc(s.system_email||'service@raeucherhaken24.com')}"></label><label>Newsletter-Absendername<input id="setNewsletterName" value="${esc(s.newsletter_sender_name||'Räucherhaken24')}"></label><label>Newsletter-Antwortadresse<input id="setNewsletterReply" type="email" value="${esc(s.newsletter_reply_to||s.system_email||'service@raeucherhaken24.com')}"></label></div></section><section class="panel settingsPanel"><h3>Vertriebskalender</h3><div class="formGrid"><label>Provisionsabrechnung am<input id="setStatementDay" type="number" min="1" max="28" value="${Number(s.commission_statement_day??27)}"></label><label>Auszahlung am Folgemonat<input id="setPayoutDay" type="number" min="1" max="28" value="${Number(s.commission_payout_day??1)}"></label></div><h3 style="margin-top:18px">6 monatliche Sternziele</h3><div class="formGrid starSettings">${ths.map((v,i)=>`<label>Stern ${i+1} ab (€ Netto)<input id="setStar${i+1}" type="number" min="1" step="500" value="${Number(v)}"></label>`).join('')}</div></section><section class="panel settingsPanel"><h3>Eigenes Passwort ändern</h3><div class="formGrid"><label class="full">Aktuelles Passwort<input id="pwCurrent" type="password"></label><label class="full">Neues Passwort<input id="pwNew" type="password" minlength="12"></label></div><div class="formActions"><button class="btn" id="changePassword">Passwort ändern</button></div></section></div><div class="shippingApiSettings"><section class="panel settingsPanel carrierSettings"><div class="carrierSettingsHead">${carrierBadge('DHL')}<div><h3>DHL Paket API</h3>${integrationStatus(dhl)}<small>${esc(dhl.last_message||'Noch nicht eingerichtet')}</small></div></div><div class="formGrid"><label>Umgebung<select id="dhlEnv"><option value="sandbox" ${sel(dhl.environment||'sandbox','sandbox')}>Sandbox / Test</option><option value="production" ${sel(dhl.environment,'production')}>Produktion</option></select></label><label>API Key / Client ID<input id="dhlClientId" placeholder="${stored(dhl,'client_id')}"></label><label>API Secret<input id="dhlClientSecret" type="password" placeholder="${stored(dhl,'client_secret')}"></label><label>GKP-Benutzer<input id="dhlUsername" placeholder="${stored(dhl,'username')}"></label><label>GKP-Passwort<input id="dhlPassword" type="password" placeholder="${stored(dhl,'password')}"></label><label>EKP / Kundennummer<input id="dhlEkp" placeholder="${stored(dhl,'ekp')}"></label><label>Abrechnungsnummer<input id="dhlBilling" placeholder="${stored(dhl,'billing_number')}"></label></div><div class="formActions"><button class="btn" id="saveDhl">DHL-Daten speichern</button><button class="btn primary" id="testDhl">Live-Verbindung prüfen</button></div></section><section class="panel settingsPanel carrierSettings"><div class="carrierSettingsHead">${carrierBadge('DPD')}<div><h3>DPD Webservice</h3>${integrationStatus(dpd)}<small>${esc(dpd.last_message||'Noch nicht eingerichtet')}</small></div></div><div class="formGrid"><label>Umgebung<select id="dpdEnv"><option value="sandbox" ${sel(dpd.environment||'sandbox','sandbox')}>Sandbox / Test</option><option value="production" ${sel(dpd.environment,'production')}>Produktion</option></select></label><label>Partner-Name<input id="dpdPartnerName" placeholder="${stored(dpd,'partner_name')}"></label><label>Partner-Token<input id="dpdPartnerToken" type="password" placeholder="${stored(dpd,'partner_token')}"></label><label>Cloud User ID<input id="dpdCloudUser" placeholder="${stored(dpd,'cloud_user_id')}"></label><label>User-Token<input id="dpdUserToken" type="password" placeholder="${stored(dpd,'user_token')}"></label><label>Kundennummer<input id="dpdCustomer" placeholder="${stored(dpd,'customer_number')}"></label></div><div class="formActions"><button class="btn" id="saveDpd">DPD-Daten speichern</button><button class="btn primary" id="testDpd">Live-Verbindung prüfen</button></div></section></div><div class="moduleNotice"><b>Sicher gespeichert:</b> API-Zugangsdaten werden serverseitig verschlüsselt abgelegt und nicht wieder im Klartext an den Browser ausgegeben. Nach dem Speichern immer die Live-Prüfung ausführen. Grün bedeutet: Zugang wurde vom jeweiligen Dienst bestätigt.</div><div class="formActions settingsSaveAll"><button class="btn primary" id="saveSettings">Allgemeine Einstellungen speichern</button></div>`;$('#saveSettings').onclick=async()=>{try{const stars=[1,2,3,4,5,6].map(i=>+$('#setStar'+i).value);await api({action:'settings_save',shop_name:$('#setName').value,shipping_threshold:+$('#setThreshold').value,shipping_cost:+$('#setShipping').value,vat_rate:+$('#setVat').value,shipping_default_carrier:$('#setDefaultCarrier').value,system_email:$('#setSystemEmail').value,newsletter_sender_name:$('#setNewsletterName').value,newsletter_reply_to:$('#setNewsletterReply').value,commission_statement_day:+$('#setStatementDay').value,commission_payout_day:+$('#setPayoutDay').value,star_thresholds:stars});toast('Einstellungen gespeichert');await load(true);renderSettings()}catch(e){toast(e.message,true)}};$('#changePassword').onclick=async()=>{try{await api({action:'password_change',current:$('#pwCurrent').value,new:$('#pwNew').value});toast('Passwort geändert');$('#pwCurrent').value=$('#pwNew').value=''}catch(e){toast(e.message,true)}};$('#saveDhl').onclick=()=>saveCarrierSettings('DHL');$('#saveDpd').onclick=()=>saveCarrierSettings('DPD');$('#testDhl').onclick=()=>testCarrier('DHL');$('#testDpd').onclick=()=>testCarrier('DPD')}
async function saveCarrierSettings(carrier){try{let environment,credentials;if(carrier==='DHL'){environment=$('#dhlEnv').value;credentials={client_id:$('#dhlClientId').value,client_secret:$('#dhlClientSecret').value,username:$('#dhlUsername').value,password:$('#dhlPassword').value,ekp:$('#dhlEkp').value,billing_number:$('#dhlBilling').value}}else{environment=$('#dpdEnv').value;credentials={partner_name:$('#dpdPartnerName').value,partner_token:$('#dpdPartnerToken').value,cloud_user_id:$('#dpdCloudUser').value,user_token:$('#dpdUserToken').value,customer_number:$('#dpdCustomer').value}}const d=await api({action:'shipping_integration_save',carrier,environment,credentials});state.shipping_integrations=d.integrations||state.shipping_integrations;toast(carrier+' Zugangsdaten gespeichert');await load(true);renderSettings()}catch(e){toast(e.message,true)}}


function toInputDT(v){if(!v)return'';return String(v).replace(' ','T').slice(0,16)}
function promoStatusText(p){if(!p.is_offer)return'–';if(p.sale_active)return'<span class="status s-paid">Aktiv</span>';if(p.sale_start_at&&new Date(p.sale_start_at)>new Date())return'<span class="status s-production">Geplant</span>';if(p.sale_end_at&&new Date(p.sale_end_at)<new Date())return'<span class="status s-cancelled">Beendet</span>';return'<span class="status s-production">Angebot</span>'}
function bindPromoQuickControls(){if(!can('edit_products'))return;$$('[data-quick-popular]').forEach(cb=>cb.onchange=async()=>{const old=!cb.checked;cb.disabled=true;try{const r=await api({action:'product_quick_update',id:cb.dataset.quickPopular,is_popular:cb.checked});const i=(state.catalog||[]).findIndex(p=>p.id===cb.dataset.quickPopular);if(i>=0)state.catalog[i]={...state.catalog[i],...(r.product||{})};toast(cb.checked?'Als beliebt markiert':'Beliebt-Markierung entfernt')}catch(e){cb.checked=old;toast(e.message,true)}finally{cb.disabled=false}});$$('[data-quick-offer]').forEach(cb=>cb.onchange=async()=>{const old=!cb.checked;cb.disabled=true;try{const r=await api({action:'product_quick_update',id:cb.dataset.quickOffer,is_offer:cb.checked});const i=(state.catalog||[]).findIndex(p=>p.id===cb.dataset.quickOffer);if(i>=0)state.catalog[i]={...state.catalog[i],...(r.product||{})};toast(cb.checked?'Angebot aktiviert':'Angebot deaktiviert')}catch(e){cb.checked=old;toast(e.message,true)}finally{cb.disabled=false}})}
function promoCell(p){const old=Number(p.old_price||0),sale=Number(p.sale_price||0);return `<div class="promoAdminCell">${promoStatusText(p)}${old>0||sale>0?`<small>${old>0?`<s>${money(old)}</s>`:''}${sale>0?` → <b>${money(sale)}</b>`:''}</small>`:'<small>kein Aktionspreis</small>'}${p.sale_start_at||p.sale_end_at?`<em>${p.sale_start_at?donly(p.sale_start_at):'sofort'} – ${p.sale_end_at?donly(p.sale_end_at):'offen'}</em>`:''}</div>`}
function productCategories(current=''){const defaults=['Räucherhaken','Fleischerhaken','Räuchermehl','Räucherlaugen','Naturgewürze','Gewürze','Thermometer','Zubehör','Sonstiges'];const vals=[...new Set([...defaults,...(state.catalog||[]).map(x=>String(x.category||'').trim()).filter(Boolean)])].sort((a,b)=>a.localeCompare(b,'de'));if(current&&!vals.includes(current))vals.unshift(current);return vals}
function productCategoryOptions(current=''){return productCategories(current).map(x=>`<option value="${esc(x)}" ${sel(current,x)}>${esc(x)}</option>`).join('')+`<option value="__new__">+ Neue Kategorie anlegen …</option>`}
function bindProductModalTabs(){const tabs=$$('[data-product-tab]'),panels=$$('[data-product-panel]');tabs.forEach(t=>t.onclick=()=>{tabs.forEach(x=>x.classList.toggle('active',x===t));panels.forEach(p=>p.classList.toggle('active',p.dataset.productPanel===t.dataset.productTab))})}
function productPackSummary(){const qty=Number($('#prContentQty')?.value||0),unit=$('#prContentUnit')?.value||'',pack=Number($('#prPackQty')?.value||1),type=$('#prPackageType')?.value||'',w=Number($('#prWeight')?.value||0),sw=Number($('#prShipWeight')?.value||0);const parts=[];if(qty>0)parts.push(`${qty.toLocaleString('de-DE')} ${unit}`);if(pack>1)parts.push(`${pack} Stück / Verkaufseinheit`);if(type)parts.push(type);if(w>0)parts.push(`Produkt ${weightLabel(w)}`);if(sw>0)parts.push(`Versand ${weightLabel(sw)}`);const el=$('#productPackSummary');if(el)el.textContent=parts.length?parts.join(' · '):'Noch keine Mengen-/Verpackungsdaten hinterlegt.'}
function legacyProductModalV83_1(id){if(!can('edit_products'))return toast('Produkte dürfen mit diesem Zugang nicht verändert werden.',true);const p=(state.catalog||[]).find(x=>x.id===id)||{},inv=(state.inventory||[]).find(x=>x.id===id)||{},cat=p.category||'Sonstiges';const catCounts={};(state.catalog||[]).forEach(x=>{const c=String(x.category||'Sonstiges');catCounts[c]=(catCounts[c]||0)+1});openModal(`<div class="modalHead"><small>PRODUKTSTAMM · V59</small><h2>${id?'Produkt bearbeiten':'Neuen Artikel anlegen'}</h2><p>Artikel professionell in fünf klar getrennten Bereichen pflegen.</p></div><div class="modalBody productEditor"><div class="productTabs"><button class="active" data-product-tab="master">1 · Stammdaten</button><button data-product-tab="category">2 · Kategorien</button><button data-product-tab="pack">3 · Menge & Verpackung</button><button data-product-tab="price">4 · Preis & Shop</button><button data-product-tab="stock">5 · Lager & Inhalt</button></div>
<section class="productTabPanel active" data-product-panel="master"><div class="formGrid"><label>Artikel-ID<input id="prId" value="${esc(p.id||'')}" ${id?'disabled':''} placeholder="automatisch bei leer"></label><label>Artikelnummer<input id="prArticle" value="${esc(p.article_no||'')}" placeholder="automatisch bei leer"></label><label>Barcode / EAN / GTIN<input id="prBarcode" value="${esc(p.barcode||'')}"></label><label>SKU<input id="prSku" value="${esc(p.sku||'')}"></label><label class="full">Produktname<input id="prName" value="${esc(p.name||'')}"></label><label>Produkttyp<select id="prType"><option value="product" ${sel(p.type||'product','product')}>Produkt</option><option value="hook" ${sel(p.type,'hook')}>Räucherhaken / Variante</option><option value="prototype" ${sel(p.type,'prototype')}>Prototyp</option></select></label></div><div class="moduleNotice"><b>Nächster Schritt:</b> Unter „2 · Kategorien“ ordnest du den Artikel einer bestehenden Kategorie zu oder legst direkt eine neue an.</div></section>
<section class="productTabPanel" data-product-panel="category"><div class="categoryEditorHero"><div><b>Produktkategorie</b><span>Die Kategorie steuert Sortierung, Produktsuche und spätere Shop-Zuordnung.</span></div><span class="categoryCurrent" id="categoryCurrent">${esc(cat)}</span></div><div class="formGrid"><label class="full">Kategorie<select id="prCategorySelect">${productCategoryOptions(cat)}</select><small>Vorhandene Kategorie auswählen. „Thermometer“ ist bereits als Shop-Kategorie vorbereitet.</small></label><label id="prNewCategoryWrap" class="full hiddenField">Neue Kategorie<input id="prNewCategory" placeholder="z. B. Grillzubehör, Geschenksets, Ersatzteile"><small>Die neue Kategorie wird mit dem ersten gespeicherten Artikel automatisch Teil des Produktstamms.</small></label></div><div class="categoryChipGrid">${productCategories(cat).map(c=>`<button type="button" class="categoryChip ${c===cat?'active':''}" data-pick-category="${esc(c)}"><b>${esc(c)}</b><span>${Number(catCounts[c]||0)} Artikel</span></button>`).join('')}</div></section>
<section class="productTabPanel" data-product-panel="pack"><div class="packIntro"><b>Menge & Verpackung</b><span id="productPackSummary"></span></div><div class="formGrid"><label>Menge pro Verkaufseinheit<input id="prContentQty" type="number" min="0" step="0.001" value="${Number(p.content_quantity??p.pack_quantity??1)}"><small>z. B. 500 bei 500 g oder 10 bei 10 Stück.</small></label><label>Mengeneinheit<select id="prContentUnit"><option ${sel(p.content_unit||'Stück','Stück')}>Stück</option><option ${sel(p.content_unit,'g')}>g</option><option ${sel(p.content_unit,'kg')}>kg</option><option ${sel(p.content_unit,'ml')}>ml</option><option ${sel(p.content_unit,'l')}>l</option><option ${sel(p.content_unit,'Set')}>Set</option><option ${sel(p.content_unit,'Paar')}>Paar</option><option ${sel(p.content_unit,'Paket')}>Paket</option></select></label><label>Stückzahl im Gebinde<input id="prPackQty" type="number" min="1" step="1" value="${Number(p.pack_quantity||1)}"><small>Wie viele Einzelteile enthält die Verkaufseinheit?</small></label><label>Anzeige / Gebinde<input id="prUnit" value="${esc(p.unit||'Stück')}" placeholder="z. B. 500 g, 10er-Set, 1 Paket"></label><label>Produktgewicht (g)<input id="prWeight" type="number" min="0" step="1" value="${Number(p.product_weight_g||0)}"></label><label>Versandgewicht (g)<input id="prShipWeight" type="number" min="0" step="1" value="${Number(p.shipping_weight_g||0)}"><small>Produkt inkl. Verpackungsmaterial.</small></label><label>Verpackungsart<select id="prPackageType"><option ${sel(p.package_type||'Einzelartikel','Einzelartikel')}>Einzelartikel</option><option ${sel(p.package_type,'Beutel')}>Beutel</option><option ${sel(p.package_type,'Dose')}>Dose</option><option ${sel(p.package_type,'Flasche')}>Flasche</option><option ${sel(p.package_type,'Karton')}>Karton</option><option ${sel(p.package_type,'Set')}>Set</option><option ${sel(p.package_type,'Paket')}>Paket</option><option ${sel(p.package_type,'Rolle')}>Rolle</option><option ${sel(p.package_type,'Sonstiges')}>Sonstiges</option></select></label><label>Preis-Grundlage<select id="prPriceBasis"><option value="auto" ${sel(p.price_basis||'auto','auto')}>Automatisch erkennen</option><option value="weight" ${sel(p.price_basis,'weight')}>Gewicht · Preis je 100 g</option><option value="piece" ${sel(p.price_basis,'piece')}>Stück · Preis je Stück</option></select></label><label>Paketlänge (cm)<input id="prPackL" type="number" min="0" step="0.1" value="${Number(p.package_length_cm||0)}"></label><label>Paketbreite (cm)<input id="prPackW" type="number" min="0" step="0.1" value="${Number(p.package_width_cm||0)}"></label><label>Pakethöhe (cm)<input id="prPackH" type="number" min="0" step="0.1" value="${Number(p.package_height_cm||0)}"></label></div></section>
<section class="productTabPanel" data-product-panel="price"><div class="formGrid"><label>Standardpreis (€)<input id="prBase" type="number" min="0" step="0.01" value="${Number(p.base||0).toFixed(2)}"></label><label>Streichpreis / alter Preis (€)<input id="prOld" type="number" min="0" step="0.01" value="${Number(p.old_price||0).toFixed(2)}"></label><label>Aktionspreis / neuer Preis (€)<input id="prSale" type="number" min="0" step="0.01" value="${Number(p.sale_price||0).toFixed(2)}"></label><label>Aktionsstart<input id="prSaleStart" type="datetime-local" value="${esc(toInputDT(p.sale_start_at))}"></label><label>Aktionsende<input id="prSaleEnd" type="datetime-local" value="${esc(toInputDT(p.sale_end_at))}"></label><label>Status<select id="prStatus"><option value="active" ${sel(p.status||'active','active')}>Aktiv</option><option value="inactive" ${sel(p.status,'inactive')}>Inaktiv</option></select></label><label class="toggleLabel"><input id="prOnline" type="checkbox" ${p.shop_visible!==false?'checked':''}><span><b>Online im Shop</b><small>Artikel öffentlich anzeigen</small></span></label><label class="toggleLabel"><input id="prPopular" type="checkbox" ${p.is_popular?'checked':''}><span><b>Beliebt</b><small>Smoky-Badge „Beliebt“ im Shop</small></span></label><label class="toggleLabel"><input id="prOffer" type="checkbox" ${p.is_offer?'checked':''}><span><b>Angebot</b><small>Smoky-Angebotsanzeige + Aktionszeitraum</small></span></label></div><div class="moduleNotice"><b>Preisautomatik:</b> Gewichtsartikel können automatisch mit Grundpreis je 100 g angezeigt werden. Bei Sets wird der Stückpreis aus der Stückzahl im Gebinde berechnet.</div></section>
<section class="productTabPanel" data-product-panel="stock"><div class="formGrid"><label>Bestand<input id="prStock" type="number" min="0" step="1" value="${Number(inv.stock??p.stock??0)}"></label><label>Mindestbestand<input id="prMinimum" type="number" min="0" step="1" value="${Number(inv.minimum??p.minimum??0)}"></label><label class="full">Beschreibung<textarea id="prDescription">${esc(p.description||'')}</textarea></label><label class="full productImagePicker"><b>Produktbild</b><input id="prImage" type="file" accept=".jpg,.jpeg,.png,.webp,.avif,.heic,.heif,image/jpeg,image/png,image/webp,image/avif,image/heic,image/heif"><small>${p.image?'Aktuelles Bild: '+esc(p.image):'Noch kein Bild hinterlegt · JPG/PNG/WebP/AVIF/HEIC/HEIF · max. 20 MB'}</small></label></div></section>
<div class="formActions"><button class="btn" id="prCancel">Abbrechen</button><button class="btn primary" id="prSave">Produkt speichern</button></div></div>`);bindProductModalTabs();const catSel=$('#prCategorySelect'),newWrap=$('#prNewCategoryWrap'),catCurrent=$('#categoryCurrent');const syncCat=()=>{newWrap.classList.toggle('hiddenField',catSel.value!=='__new__');if(catCurrent)catCurrent.textContent=catSel.value==='__new__'?'Neue Kategorie':catSel.value};catSel.onchange=syncCat;$$('[data-pick-category]').forEach(b=>b.onclick=()=>{catSel.value=b.dataset.pickCategory;syncCat();$$('[data-pick-category]').forEach(x=>x.classList.toggle('active',x===b))});syncCat();['prContentQty','prContentUnit','prPackQty','prPackageType','prWeight','prShipWeight'].forEach(x=>$('#'+x)?.addEventListener('input',productPackSummary));productPackSummary();$('#prCancel').onclick=closeModal;$('#prSave').onclick=async()=>{try{const category=catSel.value==='__new__'?$('#prNewCategory').value.trim():catSel.value;if(!category)throw new Error('Bitte im Reiter Kategorien eine Kategorie auswählen oder neu anlegen.');const data={action:'product_save',id:id||$('#prId').value.trim(),article_no:$('#prArticle').value,barcode:$('#prBarcode').value,sku:$('#prSku').value,name:$('#prName').value,category,type:$('#prType').value,unit:$('#prUnit').value,content_quantity:+$('#prContentQty').value,content_unit:$('#prContentUnit').value,package_type:$('#prPackageType').value,package_length_cm:+$('#prPackL').value,package_width_cm:+$('#prPackW').value,package_height_cm:+$('#prPackH').value,pack_quantity:+$('#prPackQty').value,price_basis:$('#prPriceBasis').value,base:+$('#prBase').value,old_price:+$('#prOld').value,sale_price:+$('#prSale').value,sale_start_at:$('#prSaleStart').value,sale_end_at:$('#prSaleEnd').value,product_weight_g:+$('#prWeight').value,shipping_weight_g:+$('#prShipWeight').value,stock:+$('#prStock').value,minimum:+$('#prMinimum').value,status:$('#prStatus').value,shop_visible:$('#prOnline').checked,is_popular:$('#prPopular').checked,is_offer:$('#prOffer').checked,description:$('#prDescription').value,image:p.image||''};const d=await api(data);const file=$('#prImage').files?.[0];if(file&&d.product?.id)await uploadProductImage(d.product.id,file);toast('Produkt gespeichert');closeModal();await load(true);if(current==='products')renderProducts();if(current==='nature_spices')renderNatureSpices()}catch(e){toast(e.message,true)}}}

function salesPeriodRange(key='all'){
  const now=new Date(),y=now.getFullYear(),m=now.getMonth();let start=null,end=null,label='Gesamtzeitraum';
  if(key==='current_half'){if(m<6){start=new Date(y,0,1);label=`1. Halbjahr ${y} (laufend)`}else{start=new Date(y,6,1);label=`2. Halbjahr ${y} (laufend)`}end=new Date(now.getTime()+1000)}
  else if(key==='previous_half'){if(m<6){start=new Date(y-1,6,1);end=new Date(y,0,1);label=`2. Halbjahr ${y-1}`}else{start=new Date(y,0,1);end=new Date(y,6,1);label=`1. Halbjahr ${y}`}}
  else if(key==='last_6_months'){start=new Date(now);start.setMonth(start.getMonth()-6);end=new Date(now.getTime()+1000);label='Letzte 6 Monate'}
  else if(key==='last_12_months'){start=new Date(now);start.setFullYear(start.getFullYear()-1);end=new Date(now.getTime()+1000);label='Letzte 12 Monate'}
  return {key,start,end,label}
}
function completedSaleOrder(o){if(!o||o.status==='cancelled'||o.payment_status==='refunded')return false;return o.payment_status==='paid'||['production','quality','packing','ready','shipped','complete'].includes(o.status)}
function productSalesStats(periodKey='all'){
  const period=salesPeriodRange(periodKey),catalog=(state.catalog||[]).filter(p=>p.type!=='prototype'),byId={};
  catalog.forEach(p=>byId[p.id]={id:p.id,article_no:p.article_no||p.sku||'',name:p.name||p.id,category:p.category||'Ohne Kategorie',base:Number(p.base||0),qty:0,revenue:0,orders:new Set(),lastSold:null,shop_visible:Boolean(p.shop_visible&&p.status==='active')});
  const orders=(state.orders||[]).filter(completedSaleOrder).filter(o=>{const d=new Date(o.created_at);if(Number.isNaN(d.getTime()))return false;if(period.start&&d<period.start)return false;if(period.end&&d>=period.end)return false;return true});
  orders.forEach(o=>(o.items||[]).forEach(x=>{const st=byId[x.id];if(!st)return;const qty=Math.max(0,Number(x.qty||1)),unit=Number(x.unit_price??x.price??0),d=new Date(o.created_at);st.qty+=qty;st.revenue+=qty*unit;st.orders.add(o.order_no||o.id);if(!st.lastSold||d>st.lastSold)st.lastSold=d}));
  const rows=Object.values(byId).map(r=>({...r,order_count:r.orders.size,orders:undefined}));
  const sold=rows.filter(r=>r.qty>0).sort((a,b)=>b.qty-a.qty||b.revenue-a.revenue),never=rows.filter(r=>r.qty===0);
  const soldQtys=sold.map(r=>r.qty).sort((a,b)=>a-b),weakThreshold=soldQtys.length?Math.max(1,soldQtys[Math.floor((soldQtys.length-1)*.25)]):0;
  const weak=sold.filter(r=>r.qty<=weakThreshold).sort((a,b)=>a.qty-b.qty||a.revenue-b.revenue);
  return {period,rows,sold,never,weak,weakThreshold,totalQty:sold.reduce((n,r)=>n+r.qty,0),totalRevenue:sold.reduce((n,r)=>n+r.revenue,0),orders:orders.length};
}
function salesStatMap(period='all'){return Object.fromEntries(productSalesStats(period).rows.map(r=>[r.id,r]))}
function sortProductsForView(rows,mode=productSortMode){const stats=salesStatMap('all');return [...rows].sort((a,b)=>{const sa=stats[a.id]||{qty:0,revenue:0},sb=stats[b.id]||{qty:0,revenue:0};switch(mode){case'article_asc':return String(a.article_no||a.sku||'').localeCompare(String(b.article_no||b.sku||''),'de',{numeric:true});case'category_asc':return String(a.category||'').localeCompare(String(b.category||''),'de')||String(a.name||'').localeCompare(String(b.name||''),'de');case'price_asc':return Number(a.base||0)-Number(b.base||0);case'price_desc':return Number(b.base||0)-Number(a.base||0);case'sales_desc':return sb.qty-sa.qty||sb.revenue-sa.revenue;case'sales_asc':return sa.qty-sb.qty||sa.revenue-sb.revenue;case'revenue_desc':return sb.revenue-sa.revenue||sb.qty-sa.qty;case'revenue_asc':return sa.revenue-sb.revenue||sa.qty-sb.qty;case'never_first':return (sa.qty>0)-(sb.qty>0)||String(a.name||'').localeCompare(String(b.name||''),'de');default:return String(a.name||'').localeCompare(String(b.name||''),'de')}})}
function productSortToolbar(){return `<div class="productSortToolbar"><div><b>Produkte sortieren</b><span>Die Sortierung betrifft nur das Orgaboard – nicht die Reihenfolge im Shop.</span></div><select id="productSortSelect"><option value="name_asc" ${productSortMode==='name_asc'?'selected':''}>Name A–Z</option><option value="article_asc" ${productSortMode==='article_asc'?'selected':''}>Artikelnummer</option><option value="category_asc" ${productSortMode==='category_asc'?'selected':''}>Kategorie</option><option value="price_asc" ${productSortMode==='price_asc'?'selected':''}>Preis aufsteigend</option><option value="price_desc" ${productSortMode==='price_desc'?'selected':''}>Preis absteigend</option><option value="sales_desc" ${productSortMode==='sales_desc'?'selected':''}>Meistverkauft zuerst</option><option value="sales_asc" ${productSortMode==='sales_asc'?'selected':''}>Wenig verkauft zuerst</option><option value="revenue_desc" ${productSortMode==='revenue_desc'?'selected':''}>Höchster Artikelumsatz</option><option value="revenue_asc" ${productSortMode==='revenue_asc'?'selected':''}>Niedrigster Artikelumsatz</option><option value="never_first" ${productSortMode==='never_first'?'selected':''}>Noch nie verkauft zuerst</option></select></div>`}
function bindProductSort(renderFn){const el=$('#productSortSelect');if(el)el.onchange=()=>{productSortMode=el.value;renderFn()}}
function analysisPeriodOptions(){return [['previous_half','Vorheriges abgeschlossenes Halbjahr'],['current_half','Aktuelles Halbjahr'],['last_6_months','Letzte 6 Monate'],['last_12_months','Letzte 12 Monate'],['all','Gesamtzeitraum']].map(([v,l])=>`<option value="${v}" ${productAnalysisPeriod===v?'selected':''}>${l}</option>`).join('')}
function salesPerformanceBadge(r,report){if(r.qty===0)return '<span class="salesPerfBadge noSales">0 Verkäufe</span>';if(r.qty<=report.weakThreshold)return '<span class="salesPerfBadge weakSales">Langsamdreher</span>';return '<span class="salesPerfBadge goodSales">verkauft</span>'}
function renderProductAnalysis(){
  if(!isAdmin())return setView('dashboard');const report=productSalesStats(productAnalysisPeriod),top=report.sold.slice(0,12),weak=report.weak.slice(0,20),never=report.never.slice(0,30),maxQty=Math.max(1,...top.map(x=>x.qty));
  ws.innerHTML=`<div class="sectionHead"><div><h2>Produkt-Analyse</h2><p>Beliebteste Produkte, Langsamdreher und Artikel ohne Verkauf datenbasiert erkennen.</p></div><div class="sectionActions"><button class="btn" id="productReportPdf">▣ PDF</button><button class="btn" id="productReportPrint">⎙ Drucken</button></div></div>
  <div class="analysisControls panel"><label><span>Auswertungszeitraum</span><select id="productAnalysisPeriod">${analysisPeriodOptions()}</select></label><div><b>${esc(report.period.label)}</b><small>Nur bezahlte bzw. weiterverarbeitete, nicht stornierte Aufträge werden als Verkauf gewertet.</small></div></div>
  <div class="metricGrid analysisMetrics">${metric('Verkaufte Einheiten',Number(report.totalQty).toLocaleString('de-DE'),report.orders+' relevante Bestellungen')}${metric('Artikelumsatz',money(report.totalRevenue),'ohne Versandkosten')}${metric('Produkte mit Verkauf',report.sold.length,report.rows.length+' Produkte ausgewertet')}${metric('Ohne Verkauf',report.never.length,'im gewählten Zeitraum',report.never.length?'attn':'')}${metric('Langsamdreher',report.weak.length,'unteres Verkaufsquartil',report.weak.length?'attn':'')}</div>
  <div class="analysisTwoCol"><section class="panel analysisPanel"><div class="panelTitle"><div><h3>Beliebteste Produkte</h3><span>nach verkaufter Menge · Top 12</span></div><b>${esc(report.period.label)}</b></div>${top.length?`<div class="bestSellerList">${top.map((r,i)=>`<div class="bestSellerRow"><strong>${i+1}</strong><div><b>${esc(r.name)}</b><small>Art.-Nr. ${esc(r.article_no||'–')} · ${esc(r.category)}</small></div><div class="bestSellerBar"><i style="width:${Math.max(3,Math.round(r.qty/maxQty*100))}%"></i></div><span><b>${Number(r.qty).toLocaleString('de-DE')} Stk.</b><small>${money(r.revenue)}</small></span></div>`).join('')}</div>`:'<div class="empty"><b>Noch keine Verkäufe</b>Für diesen Zeitraum liegen noch keine verwertbaren Verkaufsdaten vor.</div>'}</section>
  <section class="panel analysisPanel"><div class="panelTitle"><div><h3>Schwach verkaufte Produkte</h3><span>verkauft, aber im unteren 25-%-Bereich</span></div><b>≤ ${report.weakThreshold||0} Stk.</b></div>${weak.length?`<div class="slowSellerList">${weak.map(r=>`<button class="slowSellerRow" data-analysis-product="${esc(r.id)}"><div>${salesPerformanceBadge(r,report)}<b>${esc(r.name)}</b><small>${esc(r.article_no||'–')} · ${esc(r.category)}</small></div><span><b>${Number(r.qty).toLocaleString('de-DE')} Stk.</b><small>${money(r.revenue)}</small></span></button>`).join('')}</div>`:'<div class="empty"><b>Keine Langsamdreher</b></div>'}</section></div>
  <section class="panel analysisPanel"><div class="panelTitle"><div><h3>Produkte ohne Verkauf</h3><span>Keine verkaufte Einheit im gewählten Zeitraum. Neue Artikel bitte nicht vorschnell auslisten.</span></div><b>${report.never.length} Artikel</b></div>${never.length?`<div class="noSalesGrid">${never.map(r=>`<button data-analysis-product="${esc(r.id)}"><span class="salesPerfBadge noSales">0</span><div><b>${esc(r.name)}</b><small>Art.-Nr. ${esc(r.article_no||'–')} · ${esc(r.category)}</small></div></button>`).join('')}</div>${report.never.length>never.length?`<p class="analysisFootnote">Weitere ${report.never.length-never.length} Produkte stehen im PDF-/Druckbericht.</p>`:''}`:'<div class="empty"><b>Sehr gut</b>Jedes Produkt wurde im Zeitraum mindestens einmal verkauft.</div>'}</section>
  <div class="moduleNotice"><b>Bewertung:</b> „Langsamdreher“ bedeutet nicht automatisch, dass ein Produkt aus dem Sortiment sollte. Saison, Neuaufnahme, Marge, Wiederkaufrate und strategische Bedeutung müssen zusätzlich geprüft werden.</div>`;
  $('#productAnalysisPeriod').onchange=e=>{productAnalysisPeriod=e.target.value;renderProductAnalysis()};$('#productReportPdf').onclick=()=>printProductAnalysisReport(productAnalysisPeriod,true);$('#productReportPrint').onclick=()=>printProductAnalysisReport(productAnalysisPeriod,false);$$('[data-analysis-product]').forEach(b=>b.onclick=()=>productModal(b.dataset.analysisProduct));
}
function semiannualDuePeriod(){const n=new Date(),m=n.getMonth(),y=n.getFullYear();if(m>=6&&m<=8)return {reportKey:`${y}-H1`,periodKey:'previous_half'};if(m>=0&&m<=2)return {reportKey:`${y-1}-H2`,periodKey:'previous_half'};return null}
function semiannualProductNoticeHtml(){if(!isAdmin())return'';const due=semiannualDuePeriod();if(!due||localStorage.getItem('rh24-product-report-ack-'+due.reportKey)==='1')return'';const r=productSalesStats(due.periodKey),top=r.sold[0],weak=r.weak.length,never=r.never.length;return `<section class="halfYearNotice"><div class="halfYearNoticeIcon">↗</div><div><small>HALBJÄHRLICHE PRODUKTAUSWERTUNG</small><h3>${esc(r.period.label)} ist ausgewertet</h3><p>${top?`Bestseller: <b>${esc(top.name)}</b> mit ${Number(top.qty).toLocaleString('de-DE')} verkauften Einheiten. `:''}<b>${weak}</b> Langsamdreher und <b>${never}</b> Produkte ohne Verkauf sollten geprüft werden.</p></div><div class="halfYearNoticeActions"><button class="btn primary" id="dashProductAnalysis">Bericht öffnen</button><button class="btn" id="dashProductReportPdf">▣ PDF</button><button class="btn" id="dashProductReportPrint">⎙</button><button class="textBtn" id="dashProductReportAck">Als geprüft markieren</button></div></section>`}
function productAnalysisReportHtml(periodKey){const r=productSalesStats(periodKey),top=r.sold,weak=r.weak,never=r.never,generated=new Date().toLocaleString('de-DE');const tr=(rows,kind)=>rows.map((x,i)=>`<tr><td>${kind==='top'?i+1:''}</td><td>${esc(x.article_no||'–')}</td><td>${esc(x.name)}</td><td>${esc(x.category)}</td><td>${Number(x.qty||0).toLocaleString('de-DE')}</td><td>${x.order_count||0}</td><td>${money(x.revenue||0)}</td><td>${x.lastSold?donly(x.lastSold):'–'}</td></tr>`).join('');return `<!doctype html><html lang="de"><head><meta charset="utf-8"><title>Räucherhaken24 Produktbericht ${esc(r.period.label)}</title><style>body{font-family:-apple-system,BlinkMacSystemFont,"Segoe UI",Arial,sans-serif;color:#251c16;margin:0;padding:36px}header{border-bottom:4px solid #8b4e25;padding-bottom:18px;margin-bottom:24px}header small{letter-spacing:.14em;color:#a75d29;font-weight:800}h1{margin:6px 0 2px}h2{margin-top:30px;font-size:18px}.metrics{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:20px 0}.metrics div{border:1px solid #ddd3ca;border-radius:10px;padding:12px}.metrics small{display:block;color:#786b61}.metrics b{font-size:18px}table{width:100%;border-collapse:collapse;font-size:11px;margin-top:10px}th,td{padding:7px 6px;border-bottom:1px solid #e7dfd8;text-align:left}th{background:#f6f1ec;font-size:9px;text-transform:uppercase}td:nth-child(5),td:nth-child(6),td:nth-child(7){text-align:right}.note{background:#fff7e9;border:1px solid #ead5ae;border-radius:10px;padding:12px;margin:20px 0;font-size:11px}.footer{margin-top:25px;color:#777;font-size:9px}@media print{body{padding:12mm}.noPrint{display:none}h2{break-after:avoid}table{break-inside:auto}tr{break-inside:avoid}.metrics{grid-template-columns:repeat(4,1fr)}}@page{size:A4;margin:12mm}</style></head><body><header><small>RÄUCHERHAKEN24 · ORGABOARD</small><h1>Halbjahres-/Produktbericht</h1><p>${esc(r.period.label)} · erstellt ${esc(generated)}</p></header><div class="metrics"><div><small>Einheiten</small><b>${Number(r.totalQty).toLocaleString('de-DE')}</b></div><div><small>Artikelumsatz</small><b>${money(r.totalRevenue)}</b></div><div><small>Produkte mit Verkauf</small><b>${r.sold.length}</b></div><div><small>Ohne Verkauf</small><b>${r.never.length}</b></div></div><div class="note"><b>Auswertungslogik:</b> Berücksichtigt werden bezahlte bzw. bereits weiterverarbeitete und nicht stornierte Aufträge. Langsamdreher sind verkaufte Produkte im unteren Verkaufsquartil (aktuell ≤ ${r.weakThreshold||0} Einheiten).</div><h2>Beliebteste Produkte</h2><table><thead><tr><th>Rang</th><th>Art.-Nr.</th><th>Produkt</th><th>Kategorie</th><th>Menge</th><th>Aufträge</th><th>Umsatz</th><th>Letzter Verkauf</th></tr></thead><tbody>${tr(top,'top')||'<tr><td colspan="8">Keine Verkäufe</td></tr>'}</tbody></table><h2>Schwach verkaufte Produkte</h2><table><thead><tr><th></th><th>Art.-Nr.</th><th>Produkt</th><th>Kategorie</th><th>Menge</th><th>Aufträge</th><th>Umsatz</th><th>Letzter Verkauf</th></tr></thead><tbody>${tr(weak,'weak')||'<tr><td colspan="8">Keine Langsamdreher</td></tr>'}</tbody></table><h2>Produkte ohne Verkauf</h2><table><thead><tr><th></th><th>Art.-Nr.</th><th>Produkt</th><th>Kategorie</th><th>Menge</th><th>Aufträge</th><th>Umsatz</th><th>Letzter Verkauf</th></tr></thead><tbody>${tr(never,'never')||'<tr><td colspan="8">Keine Produkte ohne Verkauf</td></tr>'}</tbody></table><p class="footer">Interner Managementbericht · Räucherhaken24 · Quelle: Orgaboard-Bestelldaten</p></body></html>`}
function printProductAnalysisReport(periodKey,asPdf=false){const w=window.open('','_blank','width=1100,height=900');if(!w)return toast('Pop-up wurde blockiert',true);w.document.open();w.document.write(productAnalysisReportHtml(periodKey));w.document.close();w.focus();if(asPdf)toast('Im Druckdialog „Als PDF sichern“ wählen.');setTimeout(()=>w.print(),350)}

function productsTable(rows,natureOnly=false){const inv=Object.fromEntries((state.inventory||[]).map(x=>[x.id,x])),sales=salesStatMap('all');const qw=(p,key,inputAttr,saveAttr)=>can('edit_products')?`<div class="quickPrice weightQuick"><input ${inputAttr}="${esc(p.id)}" type="number" min="0" step="1" value="${Number(p[key]||0)}"><button class="quickPriceSave" ${saveAttr}="${esc(p.id)}" title="Gewicht speichern">✓</button></div>`:`<span>${weightLabel(p[key])}</span>`;return `<div class="panel tableWrap"><table class="dataTable productV57Table"><thead><tr><th>Bild</th><th>Art.-Nr.</th><th>Artikel</th><th>Kategorie</th><th>Menge / Gebinde</th><th>Verpackung</th><th>Verkauft</th><th>Artikelumsatz</th><th>Standardpreis</th><th>Aktion / Streichpreis</th><th>Beliebt</th><th>Angebot</th><th>Produktgewicht</th><th>Versandgewicht</th><th>Bestand</th><th>Online</th><th>Aktionen</th></tr></thead><tbody>${rows.map(p=>{const st=sales[p.id]||{qty:0,revenue:0};const amount=Number(p.content_quantity||0)>0?`${Number(p.content_quantity).toLocaleString('de-DE')} ${esc(p.content_unit||'')}`:esc(p.unit||'–');const pack=[p.package_type||'',Number(p.pack_quantity||1)>1?`${Number(p.pack_quantity)} Stück`:null].filter(Boolean).join(' · ')||'–';return `<tr><td>${p.image?`<img class="productThumb" src="../${esc(p.image)}" alt="">`:'<div class="productThumb emptyThumb">◇</div>'}</td><td><b>${esc(p.article_no||'–')}</b><small class="tableSub">${esc(p.barcode||p.sku||'')}</small></td><td class="customerCell"><b>${esc(p.name)}</b>${productNewActiveAdmin(p)?'<span class="productNewAdminPill">Neu im Sortiment</span>':''}<small>${esc(p.unit||'')}</small></td><td>${esc(p.category||'–')}</td><td><b>${amount}</b></td><td><span>${esc(pack)}</span>${Number(p.package_length_cm||0)>0?`<small class="tableSub">${Number(p.package_length_cm)} × ${Number(p.package_width_cm||0)} × ${Number(p.package_height_cm||0)} cm</small>`:''}</td><td><button class="salesCountLink" data-analysis-product="${esc(p.id)}" title="Produktanalyse öffnen"><b>${Number(st.qty||0).toLocaleString('de-DE')}</b><small>Einheiten</small></button></td><td class="money">${money(st.revenue||0)}</td><td>${can('edit_products')?`<div class="quickPrice"><input data-quick-price="${esc(p.id)}" type="number" min="0" step="0.01" value="${Number(p.base||0).toFixed(2)}"><button class="quickPriceSave" data-quick-price-save="${esc(p.id)}">✓</button></div>`:`<span class="money">${money(p.base)}</span>`}</td><td>${promoCell(p)}</td><td>${can('edit_products')?`<label class="quickOnline promoCheck"><input type="checkbox" data-quick-popular="${esc(p.id)}" ${p.is_popular?'checked':''}><span>Beliebt</span></label>`:(p.is_popular?'★':'–')}</td><td>${can('edit_products')?`<label class="quickOnline promoCheck"><input type="checkbox" data-quick-offer="${esc(p.id)}" ${p.is_offer?'checked':''}><span>Angebot</span></label>`:(p.is_offer?'%':'–')}</td><td>${qw(p,'product_weight_g','data-quick-product-weight','data-quick-product-weight-save')}</td><td>${qw(p,'shipping_weight_g','data-quick-shipping-weight','data-quick-shipping-weight-save')}</td><td>${p.type==='prototype'?'–':Number(inv[p.id]?.stock??p.stock??0)}</td><td>${can('edit_products')&&p.type!=='prototype'?`<label class="quickOnline"><input type="checkbox" data-quick-online="${esc(p.id)}" ${p.shop_visible&&p.status==='active'?'checked':''}><span>${p.shop_visible&&p.status==='active'?'Online':'Offline'}</span></label>`:(p.shop_visible&&p.status==='active'?'<span class="status s-paid">Online</span>':'<span class="status s-cancelled">Nicht sichtbar</span>')}</td><td>${can('edit_products')?`<div class="rowActions"><button class="miniBtn" data-open-product="${esc(p.id)}">Bearbeiten</button>${isAdmin()?`<button class="miniBtn dangerMini" data-delete-product="${esc(p.id)}">Artikel löschen</button>`:''}</div>`:'<span class="muted tiny">Nur Ansicht</span>'}</td></tr>`}).join('')}</tbody></table></div>`}
function legacyRenderProductsV83_2(){const q=(productSearchTerm||searchTerm||'').toLowerCase().trim(),baseRows=(state.catalog||[]).filter(p=>!q||`${p.name} ${p.sku||''} ${p.article_no||''} ${p.barcode||''} ${p.category||''} ${p.id}`.toLowerCase().includes(q)),rows=sortProductsForView(baseRows);ws.innerHTML=`<div class="sectionHead"><div><h2>Artikel & Produkte</h2><p>Kategorie, Menge, Verpackung, Preis, Verkaufszahlen und Shopstatus direkt verwalten.</p></div><div class="sectionActions"><b class="muted tiny">${rows.length} Artikel</b>${isAdmin()?'<button class="btn" id="openProductAnalysis">↗ Produkt-Analyse</button>':''}${can('edit_products')?'<button class="btn primary" id="newProduct">+ Neuen Artikel anlegen</button>':''}</div></div>${productSearchToolbar(productSearchTerm)}${productSortToolbar()}<div class="moduleNotice"><b>V57 Produktsteuerung:</b> Artikel können nach Name, Artikelnummer, Kategorie, Preis, verkaufter Menge und Artikelumsatz sortiert werden. Die Sortierung verändert den Shop nicht.</div>${rows.length?productsTable(rows):'<div class="panel"><div class="empty"><b>Keine Produkte gefunden</b>Suchbegriff oder Barcode prüfen.</div></div>'}`;if($('#newProduct'))$('#newProduct').onclick=()=>productModal(null);if($('#openProductBuilderHub'))$('#openProductBuilderHub').onclick=()=>setView('productbuilder');if($('#openProductAnalysis'))$('#openProductAnalysis').onclick=()=>setView('product_analysis');bindQuickProductControls();bindPromoQuickControls();bindProductSearch(renderProducts);bindProductSort(renderProducts);$$('[data-analysis-product]').forEach(b=>b.onclick=()=>isAdmin()?setView('product_analysis'):productModal(b.dataset.analysisProduct))}
function renderNatureSpices(){const q=(productSearchTerm||searchTerm||'').toLowerCase().trim(),baseRows=(state.catalog||[]).filter(p=>p.category==='Naturgewürze').filter(p=>!q||`${p.name} ${p.sku||''} ${p.article_no||''} ${p.barcode||''}`.toLowerCase().includes(q)),rows=sortProductsForView(baseRows);ws.innerHTML=`<div class="sectionHead"><div><h2>Naturgewürze</h2><p>135 Gewürze: Preis, Angebot, Beliebt, Verkaufszahlen, Gewicht, Bild und Shopstatus zentral pflegen.</p></div><div class="sectionActions"><b class="muted tiny">${rows.length} Artikel</b>${isAdmin()?'<button class="btn" id="openNatureAnalysis">↗ Produkt-Analyse</button>':''}${can('edit_products')?'<button class="btn primary" id="newNatureProduct">+ Naturgewürz anlegen</button>':''}</div></div>${productSearchToolbar(productSearchTerm)}${productSortToolbar()}${rows.length?productsTable(rows,true):'<div class="panel"><div class="empty"><b>Keine Naturgewürze gefunden</b></div></div>'}`;if($('#newNatureProduct'))$('#newNatureProduct').onclick=()=>productModal(null);if($('#openNatureAnalysis'))$('#openNatureAnalysis').onclick=()=>setView('product_analysis');bindQuickProductControls();bindPromoQuickControls();bindProductSearch(renderNatureSpices);bindProductSort(renderNatureSpices);$$('[data-analysis-product]').forEach(b=>b.onclick=()=>isAdmin()?setView('product_analysis'):productModal(b.dataset.analysisProduct))}
function renderShopDesign(){if(!isAdmin())return setView('dashboard');const active=state.settings?.active_theme||'standard',themes=[['standard','Standard','Aktuelles Räucherhaken24-Design','Dein jetziges Design bleibt unverändert.'],['weihnachten','Weihnachten','Tannengrün · Weihnachtsrot · Schnee','Festliche Akzente ohne den Shop neu zu gestalten.'],['nikolaus','Nikolaus','Rot · Creme · Zuckerstangen-Akzent','Kurzfristige Nikolaus-Aktion.'],['ostern','Ostern','Pastell · Frühlingsgrün · Gelb','Heller saisonaler Frühlingslook.'],['advent','Adventszeit','Nachtblau · Gold','Ruhiges, hochwertiges Adventsdesign.'],['black_week','Black Week','Graphit · Silber','Reduziertes Aktionsdesign für die ganze Woche.'],['black_friday','Black Friday','Schwarz · Rot','Deutlicher Verkaufsaktions-Look.'],['silvester','Silvester','Mitternachtsblau · Silber','Festliche Jahresabschluss-Akzente.'],['neujahr','Neujahr','Champagner · Gold · Weiß','Helles Start-ins-neue-Jahr-Design.']];ws.innerHTML=`<div class="sectionHead"><div><h2>Shop-Design auf Knopfdruck</h2><p>Es kann immer genau ein Design aktiv sein. Standard ist und bleibt das heutige Shopdesign.</p></div><a class="btn" href="../shop.html" target="_blank" rel="noopener">Shop ansehen ↗</a></div><div class="themeGrid">${themes.map(t=>`<article class="themeChoice themePreview-${t[0]} ${active===t[0]?'active':''}"><div class="themeSwatch"><i></i><i></i><i></i></div><label class="themeToggle"><input type="checkbox" data-theme-choice="${t[0]}" ${active===t[0]?'checked':''}><span><b>${t[1]}</b><small>${t[2]}</small></span></label><p>${t[3]}</p>${active===t[0]?'<strong class="activeThemeTag">AKTIV</strong>':''}</article>`).join('')}</div><div class="moduleNotice"><b>Sicher:</b> Ein Saison-Design verändert nur Farben, Akzente und einen kleinen Saisonhinweis. Navigation, Produkte, Warenkorb und Grundlayout bleiben identisch.</div>`;$$('[data-theme-choice]').forEach(cb=>cb.onchange=async()=>{if(!cb.checked){cb.checked=true;return}try{const d=await api({action:'theme_save',theme:cb.dataset.themeChoice});state.settings.active_theme=d.theme;try{localStorage.setItem('rh24_active_theme_v55',d.theme)}catch(_e){}toast('Shop-Design live aktiviert: '+cb.closest('article').querySelector('b').textContent);renderShopDesign()}catch(e){toast(e.message,true);renderShopDesign()}})}


/* V58 · Produktionsleitstand */
const productionSteps=[
  ['planung','Planung',0],['material','Material',15],['cut','Zuschnitt',28],['form','Biegen / Formen',42],['point','Spitze / Schleifen',55],['solder','Löten',65],['clean','Reinigung',72],['quality','Qualitätskontrolle',80],['pack','Verpacken',90],['ready','Versandbereit',100]
];
const productionStations=['Planung','Materiallager','Drahtzuschnitt','Biegeplatz','Schleifplatz','Lötplatz','Reinigung / Beize','Qualitätsprüfung','Verpackung'];
const productionPriorities={low:['Niedrig','p-low',1],normal:['Normal','p-normal',2],high:['Hoch','p-high',3],urgent:['Eilig','p-urgent',4]};
function productionUsers(){return (state.production_users||[]).filter(u=>u.status==='active')}
function productionUserName(id,fallback=''){const u=(state.production_users||[]).find(x=>x.id===id);return u?.display_name||fallback||'–'}
function productionAssigneeOptions(current=''){return `<option value="">Noch nicht zugewiesen</option>`+productionUsers().map(u=>`<option value="${esc(u.id)}" ${sel(current,u.id)}>${esc(u.display_name)}</option>`).join('')}
function productionLastWorker(o){return o.production_last_worker_name||'Noch kein Arbeitsschritt protokolliert'}
function productionActivityLabel(a){return a.event_type==='assignment'?'Zuweisung':a.event_type==='step'?'Arbeitsschritt':'Aktualisierung'}
function productionExecutionView(){const logs=state.production_activity||[],workers=productionUsers(),todayKey=new Date().toISOString().slice(0,10),today=logs.filter(a=>String(a.created_at||'').slice(0,10)===todayKey),prodLogs=logs.filter(a=>a.worker_role==='production'),orders=state.orders||[];return `<div class="productionExecutionHead"><div><small>AUSFÜHRUNGSNACHWEIS</small><h3>Wer hat welchen Arbeitsschritt ausgeführt?</h3><p>Jede Änderung durch ein Produktionskonto wird automatisch mit Mitarbeiter, Auftrag, Arbeitsplatz, Schritt, Fortschritt und Zeitpunkt protokolliert.</p></div><button class="btn" id="productionBackOrders">← Aufträge</button></div><div class="productionMetricGrid">${productionKpiCard('Produktionsmitarbeiter',workers.length,'aktive Konten')}${productionKpiCard('Arbeitsschritte heute',today.filter(a=>a.worker_role==='production').length,'automatisch protokolliert')}${productionKpiCard('Aktive Zuordnungen',orders.filter(o=>o.production_assignee_user_id&&!['ready','shipped','complete','cancelled'].includes(o.status)).length,'laufende Aufträge')}${productionKpiCard('Nachweise gesamt',prodLogs.length,'letzte 350 Einträge')}</div><div class="panel tableWrap"><table class="dataTable productionExecutionTable"><thead><tr><th>Zeitpunkt</th><th>Mitarbeiter</th><th>Auftrag</th><th>Kunde</th><th>Arbeitsschritt</th><th>Arbeitsplatz</th><th>Verantwortlich</th><th>Ausgeführt von</th><th>Fortschritt</th><th>Typ</th></tr></thead><tbody>${prodLogs.length?prodLogs.map(a=>{const o=orders.find(x=>(x.order_no||x.id)===a.order_no);return `<tr><td>${dt(a.created_at)}</td><td><b>${esc(a.worker_name||'–')}</b><small class="tableSub">Produktion</small></td><td><button class="rowLink" data-open-order="${esc(a.order_no)}">${esc(a.order_no)}</button></td><td>${esc(o?orderCustomer(o):'–')}</td><td><b>${esc(prodStepMeta(a.production_step)[1])}</b></td><td>${esc(a.station||'–')}</td><td><b>${Number(a.progress||0)}%</b></td><td>${esc(productionActivityLabel(a))}</td></tr>`}).join(''):`<tr><td colspan="8"><div class="empty"><b>Noch keine Ausführung protokolliert</b>Sobald ein Produktionsmitarbeiter einen Arbeitsschritt speichert, erscheint er hier.</div></td></tr>`}</tbody></table></div>`}
function renderProductionStaff(){if(!isAdmin())return setView('production');const users=state.production_users||[],orders=state.orders||[],logs=state.production_activity||[],todayKey=new Date().toISOString().slice(0,10);ws.innerHTML=`<div class="sectionHead"><div><h2>Produktionsteam</h2><p>Produktionsmitarbeiter als eigene Orgaboard-Rolle verwalten – vollständig getrennt vom Außendienst.</p></div><button class="btn primary" id="newProductionUser">+ Produktionsmitarbeiter</button></div><div class="productionStaffNotice"><b>Klare Trennung:</b><span>Produktionsmitarbeiter erhalten keine Außendienstnummer, kein Vertriebsgebiet und keine Provisionsstatistik. Ihre Arbeit wird stattdessen pro Fertigungsschritt dokumentiert.</span></div><div class="productionMetricGrid">${productionKpiCard('Aktiv',users.filter(u=>u.status==='active').length,users.length+' Konten')}${productionKpiCard('Zugewiesene Aufträge',orders.filter(o=>o.production_assignee_user_id&&!['ready','shipped','complete','cancelled'].includes(o.status)).length,'laufende Fertigung')}${productionKpiCard('Schritte heute',logs.filter(a=>a.worker_role==='production'&&String(a.created_at||'').slice(0,10)===todayKey).length,'Ausführungsnachweise')}</div><div class="panel tableWrap"><table class="dataTable productionStaffTable"><thead><tr><th>Mitarbeiter</th><th>Benutzername</th><th>Status</th><th>Aktive Aufträge</th><th>Letzte Ausführung</th><th>Letzter Login</th><th></th></tr></thead><tbody>${users.length?users.map(u=>{const active=orders.filter(o=>o.production_assignee_user_id===u.id&&!['ready','shipped','complete','cancelled'].includes(o.status));const last=logs.find(a=>a.worker_user_id===u.id&&a.worker_role==='production');return `<tr><td><b>${esc(u.display_name)}</b><small class="tableSub">${esc(u.email||'')}</small></td><td>${esc(u.username)}</td><td>${u.status==='active'?'<span class="status s-paid">Aktiv</span>':'<span class="status s-cancelled">Inaktiv</span>'}</td><td><b>${active.length}</b>${active.length?`<small class="tableSub">${active.slice(0,3).map(o=>esc(o.order_no||o.id)).join(' · ')}</small>`:''}</td><td>${last?`${esc(prodStepMeta(last.production_step)[1])}<small class="tableSub">${dt(last.created_at)}</small>`:'–'}</td><td>${dt(u.last_login)}</td><td><button class="miniBtn" data-user="${esc(u.id)}">Bearbeiten</button></td></tr>`}).join(''):`<tr><td colspan="7"><div class="empty"><b>Noch kein Produktionsmitarbeiter</b>Lege den ersten Mitarbeiter mit eigenem Orgaboard-Zugang an.</div></td></tr>`}</tbody></table></div>`;$('#newProductionUser').onclick=()=>userModal(null,'production')}

function prodStepMeta(step){return productionSteps.find(x=>x[0]===step)||productionSteps[0]}
function prodPriorityMeta(p){return productionPriorities[p]||productionPriorities.normal}
function prodPriorityBadge(p){const m=prodPriorityMeta(p);return `<span class="productionPriority ${m[1]}">${m[0]}</span>`}
function prodDueMeta(o){if(!o.production_due_at)return {label:'Kein Termin',cls:'none',days:9999};const d=new Date(o.production_due_at),today=new Date();today.setHours(0,0,0,0);const dd=new Date(d);dd.setHours(0,0,0,0);const days=Math.round((dd-today)/86400000);return {label:days<0?`${Math.abs(days)} T. überfällig`:days===0?'Heute':days===1?'Morgen':d.toLocaleDateString('de-DE'),cls:days<0?'overdue':days===0?'today':days<=3?'soon':'normal',days}}
function prodProgress(o){const stored=Number(o.production_progress||0),fallback=prodStepMeta(o.production_step)[2];return Math.max(stored,fallback)}
function prodProgressColor(p){const v=Math.max(0,Math.min(100,Number(p)||0));const hue=Math.round(4+(v/100)*126);return `hsl(${hue} 68% ${v>70?35:43}%)`}
function productionPhaseOf(o){const p=prodProgress(o);if(o.status==='ready'||o.production_step==='ready'||p>=100)return'ready';if(o.status==='quality'||o.production_step==='quality')return'quality';if((o.production_started_at||p>0)&&p<100)return'started';if(o.payment_status!=='paid')return'payment';return'waiting'}
function productionFilteredOrders(){
  let rows=(state.orders||[]).filter(o=>!['complete','cancelled','shipped'].includes(o.status));
  const q=productionSearchTerm.toLowerCase().trim();if(q)rows=rows.filter(o=>`${o.order_no||o.id} ${orderCustomer(o)} ${orderEmail(o)} ${o.customer?.phone||''} ${o.customer?.city||''} ${productionItemSummary(o).replace(/<[^>]*>/g,' ')} ${o.production_station||''} ${o.production_assignee||''} ${o.production_last_worker_name||''}`.toLowerCase().includes(q));
  if(productionPhaseFilter!=='all')rows=rows.filter(o=>productionPhaseOf(o)===productionPhaseFilter);
  if(productionPriorityFilter!=='all')rows=rows.filter(o=>(o.production_priority||'normal')===productionPriorityFilter);
  if(productionStationFilter!=='all')rows=rows.filter(o=>(o.production_station||'')===productionStationFilter);
  if(productionStepFilter!=='all')rows=rows.filter(o=>(o.production_step||'planung')===productionStepFilter);
  if(productionDueFilter!=='all')rows=rows.filter(o=>{const dm=prodDueMeta(o);return productionDueFilter==='overdue'?dm.days<0:productionDueFilter==='today'?dm.days===0:productionDueFilter==='week'?dm.days>=0&&dm.days<=7:productionDueFilter==='unscheduled'?!o.production_due_at:true});
  rows.sort((a,b)=>{if(productionSortMode==='due')return prodDueMeta(a).days-prodDueMeta(b).days;if(productionSortMode==='progress')return prodProgress(a)-prodProgress(b);if(productionSortMode==='created')return new Date(a.created_at)-new Date(b.created_at);const pa=prodPriorityMeta(a.production_priority)[2],pb=prodPriorityMeta(b.production_priority)[2];return (pb-pa)||(prodDueMeta(a).days-prodDueMeta(b).days)});return rows;
}
function productionKpiCard(label,value,sub,cls=''){return `<article class="productionMetric ${cls}"><small>${label}</small><strong>${value}</strong><span>${sub}</span></article>`}
function productionProgressBar(o){const p=prodProgress(o),step=prodStepMeta(o.production_step),c=prodProgressColor(p);return `<div class="productionProgress"><div class="productionProgressTop"><b>${esc(step[1])}</b><span>${p}%</span></div><div class="productionProgressTrack" role="progressbar" aria-valuemin="0" aria-valuemax="100" aria-valuenow="${p}"><i style="width:${p}%;--progress-color:${c}"></i></div></div>`}
function productionPhaseTabs(all){const counts={all:all.length,payment:all.filter(o=>productionPhaseOf(o)==='payment').length,waiting:all.filter(o=>productionPhaseOf(o)==='waiting').length,started:all.filter(o=>productionPhaseOf(o)==='started').length,quality:all.filter(o=>productionPhaseOf(o)==='quality').length,ready:all.filter(o=>productionPhaseOf(o)==='ready').length};const tabs=[['all','Alle Aufträge'],['payment','Zahlung offen'],['waiting','Wartet auf Start'],['started','Arbeit begonnen'],['quality','Qualitätsprüfung'],['ready','Versandbereit']];return `<div class="productionPhaseTabs">${tabs.map(([key,label])=>`<button type="button" class="${productionPhaseFilter===key?'active':''}" data-production-phase="${key}"><span>${label}</span><b>${counts[key]}</b></button>`).join('')}</div>`}
function productionFilterBar(rows){const activeStations=[...new Set((state.orders||[]).map(o=>o.production_station).filter(Boolean))].sort();return `<div class="productionFilters"><div class="productionSearch"><span>⌕</span><input id="productionSearch" type="search" value="${esc(productionSearchTerm)}" placeholder="Auftrag, Kunde, Artikel, Arbeitsplatz oder Mitarbeiter …"><button id="productionSearchClear" type="button">×</button></div><select id="productionPriorityFilter"><option value="all">Alle Prioritäten</option>${Object.entries(productionPriorities).map(([k,v])=>`<option value="${k}" ${sel(productionPriorityFilter,k)}>${v[0]}</option>`).join('')}</select><select id="productionStationFilter"><option value="all">Alle Arbeitsplätze</option>${activeStations.map(x=>`<option ${sel(productionStationFilter,x)}>${esc(x)}</option>`).join('')}</select><select id="productionDueFilter"><option value="all" ${sel(productionDueFilter,'all')}>Alle Termine</option><option value="overdue" ${sel(productionDueFilter,'overdue')}>Überfällig</option><option value="today" ${sel(productionDueFilter,'today')}>Heute fällig</option><option value="week" ${sel(productionDueFilter,'week')}>Nächste 7 Tage</option><option value="unscheduled" ${sel(productionDueFilter,'unscheduled')}>Ohne Termin</option></select><select id="productionSort"><option value="priority_due" ${sel(productionSortMode,'priority_due')}>Priorität + Termin</option><option value="due" ${sel(productionSortMode,'due')}>Termin zuerst</option><option value="progress" ${sel(productionSortMode,'progress')}>Fortschritt</option><option value="created" ${sel(productionSortMode,'created')}>Älteste zuerst</option></select><b class="productionResultCount">${rows.length} Aufträge</b></div>`}
function productionStationBoard(rows){return `<div class="productionStationGrid">${productionSteps.slice(1).map(([key,label])=>{const items=rows.filter(o=>(o.production_step||'planung')===key),pct=rows.length?Math.round(items.length/rows.length*100):0;return `<button class="productionStationCard ${productionStepFilter===key?'active':''}" data-step-filter="${key}" type="button"><span>${label}</span><strong>${items.length}</strong><small>${pct}% der offenen Produktion</small><i><em style="width:${pct}%"></em></i></button>`}).join('')}</div>`}
function productionCustomerCell(o){const c=o.customer||{};return `<div class="productionCustomer"><b>${esc(orderCustomer(o))}</b><span>${esc(c.company||'')}</span><small>${esc(c.email||'')}${c.phone?` · ${esc(c.phone)}`:''}</small><small>${esc(c.zip||'')} ${esc(c.city||'')}</small></div>`}
function productionTableV58(rows){return `<div class="panel tableWrap productionLandscape"><table class="dataTable productionTableV58"><thead><tr><th>Priorität</th><th>Auftrag / Termin</th><th>Kunde / Kontakt</th><th>Artikel / Menge</th><th>Fortschritt</th><th>Arbeitsplatz</th><th>Status</th><th>Verantwortlich</th><th>Ausgeführt von</th><th>Notiz</th><th>Aktionen</th></tr></thead><tbody>${rows.length?rows.map(o=>{const dm=prodDueMeta(o),waiting=productionPhaseOf(o)==='waiting',edit=can('edit_production');return `<tr class="productionRow ${dm.cls}"><td>${prodPriorityBadge(o.production_priority||'normal')}</td><td><button class="rowLink" data-open-order="${esc(o.order_no||o.id)}">${esc(o.order_no||o.id)}</button><small class="productionDue ${dm.cls}">${esc(dm.label)}</small></td><td>${productionCustomerCell(o)}</td><td class="productionItems">${productionItemSummary(o)}</td><td>${productionProgressBar(o)}</td><td><b>${esc(o.production_station||'Noch nicht zugewiesen')}</b><small class="tableSub">${esc(prodStepMeta(o.production_step)[1])}</small></td><td>${status(o.status)}</td><td><b>${esc(productionUserName(o.production_assignee_user_id,o.production_assignee))}</b></td><td><div class="productionWorkerCell"><b>${esc(productionLastWorker(o))}</b>${o.production_last_work_at?`<small>${dt(o.production_last_work_at)}</small>`:''}</div></td><td><span class="productionNoteCell">${esc(o.production_note||'–')}</span></td><td><div class="productionActions">${edit&&waiting&&o.payment_status==='paid'?(o.production_assignee_user_id?`<button class="miniBtn startWorkBtn" data-production-start="${esc(o.order_no||o.id)}">▶ Arbeit beginnen</button>`:state.current_user?.role==='production'?`<button class="miniBtn startWorkBtn" data-production-start="${esc(o.order_no||o.id)}">▶ Arbeit übernehmen</button>`:`<button class="miniBtn assignWorkerBtn" data-production-edit="${esc(o.order_no||o.id)}">+ Mitarbeiter zuweisen</button>`):''}${edit?`<button class="miniBtn primaryMini" data-production-edit="${esc(o.order_no||o.id)}">Steuern</button><button class="miniBtn" data-production-next="${esc(o.order_no||o.id)}" title="Nächsten Produktionsschritt setzen">→</button>`:''}<button class="miniBtn" data-production-print="${esc(o.order_no||o.id)}" title="Produktionszettel drucken">⎙</button></div></td></tr>`}).join(''):`<tr><td colspan="11"><div class="empty"><b>Keine Aufträge in dieser Auswahl</b>Filter ändern oder neue Aufträge übernehmen.</div></td></tr>`}</tbody></table></div>`}
function renderProduction(){
  const all=(state.orders||[]).filter(o=>!['complete','cancelled','shipped'].includes(o.status)),rows=productionFilteredOrders();const overdue=all.filter(o=>prodDueMeta(o).days<0).length,quality=all.filter(o=>o.status==='quality'||o.production_step==='quality').length,ready=all.filter(o=>o.status==='ready'||o.production_step==='ready').length,started=all.filter(o=>productionPhaseOf(o)==='started').length,waiting=all.filter(o=>productionPhaseOf(o)==='waiting').length,paymentOpen=all.filter(o=>productionPhaseOf(o)==='payment').length,avg=all.length?Math.round(all.reduce((a,o)=>a+prodProgress(o),0)/all.length):0;
  const critical=[...all].sort((a,b)=>(prodPriorityMeta(b.production_priority)[2]-prodPriorityMeta(a.production_priority)[2])||(prodDueMeta(a).days-prodDueMeta(b).days)).slice(0,4);
  const modeTabs=`<div class="productionModeTabs"><button type="button" class="${productionSectionMode==='orders'?'active':''}" id="productionOrdersTab">▤ Aufträge</button><button type="button" class="${productionSectionMode==='execution'?'active':''}" id="productionExecutionTab">✓ Ausgeführt von</button>${isAdmin()?'<button type="button" id="productionTeamTab">♟ Produktionsteam</button>':''}</div>`;
  if(productionSectionMode==='execution'){ws.innerHTML=`<div class="productionHero v67"><div><small>PRODUKTIONSLEITSTAND · V68</small><h2>Ausführung lückenlos dokumentiert.</h2><p>Der Mitarbeiter wird nicht eingetippt: Das eingeloggte Produktionskonto wird beim Arbeitsschritt automatisch protokolliert.</p></div></div>${modeTabs}${productionExecutionView()}`;$('#productionOrdersTab').onclick=()=>{productionSectionMode='orders';renderProduction()};$('#productionExecutionTab').onclick=()=>{};if($('#productionTeamTab'))$('#productionTeamTab').onclick=()=>setView('production_staff');if($('#productionBackOrders'))$('#productionBackOrders').onclick=()=>{productionSectionMode='orders';renderProduction()};return}
  ws.innerHTML=`<div class="productionHero v67"><div><small>PRODUKTIONSLEITSTAND · V68</small><h2>Jeder Kundenauftrag sichtbar. Jeder Arbeitsschritt einem Mitarbeiter zugeordnet.</h2><p>Bestellte Artikel, Kunde, Verantwortlicher, tatsächliche Ausführung und Fertigungsfortschritt werden in einem Ablauf geführt. Produktion ist organisatorisch vom Außendienst getrennt.</p></div><div class="productionHeroActions"><button class="btn" id="productionPrintList">⎙ Tagesliste</button><button class="btn ${productionViewMode==='table'?'primary':''}" id="productionTableView">▤ Leitstand</button><button class="btn ${productionViewMode==='board'?'primary':''}" id="productionBoardView">▦ Kanban</button></div></div>${modeTabs}<div class="productionMetricGrid">${productionKpiCard('Zahlung offen',paymentOpen,'noch nicht freigegeben',paymentOpen?'warn':'')}${productionKpiCard('Wartet auf Start',waiting,'bezahlt / noch nicht begonnen',waiting?'warn':'')}${productionKpiCard('Arbeit begonnen',started,'aktive Fertigung')}${productionKpiCard('Überfällig',overdue,'Termin überschritten',overdue?'danger':'')}${productionKpiCard('Qualitätsprüfung',quality,'in QS')}${productionKpiCard('Ø Fortschritt',avg+'%','gesamte Produktion')}</div>${critical.length?`<div class="productionFocus"><div><small>HEUTE IM FOKUS</small><b>${critical.map(o=>`${esc(o.order_no||o.id)} · ${esc(orderCustomer(o))}`).join(' &nbsp; • &nbsp; ')}</b></div><span>${ready} versandbereit</span></div>`:''}${productionPhaseTabs(all)}${productionStationBoard(all)}${productionFilterBar(rows)}${productionViewMode==='table'?productionTableV58(rows):productionBoardV58(rows)}`;
  $('#productionOrdersTab').onclick=()=>{};$('#productionExecutionTab').onclick=()=>{productionSectionMode='execution';renderProduction()};if($('#productionTeamTab'))$('#productionTeamTab').onclick=()=>setView('production_staff');bindProductionV58(rows);
}
function productionBoardV58(rows){const groups=productionSteps.slice(1);return `<div class="kanban productionKanbanV58">${groups.map(([key,label])=>`<section class="kanCol"><div class="kanHead"><b>${label}</b><span>${rows.filter(o=>(o.production_step||'planung')===key).length}</span></div><div class="kanBody">${rows.filter(o=>(o.production_step||'planung')===key).map(o=>`<article class="productionBoardCard"><div>${prodPriorityBadge(o.production_priority||'normal')}<small>${esc(prodDueMeta(o).label)}</small></div><button class="rowLink" data-open-order="${esc(o.order_no||o.id)}">${esc(o.order_no||o.id)}</button><b>${esc(orderCustomer(o))}</b><small>${esc(o.customer?.city||'')} · ${esc(o.customer?.email||'')}</small>${productionProgressBar(o)}<small>${esc(o.production_station||'Kein Arbeitsplatz')}</small><small><b>Verantwortlich:</b> ${esc(productionUserName(o.production_assignee_user_id,o.production_assignee))}</small><small><b>Letzte Ausführung:</b> ${esc(productionLastWorker(o))}</small>${can('edit_production')&&productionPhaseOf(o)==='waiting'&&o.payment_status==='paid'?(o.production_assignee_user_id?`<button class="miniBtn startWorkBtn" data-production-start="${esc(o.order_no||o.id)}">▶ Arbeit beginnen</button>`:state.current_user?.role==='production'?`<button class="miniBtn startWorkBtn" data-production-start="${esc(o.order_no||o.id)}">▶ Arbeit übernehmen</button>`:`<button class="miniBtn assignWorkerBtn" data-production-edit="${esc(o.order_no||o.id)}">+ Mitarbeiter zuweisen</button>`):''}${can('edit_production')?`<button class="miniBtn primaryMini" data-production-edit="${esc(o.order_no||o.id)}">Steuern</button>`:''}</article>`).join('')||'<div class="empty smallEmpty">Keine Aufträge</div>'}</div></section>`).join('')}</div>`}
function bindProductionV58(rows){
  const re=()=>renderProduction();$('#productionTableView').onclick=()=>{productionViewMode='table';re()};$('#productionBoardView').onclick=()=>{productionViewMode='board';re()};$('#productionSearch').oninput=e=>{productionSearchTerm=e.target.value;re();setTimeout(()=>$('#productionSearch')?.focus(),0)};$('#productionSearchClear').onclick=()=>{productionSearchTerm='';re()};$('#productionPriorityFilter').onchange=e=>{productionPriorityFilter=e.target.value;re()};$('#productionStationFilter').onchange=e=>{productionStationFilter=e.target.value;re()};$('#productionDueFilter').onchange=e=>{productionDueFilter=e.target.value;re()};$('#productionSort').onchange=e=>{productionSortMode=e.target.value;re()};$('#productionPrintList').onclick=()=>printProductionList(rows);$$('[data-production-phase]').forEach(b=>b.onclick=()=>{productionPhaseFilter=b.dataset.productionPhase;productionStepFilter='all';re()});$$('[data-step-filter]').forEach(b=>b.onclick=()=>{const key=b.dataset.stepFilter;productionStepFilter=productionStepFilter===key?'all':key;productionPhaseFilter='all';productionViewMode='table';re()});$$('[data-production-start]').forEach(b=>b.onclick=()=>productionStartWork(b.dataset.productionStart));$$('[data-production-edit]').forEach(b=>b.onclick=()=>productionEditModal(b.dataset.productionEdit));$$('[data-production-next]').forEach(b=>b.onclick=()=>productionNextStep(b.dataset.productionNext));$$('[data-production-print]').forEach(b=>b.onclick=()=>printProductionTicket(b.dataset.productionPrint));
}
async function productionStartWork(id){if(!can('edit_production'))return toast('Keine Berechtigung.',true);const o=(state.orders||[]).find(x=>(x.order_no||x.id)===id);if(!o)return;if(o.payment_status!=='paid')return toast('Arbeit kann erst nach bestätigtem Zahlungseingang begonnen werden.',true);try{await api({action:'production_update',id,priority:o.production_priority||'normal',due_at:o.production_due_at||'',station:o.production_station||'Materiallager',step:'material',progress:15,assignee_user_id:o.production_assignee_user_id||(state.current_user?.role==='production'?state.current_user.id:''),note:o.production_note||''});toast('Arbeit begonnen · Kunde sieht jetzt 15 % Fortschritt');await load(true);if(current==='production')renderProduction()}catch(e){toast(e.message,true)}}
function dateTimeLocalValue(v){if(!v)return'';const d=new Date(v);if(Number.isNaN(d.getTime()))return'';const pad=n=>String(n).padStart(2,'0');return `${d.getFullYear()}-${pad(d.getMonth()+1)}-${pad(d.getDate())}T${pad(d.getHours())}:${pad(d.getMinutes())}`}
function productionEditModal(id){if(!can('edit_production'))return toast('Keine Berechtigung zur Produktionssteuerung.',true);const o=(state.orders||[]).find(x=>(x.order_no||x.id)===id);if(!o)return;const progress=prodProgress(o),isProd=state.current_user?.role==='production',assigneeCurrent=o.production_assignee_user_id||(isProd?state.current_user.id:'');openModal(`<div class="modalHead productionModalHead"><small>PRODUKTIONSSTEUERUNG · ${esc(id)}</small><h2>${esc(orderCustomer(o))}</h2><p>${productionItemSummary(o).replace(/<[^>]*>/g,' · ')}</p></div><div class="modalBody"><div class="productionIdentityBar"><div><small>VERANTWORTLICH</small><b>${esc(productionUserName(assigneeCurrent,o.production_assignee))}</b></div><div><small>ZULETZT AUSGEFÜHRT VON</small><b>${esc(productionLastWorker(o))}</b><span>${o.production_last_work_at?dt(o.production_last_work_at):'Noch kein Schritt protokolliert'}</span></div></div><div class="productionEditGrid"><label>Priorität<select id="prodPriority">${Object.entries(productionPriorities).map(([k,v])=>`<option value="${k}" ${sel(o.production_priority||'normal',k)}>${v[0]}</option>`).join('')}</select></label><label>Fertigstellung<input id="prodDue" type="datetime-local" value="${dateTimeLocalValue(o.production_due_at)}"></label><label>Arbeitsplatz / Station<select id="prodStation"><option value="">Noch nicht zugewiesen</option>${productionStations.map(x=>`<option ${sel(o.production_station,x)}>${esc(x)}</option>`).join('')}</select></label><label>Produktionsschritt<select id="prodStep">${productionSteps.map(([k,l])=>`<option value="${k}" ${sel(o.production_step||'planung',k)}>${l}</option>`).join('')}</select></label><label>Produktionsmitarbeiter<select id="prodAssignee" ${isProd?'disabled':''}>${productionAssigneeOptions(assigneeCurrent)}</select></label><label>Fortschritt<input id="prodProgress" type="number" min="0" max="100" step="5" value="${progress}"></label><label class="full">Interne Produktionsnotiz<textarea id="prodNote" placeholder="Material, Besonderheiten, Rückfragen, Qualitätsanforderungen …">${esc(o.production_note||'')}</textarea></label></div><div class="productionStepTimeline">${productionSteps.map(([k,l,p])=>`<button type="button" data-modal-step="${k}" class="${productionSteps.findIndex(x=>x[0]===k)<=productionSteps.findIndex(x=>x[0]===(o.production_step||'planung'))?'done':''}"><i></i><span>${l}</span><small>${p}%</small></button>`).join('')}</div><div class="moduleNotice"><b>Automatischer Ausführungsnachweis:</b> Beim Speichern eines geänderten Arbeitsschritts wird das aktuell eingeloggte Produktionskonto mit Zeitpunkt protokolliert. Administratorische Planung bleibt davon getrennt.</div><div class="formActions"><button class="btn" id="prodOpenOrder">Auftrag öffnen</button><button class="btn" id="prodPrint">⎙ Produktionszettel</button><button class="btn primary" id="prodSave">Produktion speichern</button></div></div>`);$$('[data-modal-step]').forEach(b=>b.onclick=()=>{const m=prodStepMeta(b.dataset.modalStep);$('#prodStep').value=m[0];$('#prodProgress').value=m[2]});$('#prodOpenOrder').onclick=()=>{closeModal();openOrder(id)};$('#prodPrint').onclick=()=>printProductionTicket(id);$('#prodSave').onclick=async()=>{try{await api({action:'production_update',id,priority:$('#prodPriority').value,due_at:$('#prodDue').value,station:$('#prodStation').value,step:$('#prodStep').value,progress:+$('#prodProgress').value,assignee_user_id:isProd?(assigneeCurrent||state.current_user.id):$('#prodAssignee').value,note:$('#prodNote').value});toast('Produktionsdaten + Ausführungsnachweis gespeichert');closeModal();await load(true);if(current==='production')renderProduction()}catch(e){toast(e.message,true)}}}
async function productionNextStep(id){if(!can('edit_production'))return toast('Keine Berechtigung.',true);const o=(state.orders||[]).find(x=>(x.order_no||x.id)===id);if(!o)return;let i=productionSteps.findIndex(x=>x[0]===(o.production_step||'planung'));i=Math.min(productionSteps.length-1,i+1);const next=productionSteps[i];try{await api({action:'production_update',id,priority:o.production_priority||'normal',due_at:o.production_due_at||'',station:o.production_station||'',step:next[0],progress:next[2],assignee_user_id:o.production_assignee_user_id||(state.current_user?.role==='production'?state.current_user.id:''),note:o.production_note||''});toast('Weiter: '+next[1]);await load(true);if(current==='production')renderProduction()}catch(e){toast(e.message,true)}}
function productionPrintHtml(o){const items=(o.items||[]).map(it=>`<tr><td>${esc(it.article_no||it.sku||it.id||'')}</td><td><b>${esc(it.name||it.id||'Artikel')}</b><br><small>${esc(Object.values(it.meta||{}).filter(Boolean).join(' · '))}</small></td><td>${Number(it.qty||1)}</td></tr>`).join('');return `<h1>Produktionszettel</h1><div class="facts"><span><small>Auftrag</small><b>${esc(o.order_no||o.id)}</b></span><span><small>Kunde</small><b>${esc(orderCustomer(o))}</b></span><span><small>Priorität</small><b>${esc(prodPriorityMeta(o.production_priority)[0])}</b></span><span><small>Fertig bis</small><b>${esc(prodDueMeta(o).label)}</b></span><span><small>Arbeitsplatz</small><b>${esc(o.production_station||'–')}</b></span><span><small>Verantwortlich</small><b>${esc(productionUserName(o.production_assignee_user_id,o.production_assignee))}</b></span><span><small>Letzte Ausführung</small><b>${esc(productionLastWorker(o))}</b></span></div><h2>${esc(prodStepMeta(o.production_step)[1])} · ${prodProgress(o)}%</h2><table><thead><tr><th>Art.-Nr.</th><th>Artikel</th><th>Menge</th></tr></thead><tbody>${items}</tbody></table><div class="note"><b>Produktionsnotiz</b><p>${esc(o.production_note||'Keine')}</p></div><div class="checks"><span>□ Material geprüft</span><span>□ Maß / Ausführung geprüft</span><span>□ Qualität geprüft</span><span>□ Verpackung geprüft</span></div>`}
function openPrintWindow(title,body){const w=window.open('','_blank','width=1100,height=800');if(!w)return toast('Druckfenster wurde blockiert.',true);w.document.write(`<!doctype html><html><head><meta charset="utf-8"><title>${esc(title)}</title><style>body{font-family:Arial,sans-serif;color:#24170f;padding:28px}h1{font-size:28px;margin:0 0 20px}h2{margin-top:24px}.facts{display:grid;grid-template-columns:repeat(3,1fr);gap:10px}.facts span{border:1px solid #ddd;padding:12px}.facts small{display:block;color:#777;text-transform:uppercase;font-size:9px}table{width:100%;border-collapse:collapse;margin:20px 0}th,td{border:1px solid #ddd;padding:10px;text-align:left}.note{border:1px solid #ddd;padding:14px}.checks{display:flex;gap:24px;margin-top:24px;font-weight:bold}@media print{body{padding:0}}</style></head><body>${body}<script>setTimeout(()=>window.print(),250)<\/script></body></html>`);w.document.close()}
function printProductionTicket(id){const o=(state.orders||[]).find(x=>(x.order_no||x.id)===id);if(o)openPrintWindow('Produktionszettel '+id,productionPrintHtml(o))}
function printProductionList(rows){const body=`<h1>Produktions-Tagesliste</h1><p>Stand: ${new Date().toLocaleString('de-DE')}</p><table><thead><tr><th>Priorität</th><th>Auftrag</th><th>Fällig</th><th>Kunde</th><th>Schritt</th><th>Arbeitsplatz</th><th>Verantwortlich</th><th>Ausgeführt von</th><th>Fortschritt</th></tr></thead><tbody>${rows.map(o=>`<tr><td>${esc(prodPriorityMeta(o.production_priority)[0])}</td><td>${esc(o.order_no||o.id)}</td><td>${esc(prodDueMeta(o).label)}</td><td>${esc(orderCustomer(o))}</td><td>${esc(prodStepMeta(o.production_step)[1])}</td><td>${esc(o.production_station||'–')}</td><td>${esc(productionUserName(o.production_assignee_user_id,o.production_assignee))}</td><td>${esc(productionLastWorker(o))}</td><td>${prodProgress(o)}%</td></tr>`).join('')}</tbody></table>`;openPrintWindow('Produktions-Tagesliste',body)}

// V68.1 hardening: authoritative production-aware admin/user functions.
function renderAdministration(){if(!isAdmin())return setView('dashboard');const users=state.users||[],activity=state.activity||[],mails=state.mail_log||[];ws.innerHTML=`<div class="sectionHead"><div><h2>Administration</h2><p>Benutzer, E-Mail-Zugänge, Rollen und sämtliche protokollierten Änderungen kontrollieren.</p></div><div class="sectionActions"><button class="btn" id="openRightsCenter">Zugriffsrechte</button><button class="btn primary" id="newUser">+ Benutzer anlegen</button></div></div><div class="adminSummary">${metric('Administratoren',users.filter(u=>u.role==='admin'&&u.status==='active').length,'Vollzugriff')}${metric('Produktions-Logins',users.filter(u=>u.role==='production'&&u.status==='active').length,'Fertigung · kein Außendienst')}${metric('Außendienst-Logins',users.filter(u=>u.role==='field_sales'&&u.status==='active').length,'Vertrieb · Provision')}${metric('Protokolleinträge',activity.length,'letzte Aktivitäten')}</div><div class="adminColumns"><section class="panel"><div class="panelTitle"><h3>Benutzer & Zugänge</h3><span>klar getrennte Rollen</span></div><div class="tableWrap"><table class="dataTable"><thead><tr><th>Benutzer</th><th>Rolle</th><th>Zuordnung</th><th>Willkommensmail</th><th>Letzter Login</th><th>Status</th><th></th></tr></thead><tbody>${users.map(u=>`<tr><td><b>${esc(u.display_name)}</b><br><small>${esc(u.username)} · ${esc(u.email||'')}</small></td><td>${userRoleBadge(u.role)}</td><td>${u.role==='field_sales'?esc(repName(u.sales_rep_id)):u.role==='production'?'<b>Produktionsteam</b>':'–'}</td><td>${u.welcome_sent_at?dt(u.welcome_sent_at):'–'}</td><td>${dt(u.last_login)}</td><td>${u.status==='active'?'Aktiv':'Inaktiv'}</td><td><div class="rowActions"><button class="miniBtn" data-user="${esc(u.id)}">Bearbeiten</button>${u.role!=='admin'?`<button class="miniBtn" data-admin-rights="${esc(u.id)}">Rechte</button><button class="miniBtn" data-resend-welcome="${esc(u.id)}">Mail senden</button>`:''}</div></td></tr>`).join('')}</tbody></table></div></section><section class="panel"><div class="panelTitle"><h3>System-E-Mails</h3><span>Versandstatus</span></div><div class="auditList">${mails.slice(0,80).map(m=>`<article><time>${dt(m.created_at)}</time><div><b>${esc(m.recipient)}</b><span>${esc(m.mail_type)} · ${m.status==='sent'?'versendet':'FEHLER'} · ${esc(m.subject)}</span></div></article>`).join('')||'<div class="empty">Noch keine System-E-Mails.</div>'}</div></section><section class="panel adminWide"><div class="panelTitle"><h3>Änderungsprotokoll</h3><span>nachvollziehbar</span></div><div class="auditList">${activity.slice(0,150).map(a=>`<article><time>${dt(a.created_at)}</time><div><b>${esc(a.actor)}</b><span>${esc(a.action_name)} · ${esc(a.entity_type)} ${esc(a.entity_id)}</span></div></article>`).join('')||'<div class="empty">Noch keine Protokolleinträge.</div>'}</div></section></div>`;$('#newUser').onclick=()=>userModal(null);$('#openRightsCenter').onclick=()=>setView('rights');$$('[data-admin-rights]').forEach(b=>b.onclick=()=>rightsModal(b.dataset.adminRights));$$('[data-resend-welcome]').forEach(b=>b.onclick=async()=>{if(!confirm('Begrüßungs- und Einrichtungslink erneut per E-Mail senden?'))return;try{await api({action:'welcome_resend',id:b.dataset.resendWelcome});toast('Begrüßungsmail versendet');await load(true);renderAdministration()}catch(e){toast(e.message,true)}})}
function userModal(id,forcedRole=''){const u=(state.users||[]).find(x=>x.id===id)||{},reps=state.sales_reps||[],initialRole=forcedRole||u.role||'field_sales',perms=Array.isArray(u.permissions)?u.permissions:roleDefaultPerms(initialRole);openModal(`<div class="modalHead"><small>BENUTZER & RECHTE</small><h2>${id?'Benutzer bearbeiten':initialRole==='production'?'Produktionsmitarbeiter anlegen':'Benutzer anlegen'}</h2><p>${initialRole==='production'?'Eigenes Orgaboard-Konto für die Fertigung · keine Außendienst-Zuordnung.':''}</p></div><div class="modalBody"><div class="formGrid"><label>Benutzername<input id="usrName" value="${esc(u.username||'')}" placeholder="vorname.nachname"></label><label>Anzeigename<input id="usrDisplay" value="${esc(u.display_name||'')}"></label><label>E-Mail<input id="usrEmail" type="email" value="${esc(u.email||'')}"></label><label>Rolle<select id="usrRole"><option value="admin" ${sel(initialRole,'admin')}>Administrator · Vollzugriff</option><option value="production" ${sel(initialRole,'production')}>Produktion · Fertigung, kein Außendienst</option><option value="field_sales" ${sel(initialRole,'field_sales')}>Außendienst · Vertrieb & Provision</option></select></label><label class="full" id="usrRepLabel">Außendienst-Zuordnung<select id="usrRep"><option value="">Keine</option>${reps.map(r=>`<option value="${esc(r.id)}" ${sel(u.sales_rep_id||'',r.id)}>${esc(r.employee_no)} · ${esc(r.name)}</option>`).join('')}</select></label><label>Status<select id="usrStatus"><option value="active" ${sel(u.status||'active','active')}>Aktiv</option><option value="inactive" ${sel(u.status,'inactive')}>Inaktiv</option></select></label><label>Neues / temporäres Passwort<input id="usrPassword" type="password" minlength="12" placeholder="${id?'leer = unverändert':'mindestens 12 Zeichen'}"></label></div><div class="roleExplain" id="usrRoleExplain"></div><div class="formActions"><button class="btn" id="usrRights" ${id&&initialRole!=='admin'?'':'disabled'}>Detaillierte Rechte</button><button class="btn primary" id="saveUser">Benutzer speichern</button></div></div>`);const syncRole=()=>{const role=$('#usrRole').value,isField=role==='field_sales';$('#usrRep').disabled=!isField;$('#usrRepLabel').style.opacity=isField?'1':'.45';if(!isField)$('#usrRep').value='';$('#usrRoleExplain').innerHTML=role==='production'?'<b>Produktionsrolle</b><span>Produktion ansehen/bearbeiten, Lager ansehen und Nachrichten nutzen. Keine Kundendatenpflege, keine Zahlungen, keine Provision, kein Außendienst.</span>':role==='field_sales'?'<b>Außendienstrolle</b><span>Vertrieb, Kundendaten und persönliche Statistik. Produktions- und Administrationsrechte sind getrennt.</span>':'<b>Administrator</b><span>Vollzugriff auf alle Bereiche.</span>'};$('#usrRole').onchange=syncRole;syncRole();$('#usrRights').onclick=()=>{closeModal();rightsModal(u.id)};$('#saveUser').onclick=async()=>{try{const role=$('#usrRole').value;const sendPerms=id&&role===initialRole?perms:roleDefaultPerms(role);await api({action:'user_save',id:u.id||'',username:$('#usrName').value,display_name:$('#usrDisplay').value,email:$('#usrEmail').value,role,sales_rep_id:role==='field_sales'?$('#usrRep').value:'',status:$('#usrStatus').value,password:$('#usrPassword').value,permissions:sendPerms});toast(role==='production'?'Produktionsmitarbeiter gespeichert':'Benutzer gespeichert');closeModal();await load(true);if(current==='production_staff')renderProductionStaff();else renderAdministration()}catch(e){toast(e.message,true)}}}


/* V77 · Professionelles Rechnungstool */
function docTypeLabelV77(t){return t==='delivery_note'?'Lieferschein':t==='cancellation'?'Stornorechnung':'Rechnung'}
function docStatusPillV77(d){if(d.status==='issued')return '<span class="status s-paid">Ausgegeben</span>';if(d.status==='cancelled')return '<span class="status s-cancelled">Storniert</span>';return '<span class="status s-payment_pending">Entwurf</span>'}
function docEmailPillV77(d){if(d.email_status==='sent')return '<span class="invoiceMail sent">E-Mail versendet</span>';if(d.email_status==='failed')return '<span class="invoiceMail failed">E-Mail Fehler</span>';return '<span class="invoiceMail">noch nicht versendet</span>'}
function invoiceProfileReadyV77(){return Boolean(state?.invoice_readiness?.ready)}
function invoiceOrderByNoV77(no){return (state.orders||[]).find(o=>(o.order_no||o.id)===no)||null}
function invoiceDocumentRowsV77(){const q=(documentSearchTerm||'').toLowerCase().trim();return (state.documents||[]).filter(d=>{const c=d.payload?.customer||{},hay=`${d.document_no} ${d.order_no} ${docTypeLabelV77(d.document_type)} ${c.name||''} ${c.company||''} ${c.email||''}`.toLowerCase();if(q&&!hay.includes(q))return false;if(documentTypeFilter!=='all'&&d.document_type!==documentTypeFilter)return false;if(documentStatusFilter!=='all'&&d.status!==documentStatusFilter)return false;return true}).sort((a,b)=>new Date(b.created_at||0)-new Date(a.created_at||0))}
function renderDocuments(){
 const docs=invoiceDocumentRowsV77(),all=state.documents||[],orders=(state.orders||[]).filter(o=>o.status!=='cancelled'),invoices=all.filter(d=>d.document_type==='invoice'),issued=invoices.filter(d=>d.status==='issued'),sent=all.filter(d=>d.email_status==='sent'),cancelled=invoices.filter(d=>d.status==='cancelled'),missing=orders.filter(o=>!existingDocument(o.order_no||o.id,'invoice')||!existingDocument(o.order_no||o.id,'delivery_note')).length,readiness=state.invoice_readiness||{ready:false,missing:[]};
 const gross=issued.reduce((s,d)=>s+Number(d.payload?.totals?.gross||0),0);
 ws.innerHTML=`<div class="sectionHead"><div><h2>Rechnungstool</h2><p>Rechnungen, Lieferscheine, PDF, E-Mail-Versand, Storno und Dokumenthistorie zentral steuern.</p></div><div class="sectionActions">${isAdmin()?'<button class="btn" id="invoiceProfileBtn">⚙ Rechnungsdaten</button><button class="btn primary" id="invoiceGenerateMissing">Fehlende Dokumente erzeugen</button>':''}</div></div>
 <section class="invoiceHero ${readiness.ready?'ready':'warning'}"><div><small>RECHNUNGSSYSTEM · V77</small><h3>${readiness.ready?'Bereit für automatische Shop-Rechnungen':'Einrichtung noch nicht vollständig'}</h3><p>${readiness.ready?'Online-Bestellungen können Rechnung und Lieferschein automatisch als PDF erhalten. Ausgegebene Rechnungen werden gesperrt und revisionssicher protokolliert.':'Vor dem automatischen Kundenversand fehlen: '+esc((readiness.missing||[]).join(', '))}</p></div><div class="invoiceHeroState"><b>${readiness.ready?'✓ BEREIT':'! EINRICHTEN'}</b><span>${missing} Bestellung(en) ohne vollständiges Dokumentpaar</span></div></section>
 <div class="metricGrid invoiceMetrics">${metric('Rechnungen',invoices.length,issued.length+' ausgegeben')}${metric('PDF/E-Mail',sent.length,'Dokumente versendet')}${metric('Rechnungsvolumen',money(gross),'ausgegebene Rechnungen')}${metric('Storniert',cancelled.length,'mit Dokumenthistorie')}</div>
 <div class="panel invoiceToolbar"><label class="invoiceSearch"><span>⌕</span><input id="documentSearch" type="search" placeholder="Rechnungsnummer, Bestellung, Kunde, Firma, E-Mail …" value="${esc(documentSearchTerm)}"></label><select id="documentTypeFilter"><option value="all">Alle Dokumenttypen</option><option value="invoice" ${sel(documentTypeFilter,'invoice')}>Rechnungen</option><option value="delivery_note" ${sel(documentTypeFilter,'delivery_note')}>Lieferscheine</option><option value="cancellation" ${sel(documentTypeFilter,'cancellation')}>Stornorechnungen</option></select><select id="documentStatusFilter"><option value="all">Alle Status</option><option value="draft" ${sel(documentStatusFilter,'draft')}>Entwurf</option><option value="issued" ${sel(documentStatusFilter,'issued')}>Ausgegeben</option><option value="cancelled" ${sel(documentStatusFilter,'cancelled')}>Storniert</option></select><button class="btn" id="documentFilterReset">Zurücksetzen</button></div>
 <div class="panel tableWrap invoiceTableWrap">${docs.length?`<table class="dataTable invoiceTable"><thead><tr><th>Dokument</th><th>Bestellung</th><th>Kunde</th><th>Datum</th><th>Betrag</th><th>Zahlung</th><th>Status</th><th>E-Mail</th><th>Aktionen</th></tr></thead><tbody>${docs.map(d=>{const c=d.payload?.customer||{},o=invoiceOrderByNoV77(d.order_no),amount=d.document_type==='delivery_note'?null:Number(d.payload?.totals?.gross||0);return `<tr><td><b>${esc(d.document_no)}</b><small class="tableSub">${esc(docTypeLabelV77(d.document_type))} · V${Number(d.version_no||1)}</small></td><td><button class="rowLink" data-open-order="${esc(d.order_no)}">${esc(d.order_no)}</button></td><td><b>${esc(c.name||'–')}</b><small class="tableSub">${esc(c.company||c.email||'')}</small></td><td>${d.issued_at?donly(d.issued_at):donly(d.created_at)}</td><td class="money">${amount===null?'–':money(amount)}</td><td>${o?pay(o.payment_status||'pending'):'–'}</td><td>${docStatusPillV77(d)}</td><td>${docEmailPillV77(d)}</td><td><div class="rowActions"><button class="miniBtn primaryMini" data-invoice-pdf="${esc(d.id)}">PDF</button><button class="miniBtn" data-invoice-print="${esc(d.id)}">Drucken</button><button class="miniBtn" data-invoice-open="${esc(d.id)}">Öffnen</button><button class="miniBtn" data-invoice-mail="${esc(d.order_no)}">E-Mail</button></div></td></tr>`}).join('')}</tbody></table>`:'<div class="empty"><b>Keine Dokumente gefunden</b>Filter zurücksetzen oder fehlende Dokumente erzeugen.</div>'}</div>
 <div class="moduleNotice"><b>Ausgabeschutz:</b> Sobald eine Rechnung als ausgegeben markiert ist, ist sie gesperrt. Eine Stornierung erzeugt einen eigenen Stornobeleg mit eigener Nummer; die ursprüngliche Rechnung bleibt in der Historie erhalten.</div>`;
 if($('#invoiceProfileBtn'))$('#invoiceProfileBtn').onclick=invoiceProfileModalV77;if($('#invoiceGenerateMissing'))$('#invoiceGenerateMissing').onclick=documentGenerateMissingV77;
 const inp=$('#documentSearch');if(inp)inp.oninput=()=>{documentSearchTerm=inp.value;clearTimeout(documentSearchTimer);documentSearchTimer=setTimeout(()=>{renderDocuments();setTimeout(()=>{const n=$('#documentSearch');if(n){n.focus();n.setSelectionRange(n.value.length,n.value.length)}},0)},250)};
 $('#documentTypeFilter').onchange=e=>{documentTypeFilter=e.target.value;renderDocuments()};$('#documentStatusFilter').onchange=e=>{documentStatusFilter=e.target.value;renderDocuments()};$('#documentFilterReset').onclick=()=>{documentSearchTerm='';documentTypeFilter='all';documentStatusFilter='all';renderDocuments()};
 $$('[data-invoice-pdf]').forEach(b=>b.onclick=()=>openDocumentPdfV77(b.dataset.invoicePdf));$$('[data-invoice-print]').forEach(b=>b.onclick=()=>printDocumentByIdV77(b.dataset.invoicePrint));$$('[data-invoice-open]').forEach(b=>b.onclick=()=>documentModalV77((state.documents||[]).find(d=>d.id===b.dataset.invoiceOpen)));$$('[data-invoice-mail]').forEach(b=>b.onclick=()=>emailDocumentsV77(b.dataset.invoiceMail));
}
function openDocumentPdfV77(id,download=false){window.open('./document-pdf.php?id='+encodeURIComponent(id)+(download?'&download=1':''),'_blank','noopener')}
function printDocumentByIdV77(id){const d=(state.documents||[]).find(x=>x.id===id);if(!d)return;printDocument(d,d.payload||{})}
async function emailDocumentsV77(orderNo){if(!confirm('Rechnung und Lieferschein jetzt erneut per E-Mail an den Kunden senden?'))return;try{toast('PDF-Dokumente werden versendet …');await api({action:'document_email',order_no:orderNo});toast('Rechnung und Lieferschein wurden per E-Mail versendet');await load(true);if(current==='documents')renderDocuments()}catch(e){toast(e.message,true)}}
async function documentGenerateMissingV77(){const targets=(state.orders||[]).filter(o=>o.status!=='cancelled'&&(!existingDocument(o.order_no||o.id,'invoice')||!existingDocument(o.order_no||o.id,'delivery_note')));if(!targets.length)return toast('Alle Bestellungen haben bereits Rechnung und Lieferschein.');if(!confirm(targets.length+' Bestellung(en): fehlende Rechnung/Lieferschein erzeugen?'))return;let done=0;for(const o of targets){try{await api({action:'document_bundle_generate',order_no:o.order_no||o.id,issue:false});done++}catch(e){}}toast(done+' Dokumentpaare erzeugt');await load(true);renderDocuments()}
async function openDocumentForOrder(orderNo,type){try{const d=await api({action:'document_get',order_no:orderNo,type});const idx=(state.documents||[]).findIndex(x=>x.id===d.document.id);if(idx>=0)state.documents[idx]=d.document;else(state.documents||(state.documents=[])).unshift(d.document);documentModalV77(d.document)}catch(e){toast(e.message,true)}}
function documentModal(doc){return documentModalV77(doc)}
function documentModalV77(doc){if(!doc)return;const p=doc.payload||{},c=p.customer||{},items=p.items||[],tot=p.totals||{},edit=can('edit_documents')&&doc.status==='draft'&&!doc.locked_at,isInvoice=doc.document_type!=='delivery_note';openModal(`<div class="modalHead"><small>${esc(docTypeLabelV77(doc.document_type).toUpperCase())} · VERSION ${Number(doc.version_no||1)}</small><h2>${esc(doc.document_no)}</h2><p>Bestellung ${esc(doc.order_no)} · ${esc(documentStatusLabel(doc.status))}</p></div><div class="modalBody">${doc.status==='issued'?'<div class="moduleNotice"><b>Ausgegeben & gesperrt:</b> Dieses Dokument kann nicht mehr still verändert werden. Für eine Rechnung steht ein eigener Stornobeleg zur Verfügung.</div>':''}<div class="documentActionStrip"><button class="btn primary" id="docPdfV77">PDF öffnen</button><button class="btn" id="docDownloadV77">PDF herunterladen</button><button class="btn" id="docPrintV77">Drucken</button><button class="btn" id="docMailV77">Per E-Mail</button><button class="btn" id="docHistoryV77">Historie</button></div><div class="detailGrid"><div class="detailBox"><h4>Empfänger</h4>${edit?`<div class="formGrid"><label class="full">Name<input id="docName" value="${esc(c.name||'')}"></label><label class="full">Firma<input id="docCompany" value="${esc(c.company||'')}"></label><label class="full">Straße<input id="docStreet" value="${esc(c.street||'')}"></label><label>PLZ<input id="docZip" value="${esc(c.zip||'')}"></label><label>Ort<input id="docCity" value="${esc(c.city||'')}"></label><label class="full">E-Mail<input id="docEmail" value="${esc(c.email||'')}"></label></div>`:`<p><b>${esc(c.name||'')}</b><br>${esc(c.company||'')}<br>${esc(c.street||'')}<br>${esc(c.zip||'')} ${esc(c.city||'')}<br>${esc(c.email||'')}</p>`}</div><div class="detailBox"><h4>Dokumentstatus</h4><p>Nummer: <b>${esc(doc.document_no)}</b><br>Status: <b>${esc(documentStatusLabel(doc.status))}</b><br>Version: <b>${Number(doc.version_no||1)}</b><br>Ausgegeben: <b>${doc.issued_at?donly(doc.issued_at):'noch nicht'}</b><br>E-Mail: <b>${doc.email_status==='sent'?'versendet':'noch nicht / Fehler'}</b></p></div><div class="detailBox full"><h4>Positionen</h4><div id="docItems">${items.map((x,i)=>edit?`<div class="docItemEdit" data-doc-row="${i}"><input class="docArt" value="${esc(x.article_no||'')}" placeholder="Art.-Nr."><input class="docName" value="${esc(x.name||x.id||'')}" placeholder="Bezeichnung"><input class="docQty" type="number" min="0" step="1" value="${Number(x.qty||1)}"><input class="docPrice" type="number" min="0" step="0.01" value="${Number(x.unit_price||0)}"></div>`:`<div class="docItemRead"><span><b>${Number(x.qty||1)} × ${esc(x.name||x.id||'')}</b><small>${x.article_no?'Art.-Nr. '+esc(x.article_no):''}</small></span>${isInvoice?`<strong>${money(Number(x.line_total??((x.unit_price||0)*(x.qty||1))))}</strong>`:''}</div>`).join('')}</div></div>${isInvoice?`<div class="detailBox full"><h4>Summen</h4>${edit?`<div class="formGrid"><label>Warenwert<input id="docSubtotal" type="number" step="0.01" value="${Number(tot.subtotal||0)}"></label><label>Versand<input id="docShipping" type="number" step="0.01" value="${Number(tot.shipping||0)}"></label><label>MwSt.<input id="docVat" type="number" step="0.01" value="${Number(tot.vat||0)}"></label><label>Gesamt<input id="docGross" type="number" step="0.01" value="${Number(tot.gross||0)}"></label></div>`:`<div class="detailTotals"><div><span>Netto</span><b>${money(tot.net||0)}</b></div><div><span>MwSt.</span><b>${money(tot.vat||0)}</b></div><div class="grand"><span>Gesamt</span><b>${money(tot.gross||0)}</b></div></div>`}</div>`:''}<div class="detailBox full"><h4>Dokumentnotiz</h4>${edit?`<textarea id="docNote">${esc(p.document_note||'')}</textarea>`:`<p>${esc(p.document_note||'Keine Notiz').replace(/\n/g,'<br>')}</p>`}</div></div><div class="formActions">${edit?'<button class="btn" id="docSaveV77">Entwurf speichern</button><button class="btn primary" id="docIssueV77">Als ausgegeben sperren</button>':''}${doc.document_type==='invoice'&&doc.status==='issued'?'<button class="btn danger" id="docCancelV77">Stornorechnung erstellen</button>':''}<button class="btn" id="docCloseV77">Schließen</button></div></div>`);
 $('#docPdfV77').onclick=()=>openDocumentPdfV77(doc.id);$('#docDownloadV77').onclick=()=>openDocumentPdfV77(doc.id,true);$('#docPrintV77').onclick=()=>printDocument(doc,collectDocumentPayload(doc,false));$('#docMailV77').onclick=()=>emailDocumentsV77(doc.order_no);$('#docHistoryV77').onclick=()=>documentHistoryModalV77(doc.id);$('#docCloseV77').onclick=closeModal;
 if(edit&&$('#docSaveV77'))$('#docSaveV77').onclick=async()=>{try{const payload=collectDocumentPayload(doc,true),note=prompt('Änderungsgrund für das Protokoll:','Entwurf bearbeitet')||'Entwurf bearbeitet';const d=await api({action:'document_save',id:doc.id,payload,change_note:note});toast('Entwurfversion gespeichert');closeModal();await load(true);documentModalV77(d.document)}catch(e){toast(e.message,true)}};
 if(edit&&$('#docIssueV77'))$('#docIssueV77').onclick=async()=>{if(!confirm('Dokument jetzt endgültig ausgeben und sperren?'))return;try{const d=await api({action:'document_status',id:doc.id,status:'issued'});toast('Dokument ausgegeben und gesperrt');closeModal();await load(true);documentModalV77(d.document)}catch(e){toast(e.message,true)}};
 if($('#docCancelV77'))$('#docCancelV77').onclick=async()=>{const reason=prompt('Stornogrund (Pflicht):','Bestellung storniert / Korrektur');if(!reason)return;try{await api({action:'invoice_cancel',id:doc.id,reason,email_customer:true});toast('Stornorechnung erstellt und dokumentiert');closeModal();await load(true);if(current==='documents')renderDocuments()}catch(e){toast(e.message,true)}};
}
async function documentHistoryModalV77(id){try{const d=await api({action:'document_history',id});openModal(`<div class="modalHead"><small>DOKUMENTHISTORIE</small><h2>Änderungsverlauf</h2><p>Jede Entwurfsänderung bleibt nachvollziehbar.</p></div><div class="modalBody"><div class="auditList">${(d.versions||[]).map(v=>`<article><time>${dt(v.created_at)}</time><div><b>Version ${Number(v.version_no||1)}</b><span>${esc(v.change_note||'')} · ${esc(v.edited_by_name||'System')}</span></div></article>`).join('')||'<div class="empty">Keine Versionshistorie vorhanden.</div>'}</div></div>`)}catch(e){toast(e.message,true)}}
function invoiceProfileModalV77(){const p=state.invoice_profile||{},r=state.invoice_readiness||{};openModal(`<div class="modalHead"><small>RECHNUNGSTOOL · EINRICHTUNG</small><h2>Rechnungsdaten</h2><p>${r.ready?'Alle Pflichtfelder für den automatischen Versand sind eingerichtet.':'Fehlend: '+esc((r.missing||[]).join(', '))}</p></div><div class="modalBody"><div class="invoiceProfileStatus ${r.ready?'ready':'warning'}"><b>${r.ready?'✓ Automatik bereit':'! Einrichtung erforderlich'}</b><span>Diese Angaben erscheinen auf Rechnung und Lieferschein.</span></div><div class="formGrid"><label class="full">Unternehmensname<input id="ipCompany" value="${esc(p.company_name||'Räucherhaken24')}"></label><label>Inhaber / Rechtsform<input id="ipOwner" value="${esc(p.owner||'')}"></label><label>Land<input id="ipCountry" value="${esc(p.country||'Deutschland')}"></label><label class="full">Straße / Hausnummer<input id="ipStreet" value="${esc(p.street||'')}"></label><label>PLZ<input id="ipZip" value="${esc(p.zip||'')}"></label><label>Ort<input id="ipCity" value="${esc(p.city||'')}"></label><label>Telefon<input id="ipPhone" value="${esc(p.phone||'')}"></label><label>E-Mail<input id="ipEmail" type="email" value="${esc(p.email||'')}"></label><label class="full">Webseite<input id="ipWebsite" value="${esc(p.website||'')}"></label><label>Steuernummer<input id="ipTax" value="${esc(p.tax_no||'')}"></label><label>USt-IdNr. (falls vorhanden)<input id="ipVat" value="${esc(p.vat_id||'')}"></label><label class="full">IBAN<input id="ipIban" value="${esc(p.iban||'')}"></label><label>BIC<input id="ipBic" value="${esc(p.bic||'')}"></label><label>Bank<input id="ipBank" value="${esc(p.bank_name||'')}"></label><label>Zahlungsziel (Tage)<input id="ipDays" type="number" min="1" max="60" value="${Number(p.payment_days||7)}"></label><label class="full">Fußtext<textarea id="ipFooter">${esc(p.footer||'')}</textarea></label><label class="shopToggle full"><input id="ipAuto" type="checkbox" ${p.auto_email!==false?'checked':''}><span><b>Online-Bestellungen automatisch per E-Mail</b><small>Rechnung + Lieferschein werden als PDF-Anhang verschickt, sobald das Rechnungsprofil vollständig ist.</small></span></label></div><div class="formActions"><button class="btn" id="ipCancel">Abbrechen</button><button class="btn primary" id="ipSave">Rechnungsdaten speichern</button></div></div>`);$('#ipCancel').onclick=closeModal;$('#ipSave').onclick=async()=>{try{const d=await api({action:'invoice_profile_save',company_name:$('#ipCompany').value,owner:$('#ipOwner').value,street:$('#ipStreet').value,zip:$('#ipZip').value,city:$('#ipCity').value,country:$('#ipCountry').value,phone:$('#ipPhone').value,email:$('#ipEmail').value,website:$('#ipWebsite').value,tax_no:$('#ipTax').value,vat_id:$('#ipVat').value,iban:$('#ipIban').value,bic:$('#ipBic').value,bank_name:$('#ipBank').value,payment_days:$('#ipDays').value,footer:$('#ipFooter').value,auto_email:$('#ipAuto').checked});state.invoice_profile=d.profile;state.invoice_readiness=d.readiness;toast(d.readiness.ready?'Rechnungstool vollständig eingerichtet':'Gespeichert · noch fehlen: '+d.readiness.missing.join(', '),!d.readiness.ready);closeModal();renderDocuments()}catch(e){toast(e.message,true)}}}


/* V82 · Professioneller Produkt-Baukasten */
function productBuilderHelp(label,text){return `<span class="builderLabelText">${esc(label)}<button type="button" class="fieldHelp" data-help="${esc(text)}" title="${esc(text)}" aria-label="Hilfe zu ${esc(label)}">?</button></span>`}
function productBuilderDefaultNewUntil(){const d=new Date();d.setDate(d.getDate()+30);return d.toISOString().slice(0,10)}
function productNewActiveAdmin(p){if(!p?.is_new)return false;if(!p.new_until)return true;const t=new Date(String(p.new_until).slice(0,10)+'T23:59:59');return !Number.isNaN(+t)&&t>=new Date()}
function legacyRenderProductsV83_3(){const q=(productSearchTerm||searchTerm||'').toLowerCase().trim(),baseRows=(state.catalog||[]).filter(p=>!q||`${p.name} ${p.sku||''} ${p.article_no||''} ${p.barcode||''} ${p.category||''} ${p.id}`.toLowerCase().includes(q)),rows=sortProductsForView(baseRows),drafts=baseRows.filter(p=>p.status!=='active'||!p.shop_visible).length,newCount=baseRows.filter(productNewActiveAdmin).length;ws.innerHTML=`<div class="sectionHead"><div><h2>Artikel & Produkte</h2><p>Produkte sicher erstellen, prüfen, veröffentlichen und anschließend im bestehenden Shopdesign verwalten.</p></div><div class="sectionActions"><b class="muted tiny">${rows.length} Artikel</b>${isAdmin()?'<button class="btn" id="openProductAnalysis">↗ Produkt-Analyse</button>':''}${can('edit_products')?'<button class="btn primary productBuilderLaunch" id="newProduct">＋ Produkt-Baukasten</button>':''}</div></div><section class="productBuilderIntro"><div><small>PRODUKT-BAUKASTEN · V86</small><h3>Neuen Artikel Schritt für Schritt veröffentlichen</h3><p>Felder erklären sich per <b>?</b>-Hilfe. Entwurf und Veröffentlichung sind getrennt. Das Shopdesign ist geschützt und wird durch Produktdaten niemals verändert.</p></div><div class="productBuilderIntroStats"><article><b>${drafts}</b><span>Entwürfe / offline</span></article><article><b>${newCount}</b><span>Neu im Sortiment</span></article><article><b>6</b><span>klare Schritte</span></article></div></section>${productSearchToolbar(productSearchTerm)}${productSortToolbar()}<div class="moduleNotice"><b>Designschutz:</b> Der Baukasten schreibt ausschließlich Produktdaten in die Datenbank. Neue Artikel werden über das feste Räucherhaken24-Shoptemplate dargestellt; Navigation, Farben, Kopfbereich und Layout bleiben unverändert.</div>${rows.length?productsTable(rows):'<div class="panel"><div class="empty"><b>Keine Produkte gefunden</b>Suchbegriff oder Barcode prüfen.</div></div>'}`;if($('#newProduct'))$('#newProduct').onclick=()=>productModal(null);if($('#openProductAnalysis'))$('#openProductAnalysis').onclick=()=>setView('product_analysis');bindQuickProductControls();bindPromoQuickControls();bindProductSearch(renderProducts);bindProductSort(renderProducts);$$('[data-analysis-product]').forEach(b=>b.onclick=()=>isAdmin()?setView('product_analysis'):productModal(b.dataset.analysisProduct))}

/* V82 · Produktzentrale + sicherer Löschworkflow */
let productManagerStatusFilter='all',productManagerCategoryFilter='all',productManagerViewMode='table';
const PRODUCT_SYSTEM_IDS=new Set(['prototype-project','std','aal','ultra','kralle','filet','doppel','fleisch','mehl-buche','mehl-erle','mehl-birke','mehl-eiche','mehl-kirsche','lauge-forelle-0','lauge-forelle-1','lauge-forelle-2','lauge-forelle-3','lauge-forelle-4','lauge-forelle-5','lauge-forelle-6','lauge-forelle-7','lauge-forelle-8','lauge-forelle-9','lauge-aal-0','lauge-aal-1','lauge-aal-2']);
function productManagerCounts(){const rows=state.catalog||[],inv=Object.fromEntries((state.inventory||[]).map(x=>[x.id,x]));return {total:rows.length,online:rows.filter(p=>p.status==='active'&&p.shop_visible).length,draft:rows.filter(p=>p.status!=='active'||!p.shop_visible).length,new:rows.filter(productNewActiveAdmin).length,offer:rows.filter(p=>p.is_offer).length,low:rows.filter(p=>p.type!=='prototype'&&Number(inv[p.id]?.stock??p.stock??0)<=Number(inv[p.id]?.minimum??p.minimum??0)).length}}
function productManagerFilterRows(){const q=(productSearchTerm||'').toLowerCase().trim(),inv=Object.fromEntries((state.inventory||[]).map(x=>[x.id,x]));let rows=(state.catalog||[]).filter(p=>!q||`${p.name} ${p.sku||''} ${p.article_no||''} ${p.barcode||''} ${p.category||''} ${p.id}`.toLowerCase().includes(q));if(productManagerCategoryFilter!=='all')rows=rows.filter(p=>String(p.category||'Sonstiges')===productManagerCategoryFilter);if(productManagerStatusFilter==='online')rows=rows.filter(p=>p.status==='active'&&p.shop_visible);else if(productManagerStatusFilter==='draft')rows=rows.filter(p=>p.status!=='active'||!p.shop_visible);else if(productManagerStatusFilter==='new')rows=rows.filter(productNewActiveAdmin);else if(productManagerStatusFilter==='offer')rows=rows.filter(p=>p.is_offer);else if(productManagerStatusFilter==='low')rows=rows.filter(p=>p.type!=='prototype'&&Number(inv[p.id]?.stock??p.stock??0)<=Number(inv[p.id]?.minimum??p.minimum??0));return sortProductsForView(rows)}
function productManagerStatusCard(key,label,value,sub){return `<button type="button" class="productOverviewCard ${productManagerStatusFilter===key?'active':''}" data-product-overview="${key}"><small>${esc(label)}</small><b>${Number(value||0).toLocaleString('de-DE')}</b><span>${esc(sub)}</span></button>`}
function productStatusBadge(p){return p.status==='active'&&p.shop_visible?'<span class="productStateBadge online">Online</span>':'<span class="productStateBadge draft">Entwurf / offline</span>'}
function productManagerCardGrid(rows){const inv=Object.fromEntries((state.inventory||[]).map(x=>[x.id,x]));return `<div class="productManagerGrid">${rows.map(p=>{const stock=p.type==='prototype'?null:Number(inv[p.id]?.stock??p.stock??0),min=p.type==='prototype'?null:Number(inv[p.id]?.minimum??p.minimum??0),low=stock!==null&&stock<=min;return `<article class="productManagerCard"><div class="productManagerImage">${p.image?`<img src="../${esc(p.image)}" alt="">`:'<span>◇</span>'}<div class="productManagerBadges">${productStatusBadge(p)}${productNewActiveAdmin(p)?'<span class="productStateBadge new">Neu</span>':''}${p.is_offer?'<span class="productStateBadge offer">Angebot</span>':''}</div></div><div class="productManagerCardBody"><small>${esc(p.article_no||'ohne Art.-Nr.')} · ${esc(p.category||'Sonstiges')}</small><h3>${esc(p.name)}</h3><p>${esc(p.short_description||p.description||'Noch keine Kurzbeschreibung hinterlegt.')}</p><div class="productManagerFacts"><div><span>Preis</span><b>${money(p.base||0)}</b></div><div><span>Bestand</span><b class="${low?'lowStock':''}">${stock===null?'–':stock}</b></div><div><span>Einheit</span><b>${esc(p.unit||'–')}</b></div></div><div class="productManagerActions"><button class="btn primary" data-open-product="${esc(p.id)}">Bearbeiten</button>${p.status==='active'&&p.shop_visible?`<a class="btn" href="../artikel.php?id=${encodeURIComponent(p.id)}" target="_blank" rel="noopener">Shop-Vorschau</a>`:''}${isAdmin()?`<button class="btn dangerOutline" data-delete-product="${esc(p.id)}">Artikel löschen</button>`:''}</div></div></article>`}).join('')}</div>`}
function productManagerToolbar(){const cats=[...new Set((state.catalog||[]).map(p=>String(p.category||'Sonstiges')))].sort((a,b)=>a.localeCompare(b,'de'));return `<div class="panel productManagerToolbar"><div><label>Status<select id="productManagerStatus"><option value="all" ${sel(productManagerStatusFilter,'all')}>Alle Produkte</option><option value="online" ${sel(productManagerStatusFilter,'online')}>Nur online</option><option value="draft" ${sel(productManagerStatusFilter,'draft')}>Entwürfe / offline</option><option value="new" ${sel(productManagerStatusFilter,'new')}>Neu im Sortiment</option><option value="offer" ${sel(productManagerStatusFilter,'offer')}>Angebote</option><option value="low" ${sel(productManagerStatusFilter,'low')}>Meldebestand erreicht</option></select></label><label>Kategorie<select id="productManagerCategory"><option value="all">Alle Kategorien</option>${cats.map(c=>`<option value="${esc(c)}" ${sel(productManagerCategoryFilter,c)}>${esc(c)}</option>`).join('')}</select></label></div><div class="productViewSwitch"><button type="button" data-product-view="table" class="${productManagerViewMode==='table'?'active':''}">☷ Tabelle</button><button type="button" data-product-view="cards" class="${productManagerViewMode==='cards'?'active':''}">▦ Karten</button></div></div>`}
function renderProducts(){const counts=productManagerCounts(),rows=productManagerFilterRows();ws.innerHTML=`<div class="sectionHead"><div><h2>Produktzentrale</h2><p>Produkte übersichtlich erstellen, filtern, bearbeiten, veröffentlichen oder kontrolliert entfernen – ohne das Shopdesign zu verändern.</p></div><div class="sectionActions"><b class="muted tiny">${rows.length} Treffer</b>${isAdmin()?'<button class="btn" id="openProductAnalysis">↗ Produkt-Analyse</button>':''}${can('edit_products')?'<button class="btn productBuilderDirect" id="openProductBuilderHub">▣ Produkt-Baukasten</button><button class="btn primary productBuilderLaunch" id="newProduct">＋ Neues Produkt</button>':''}</div></div><section class="productBuilderIntro productManagerHero"><div><small>PRODUKT-BAUKASTEN · V86</small><h3>Produktverwaltung mit klarer Übersicht</h3><p>Oben sehen Sie sofort Online-Artikel, Entwürfe, Neuheiten, Angebote und kritische Bestände. Der Baukasten bleibt Schritt für Schritt aufgebaut und schützt weiterhin das bestehende Shopdesign.</p></div><div class="productBuilderIntroStats"><article><b>${counts.online}</b><span>online</span></article><article><b>${counts.draft}</b><span>Entwürfe</span></article><article><b>${counts.new}</b><span>Neuheiten</span></article></div></section><div class="productOverviewGrid">${productManagerStatusCard('all','Gesamt',counts.total,'alle Artikel')}${productManagerStatusCard('online','Online',counts.online,'im Shop sichtbar')}${productManagerStatusCard('draft','Entwürfe',counts.draft,'offline / in Arbeit')}${productManagerStatusCard('new','Neu im Sortiment',counts.new,'aktive Neu-Kennzeichnung')}${productManagerStatusCard('offer','Angebote',counts.offer,'Aktionsartikel')}${productManagerStatusCard('low','Meldebestand',counts.low,'Bestand prüfen')}</div>${productSearchToolbar(productSearchTerm)}${productManagerToolbar()}${productSortToolbar()}<div class="moduleNotice productSafetyNotice"><b>Design- und Löschschutz:</b> Produktdaten verändern niemals das Shoplayout. „Aus Shop nehmen“ blendet einen Artikel nur aus. Endgültiges Löschen ist nur für eigene Baukastenartikel möglich; fest eingebaute Systemartikel sind gegen versehentliches Löschen geschützt.</div>${rows.length?(productManagerViewMode==='cards'?productManagerCardGrid(rows):productsTable(rows)):'<div class="panel"><div class="empty"><b>Keine Produkte gefunden</b>Filter, Kategorie oder Suchbegriff zurücksetzen.</div></div>'}`;if($('#newProduct'))$('#newProduct').onclick=()=>productModal(null);if($('#openProductBuilderHub'))$('#openProductBuilderHub').onclick=()=>setView('productbuilder');if($('#openProductAnalysis'))$('#openProductAnalysis').onclick=()=>setView('product_analysis');bindQuickProductControls();bindPromoQuickControls();bindProductSearch(renderProducts);bindProductSort(renderProducts);$('#productManagerStatus').onchange=e=>{productManagerStatusFilter=e.target.value;renderProducts()};$('#productManagerCategory').onchange=e=>{productManagerCategoryFilter=e.target.value;renderProducts()};$$('[data-product-overview]').forEach(b=>b.onclick=()=>{productManagerStatusFilter=b.dataset.productOverview;renderProducts()});$$('[data-product-view]').forEach(b=>b.onclick=()=>{productManagerViewMode=b.dataset.productView;renderProducts()});$$('[data-analysis-product]').forEach(b=>b.onclick=()=>isAdmin()?setView('product_analysis'):productModal(b.dataset.analysisProduct));$$('[data-delete-product]').forEach(b=>b.onclick=()=>productDeleteModal(b.dataset.deleteProduct))}

function renderProductBuilderHub(){
 if(!can('view_products'))return setView('dashboard');
 const counts=productManagerCounts(),recent=[...(state.catalog||[])].sort((a,b)=>String(b.updated_at||b.created_at||'').localeCompare(String(a.updated_at||a.created_at||''))).slice(0,6);
 ws.innerHTML=`<div class="sectionHead"><div><h2>Produkt-Baukasten</h2><p>Neue Artikel kontrolliert anlegen, live prüfen und veröffentlichen. Das bestehende Shopdesign bleibt fest.</p></div><div class="sectionActions"><span class="builderActivePill">V86 AKTIV</span><button class="btn" id="builderHubProducts">Produktzentrale</button>${can('edit_products')?'<button class="btn primary" id="builderHubNew">＋ Neuen Artikel aufbauen</button>':''}</div></div>
 <section class="builderHubHero"><div><small>PRODUKT-BAUKASTEN · V86</small><h3>Artikel anlegen, ohne das Shopdesign anzufassen.</h3><p>Der Baukasten führt in sechs Schritten durch Stammdaten, Kategorie, Inhalte, Preis/Versand, Shopkennzeichnungen und Abschlussprüfung. Rechts im Baukasten läuft permanent die Shopkarten-Vorschau.</p><div class="builderHubChecks"><span>✓ Feldhilfen</span><span>✓ Live-Vorschau</span><span>✓ Entwurf</span><span>✓ Veröffentlichen</span><span>✓ Artikel löschen</span></div></div><div class="builderHubPreview"><small>VORSCHAU IM BAUKASTEN</small><div class="builderHubMock"><div class="builderHubMockImage">Produktbild</div><div><span>Neu im Sortiment</span><h4>Ihr neuer Artikel</h4><p>Kurzbeschreibung und Preis werden live übernommen.</p><b>0,00 €</b></div></div></div></section>
 <div class="productOverviewGrid builderHubStats">${productManagerStatusCard('all','Gesamt',counts.total,'alle Artikel')}${productManagerStatusCard('online','Online',counts.online,'im Shop sichtbar')}${productManagerStatusCard('draft','Entwürfe',counts.draft,'noch nicht veröffentlicht')}${productManagerStatusCard('new','Neu im Sortiment',counts.new,'Neu-Kennzeichnung')}${productManagerStatusCard('offer','Angebote',counts.offer,'Aktionsartikel')}${productManagerStatusCard('low','Meldebestand',counts.low,'Bestand prüfen')}</div>
 <div class="builderHubGrid"><article><small>1 · NEUER ARTIKEL</small><h3>Produkt-Baukasten öffnen</h3><p>Sechs Schritte mit Erklärung an jedem wichtigen Feld und sichtbarer Live-Vorschau.</p>${can('edit_products')?'<button class="btn primary" id="builderHubNew2">Baukasten starten</button>':'<span class="muted">Nur Ansicht</span>'}</article><article><small>2 · PRODUKTVERWALTUNG</small><h3>Bestehende Artikel bearbeiten</h3><p>Preis, Bestand, Shopstatus, Kennzeichnungen und Inhalte zentral kontrollieren.</p><button class="btn" id="builderHubManage">Produktzentrale öffnen</button></article><article><small>3 · LÖSCHEN</small><h3>Artikel kontrolliert entfernen</h3><p>Eigene Baukastenartikel können als Administrator endgültig gelöscht werden. Systemartikel bleiben geschützt.</p><button class="btn dangerOutline" id="builderHubDelete">Artikel zum Löschen auswählen</button></article></div>
 <section class="panel builderHubRecent"><div class="panelTitle"><h3>Zuletzt bearbeitete Produkte</h3><span>${recent.length} angezeigt</span></div>${recent.length?`<div class="builderRecentGrid">${recent.map(p=>`<article><div>${p.image?`<img src="../${esc(p.image)}" alt="">`:'◇'}</div><section><small>${esc(p.article_no||'ohne Art.-Nr.')}</small><b>${esc(p.name)}</b><span>${p.shop_visible&&p.status==='active'?'Online':'Entwurf / offline'}</span></section><div><button class="miniBtn" data-builder-edit="${esc(p.id)}">Bearbeiten</button>${isAdmin()?`<button class="miniBtn dangerMini" data-builder-delete="${esc(p.id)}">Artikel löschen</button>`:''}</div></article>`).join('')}</div>`:'<div class="empty"><b>Noch keine Produkte</b>Starte oben den Produkt-Baukasten.</div>'}</section>`;
 $('#builderHubProducts').onclick=()=>setView('products');if($('#builderHubNew'))$('#builderHubNew').onclick=()=>productModal(null);if($('#builderHubNew2'))$('#builderHubNew2').onclick=()=>productModal(null);$('#builderHubManage').onclick=()=>setView('products');$('#builderHubDelete').onclick=()=>{productManagerStatusFilter='all';productManagerViewMode='table';setView('products')};$$('[data-builder-edit]').forEach(b=>b.onclick=()=>productModal(b.dataset.builderEdit));$$('[data-builder-delete]').forEach(b=>b.onclick=()=>productDeleteModal(b.dataset.builderDelete));$$('[data-product-overview]').forEach(b=>b.onclick=()=>{productManagerStatusFilter=b.dataset.productOverview;setView('products')});
}

async function productSetOffline(id){try{await api({action:'product_quick_update',id,shop_visible:false});toast('Artikel wurde aus dem Shop genommen. Produktdaten bleiben erhalten.');await load(true);renderProducts()}catch(e){toast(e.message,true)}}
function productDeleteModal(id){if(!isAdmin())return toast('Endgültiges Löschen ist nur für Administratoren erlaubt.',true);const p=(state.catalog||[]).find(x=>x.id===id);if(!p)return toast('Produkt nicht gefunden.',true);const system=Boolean(p.system_product)||PRODUCT_SYSTEM_IDS.has(String(p.id));openModal(`<div class="modalHead deleteProductHead"><small>PRODUKTVERWALTUNG · LÖSCHSCHUTZ</small><h2>${system?'Systemartikel schützen':'Produkt entfernen'}</h2><p>${esc(p.article_no||'–')} · ${esc(p.name)}</p></div><div class="modalBody"><div class="deleteProductSummary"><div>${p.image?`<img src="../${esc(p.image)}" alt="">`:'<span>◇</span>'}</div><section><small>PRODUKT</small><h3>${esc(p.name)}</h3><p>Artikel-ID: <b>${esc(p.id)}</b><br>Artikelnummer: <b>${esc(p.article_no||'–')}</b><br>Status: <b>${p.status==='active'&&p.shop_visible?'Online':'Offline / Entwurf'}</b></p></section></div><div class="deleteChoiceGrid"><article><b>1 · Aus Shop nehmen</b><span>Empfohlen, wenn der Artikel später wieder gebraucht wird. Produktdaten, Bild, Lagerdaten und Historie bleiben erhalten.</span><button class="btn" id="productOfflineBtn" ${p.shop_visible?'':'disabled'}>Aus Shop nehmen</button></article><article class="dangerChoice"><b>2 · Endgültig löschen</b><span>${system?'Dieser fest eingebaute Räucherhaken24-Systemartikel kann nicht endgültig gelöscht werden. So bleiben statische Produktseiten und Systemverknüpfungen intakt.':'Entfernt den Artikel dauerhaft aus Produktstamm, Lager und Kalkulationsprofil. Historische Bestellungen werden nicht verändert.'}</span>${system?'<div class="protectedProductBadge">GESCHÜTZTER SYSTEMARTIKEL</div>':`<label>Zur Bestätigung Produktname exakt eingeben<input id="deleteProductConfirm" autocomplete="off" placeholder="${esc(p.name)}"></label><button class="btn dangerBtn" id="productDeleteFinal" disabled>Endgültig löschen</button>`}</article></div><div class="formActions"><button class="btn" id="productDeleteCancel">Schließen</button></div></div>`);$('#productDeleteCancel').onclick=closeModal;$('#productOfflineBtn').onclick=async()=>{closeModal();await productSetOffline(id)};if(!system){const inp=$('#deleteProductConfirm'),btn=$('#productDeleteFinal');inp.oninput=()=>{btn.disabled=inp.value.trim()!==String(p.name).trim()};btn.onclick=async()=>{if(btn.disabled)return;if(!confirm('Produkt wirklich endgültig löschen? Dieser Vorgang kann nicht rückgängig gemacht werden.'))return;try{btn.disabled=true;btn.textContent='Löscht …';await api({action:'product_delete',id:p.id,confirm_name:inp.value.trim(),confirm_delete:true});toast('Produkt endgültig gelöscht.');closeModal();await load(true);renderProducts()}catch(e){toast(e.message,true);btn.disabled=false;btn.textContent='Endgültig löschen'}}}}
function productModal(id){
 if(!can('edit_products'))return toast('Produkte dürfen mit diesem Zugang nicht verändert werden.',true);
 const p=(state.catalog||[]).find(x=>x.id===id)||{},inv=(state.inventory||[]).find(x=>x.id===id)||{},cat=p.category||'Sonstiges',isEditing=Boolean(id),catCounts={};(state.catalog||[]).forEach(x=>{const c=String(x.category||'Sonstiges');catCounts[c]=(catCounts[c]||0)+1});
 const features=Array.isArray(p.features)?p.features:[],newUntil=String(p.new_until||'').slice(0,10)||(isEditing?'':productBuilderDefaultNewUntil()),isNew=p.is_new===undefined?!isEditing:Boolean(p.is_new),imageSrc=p.image?'../'+p.image:'';
 const crossInitial=Array.isArray(p.cross_sell)?p.cross_sell.map(x=>({...x})):[],crossCatalog=(state.catalog||[]).filter(x=>x.id!==id&&x.type!=='prototype'),crossEnabled=p.cross_sell_enabled===undefined?(crossInitial.length>0):Boolean(p.cross_sell_enabled);let crossSelected=crossInitial;
 openModal(`<div class="modalHead productBuilderHead"><div class="builderRuntimeBadge">V86 · LIVE-VORSCHAU + CROSS-SELLING</div><small>PRODUKT-BAUKASTEN · V86</small><h2>${isEditing?'Produkt professionell bearbeiten':'Neuen Artikel aufbauen'}</h2><p>Sechs Schritte · Live-Vorschau · Cross-Selling · Entwurf/Veröffentlichung getrennt · Shopdesign geschützt.</p></div><div class="modalBody productBuilder"><div class="productBuilderProgress"><button class="active" data-builder-step="1"><b>1</b><span>Grunddaten</span></button><button data-builder-step="2"><b>2</b><span>Kategorie</span></button><button data-builder-step="3"><b>3</b><span>Inhalt</span></button><button data-builder-step="4"><b>4</b><span>Preis & Versand</span></button><button data-builder-step="5"><b>5</b><span>Shop</span></button><button data-builder-step="6"><b>6</b><span>Prüfen</span></button></div><div class="productBuilderLayout"><div class="productBuilderMain">
 <section class="builderStep active" data-builder-panel="1"><div class="builderSectionHead"><small>SCHRITT 1</small><h3>Grunddaten des Artikels</h3><p>Alles, womit Produkt, Lager und Bestellung eindeutig identifiziert werden.</p></div><div class="formGrid builderForm"><label>${productBuilderHelp('Produktname','Der öffentlich sichtbare Name. Klar und konkret formulieren, z. B. „Räucherhaken Kralle – 10er Set“.')}<input id="prName" value="${esc(p.name||'')}" placeholder="z. B. Räucherhaken Kralle – 10er Set"></label><label>${productBuilderHelp('Produkttyp','Standardprodukt für normale Artikel; Räucherhaken für Hakenprodukte; Prototyp bleibt nicht öffentlich im Shop.')}<select id="prType"><option value="product" ${sel(p.type||'product','product')}>Standardprodukt</option><option value="hook" ${sel(p.type,'hook')}>Räucherhaken</option><option value="prototype" ${sel(p.type,'prototype')}>Prototyp / Sonderanfertigung</option></select></label><label>${productBuilderHelp('Artikel-ID','Interne technische ID. Bei neuen Artikeln am besten leer lassen; das System erzeugt eine sichere ID.')}<input id="prId" value="${esc(p.id||'')}" ${isEditing?'disabled':''} placeholder="automatisch erzeugen"></label><label>${productBuilderHelp('Artikelnummer','Interne/öffentliche Artikelnummer. Leer lassen = nächste freie Nummer wird automatisch vergeben.')}<input id="prArticle" value="${esc(p.article_no||'')}" placeholder="automatisch vergeben"></label><label>${productBuilderHelp('SKU','Eigene Lager- oder Herstellerkennung. Optional, aber für interne Suche sehr hilfreich.')}<input id="prSku" value="${esc(p.sku||'')}" placeholder="z. B. RH-KR-10"></label><label>${productBuilderHelp('EAN / Barcode','Nur eintragen, wenn der Artikel tatsächlich einen Barcode/EAN besitzt. Muss eindeutig sein.')}<input id="prBarcode" value="${esc(p.barcode||'')}" placeholder="optional"></label></div></section>
 <section class="builderStep" data-builder-panel="2"><div class="builderSectionHead"><small>SCHRITT 2</small><h3>Kategorie & Einordnung</h3><p>Die Kategorie steuert Suche und Sortierung. Neue Kategorien können direkt angelegt werden.</p></div><div class="categoryEditorHero"><div><b>Produktkategorie</b><span>Vorhandene Kategorie wählen oder eine neue fachlich eindeutige Kategorie anlegen.</span></div><span class="categoryCurrent" id="categoryCurrent">${esc(cat)}</span></div><div class="formGrid builderForm"><label class="full">${productBuilderHelp('Kategorie','Bestimmt, wo der Artikel fachlich einsortiert wird. Das Shop-Layout selbst wird dadurch nicht verändert.')}<select id="prCategorySelect">${productCategoryOptions(cat)}</select></label><label id="prNewCategoryWrap" class="full hiddenField">${productBuilderHelp('Neue Kategorie','Kurzer Sammelbegriff, z. B. „Grillzubehör“. Keine Produktnamen als Kategorie verwenden.')}<input id="prNewCategory" placeholder="z. B. Grillzubehör"></label></div><div class="categoryChipGrid">${productCategories(cat).map(c=>`<button type="button" class="categoryChip ${c===cat?'active':''}" data-pick-category="${esc(c)}"><b>${esc(c)}</b><span>${Number(catCounts[c]||0)} Artikel</span></button>`).join('')}</div></section>
 <section class="builderStep" data-builder-panel="3"><div class="builderSectionHead"><small>SCHRITT 3</small><h3>Inhalt, Nutzen & Produktbild</h3><p>Hier entsteht die Verkaufsinformation. Kurze Einleitung, ausführliche Beschreibung und klare Vorteile.</p></div><div class="formGrid builderForm"><label class="full">${productBuilderHelp('Kurzbeschreibung','1–2 Sätze für Produktkarten und schnelle Orientierung. Ideal sind 80–220 Zeichen.')}<textarea id="prShortDescription" maxlength="320" placeholder="Was ist das Produkt und für wen ist es geeignet?">${esc(p.short_description||'')}</textarea><small><span id="shortDescCount">0</span> / 320 Zeichen</small></label><label class="full">${productBuilderHelp('Ausführliche Beschreibung','Erklärt Anwendung, Nutzen, Material, Besonderheiten und relevante Hinweise. Keine Werbeversprechen erfinden.')}<textarea id="prDescription" class="builderLongText" placeholder="Professionelle Produktbeschreibung …">${esc(p.description||'')}</textarea></label><label class="full">${productBuilderHelp('Vorteile / Merkmale','Pro Zeile ein konkreter Vorteil. Maximal 10 Punkte. Diese erscheinen übersichtlich auf der dynamischen Produktseite.')}<textarea id="prFeatures" placeholder="Sicherer Halt\nHochwertiges Material\nFür Einsteiger geeignet">${esc(features.join('\n'))}</textarea></label><label class="full productImagePicker builderImagePicker">${productBuilderHelp('Produktbild','Empfohlen: scharfes Produktfoto mit ruhigem/weißem Hintergrund. JPG/JPEG, PNG, WebP, AVIF, HEIC oder HEIF. Bis 20 MB. Geeignete Bilder werden automatisch auf max. 2000 px optimiert und als WebP für den Shop vorbereitet.')}<input id="prImage" type="file" accept=".jpg,.jpeg,.png,.webp,.avif,.heic,.heif,image/jpeg,image/png,image/webp,image/avif,image/heic,image/heif"><small>${p.image?'Aktuelles Bild: '+esc(p.image):'Für Veröffentlichung wird ein Produktbild empfohlen · JPG/PNG/WebP/AVIF/HEIC/HEIF · max. 20 MB · automatische WebP-Optimierung.'}</small></label></div></section>
 <section class="builderStep" data-builder-panel="4"><div class="builderSectionHead"><small>SCHRITT 4</small><h3>Preis, Menge, Verpackung & Versand</h3><p>Die Werte steuern Preisangaben, Grundpreise, Lager und Versandgewicht.</p></div><div class="formGrid builderForm"><label>${productBuilderHelp('Standardpreis (€)','Brutto-Verkaufspreis des Artikels. Bei einer Aktion bleibt dies der normale Basispreis.')}<input id="prBase" type="number" min="0" step="0.01" value="${Number(p.base||0).toFixed(2)}"></label><label>${productBuilderHelp('Anzeige / Gebinde','Text beim Preis, z. B. „500 g“, „10er-Set“ oder „1 Stück“.')}<input id="prUnit" value="${esc(p.unit||'Stück')}" placeholder="z. B. 10er-Set"></label><label>${productBuilderHelp('Menge pro Verkaufseinheit','Numerische Menge, z. B. 500 bei 500 g oder 10 bei einem 10er-Set.')}<input id="prContentQty" type="number" min="0" step="0.001" value="${Number(p.content_quantity??p.pack_quantity??1)}"></label><label>${productBuilderHelp('Mengeneinheit','Einheit für die Mengenberechnung und ggf. Grundpreisangabe.')}<select id="prContentUnit"><option ${sel(p.content_unit||'Stück','Stück')}>Stück</option><option ${sel(p.content_unit,'g')}>g</option><option ${sel(p.content_unit,'kg')}>kg</option><option ${sel(p.content_unit,'ml')}>ml</option><option ${sel(p.content_unit,'l')}>l</option><option ${sel(p.content_unit,'Set')}>Set</option><option ${sel(p.content_unit,'Paar')}>Paar</option><option ${sel(p.content_unit,'Paket')}>Paket</option></select></label><label>${productBuilderHelp('Stückzahl im Gebinde','Wie viele Einzelteile enthält eine Verkaufseinheit? Beispiel: 10 bei einem 10er-Hakenset.')}<input id="prPackQty" type="number" min="1" step="1" value="${Number(p.pack_quantity||1)}"></label><label>${productBuilderHelp('Verpackungsart','Interne Verpackungsform für Lager/Versand.')}<select id="prPackageType"><option ${sel(p.package_type||'Einzelartikel','Einzelartikel')}>Einzelartikel</option><option ${sel(p.package_type,'Beutel')}>Beutel</option><option ${sel(p.package_type,'Dose')}>Dose</option><option ${sel(p.package_type,'Flasche')}>Flasche</option><option ${sel(p.package_type,'Karton')}>Karton</option><option ${sel(p.package_type,'Set')}>Set</option><option ${sel(p.package_type,'Paket')}>Paket</option><option ${sel(p.package_type,'Rolle')}>Rolle</option><option ${sel(p.package_type,'Sonstiges')}>Sonstiges</option></select></label><label>${productBuilderHelp('Produktgewicht (g)','Reines Artikelgewicht. Hilft bei Produktinformation und Kalkulation.')}<input id="prWeight" type="number" min="0" step="1" value="${Number(p.product_weight_g||0)}"></label><label>${productBuilderHelp('Versandgewicht (g)','Gewicht inklusive Verkaufs-/Versandverpackung. Relevant für den Versand.')}<input id="prShipWeight" type="number" min="0" step="1" value="${Number(p.shipping_weight_g||0)}"></label><label>${productBuilderHelp('Preis-Grundlage','Automatisch erkennt typische Gewichts-/Stückartikel. Nur bei Sonderfällen manuell festlegen.')}<select id="prPriceBasis"><option value="auto" ${sel(p.price_basis||'auto','auto')}>Automatisch</option><option value="weight" ${sel(p.price_basis,'weight')}>Gewicht · je 100 g</option><option value="piece" ${sel(p.price_basis,'piece')}>Stück · je Stück</option></select></label><label>${productBuilderHelp('Bestand','Aktuell verkaufsfähige Menge im Lager.')}<input id="prStock" type="number" min="0" step="1" value="${Number(inv.stock??p.stock??0)}"></label><label>${productBuilderHelp('Mindestbestand','Bei Unterschreitung erscheint eine Lagerwarnung im Orgaboard.')}<input id="prMinimum" type="number" min="0" step="1" value="${Number(inv.minimum??p.minimum??0)}"></label><label>${productBuilderHelp('Paketlänge (cm)','Optional. Außenmaß der Versand-/Produktverpackung.')}<input id="prPackL" type="number" min="0" step="0.1" value="${Number(p.package_length_cm||0)}"></label><label>${productBuilderHelp('Paketbreite (cm)','Optional. Außenmaß der Versand-/Produktverpackung.')}<input id="prPackW" type="number" min="0" step="0.1" value="${Number(p.package_width_cm||0)}"></label><label>${productBuilderHelp('Pakethöhe (cm)','Optional. Außenmaß der Versand-/Produktverpackung.')}<input id="prPackH" type="number" min="0" step="0.1" value="${Number(p.package_height_cm||0)}"></label></div></section>
 <section class="builderStep" data-builder-panel="5"><div class="builderSectionHead"><small>SCHRITT 5</small><h3>Shopdarstellung & Kennzeichnungen</h3><p>Der Artikel bekommt Kennzeichnungen – das eigentliche Shopdesign bleibt fest.</p></div><div class="builderDesignLock"><div class="lockIcon">◆</div><div><b>Shopdesign gesperrt</b><span>Der Produkt-Baukasten kann keine Farben, Navigation, Kopfzeile, Kategorienavigation oder Seitenstruktur verändern.</span></div></div><div class="builderToggleGrid"><label class="builderToggle newToggle"><input id="prNew" type="checkbox" ${isNew?'checked':''}><span><b>Neu im Sortiment</b><small>Zeigt im Shop eine grüne Kennzeichnung „Neu im Sortiment“.</small></span></label><label class="builderToggle"><input id="prPopular" type="checkbox" ${p.is_popular?'checked':''}><span><b>Beliebt</b><small>Zusätzliche Beliebt-Kennzeichnung.</small></span></label><label class="builderToggle"><input id="prOffer" type="checkbox" ${p.is_offer?'checked':''}><span><b>Angebot</b><small>Aktionspreis und Angebotskennzeichnung aktivieren.</small></span></label></div><div class="formGrid builderForm"><label id="prNewUntilWrap">${productBuilderHelp('„Neu“-Kennzeichnung bis','Danach verschwindet nur die Kennzeichnung automatisch. Der Artikel bleibt weiterhin im Shop.')}<input id="prNewUntil" type="date" value="${esc(newUntil)}"></label><label>${productBuilderHelp('Streichpreis (€)','Alter/vergleichbarer eigener Preis bei einer echten Aktion. Muss über dem Aktionspreis liegen.')}<input id="prOld" type="number" min="0" step="0.01" value="${Number(p.old_price||0).toFixed(2)}"></label><label>${productBuilderHelp('Aktionspreis (€)','Temporärer Verkaufspreis. Nur bei aktivierter Angebotskennzeichnung verwenden.')}<input id="prSale" type="number" min="0" step="0.01" value="${Number(p.sale_price||0).toFixed(2)}"></label><label>${productBuilderHelp('Aktionsstart','Optionaler Zeitpunkt, ab dem der Aktionspreis gelten soll.')}<input id="prSaleStart" type="datetime-local" value="${esc(toInputDT(p.sale_start_at))}"></label><label>${productBuilderHelp('Aktionsende','Optional. Nach Ablauf gilt automatisch wieder der Standardpreis.')}<input id="prSaleEnd" type="datetime-local" value="${esc(toInputDT(p.sale_end_at))}"></label><label class="full">${productBuilderHelp('SEO-Titel','Optionaler Titel für Suchmaschinen/Browser. Wenn leer, wird der Produktname genutzt.')}<input id="prSeoTitle" maxlength="180" value="${esc(p.seo_title||'')}"></label><label class="full">${productBuilderHelp('SEO-Beschreibung','Kurze Suchmaschinenbeschreibung. Sachlich, ca. 120–160 Zeichen.')}<textarea id="prSeoDescription" maxlength="320">${esc(p.seo_description||'')}</textarea></label></div><div class="crossSellBuilder"><div class="crossSellHead"><div><small>VERKAUFSVERKNÜPFUNG</small><h4>Cross-Selling & passende Empfehlungen</h4><p>Lege fest, welche Zusatzartikel, Upgrades oder Alternativen auf der Produktseite angeboten werden. Reihenfolge und Empfehlungstyp bleiben vollständig steuerbar.</p></div><span id="crossSellCount" class="builderActivePill">0 verknüpft</span></div><div class="builderToggleGrid crossSellToggles"><label class="builderToggle"><input id="prCrossEnabled" type="checkbox" ${crossEnabled?'checked':''}><span><b>Cross-Selling aktiv</b><small>Zeigt passende Produkte unterhalb des Hauptartikels.</small></span></label><label class="builderToggle"><input id="prCrossAuto" type="checkbox" ${p.cross_sell_auto?'checked':''}><span><b>Automatisch auffüllen</b><small>Fehlende Plätze werden intelligent mit aktiven Artikeln ergänzt.</small></span></label><label class="builderToggle"><input id="prCrossReciprocal" type="checkbox" ${p.cross_sell_reciprocal?'checked':''}><span><b>Gegenseitig verknüpfen</b><small>Ausgewählte Produkte erhalten diesen Artikel ebenfalls als passende Empfehlung.</small></span></label></div><div class="formGrid builderForm crossSellSettings"><label>${productBuilderHelp('Überschrift','Öffentliche Überschrift über den Empfehlungen, z. B. „Passt perfekt dazu“.')}<input id="prCrossTitle" maxlength="160" value="${esc(p.cross_sell_title||'Passt perfekt dazu')}"></label><label>${productBuilderHelp('Maximal anzeigen','Wie viele Cross-Selling-Produkte maximal auf der Produktseite erscheinen.')}<select id="prCrossMax">${[1,2,3,4,5,6,7,8].map(n=>`<option value="${n}" ${sel(Number(p.cross_sell_max||4),n)}>${n} Produkte</option>`).join('')}</select></label></div><div class="crossSellTools"><label><span>Produkte suchen</span><input id="prCrossSearch" type="search" placeholder="Name, Kategorie, Artikelnummer …"></label><button type="button" class="btn" id="crossAutoSuggest">✦ Vorschläge erzeugen</button><button type="button" class="btn" id="crossClearAll">Auswahl leeren</button></div><div class="crossSellColumns"><section><div class="crossSellSubhead"><b>Ausgewählte Empfehlungen</b><span>Typ, Priorität und Verkaufshinweis festlegen</span></div><div id="crossSellSelected" class="crossSellSelected"></div></section><section><div class="crossSellSubhead"><b>Produkte hinzufügen</b><span>Aktive Produkte aus dem Sortiment</span></div><div id="crossSellCandidates" class="crossSellCandidates"></div></section></div><div class="moduleNotice"><b>Cross-Selling-Logik:</b> „Häufig zusammen gekauft“ für Ergänzungen, „Passendes Zubehör“ für notwendiges/komfortables Zubehör, „Upgrade“ für höherwertige Alternativen und „Alternative“ für vergleichbare Artikel. Im Shop werden nur aktive, veröffentlichte Produkte ausgegeben.</div></div></section>
 <section class="builderStep" data-builder-panel="6"><div class="builderSectionHead"><small>SCHRITT 6</small><h3>Prüfen & veröffentlichen</h3><p>Vor der Veröffentlichung prüft der Baukasten die wichtigsten Angaben. Entwurf bleibt unsichtbar.</p></div><div id="builderChecklist" class="builderChecklist"></div><div class="builderPublishInfo"><b>Was passiert beim Veröffentlichen?</b><ol><li>Produktdaten werden gespeichert.</li><li>Das Produkt wird auf <b>aktiv + online</b> gesetzt.</li><li>Der Shop liest es über den bestehenden Produktkatalog ein.</li><li>Die Darstellung erfolgt ausschließlich im bestehenden Räucherhaken24-Template.</li></ol></div></section>
 </div><aside class="productBuilderPreview"><div class="builderPreviewSticky"><div class="builderPreviewHead"><small>LIVE-VORSCHAU</small><b>Shopkarte · festes Design</b></div><article class="builderShopCard"><div class="builderShopImage" id="builderPreviewImage">${imageSrc?`<img src="${esc(imageSrc)}" alt="">`:'<span>Produktbild</span>'}</div><div class="builderShopBody"><div class="builderPreviewBadges" id="builderPreviewBadges"></div><small id="builderPreviewArticle">Art.-Nr. automatisch</small><h3 id="builderPreviewName">${esc(p.name||'Produktname')}</h3><p id="builderPreviewShort">${esc(p.short_description||'Ihre Kurzbeschreibung erscheint hier.')}</p><div class="builderPreviewMeta"><span id="builderPreviewCategory">${esc(cat)}</span><span id="builderPreviewUnit">${esc(p.unit||'Stück')}</span></div><div class="builderPreviewBuy"><strong id="builderPreviewPrice">${Number(p.base||0)>0?money(p.base):'Preis folgt'}</strong><button type="button" tabindex="-1">In den Warenkorb</button></div></div></article><div class="builderPreviewNotice"><b>Designschutz aktiv</b><span>Diese Vorschau entspricht der festen dynamischen Produktkarte. Der Baukasten speichert keine Designwerte.</span></div></div></aside></div><div class="builderFooter"><div><button class="btn" id="builderPrev" disabled>← Zurück</button><button class="btn" id="builderNext">Weiter →</button></div><div>${isEditing&&isAdmin()?`<button class="btn dangerOutline" id="prDelete">Produkt löschen</button>`:''}<button class="btn" id="prCancel">Abbrechen</button><button class="btn" id="prDraft">Entwurf speichern</button><button class="btn primary publishBtn" id="prPublish">Veröffentlichen</button></div></div></div>`);
 const catSel=$('#prCategorySelect'),newWrap=$('#prNewCategoryWrap'),catCurrent=$('#categoryCurrent'),imgInput=$('#prImage');let step=1,localImageUrl='';
 const syncCat=()=>{newWrap.classList.toggle('hiddenField',catSel.value!=='__new__');if(catCurrent)catCurrent.textContent=catSel.value==='__new__'?'Neue Kategorie':catSel.value;updatePreview()};catSel.onchange=syncCat;$$('[data-pick-category]').forEach(b=>b.onclick=()=>{catSel.value=b.dataset.pickCategory;syncCat();$$('[data-pick-category]').forEach(x=>x.classList.toggle('active',x===b))});
 const crossProduct=id=>crossCatalog.find(x=>x.id===id)||{};
 const crossRelationLabels={frequently_bought:'Häufig zusammen gekauft',accessory:'Passendes Zubehör',upsell:'Upgrade / Premium',alternative:'Alternative'};
 function crossDefaultRelation(prod){const current=(currentCategory()||cat||'').toLowerCase(),other=String(prod.category||'').toLowerCase();return current&&other===current?'alternative':'frequently_bought'}
 function crossRender(){
  crossSelected=(crossSelected||[]).filter((x,i,a)=>x&&x.id&&x.id!==id&&a.findIndex(y=>y.id===x.id)===i).slice(0,24).map((x,i)=>({id:x.id,relation:crossRelationLabels[x.relation]?x.relation:'accessory',priority:Math.max(1,Math.min(99,Number(x.priority||((i+1)*10)))),note:String(x.note||'').slice(0,140)}));
  const selected=$('#crossSellSelected'),candidates=$('#crossSellCandidates'),count=$('#crossSellCount'),q=String($('#prCrossSearch')?.value||'').trim().toLowerCase();if(count)count.textContent=`${crossSelected.length} verknüpft`;
  if(selected)selected.innerHTML=crossSelected.length?crossSelected.map((r,i)=>{const prod=crossProduct(r.id);return `<article class="crossSellSelectedCard" data-cross-selected="${esc(r.id)}"><div class="crossSellThumb">${prod.image?`<img src="../${esc(prod.image)}" alt="">`:'<span>◇</span>'}</div><div class="crossSellSelectedMain"><b>${esc(prod.name||r.id)}</b><small>${esc(prod.article_no||r.id)} · ${esc(prod.category||'Sonstiges')} · ${money(prod.base||0)}</small><div class="crossSellRow"><select data-cross-relation="${esc(r.id)}">${Object.entries(crossRelationLabels).map(([k,l])=>`<option value="${k}" ${sel(r.relation,k)}>${l}</option>`).join('')}</select><label>Priorität <input type="number" min="1" max="99" value="${r.priority}" data-cross-priority="${esc(r.id)}"></label></div><input class="crossSellNote" maxlength="140" value="${esc(r.note)}" data-cross-note="${esc(r.id)}" placeholder="Optionaler Verkaufshinweis, z. B. Für das komplette Räucherset"></div><button type="button" class="miniBtn dangerOutline" data-cross-remove="${esc(r.id)}">Entfernen</button></article>`}).join(''):'<div class="empty compactEmpty"><b>Noch keine Verknüpfungen</b>Produkte rechts auswählen oder Vorschläge erzeugen.</div>';
  const selectedIds=new Set(crossSelected.map(x=>x.id));let rows=crossCatalog.filter(p=>!selectedIds.has(p.id)&&(!q||`${p.name} ${p.article_no||''} ${p.category||''} ${p.sku||''}`.toLowerCase().includes(q)));rows=rows.sort((a,b)=>(Number(b.is_popular)-Number(a.is_popular))||String(a.category||'').localeCompare(String(b.category||''),'de')||String(a.name||'').localeCompare(String(b.name||''),'de')).slice(0,30);
  if(candidates)candidates.innerHTML=rows.length?rows.map(prod=>`<article class="crossSellCandidate"><div class="crossSellThumb">${prod.image?`<img src="../${esc(prod.image)}" alt="">`:'<span>◇</span>'}</div><div><b>${esc(prod.name)}</b><small>${esc(prod.article_no||prod.id)} · ${esc(prod.category||'Sonstiges')}</small><strong>${Number(prod.base||0)>0?money(prod.base):'Preis folgt'}</strong></div><button type="button" class="miniBtn" data-cross-add="${esc(prod.id)}">＋ Hinzufügen</button></article>`).join(''):'<div class="empty compactEmpty">Keine weiteren passenden Produkte gefunden.</div>';
  $$('[data-cross-add]').forEach(b=>b.onclick=()=>{const prod=crossProduct(b.dataset.crossAdd);crossSelected.push({id:prod.id,relation:crossDefaultRelation(prod),priority:Math.min(99,(crossSelected.length+1)*10),note:''});crossRender();updateChecklist()});
  $$('[data-cross-remove]').forEach(b=>b.onclick=()=>{crossSelected=crossSelected.filter(x=>x.id!==b.dataset.crossRemove);crossRender();updateChecklist()});
  $$('[data-cross-relation]').forEach(el=>el.onchange=()=>{const r=crossSelected.find(x=>x.id===el.dataset.crossRelation);if(r)r.relation=el.value});
  $$('[data-cross-priority]').forEach(el=>el.onchange=()=>{const r=crossSelected.find(x=>x.id===el.dataset.crossPriority);if(r)r.priority=Math.max(1,Math.min(99,Number(el.value||50)));crossSelected.sort((a,b)=>a.priority-b.priority);crossRender()});
  $$('[data-cross-note]').forEach(el=>el.oninput=()=>{const r=crossSelected.find(x=>x.id===el.dataset.crossNote);if(r)r.note=el.value.slice(0,140)});
 }
 function crossSuggest(){const cur=(currentCategory()||cat||'').toLowerCase(),tokens=cur.includes('haken')?['lauge','mehl','gewürz','zubehör','thermo']:cur.includes('lauge')?['haken','mehl','gewürz']:cur.includes('mehl')?['haken','lauge','gewürz']:cur.includes('gewürz')?['haken','lauge','mehl','zubehör']:['zubehör','haken','lauge','mehl','gewürz'];const have=new Set(crossSelected.map(x=>x.id));const ranked=crossCatalog.filter(x=>!have.has(x.id)&&x.status==='active'&&x.shop_visible).map(x=>{const hay=`${x.category||''} ${x.name||''}`.toLowerCase();let score=Number(x.is_popular?6:0)+Number(x.is_offer?2:0);tokens.forEach((tok,i)=>{if(hay.includes(tok))score+=10-i});if(String(x.category||'').toLowerCase()===cur)score+=2;return{x,score}}).sort((a,b)=>b.score-a.score||String(a.x.name).localeCompare(String(b.x.name),'de')).slice(0,Math.max(1,Number($('#prCrossMax')?.value||4)));ranked.forEach(({x},i)=>crossSelected.push({id:x.id,relation:crossDefaultRelation(x),priority:10+(i*10),note:''}));crossRender();updateChecklist()}
 const setStep=n=>{step=Math.max(1,Math.min(6,n));$$('[data-builder-step]').forEach(b=>b.classList.toggle('active',Number(b.dataset.builderStep)===step));$$('[data-builder-panel]').forEach(x=>x.classList.toggle('active',Number(x.dataset.builderPanel)===step));$('#builderPrev').disabled=step===1;$('#builderNext').style.display=step===6?'none':'';if(step===6)updateChecklist();document.querySelector('.modalBackdrop')?.scrollTo({top:0,behavior:'smooth'})};$$('[data-builder-step]').forEach(b=>b.onclick=()=>setStep(Number(b.dataset.builderStep)));$('#builderPrev').onclick=()=>setStep(step-1);$('#builderNext').onclick=()=>setStep(step+1);
 const currentCategory=()=>catSel.value==='__new__'?$('#prNewCategory').value.trim():catSel.value;
 function updatePreview(){const name=$('#prName')?.value.trim()||'Produktname',short=$('#prShortDescription')?.value.trim()||'Ihre Kurzbeschreibung erscheint hier.',article=$('#prArticle')?.value.trim()||'automatisch',unit=$('#prUnit')?.value.trim()||'Stück',price=Number($('#prBase')?.value||0),isN=$('#prNew')?.checked,pop=$('#prPopular')?.checked,off=$('#prOffer')?.checked;$('#builderPreviewName').textContent=name;$('#builderPreviewShort').textContent=short;$('#builderPreviewArticle').textContent='Art.-Nr. '+article;$('#builderPreviewCategory').textContent=currentCategory()||'Neue Kategorie';$('#builderPreviewUnit').textContent=unit;$('#builderPreviewPrice').textContent=price>0?money(price):'Preis folgt';$('#builderPreviewBadges').innerHTML=(isN?'<span class="previewBadge new">Neu im Sortiment</span>':'')+(pop?'<span class="previewBadge popular">Beliebt</span>':'')+(off?'<span class="previewBadge offer">Angebot</span>':'');$('#prNewUntilWrap')?.classList.toggle('disabledBuilderField',!isN);const c=$('#shortDescCount');if(c)c.textContent=String($('#prShortDescription')?.value.length||0);updateChecklist()}
 function updateChecklist(){const checks=[['Produktname',Boolean($('#prName')?.value.trim())],['Kategorie',Boolean(currentCategory().trim())],['Preis größer 0',Number($('#prBase')?.value||0)>0],['Kurzbeschreibung',($('#prShortDescription')?.value.trim().length||0)>=20],['Ausführliche Beschreibung',($('#prDescription')?.value.trim().length||0)>=30],['Produktbild',Boolean(imgInput?.files?.[0]||p.image)],['Gebinde / Einheit',Boolean($('#prUnit')?.value.trim())],['Cross-Selling',!$('#prCrossEnabled')?.checked||crossSelected.length>0||$('#prCrossAuto')?.checked]];const box=$('#builderChecklist');if(box)box.innerHTML=checks.map(([l,ok])=>`<div class="${ok?'ok':'missing'}"><span>${ok?'✓':'!'}</span><b>${l}</b><small>${ok?'bereit':'bitte ergänzen'}</small></div>`).join('')}
 const collect=mode=>({action:'product_save',publish_mode:mode,id:isEditing?id:$('#prId').value.trim(),article_no:$('#prArticle').value.trim(),barcode:$('#prBarcode').value.trim(),sku:$('#prSku').value.trim(),name:$('#prName').value.trim(),category:currentCategory().trim(),type:$('#prType').value,unit:$('#prUnit').value.trim(),content_quantity:+$('#prContentQty').value,content_unit:$('#prContentUnit').value,package_type:$('#prPackageType').value,package_length_cm:+$('#prPackL').value,package_width_cm:+$('#prPackW').value,package_height_cm:+$('#prPackH').value,pack_quantity:+$('#prPackQty').value,price_basis:$('#prPriceBasis').value,base:+$('#prBase').value,old_price:+$('#prOld').value,sale_price:+$('#prSale').value,sale_start_at:$('#prSaleStart').value,sale_end_at:$('#prSaleEnd').value,product_weight_g:+$('#prWeight').value,shipping_weight_g:+$('#prShipWeight').value,stock:+$('#prStock').value,minimum:+$('#prMinimum').value,status:mode==='publish'?'active':'inactive',shop_visible:mode==='publish',is_popular:$('#prPopular').checked,is_offer:$('#prOffer').checked,is_new:$('#prNew').checked,new_until:$('#prNew').checked?$('#prNewUntil').value:'',short_description:$('#prShortDescription').value.trim(),description:$('#prDescription').value.trim(),features:$('#prFeatures').value.split(/\r?\n/).map(x=>x.trim()).filter(Boolean),seo_title:$('#prSeoTitle').value.trim(),seo_description:$('#prSeoDescription').value.trim(),cross_sell_enabled:$('#prCrossEnabled').checked,cross_sell_title:$('#prCrossTitle').value.trim(),cross_sell_max:+$('#prCrossMax').value,cross_sell_auto:$('#prCrossAuto').checked,cross_sell_reciprocal:$('#prCrossReciprocal').checked,cross_sell:crossSelected.map(x=>({id:x.id,relation:x.relation,priority:+x.priority,note:x.note||''})),image:p.image||''});
 const validatePublish=()=>{const d=collect('publish'),missing=[];if(!d.name)missing.push('Produktname');if(!d.category)missing.push('Kategorie');if(d.base<=0)missing.push('Standardpreis');if(d.short_description.length<20)missing.push('Kurzbeschreibung (mind. 20 Zeichen)');if(d.description.length<30)missing.push('Beschreibung (mind. 30 Zeichen)');if(!imgInput.files?.[0]&&!p.image)missing.push('Produktbild');if(!d.unit)missing.push('Gebinde / Einheit');if(d.type==='prototype')missing.push('Prototypen können nicht als normaler Shopartikel veröffentlicht werden');if(d.cross_sell_enabled&&!d.cross_sell_auto&&!d.cross_sell.length)missing.push('Cross-Selling: Produkt wählen oder Automatik aktivieren');if(d.is_offer&&d.sale_price>0&&d.old_price>0&&d.sale_price>=d.old_price)missing.push('Aktionspreis muss unter Streichpreis liegen');return missing};
 async function saveBuilder(mode){try{if(mode==='publish'){const missing=validatePublish();if(missing.length){setStep(6);toast('Veröffentlichung noch nicht möglich: '+missing.join(', '),true);return}if(!confirm('Artikel jetzt im bestehenden Räucherhaken24-Shopdesign veröffentlichen?'))return}const file=imgInput.files?.[0],payload=collect(mode);let d;if(file){d=await api({...payload,publish_mode:'draft',status:'inactive',shop_visible:false});if(!d.product?.id)throw new Error('Produkt-ID für Bild-Upload fehlt');await uploadProductImage(d.product.id,file);if(mode==='publish')d=await api({...payload,id:d.product.id,publish_mode:'publish',status:'active',shop_visible:true})}else d=await api(payload);toast(mode==='publish'?'Artikel veröffentlicht · Shopdesign unverändert':'Entwurf gespeichert · noch nicht öffentlich');closeModal();await load(true);if(current==='products')renderProducts();if(current==='nature_spices')renderNatureSpices()}catch(e){toast(e.message,true)}}
 $('#prDraft').onclick=()=>saveBuilder('draft');$('#prPublish').onclick=()=>saveBuilder('publish');$('#prCancel').onclick=closeModal;if($('#prDelete'))$('#prDelete').onclick=()=>{closeModal();productDeleteModal(id)};
 ['prName','prArticle','prShortDescription','prBase','prUnit','prNew','prPopular','prOffer','prNewUntil','prSeoTitle','prSeoDescription','prDescription','prFeatures','prStock'].forEach(x=>$('#'+x)?.addEventListener('input',updatePreview));$('#prNew').onchange=updatePreview;$('#prPopular').onchange=updatePreview;$('#prOffer').onchange=updatePreview;$('#prNewCategory').oninput=updatePreview;
 if($('#prCrossSearch'))$('#prCrossSearch').oninput=crossRender;if($('#crossAutoSuggest'))$('#crossAutoSuggest').onclick=crossSuggest;if($('#crossClearAll'))$('#crossClearAll').onclick=()=>{crossSelected=[];crossRender();updateChecklist()};['prCrossEnabled','prCrossAuto','prCrossReciprocal','prCrossTitle','prCrossMax'].forEach(x=>$('#'+x)?.addEventListener('change',()=>{crossRender();updateChecklist()}));
 crossRender();
 imgInput.onchange=async()=>{const f=imgInput.files?.[0],box=$('#builderPreviewImage');if(localImageUrl){URL.revokeObjectURL(localImageUrl);localImageUrl=''}if(f){try{await previewPreparedProductImage(f,box)}catch(e){imgInput.value='';toast(e.message,true);box.innerHTML=imageSrc?`<img src="${esc(imageSrc)}" alt="">`:'<span>Produktbild</span>'}}else box.innerHTML=imageSrc?`<img src="${esc(imageSrc)}" alt="">`:'<span>Produktbild</span>';updateChecklist()};
 syncCat();updatePreview();setStep(1)
}

// delegated actions
document.addEventListener('click',e=>{const o=e.target.closest('[data-open-order]');if(o)return can('view_orders')?openOrder(o.dataset.openOrder):(can('edit_production')?productionEditModal(o.dataset.openOrder):toast('Nur Produktionsansicht freigegeben.',true));const p=e.target.closest('[data-open-proto]');if(p)return openPrototype(p.dataset.openProto);const c=e.target.closest('[data-open-customer]');if(c)return openCustomer(c.dataset.openCustomer);const d=e.target.closest('[data-open-dealer]');if(d)return dealerModal(d.dataset.openDealer);const dv=e.target.closest('[data-dealer-visit]');if(dv)return dealerVisitModal(dv.dataset.dealerVisit);const pr=e.target.closest('[data-open-product]');if(pr)return productModal(pr.dataset.openProduct);const s=e.target.closest('[data-save-stock]');if(s)return saveStock(s.dataset.saveStock);const i=e.target.closest('[data-print-invoice]');if(i){const order=(state.orders||[]).find(x=>(x.order_no||x.id)===i.dataset.printInvoice);if(order)return printInvoice(order)}const doc=e.target.closest('[data-doc-order]');if(doc)return openDocumentForOrder(doc.dataset.docOrder,doc.dataset.docType);const th=e.target.closest('[data-thread]');if(th)return openMessageThread(th.dataset.thread);const us=e.target.closest('[data-user]');if(us)return userModal(us.dataset.user);const ct=e.target.closest('[data-content]');if(ct)return contentModal(ct.dataset.content);const ca=e.target.closest('[data-customer-appointment]');if(ca&&can('edit_appointments')){const customerId=ca.dataset.customerAppointment,cu=(state.customers||[]).find(x=>x.id===customerId);appointmentSelectedDate=new Date().toISOString().slice(0,10);appointmentPeriod=appointmentSelectedDate.slice(0,7);appointmentRepId=isAdmin()?(cu?.sales_rep_id||''):(state.current_user?.sales_rep_id||'');appointmentData=null;return (async()=>{await appointmentLoad(appointmentPeriod,appointmentRepId);setView('appointments');appointmentModal(null,customerId)})()}const no=e.target.closest('[data-new-order]');if(no&&can('create_orders'))return manualOrderModal(no.dataset.newOrder,[],(state.customers||[]).find(c=>c.id===no.dataset.newOrder)?.sales_rep_id||'');const pc=e.target.closest('[data-pick-customer]');if(pc){salesCustomerId=pc.dataset.pickCustomer;salesAdvice=null;return renderSales()}const er=e.target.closest('[data-edit-rep]');if(er&&isAdmin())return salesRepModal(er.dataset.editRep)});
$$('.navItem').forEach(b=>b.onclick=()=>{setView(b.dataset.view);$('#sideNav').classList.remove('open')});$$('[data-mobile-view]').forEach(b=>b.onclick=()=>setView(b.dataset.mobileView));$$('[data-quick-view]').forEach(b=>b.onclick=()=>setView(b.dataset.quickView));const moreBtn=$('[data-mobile-more]');if(moreBtn)moreBtn.onclick=()=>$('#sideNav').classList.toggle('open');$('#mobileMenu').onclick=()=>$('#sideNav').classList.toggle('open');$('#refreshBtn').onclick=()=>load();$('#modalClose').onclick=closeModal;$('#modalBackdrop').onclick=e=>{if(e.target.id==='modalBackdrop')closeModal()};$('#globalSearch').oninput=e=>{const value=e.target.value.trim();if(['products','productbuilder','nature_spices','product_analysis'].includes(current)){productSearchTerm=value;clearTimeout(productSearchTimer);productSearchTimer=setTimeout(()=>{if(current!=='products')setView('products',true);else renderProducts()},260);return}searchTerm=value;if(!searchTerm)return render();if(current==='dashboard')setView('orders',true);else render()};$('#globalSearch').onkeydown=e=>{if(e.key==='Enter'&&['products','productbuilder','nature_spices','product_analysis'].includes(current)){e.preventDefault();clearTimeout(productSearchTimer);productSearchTerm=e.target.value.trim();if(current!=='products')setView('products',true);else renderProducts()}};
document.addEventListener('keydown',e=>{if(e.key==='Escape')closeModal()});
console.info('RH24 Orgaboard V86 · Cross-Selling im Produkt-Baukasten · Chef-Leitstand · Live geladen');
window.__rh24FrontendStarted=true;
try{const initialView=new URLSearchParams(location.search).get('view');if(initialView&&viewNames[initialView])current=initialView}catch(_e){}
load();
})();
